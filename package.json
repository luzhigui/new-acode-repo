{
  "name": "guangmingding-5v5",
  "version": "1.0.0",
  "private": true,
  "description": "光明顶5v5 · 网页版打包安卓 APK",
  "scripts": {
    "build:web": "node scripts/prepare-web.mjs",
    "android:sync": "npm run build:web && npx cap sync android",
    "android:build": "cd android && gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "sharp": "^0.35.4"
  }
}
