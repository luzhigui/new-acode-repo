﻿
---

# 光明顶 5v5 游戏设定汇总

版本：V5.2.1  
整理日期：2026-07-24

---

## 一、核心战斗公式

### 1.1 单位基础属性生成

每个单位有一个 **M 值**，所有属性由 M 值随机拆分：

```
hp  = rand(ceil(m × 0.4), floor(m × 0.6))
rem = m - hp
```

`hp` 是生命基数，最终 `maxHp = hp × 2.5`。

攻击/防御分配：

| 职业 | 防御分配 | 攻击分配 | 约束 |
|------|----------|----------|------|
| 防战 | def = rand(ceil(rem×0.5), rem-1) | atk = rem - def | def - atk ≤ 20 |
| 其他 | def = rand(ceil(rem×0.3), floor(rem×0.5)) | atk = rem - def | 3 ≤ atk - def ≤ 13 |

### 1.2 职业属性加成

| 职业 | 攻击 | 防御 | 生命上限 |
|------|------|------|----------|
| 战士 | +3 | +2 | +25 |
| 防战 | -7 | +0 | +30 |
| 远程 | +6 | -2 | -25 |
| 飞行 | +2 | -2 | -25 |

**【修正】战士防御加成从 +3 改为 +2，防战攻击加成从 -6 改为 -7，统一读取 `ROLE_BONUS` 配置表。**

### 1.3 基础伤害公式

```
calcDamage(atk, def) = atk × (atk / (atk + def))
```

- def ≤ 0 时，伤害 = atk
- 保底伤害 = atk × 0.1

### 1.4 实际攻击伤害计算流程

```
atkBase = atk（保留小数点后一位）
defBase = def（保留小数点后一位）
atkVar = rand(0, 6)
defVar = rand(0, 4)
hpBonus = rand(0, 5)

attackerBuffStats 和 defenderBuffStats 分别由 computeBuffStats 计算

displayAtk = atkBase + atk × attackerBuffStats.atkBonus
displayDef = defBase + def × defenderBuffStats.defBonus
atkAct = displayAtk + atkVar
defAct = displayDef + defVar
target.hp += hpBonus
```

#### 1.4.1 非防战伤害
```
raw = calcDamage(atkAct, defAct)
```

#### 1.4.2 防战伤害（K值破防）

```
displayDef = def + def × attackerBuffStats.defBonus
lv = getFangLevel(displayDef, m)
k  = FANG_K[lv]
penPart = calcDamage(atkAct, defAct)
raw = penPart + displayDef × k + maxHp × 0.02
```

FANG_LEVELS 阈值表（def/m 比例）：

| 等级 | 比例阈值 | K值 |
|------|----------|-----|
| 0 | < 0.244 | 0 |
| 1 | ≥ 0.244 | 0.01 |
| 2 | ≥ 0.264 | 0.03 |
| 3 | ≥ 0.279 | 0.06 |
| 4 | ≥ 0.292 | 0.10 |
| 5 | ≥ 0.306 | 0.15 |
| 6 | ≥ 0.322 | 0.21 |
| 7 | ≥ 0.342 | 0.30 |
| 8 | ≥ 0.373 | 0.42 |
| 9 | ≥ 0.445 | 0.60 |
| 10 | ≥ 0.520 | 0.86 |
| 11 | — | 1.20 |

### 1.5 伤害波动事件

- atkVar = 6：攻击者暴击台词，critCount++
- defVar = 4：防御者嘲讽台词
- hpBonus = 5：防御者血量台词
- 张无忌近战前三次有特殊台词

### 1.6 精英技能对伤害的修正

raw 叠加以下效果：

| 技能 | 触发条件 | 效果 |
|------|----------|------|
| 混元霹雳劲 | 成昆 | raw += (maxHp - hp) × 0.3 |
| 叛逆突袭 | 宋青书 | raw += target.hp × 0.12 |
| 鹿角杖法 | 鹤笔翁 | 忽略目标 30% 防御；目标已中玄冥毒则伤害 ×1.5 |

### 1.7 眩晕机制

- 触发：攻击被闪避反击时，攻击者被眩晕
- 效果：本回合无法行动、无法闪避、无法触发被动
- 标记：`_stunned = true`
- 清除：回合开始时统一重置
- 视觉：😵‍💫 图标

### 1.8 最终伤害

```
dmg = floor(raw)
hpAfter = target.hp - dmg
hpAfter ≤ 0 → 阵亡
```

### 1.9 计算精度

内部计算全程保留小数点后一位（0.1 步长），最终伤害输出 `Math.floor` 取整显示。攻防属性内部可精确到 0.5。

---

## 二、单位系统与特殊角色

### 2.1 职业

| 职业 | 攻击类型 | 被遮挡行为 | 备注 |
|------|----------|------------|------|
| 战士 | 近战 | 休息回血 15 | 斩杀阈值 15%（小昭在场 20%）；每次攻击降目标 2 点防御 |
| 防战 | 近战 | 休息回血 15 | K值破防；坚盾每受击 +0.5 防（上限6层） |
| 远程 | 远程 | 不受遮挡 | 3% 未命中；每次造成伤害后永久 +2 攻击 |
| 飞行 | 近战 | **被遮挡仍可攻击** | 6% 未命中；基础闪避 15%（每攻击 +2%）；破阵穿透 |

**【修正】战士备注补充破防效果；飞行遮挡行为修正为"被遮挡仍可攻击"；休息回血统一为 15。**

### 2.2 特殊角色

#### 张无忌

- 默认远程，固定 5 号位
- 九阳神功：每次攻击后回复 maxHp × 5%
- 乾坤大挪移：己方 4/6 号位被攻击时，反弹 15% 伤害给攻击者，自伤 10%（仅远程形态、未眩晕时生效）
- 形态切换：所在列前排无友军时转近战
  - **叠加两次战士加成**：atk +6，def +4，maxHp +50，hp +50
  - 职业变战士
  - 近战后正常被遮挡
  - 前三次近战特殊台词
  - 第三次触发融会贯通：额外造成 **（对方攻击 - 防御）× 系数** 的伤害

**【修正】变身改为叠加两次战士加成；融会贯通改为攻防差值计算；近战后可被正常遮挡。**

#### 韦一笑

- 默认飞行，固定 6 号位
- 基础闪避 20%
- 闪避反击：闪避时对攻击者造成 (atk + def) × 0.5 真实伤害，攻击者被眩晕
- 吸血：每次造成伤害恢复 dmg × 18%，同时提升 maxHp 同数值
- **残血幻影：血量低于一定比例时获得额外效果（具体待补充）**

**【修正】补充残血幻影特性，具体参数待补入配置表。**

#### 小昭·姐（蝶变附身）

- 固定 4 号位，`isXiaoZhaoSister = true`
- 回合开始随机附身一名队友，转移一半攻/防/血上限给宿主
- 附身期间自身不可选中，`_flyMode = 'butterfly'`
- 回合结束飞回，宿主返还一半附身期间获得的生命上限
- 小昭姐在场时，团队 Buff 有强化效果

#### 小昭·妹（蛛变飞天）

- 固定 4 号位，与姐姐互斥，`isXiaoZhaoBrother = true`
- 每回合随机变职业（不与上回重复），记录精通，每次变身 +5 生命上限
- 飞天免疫：受致命伤时化为蜘蛛免疫伤害，**每场限 3 次**
- 回合结束落下，攻击**随机**敌人，附带穿透伤害 + 精通加成
- 团队 Buff 过期后单独续上职业匹配的 Buff

**【修正】飞天次数改为 3 次；落地攻击改为随机目标。**

---

## 三、战斗流程与行动顺序

### 3.1 回合流程

1. 回合开始：输出分隔符、快乐回血、玄冥毒、召唤拒马、小昭蝶变/蛛变、宋青书性奋/苦练、概率连击选人、Buff 摘要、重置状态
2. 苦练优先行动
3. 交替行动：敌方开始，按站位最前轮流，眩晕跳过，每步切换阵营
4. 额外行动（不占交替步数）：概率连击、小昭蝶击、性奋额外攻击、玄冥二老联动
5. 回合结束：销毁拒马、清除休息、Buff 回合 -1、判断胜负

### 3.2 行动单位选择

取 alive 且 !_acted 且 !_stunned 的单位，按 pos 升序取第一个。

### 3.3 攻击流程

选择目标 → 未命中判定 → Buff 加成计算 → 闪避判定 → 伤害计算 → 应用伤害 → 攻击后效果 → 额外攻击判定

### 3.4 目标选择规则

| 攻击者 | 目标 |
|--------|------|
| 宋青书 | 敌方血量百分比最高 |
| 韦一笑 | 敌方血量最低 |
| 近战/拒马 | 敌方前排随机 |
| 飞行 | **所在列空时触发破阵穿透（+10攻击，锁后排），否则随机** |
| 远程 | 敌方存活随机 |

**【修正】飞行目标选择补充破阵穿透逻辑。**

---

## 四、站位系统与遮挡

### 4.1 九宫格站位

```
7 8 9
4 5 6
1 2 3
```

**【修正】前排不是固定 1/2/3，而是每列最靠前的存活单位。**

### 4.2 前排判定

每列取 pos 最小的存活单位，三列各取一个。

### 4.3 遮挡判定

- 飞行不受遮挡限制（被遮挡仍可攻击）
- 同列有更靠前存活友军（不含拒马）时，后排近战被遮挡
- 被遮挡时休息回血 15

### 4.4 相邻位置

目标周围 8 格（含对角），用于流星赶月溅射。

---

## 五、海克斯 Buff 系统

### 5.1 通用规则

- 每关选 1 个 Buff，默认持续 4 回合
- 最多同时 2 个，超出移除剩余最少者
- 小昭在场时部分 Buff 强化

### 5.2 Buff 列表

| Buff | 普通团队版 | 小昭姐强化版 | 小昭妹永久版 |
|------|-----------|-------------|-------------|
| 概率连击 | 随机一人 80% | 100% + 无视遮挡 | 自己 80% |
| carry | 5号位供养 | 4/5/6号位 | 自己固定两层精通加成 |
| 流云身法 | 全体 +25% | 无视行动状态 | 自己永久 25% |
| 圣火令 | 两列攻 +30%，两行防 +30% | 小昭额外 +30%攻防 | 仅自己每回合 +30% |
| 巨马阵 | 25血/0攻/5防 | 30血/0攻/30防，反伤5 | 自己招普通马 |
| 流星赶月 | 远程 +50%，溅射 50% | 溅射后 +2攻/人 | 远程时普通效果 |
| 嗜血狂刀 | 吸血 80% | 额外再砍一刀 | 战士时普通效果 |
| 严阵以待 | 防战防 +50%，反弹 50% | 反弹时回复等量 | 防战时普通效果 |
| 乘风突袭 | 飞行 80%波及，60%击退 | 100%波及，80%击退 | 飞行时普通效果 |
| 热血奋战 | 15%，每3次翻倍 | 20%，每2次翻倍 | 强化版 |
| 惑人心智 | 80%扰乱敌，40%扰己 | 95%/50%，持续3回合 | 15%永久惑心 |

### 5.3 闪避计算

```
finalHit = (1 - baseDodge) × (1 - buffDodge)
totalDodge = 1 - finalHit
```

- 基础闪避：韦一笑 20%，其他飞行 15%，非飞行 0%
- Buff 闪避：cloudBody 25%

### 5.4 严阵以待反弹公式

```
reboundDmg = floor((atkAct - calcDamage(atkAct, defAct)) / 2)
```

---

## 六、精英技能与特殊机制

### 6.1 精英怪配置

| 关卡 | 精英 | 职业 | M | 技能 | 站位 |
|------|------|------|---|------|------|
| 3 | 宋青书 | 飞行 | 107 | rebelStrike | 5 |
| 4 | 宋青书 | 飞行 | 107 | rebelStrike | 5 |
| 4 | 周芷若 | 战士 | 107 | nineYinClaw | 2 |
| 5 | 鹿杖客 | 远程 | 112 | xuanmingPalm | 7 |
| 5 | 鹤笔翁 | 飞行 | 112 | hornStrike | 4 |
| 6 | 成昆 | 防战 | 112 | phantomThunder | 1 |

### 6.2 精英技能详情

**灭绝双剑** — 持有者：**【待确认】**  
HP < 50% 时，受攻击 80% 反击 atk × 0.8，每回合 1 次。当前代码持有者为"灭绝师太"，游戏中无此角色。

**【修正】标注持有者待确认或未实装。**

**九阴白骨爪** — 周芷若  
首次必触发，后续 80% 概率追击，连锁 80% 概率继续。  
嫉妒（张无忌在场）：基础伤害 2（无无忌 1.5），maxHp 比例 1.5%（无无忌 1%），斩杀线 18%（无无忌 15%）。

**【修正】补充连锁概率和嫉妒具体数值。**

**叛逆突袭** — 宋青书  
锁定血量百分比最高目标，附加目标当前生命 12% 真实伤害。

**混元霹雳劲** — 成昆  
附加已损失生命 × 30% 真实伤害。

**玄冥神掌** — 鹿杖客  
中毒：4% → 2% → 1% → 消失，持续 3 回合。

**鹿角杖法** — 鹤笔翁  
忽略 30% 防御；目标中毒时伤害 +50%。

**苦练** — 宋青书  
无周芷若时每回合最先行动；行动前全体队友 +1.5 攻 +0.5 防 +2 血上限，自身翻倍。

**新婚** — 宋青书/周芷若  
宋青书每次攻击扣周芷若 1 血，叠一层快乐（首层 16%，后续降级）。

**性奋** — 宋青书  
周芷若在场时，宋青书每次攻击后可额外攻击一次。  
代价：**每次攻击触发惩罚，生命上限递减（第1次-1，第2次-2……跨回合累计，最低降至1）。**

**【修正】补充代价描述；改为每次攻击触发惩罚。**

**幻影伪装** — 成昆  
攻击后模仿对方单位，回复 **已损失生命 × 6% × 存活敌人数**。  
对方攻击时 **30% + 每损失 10% 生命 +6% 概率** 混乱攻击队友。被模仿者免疫混乱。攻击前清除旧模仿状态。

**【修正】回复量和混乱概率修正为实际数值。**

### 6.3 宋周联动机制

- 苦练：无周芷若时最先行动
- 新婚：宋青书攻击扣周芷若血，叠快乐
- 性奋：周芷若在场时额外攻击，每次攻击触发惩罚
- 嫉妒：张无忌在场时九阴白骨爪数值全面提升

### 6.4 快乐回血

每层快乐按百分比回血后降级：16% → 10% → 6% → 3% → 消失。

---

## 七、蝶蛛双生系统

### 7.1 概述

小昭拆分为姐妹双形态，每局随机出现一个，互斥。

### 7.2 姐姐·蝶变附身

回合开始随机附身队友，转移一半攻/防/血上限。回合结束飞回。无可用队友时阵亡。

### 7.3 妹妹·蛛变飞天

每回合随机变职业，记录精通，+5 血上限。受致命伤飞天免疫，**每场 3 次**。回合结束落下攻击**随机**敌人。

**【修正】飞天 3 次，落地攻击随机。**

### 7.4 永久海克斯

团队 Buff 存在时用团队版；消失后妹妹单独续上职业匹配的 Buff。

---

## 八、全局状态管理

### GlobalStore

统一管理 `fastForwardActive`、`currentBattleState`、`battleEvents` 等全局变量。读取 `GlobalStore.get`，写入 `GlobalStore.set`。


## 九、海克斯效果函数库

### 9.1 模块概述

海克斯的效果执行逻辑统一放在 `core/50buff-effects.js`，按身份三分支：

| 分支 | 命名 | 生效条件 |
|------|------|----------|
| 普通团队版 | `_Normal` | 无小昭在场时 |
| 姐姐强化版 | `_Sister` | 小昭姐在场时 |
| 妹妹永久版 | `_Brother` | 团队海克斯过期后，小昭妹单独续上 |

`core/04buff-system.js` 负责数值计算（`computeBuffStats`），`core/50buff-effects.js` 负责效果执行（伤害、回血、换位、溅射等），两者通过 `applyBuffEffectsAfterAttack` 桥接。

### 9.2 涵盖海克斯

- 嗜血狂刀：吸血 + 姐姐强化版额外再砍一刀
- 热血奋战：回复已损失生命；妹妹版 20%/每 2 次翻倍
- 乘风突袭：波及同行 + 击退；姐姐版 100%/80% 概率
- 流星赶月：远程伤害加深 + 周围 8 格溅射 + 主目标减防
- 严阵以待：反弹伤害；姐姐版反弹同时回复等量血量
- 流云身法：闪避加成
- 圣火令：列攻行防；妹妹永久自加成
- Carry 加成：5 号位供养；姐姐版扩大至 4/5/6 号位
- 惑人心智：换位扰乱；姐姐版 95%/50%

---

## 十、回放系统

### 10.1 模块概述

`modules/100-replay.js` 提供完整战斗回放功能，挂载到 `window.ReplayManager`。

### 10.2 API

| 方法 | 说明 |
|------|------|
| `startRecording(snapshot)` | 记录战斗初始快照 |
| `pushStep(step, round, ally, enemy)` | 记录每步日志 + 事件 + 双方状态 |
| `finishRecording(winner)` | 标记胜者，触发下载 |
| `download(filename)` | 导出 JSON 回放文件 |
| `importFile(file)` | 导入回放文件，自动重放 |
| `startReplay(data)` | 创建 ReplayPlayer 逐帧播放 |

### 10.3 回放播放器

- 逐步渲染日志
- 支持速度控制（默认 800ms/步）
- 显示回合分隔符和胜者
- 可随时停止

---

## 十一、攻防显示

- 战场格子：显示加成后的最终值，有加成时高亮金色
- 攻击加成：`floor(baseAtk × buffStats.atkBonus)`
- 防御加成：`floor(baseDef × buffStats.defBonus)`
- 详细弹窗：点击格子展示基础值 + Buff 加成 + 坚盾层数 = 最终值

---

## 十二、测试与体检

### 12.1 测试入口

`tests/30test-runner.html`

### 12.2 体检分组

启动与加载、九宫格基础、血条与属性、Buff 系统、状态样式、音效、特效、精英、数据一致性、核心参数/公式、战斗引擎、日志与 UI。约 70 条规则，覆盖 6 个关卡。

### 12.3 单元测试

`tests/25unit-tests.js` 包含核心函数测试。

---

## 十三、备注与后续规划

### 13.1 已确认待修改

| 项目 | 说明 | 优先级 |
|------|------|--------|
| 职业加成统一读配置表 | `applyBonus` 改为读 `ROLE_BONUS` | 高 |
| 韦一笑残血幻影 | 补入配置表 | 中 |
| 张无忌变身改双重战士加成 | 去掉写死数值 | 高 |
| 张无忌融会贯通改攻防差值 | 具体系数待定 | 中 |
| 张无忌近战正常被遮挡 | 去掉 `_blocked = false` | 高 |
| 性奋代价改每次攻击触发 | 从每回合一次改为每次攻击 | 高 |
| 灭绝双剑归属 | 确认持有者或标注未实装 | 低 |
| 战士破防效果补文档 | 已补入 2.1 | 已完成 |
| 小昭妹飞天 3 次 | 文档已改 | 已完成 |
| 小昭妹落地攻击随机 | 文档已改 | 已完成 |
| 幻影伪装数值修正 | 回复量、混乱概率 | 已完成 |

### 13.2 后续重构规划

| 项目 | 说明 |
|------|------|
| 精英技能伤害修正改组件模式 | 宋青书、成昆、鹤笔翁各自携带修正函数，不再在管线里硬编码判断 |
| 联动系统改事件队列 | 玄冥二老等联动攻击改为事件驱动，不硬编码在攻击流程中 |
| 被遮挡 +5 防御 +5 护盾 | 护盾显示方案待定 |
| 距离计算工具函数 | 后续用于技能判定 |
| Buff 列表分三列重写 | 普通团队 / 姐姐强化 / 妹妹永久，文档 5.2 已完成 |

### 13.3 数值常量

- `MAX_ROUND = 35`
- `ATK_VAR = 6`, `DEF_VAR = 4`
- `HP_BONUS_MIN = 0`, `HP_BONUS_MAX = 5`
- 内部计算精度：小数点后一位（0.1 步长）
- 最终伤害输出：整数（`Math.floor`）
- 休息回血：15
- 战士斩杀阈值：15%（小昭在场 20%）
- 战士破防：每次攻击 -2 防御
- 坚盾：每受击 +0.5 防御，上限 6 层
- 远程成长：每次造成伤害 +2 攻击

### 13.4 已下线功能

- 网络 BGM 源已删除，仅保留本地 `assets/sfx_xinai.mp3`
- 环境诊断页签已移除

### 13.5 文件目录

```
core/      核心配置、单位、工具、Buff、马、战斗引擎、海克斯效果
player/    播放器、Buff UI、文本、玩家核心
ui/        主控、渲染、工具函数
fx/        特效（箭矢、飞撞、换位、击退、闪避、蝶蛛）
modules/   精英技能、错误捕获、音频、全局状态管理、回放
tests/     测试、体检、题库
tools/     工具箱、自动战斗、构建脚本
assets/    音效
```

---

## 十四、版本号

当前所有模块版本号：`V5.2.1`（design game 修正版，代码版本 V5.2.0 待同步修改）