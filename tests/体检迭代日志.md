# 体检代码迭代日志

> 由「体检代码迭代」自动化逐轮追加（实际调度：每 **10 分钟**一趟；本行原写"30 分钟迭代"，与实际 schedule 不符，于 2026-09-22 第 3 轮复盘修正）。每轮格式：时间戳 → 跑了什么 → 发现的问题 → 改了什么 → 验证结果 → 下轮目标。
> 连续迭代时先读本文件尾部，避免重复修同一个问题、避免两轮改动互相打架。
> **每趟无论有无改动都要追加一行**（哪怕只写"本轮无改动，各项全绿"），否则复盘员无法区分"没跑"和"跑了没记"。

---

## 第 1 轮 — 2026-09-21 23:05（首次迭代）

### 跑了什么

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| 体检代码语法 | `node --check` × 121 / 122 / 123 / 124 + health-rules 全 24 条 | 全过，0 报错 |
| 规则回放 | `node tests/rules-replay.mjs`（20 种子 × 6 关 = 120 场 / 24 规则） | 无失败规则，但 **8 条恒 skip（空转）** |
| 静态快检 | `node tests/123static-scan.js` | **零输出、退出码 0**（静默假绿） |
| 基线检查 | baseline-check.mjs / smoke-headless.mjs | 本轮未跑（下轮排期） |

### 发现的问题（按价值排序）

1. **静态快检在 node 下是"没体检却显示健康"**：`123static-scan.js` 只 export 函数、无任何自举入口，且基准目录用 `window.location.href`、读文件用 `fetch`——浏览器专属。`node tests/123static-scan.js` 只 import 不执行，零输出退出码 0。这意味着任何自动化/CI 跑它都拿到绿灯，实际一条都没扫。
2. **扫描器误报 `modules/26elite-sixsects.js`**：补上 node 入口后真跑，报"使用 eventBus 但未 import（将运行时报错）"。核查确认 `eventBus` 是 `register(eventBus, A, B, log)` 的**形参**，函数体内 `eventBus.on/emit` 合法（120 场回放全绿佐证）。根因是此前的排除规则只挡"参数列表里那一次出现"，挡不住函数体内的后续使用。
3. **8 条恒 skip 规则只报数字、退出码恒 0**：空转规则一次都没真断言，却显示"无失败规则"——体检自己假绿。

### 改了什么

| 文件 | 改动 | 理由 |
| --- | --- | --- |
| `tests/123static-scan.js`（V1.0.0 → V1.1.0） | ① 加 `IS_NODE` 判定 + node `fetch` 垫片（fs 读 file://）+ `getBaseUrl()` 统一基准目录；② 文件末尾加 CLI 自举（仅 `node` 直接执行时触发，被 import 不触发） | 让静态快检能在 node/CI 下真跑，输出结论 + 致命问题非 0 退出 |
| `tests/123static-scan.js` | `scanSharedSymbolImport` 新增形参/解构形参排除：`[(,{]\s*sym\s*[,)]` 命中即整体跳过该符号 | 消除"注入型符号"误报（26elite-sixsects 的 `eventBus`） |
| `tests/rules-replay.mjs`（V6.1.11 → V6.1.16） | ① 恒 skip 规则收集进 `deadNames` 并打印**完整名单**；② 新增 `DEAD=1` 严格模式，存在空转规则即非 0 退出 | 空转规则不再"静默绿"，可直接当 CI 红线 |

### 验证结果

- `node tests/123static-scan.js` → 扫 **79 个文件 / 153ms**，三类 issue 全 0，退出码 0（此前零输出假绿）
- 负向测试（`scanSharedSymbolImport` 三种输入）：真缺失 → 报 1 条 ✅；形参注入 → 0 条 ✅；解构注入 → 0 条 ✅（确认不是"一刀切不报"）
- `node tests/rules-replay.mjs` → 无失败规则，恒 skip 8 条（名单已列出），默认退出码 0
- `DEAD=1 node tests/rules-replay.mjs` → 退出码 1 ✅（严格模式生效）
- `node --check` 两个改动文件均通过

### 下轮目标

1. **排查 8 条恒 skip 空转规则**（本轮已列名单）：逐条判断是"回放 120 场覆盖不到该机制"还是"规则逻辑写死 skip"，前者补回放覆盖（注入/阵容），后者改规则。名单：白骨爪回血刷屏(123)、姐姐附身叠加(127)、小昭妹飞天免疫次数超限(129)、九阴白骨爪伤害(132)、死亡特效缺失(133)、张无忌近身切换时机(134)、张三丰核心机制回归(141)、张无忌九阳神功回复量(143)。
2. 补跑 `baseline-check.mjs` / `smoke-headless.mjs`，纳入每轮必跑项。

---

## 第 2 轮 — 2026-09-22 00:08（基线回归红线修复）

### 跑了什么

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| 静态快检 | `node tests/123static-scan.js` | 79 文件 / 151ms，三类 issue 全 0，退出码 0 |
| 规则回放 | `node tests/rules-replay.mjs` | 无失败规则；恒 skip 仍 8 条（上轮名单，本轮未动） |
| 基线比对 | `node tests/baseline-check.mjs` | **BASELINE-DIFF 共 18 场不同，退出码却为 0（假绿）** |
| 冒烟 | `node tests/smoke-headless.mjs` | 全过 |

### 发现的问题（本轮只修基线这一簇，共 3 点）

1. **基线比对假绿**：`baseline-check.mjs` 打印完 DIFF 后不设退出码，18/18 场不同照样退出 0——自动化/CI 拿到绿灯，红线名存实亡。
2. **基线整体过期**：`baseline-v1.json` 录于 2026-08-26，游戏已迭代多版；夜间体检日志连续 3 趟确认"历史基线过期、非本趟引入"，但一直没人重录。
3. **三份重复 runner**：正式录制器 `140-baseline.js` 在 Windows 下因 `new URL(url).pathname` 得到 `/C:/...` 跑不了，于是派生了两个自封"临时"的文件——`baseline-check.mjs`（比对+假绿）和 `baseline-diff.mjs`、`baseline-entries.mjs`（seed=42:5 的一次性调试脚本），四份 `runBaselineCase` 逻辑重复。

### 改了什么

| 文件 | 改动 | 理由 |
| --- | --- | --- |
| `tests/140-baseline.js`（V1.0.0 → V1.2.0） | ① fetch 垫片改用 `fileURLToPath(new URL(url))` 修复 Windows 路径；② 新增 `--check` 模式：逐场比对 winner/rounds/facts，一致退出 0，任一差异退出 1 并提示"重录基线 vs 回归"的处置口径；③ 保留原三模式（全量=重录 / 带参数=只打印） | 吸收临时 runner 的全部有效功能，回归红线归位到正式文件 |
| `tests/baselines/baseline-v1.json` | 按当前业务行为重录 18 场 | 夜间体检已三趟确认旧基线属"历史过期非回归"，业务代码为准 |
| `tests/baseline-check.mjs`、`baseline-diff.mjs`、`baseline-entries.mjs` | 删除 | 均自封"临时 runner 跑完即删"，功能已吸收进 140-baseline.js；全仓 grep 无任何引用（SCAN_FILES 也不含它们，静态快检扫数不变 79） |

### 验证结果

- `node --check tests/140-baseline.js` 通过；带参数 `1:1 42:3` 部分跑正常且不覆盖基线
- 负向测试 1：对**旧基线**跑 `--check` → 18 场 DIFF、退出码 1 ✅
- 负向测试 2：篡改新基线 1 场的 winner → `--check` 精确报"共 1 场不同"、退出码 1 ✅，还原后恢复 MATCH/0 ✅
- 正向：重录后 `node tests/140-baseline.js --check` → `BASELINE-MATCH 18 场与基线一致`、退出码 0
- 删除 3 个文件后复跑：静态快检 79 文件全 0、规则回放结果不变（8 条空转名单未动）、冒烟全过

### 下轮目标

1. **8 条恒 skip 空转规则逐条排查**（原第 1 轮遗留，连续两轮未动）：区分"回放覆盖不到"（补回放覆盖/阵容）vs"规则逻辑写死 skip"（改规则）。名单：白骨爪回血刷屏(123)、姐姐附身叠加(127)、小昭妹飞天免疫次数超限(129)、九阴白骨爪伤害(132)、死亡特效缺失(133)、张无忌近身切换时机(134)、张三丰核心机制回归(141)、张无忌九阳神功回复量(143)。
2. rules-replay 提到 `[fact契约] butterflyNoHost 缺字段: sisterUid`——确认是契约提醒还是漏报源头。

---

## 第 3 轮 — 2026-09-22 00:2x（恒空转规则归因 + 133 死代码/漏报修复）

### 跑了什么

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| 静态快检 | `node tests/123static-scan.js` | 79 文件 / 162ms，三类 issue 全 0，退出码 0 |
| 规则回放 | `node tests/rules-replay.mjs` | 改前：无失败、恒 skip 8 条；改后：无失败、恒 skip **7** 条 |
| 基线比对 | `node tests/baseline-check.mjs` | **跑不了**：该文件第 2 轮已删（功能收编进 140-baseline.js），报 MODULE_NOT_FOUND，退出码 1 |
| 基线比对（替身） | `node tests/140-baseline.js --check` | `BASELINE-MATCH 18 场与基线一致`，退出码 0 |
| 冒烟 | `node tests/smoke-headless.mjs` | 全过 |
| 语法 | `node --check` × 改动两文件 | 均通过 |

> 注：`baseline-check.mjs` 是任务清单里的历史命令，第 2 轮已删除。本轮起基线红线固定用 `node tests/140-baseline.js --check`。

### 发现的问题（本轮修 2 个，其余留给下轮）

**问题 1 —— `133-death-effect.js` 恒空转且结构上不可能变绿（主修）**

根因三行：
- 根因：规则循环首行 `if (!e || !e.text) continue;` 把**所有 attack-group 全滤掉了**——组顶层没有 `text`，斩杀/爪击文本全在 `entries` 子条目上；叠加 `saw` 只在三条 `return{fail:true}` 分支里置位，文件末尾 `return {fail:false}` 永不可达。
- 证据：改前 120 场 `skip=120`；去掉 `!e.text` 守卫后立即 `pass=80 fail=13`，证明此前一条都没扫到。
- 影响范围：规则一次都没真断言，却长期挂在"恒 skip 名单"里，`DEAD=1` 红线被它永久占坑、失去区分度。

连带查出两处**会让修复直接变假红**的缺陷（改完守卫后炸出 13 条，逐条取证定位）：
- **规则过期**：路径 3 见 `isDodge && isDead` 即判复发。但 `player/46attack-group.js` L141 现为 `if (entry.isDodge…)` 守卫已移除，`isDodge+isDead` 并存是**修复后的合法态** → 照旧判必误报。
- **通道混淆**：按文本"斩杀"一把抓会把白骨爪斩杀算进来，而白骨爪**根本不走组 isDead**。实跑取证两条通道的字段差异：
  - 战士斩杀子条目 `keys=[type,text]`，组 `isDead=true` ✅（死亡标记落组上，供 player/46 读）
  - 九阴白骨爪斩杀子条目 `keys=[type,text,isClawHit,clawAttackerUid,clawTargetUid,isExecute]`，组 `isDead=undefined` —— 爪伤是攻击结算后才由 `core/16` CLAW_CHAIN 落地，组 isDead 只反映基础攻击；死亡特效走 `render/31 translateExecute`（`dead: data.isDead ?? true`），唯一硬依赖是子条目上的 `clawTargetUid + isExecute`

**问题 2 —— 恒 skip 规则无法归因，体检给不出可行动结论（辅修）**

根因三行：
- 根因：回放器只输出"恒 skip N 条"名单，不区分「规则逻辑写死 skip」（体检侧 bug）与「业务侧 fact 压根没写进 `step.log`」（数据源缺失，改规则没用）。
- 证据：factType 直方图（8 种子 × 6 关）共 32 种 fact，`clawNoHeal=6` 而 `clawHit/clawExecute/clawHeal` **全为 0**；关键字扫描 `九阴白骨爪=0 / 斩杀=0 / 追击=0`，`白骨爪=17` 全是"宋青书已满血"那条。
- 影响范围：123/132/133 三条规则空转的根因在业务侧，但这一结论无法从体检输出中读到，只能逐条肉眼看规则源码。

### 改了什么

| 文件 | 改动 | 理由 |
| --- | --- | --- |
| `tests/health-rules/133-death-effect.js`（V6.0.0 → V6.1.17） | ① 删掉 `!e.text` 守卫，改为遍历 `entries` 子条目（并加 `Array.isArray` 兜住非数组 `entries`）；② `saw` 改为「观测到机制即置位」，issues[] 收集，末尾无问题才 pass；③ 战士斩杀与白骨爪斩杀按 `!en.isClawHit` 拆成两条独立路径，各按真实契约断言；④ 路径 3 由"见即判复发"改为校验 hpAfter 归零 + 组内 `deadFlag` | ①修漏报、②修死代码、③④修误报 |
| `tests/rules-replay.mjs`（V6.1.16 → V6.1.17） | ① 新增 `FACT_SPECS` 导入与 `factHist` 直方图；② 输出 `=== fact 覆盖 ===` 段：本批次产出种数 / 契约登记种数 + **零产出 factType 清单** + 契约外 factType 清单 | 让恒空转规则可归因到"数据源缺失"而非"规则写错"，排查不再靠肉眼 |

未动任何 `core/ ui/ player/ fx/ modules/` 业务代码。

### 验证结果

- `node --check` 两个改动文件均通过
- 负向测试（合成战报 9 例，**全过**）：
  - 负向 4 例 → 战士斩杀但组 `isDead=false` / 白骨爪斩杀缺 `clawTargetUid` / 闪避击杀 `hpAfter` 未归零 / 闪避击杀无 `deadFlag`，均 `fail` 且带定位信息 ✅
  - 正向 5 例 → 三条路径健康态 `pass`、无死亡场景 `skip`、空战报 `skip` ✅（确认不是"一刀切不报"）
- 复跑 `node tests/rules-replay.mjs`：`死亡特效缺失(回归) pass=93 fail=0 skip=27`（改前 skip=120），恒 skip 名单 **8 → 7 条**
- fact 覆盖段生效：产出 43 种 / 契约登记 74 种，列出 31 种零产出 factType（含 `clawHit` `clawExecute` `clawHeal` `nineYangHeal` `warriorExecute` `meteorShowerMain` 等）
- 静态快检 79 文件全 0、基线 `BASELINE-MATCH 18 场`、冒烟全过、`DEAD=1` 退出码 1（红线仍生效）
- 临时探针脚本 5 个已删除，未留在仓内

### 下轮目标

1. **123/132 同源漏报（证据已就位，可直接改）**：两条规则都只扫顶层 `e.text` / `e.isClawHit`，但爪击日志在 `attack-group.entries` 子条目里——与 133 修的是同一个坑。
   - `132-claw-damage.js`：顶层 `e.text.indexOf('九阴白骨爪')` 恒为 -1 → 改扫 `entries`，并用子条目的 `dmg`/`isExecute` 字段（文本正则已支持小数，可保留）
   - `123-claw-heal-spam.js`：顶层 `e.isClawHit` / `e.isHealEntry` 恒取不到 → 同样下沉到 `entries`
   - 预期这两条修完，恒 skip 由 7 → 5 条
2. **剩余 5 条空转规则按 fact 覆盖清单归因**：`九阳神功回复量(143)` 对应 `nineYangHeal` 零产出、`张三丰核心机制(141)` 对应 `fortifyRebound` 零产出——先判断是"业务侧 fact 未写入 step.log"（需报给业务侧修）还是"回放阵容/Buff 覆盖不到"（补回放覆盖），别急着改规则。
3. `butterflyNoHost 缺字段: sisterUid` 契约提醒仍每轮刷 2 条，待定性是契约漏登记还是真漏报。

---

## 复盘员修剪记录 — 2026-09-22 01:05（第 1 轮复盘）

> 非迭代轮，由「体检代码复盘」自动化执行。全量回归验证通过（冒烟/基线/回放/静态快检/DEAD 红线/语法全绿），三轮迭代改动全部成立、无需回滚。完整结论见 `tests/体检复盘报告.md` 第 1 轮。本轮修剪 2 处：

| 文件 | 动作 | 理由 |
| --- | --- | --- |
| `tests/health-rules/133-death-effect.js` | 版本头字节数 `~4.6KB` → `~6.3KB`（V6.1.17 编号不变） | 第 3 轮大改后体积涨到 6451 字节，版本头未同步；改后 `node --check` 通过、回放 pass=93 fail=0 不变 |
| `tests/_baseline-result.json` | 删除（全仓 grep 零引用后执行） | 第 2 轮删除三个临时 runner 后遗留的孤儿产物，无任何脚本读写，内容为陈旧基线输出 |

给迭代轮的提醒：下轮目标"修 123/132 后恒 skip 7→5"的预期与同轮 fact 覆盖证据（claw 类全零产出、关键字命中 0）冲突，动手前先跑 `KEYWORDS=白骨爪` 确认文本是否真出现在 entries 里，预期与证据冲突时优先信证据。基线红线命令固定为 `node tests/140-baseline.js --check`（baseline-check.mjs 已删）。

---

## 第 4 轮 — 2026-09-22 07:4x（123/132 假红修复 + 未记档 V6.1.18 补档）

### 跑了什么

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| 静态快检 | `node tests/123static-scan.js` | 79 文件，三类 issue 全 0，退出码 0 |
| 规则回放（改前） | `node tests/rules-replay.mjs` | **2 条规则报失败**：白骨爪回血刷屏 fail=11、九阴白骨爪伤害 fail=4；恒 skip 5 条（127/129/134/141/143） |
| 基线比对 | `node tests/140-baseline.js --check` | `BASELINE-MATCH 18 场`，退出码 0 |
| 冒烟 | `node tests/smoke-headless.mjs` | 全过（20 局 + player/47 导出齐全） |
| 语法 | `node --check` × 2 改动文件 | 均通过 |

### 开场即发现：文档与代码脱节（未记档改动）

日志尾（第 3 轮 + 复盘员修剪）之后存在**一轮未记档的改动**：`123` 已是 V6.1.18、`132` 头部自称 V6.1.18b，但迭代日志无对应轮次，且 `tests/` 遗留 3 个 `_probe` 临时文件。更关键的是 **132 的 V6.1.18b 头写了「连锁递减改为仅在同一 attack-group 内比较」，代码里 `prev` 仍在 j 循环外、根本没实现**——本轮顺延版本号为 V6.1.19 并补档。

### 发现的问题（本轮修 2 个）

**问题 1 —— 132 连锁递减判据跨组比较（头/码脱节，5 条假红，主修）**

根因三行：
- 根因：`prev` 声明在 j 循环外，同目标**跨组**比较伤害；组间目标可能被治疗（已损失血量变小 → ratioDmg 变小），跨组下降是合法态
- 证据：探针全量取证 20 种子 × stage4——**组内递减 0 处**（组内比较零漏报风险）、跨组递减 5 处（seed 3/10×2/11 + 1 条跨场残留，恰为现有 fail 样例 6.8→6.3 / 7.8→7.5 / 5.5→5.3）
- 影响范围：该规则每轮必炸假红，掩盖真实回归信号

**问题 2 —— 123「夹在中间」判据写错轴（11 条全误报，主修）**

根因三行：
- 根因：V6.1.18 用整场 log 的**首尾 claw 组下标**当区间，回血落区间内即判复发；真实战报一场有多个 claw 序列（如 seed1：claw组@6,27,48,68,124），每个序列末尾的合法汇总全落区间内
- 证据：探针邻域 dump——全部 11 条被判"夹中间"的回血都紧跟在某个含 clawHit 的组之后（j+1），正是"序列末尾统一汇总"的合法位置；而原刷屏 bug 的真实形态是回血被 push 进 claw 循环、落在**同一 attack-group 的两条 clawHit 之间**
- 影响范围：stage4 场次凡有 ≥2 个 claw 序列即误报，fail=11 全假

连带记录（本轮不修，报业务侧）：**孤儿回血**——无任何 clawHit 子条目的周芷若攻击组之后仍发「宋青书已满血，白骨爪未能回复生命」（seed1 j=88/89、seed10 j=53/54）。语义不明（claw 未 proc？还是漏渲染 claw 条目？），不猜业务语义，规则侧只排除不计罚。

### 改了什么

| 文件 | 改动 | 理由 |
| --- | --- | --- |
| `tests/health-rules/132-claw-damage.js`（V6.1.18 → V6.1.19） | `prev` 下沉到 j 循环内（每条 attack-group 重置），落实文件头承诺的组内比较；三条判据全保留 | 补上 V6.1.18b 没写的实现，修 5 条假红 |
| `tests/health-rules/123-claw-heal-spam.js`（V6.1.18 → V6.1.19） | ① 主信号改组内轴：同组内回血子条目两侧都有 clawHit 才判复发；② 辅助信号改回血信号总数（组内回血子条目 + 紧跟含 claw 组的顶层汇总）> 含 claw 组数；③ 不紧跟含 claw 组的顶层回血不计入（孤儿回血排除） | ①修误报主因（判据对轴）、②让辅助信号重新可触发（旧结构下结构性死分支）、③孤儿回血待定性防误报 |
| `tests/体检迭代日志.md` / `记录-更改履历.md` | 追加本轮 + 补记未记档 V6.1.18 的内容与遗留 | 文档债清偿 |
| `tests/_probe-*.mjs` × 5 | 全部删除（含未记档轮遗留 3 个 + 本轮取证 2 个） | 临时探针不入仓 |

未动任何 `core/ ui/ player/ fx/ modules/` 业务代码。

### 验证结果

- 合成负向/正向测试 **15/15 全过**（132：同组递减/底线/斩杀残留 3 负 + 跨组下降/同组递增/无爪击/不同目标 4 正；123：组内夹击 isHealEntry 与文本型 2 负 + 汇总超序列 1 负 + 合法双序列/孤儿回血/隔条汇总/无爪击/他人回血 5 正）——确认不是「一刀切不报」
- 复跑 `node tests/rules-replay.mjs`：`白骨爪回血刷屏 fail=11→0（pass=20）`、`九阴白骨爪伤害 fail=4→0（pass=20）`，**RESULT 无失败规则**；恒 skip 名单不变（5 条）
- 静态快检 79 文件全 0、基线 `BASELINE-MATCH 18 场`、冒烟全过、`DEAD=1` 退出码 1（红线仍生效，恒 skip 属数据源缺失）
- 临时探针 5 个已删除

### 下轮目标

1. **孤儿回血定性**（已留证：seed1 j=88/89、seed10 j=53/54，组内 0 条 clawHit 却发白骨爪回血汇总）——报业务侧确认是「claw 未 proc 也发汇总」的合法设计还是漏渲染，规则侧再决定是否恢复计入
2. **恒 skip 5 条（127/129/134/141/143）按 fact 覆盖清单归因**：对应 factType（butterflyNoHost/spiderFly/zhangTaunt/fortifyRebound/nineYangHeal）全部零产出，属数据源缺失，应整理成业务侧待修清单，别急着改规则
3. `butterflyNoHost 缺字段: sisterUid` 契约提醒仍每轮刷 2 条，待定性是契约漏登记还是真漏报

---

## 复盘员修剪记录 — 2026-09-22 07:55（第 2 轮复盘）

> 非迭代轮，由「体检代码复盘」自动化执行，覆盖第 4 轮（07:4x）。全量回归验证通过（冒烟 / 基线 BASELINE-MATCH 18 / 回放 0 fail 恒 skip 5 / DEAD=1 退出 1 / 静态快检 79 文件全 0 / node --check + py_compile 全过），第 4 轮改动全部成立、无需回滚，123/132 复跑确认 fail 11→0 / 4→0 且未波及其他 22 条。完整结论见 `tests/体检复盘报告.md` 第 2 轮。本轮无修剪项（仅清理 py_compile 产生的 `tools/__pycache__/` 缓存目录）。

给迭代轮的两个重点提醒（本轮系统性发现）：

1. **第 1~4 轮迭代 + 两轮复盘的全部改动仍未提交**（git 最后提交停在 `94e4a42`，11 个体检侧文件 modified/deleted 挂在工作区）。建议迭代轮每轮结束时 commit 一次，防误操作丢成果。
2. **趟次与日志要对账**：01:05~07:4x 之间名义多趟只落了 1 轮日志，且 V6.1.18 那趟跑了改动没写日志（第 4 轮才被发现补档）。请迭代自动化每趟无论有无改动都追加一行（"本轮无改动，各项全绿"也行），别让复盘员猜"没跑"还是"没记"。另：日志头写"30 分钟迭代"与实际 10 分钟 schedule 口径不一致，顺手对齐。

---

## 第 5 轮 — 2026-09-22 08:5x（127 恒空转真因修复：基础值路径错误）

### 跑了什么

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| 静态快检 | `node tests/123static-scan.js` | 79 文件 / 150ms，三类 issue 全 0，退出码 0 |
| 规则回放（改前） | `node tests/rules-replay.mjs` | 无失败规则；恒 skip 5 条（127/129/134/141/143）；`butterflyNoHost 缺字段 sisterUid` 警告 2 条 |
| 基线比对 | `node tests/140-baseline.js --check` | `BASELINE-MATCH 18 场`，退出码 0 |
| 冒烟 | `node tests/smoke-headless.mjs` | 全过（20 局 + FACT_SPECS 74 条 + player/47 导出齐全） |
| 关键字探针 | `KEYWORDS='化为蝴蝶附身\|飞回\|蝶变\|香消玉殒'` | 命中 445 / 444 / 892 / 2——附身文本大量存在，与 127 恒 skip 直接矛盾 |
| 语法 | `node --check tests/health-rules/127-butterfly-stack.js` | 通过 |

### 发现的问题（本轮修 1 个 + 定性 2 个遗留）

**问题 1 —— 127 恒空转真因是规则扫错路径，此前"数据源缺失"的归因错误（主修）**

根因三行：
- 根因：`127-butterfly-stack.js` 守卫读 `sister._baseAtk/_baseDef`，但业务口径基础值在 `unit.state._baseAtk/_baseDef`（core/02unit.js L157、modules/29battle-init.js L116/L178），单位根上从无此字段 → 守卫恒真 → 自登记以来 skip=120，从未断言过一次
- 证据：`butterflyAttach` fact 有产出（128 号规则同 log 同阵容 pass=48）+「化为蝴蝶附身」文本 120 场命中 445 次——数据源一直在，第 4 轮归因"127 ↔ butterflyNoHost 零产出"是错的
- 影响范围：附身 Buff 重复计算类回归完全无监控；全仓 grep 确认仅此一条规则用错 `_baseAtk` 路径，129/134/141/143 四条的"fact 零产出"归因复核无误（spiderFly/zhangTaunt/fortifyRebound/nineYangHeal 确在零产出清单）

**定性 1 —— `butterflyNoHost 缺字段: sisterUid`（挂了 4 轮，本轮定性：业务侧产出 bug）**
契约（infra/58 L97）要求 `sisterUid`，渲染层（render/30 L617）也要读它，但产出点 modules/27 L273 只写 `{ unitName: sister.name }`。修复=产出点补 `sisterUid: sister.uid`。属业务代码，体检侧不动，已入业务侧待修清单。

**定性 2 —— 孤儿回血 / 恒 skip 剩 4 条**：维持上轮结论，进业务侧待修清单（见履历 V6.1.20 第四节）。

### 改了什么

| 文件 | 改动 | 理由 |
| --- | --- | --- |
| `tests/health-rules/127-butterfly-stack.js`（V6.1.11 → V6.1.20） | 三处基础值读取下沉到 `sister.state._baseAtk/_baseDef`，守卫先校验 `state` 存在；双向判据与方向自解析逻辑不变 | 修恒空转真因（路径错误），让规则首次可真实断言 |

未动任何 `core/ ui/ player/ fx/ modules/` 业务代码。临时探针 1 个已删除。

### 验证结果

- 合成负向/正向测试 **8/8 全过**：右向攻含 Buff(5≠4) / 右向防非 0 / 左向防错(3≠5) / 左向攻非 0 → 4 条全 fail；双向正确 2 例 → pass；无附身文本 / 姐姐无 state → skip（确认不是"一刀切不报"）
- 复跑 `node tests/rules-replay.mjs`：`姐姐附身叠加(回归)` skip=120 → **pass=48 fail=0 skip=72**（48=有附身场数，与 128 对齐；业务行为判定全绿，无新假红）；RESULT 无失败规则；**恒 skip 名单 5 → 4 条**
- 静态快检 79 文件全 0、基线 `BASELINE-MATCH 18 场`、冒烟全过、`DEAD=1` 退出码 1（红线仍生效，剩 4 条属业务侧数据源缺失）
- 体检体检：SCAN_FILES 与 health-rules 目录 24 文件一一对应（123–146 无缺登），120–149 段位编号无重复无跳号，规则回放自动装载 24 条与文件数一致

### 下轮目标

1. **业务侧待修清单三件套移交/跟进**（体检侧已无可修项）：① `butterflyNoHost` 产出点补 `sisterUid`（modules/27 L273）；② 恒 skip 4 条对应 fact（spiderFly/zhangTaunt/fortifyRebound/nineYangHeal）产出点核查；③ 孤儿回血定性
2. 若业务侧修了 ①，下轮验证：回放 stderr 不再刷契约警告 + 139 号规则（spider-butterfly-target）对 noHost 场景的 uid 追踪恢复
3. 复盘员提醒的 commit 习惯：本轮起每轮结束 commit 体检侧改动（见下）

---

## 第 6 轮 — 2026-09-22 10:0x~10:1x（回放器吞并渲染数组 + 129/143 条目层级盲区）

### 开场即发现：又一轮未记档改动（同第 4 轮病症复发）

日志尾之后、上次提交（`ca82a83` 09:00）之后，工作区又出现一轮**没有写日志**的体检侧改动（mtime 09:56）：
`122health-utils.js` V6.1.14→V6.1.15、`health-rules/140-wei-dodge-cloud.js` V6.0.0→V6.0.1。
（同时 `记录-更改履历.md` 09:05 有一条 **V6.1.21 业务侧**条目「全自动档姐姐附身方向默认向左」，改的是 ui/65+player/42+player/49，不属体检侧，本轮不碰。）

本轮先核验这两处改动是否成立，再补档（**核验结论：两处均成立，无回滚项**）：

- **122 V6.1.15（checkBuffIcons 阵营过滤）成立**：三处口径对拍一致 —— `core/11` L27-28 `A._activeBuffs = state.activeBuffs.filter(b => b.target === CAMP_TYPES.ALLY || !b.target)`、`render/32` L368-371 同式、`infra/56` L116-119 `CAMP_TYPES.ALLY === 'ally'`。且全文件仅此一处读 `ctx.activeBuffs`，其后 L252/253/285/288 消费的都是已过滤的局部变量 → **改透了，无第二处漏改**。`checkFxOrphans` 的勘误也核对无误：真实调用点确为 121 L552 `runSettleChecks`（GAMEOVER 后执行），文案「战斗结束后」与实际时机一致，判据阈值（>5）未动。
- **140-wei V6.0.1（补"末回合中途授予"成因说明）成立**：全仓 grep 确认 `buffDodgeBonus` 只有 `core/11` L190（A 队）与 L221（B 队）两处写入，**均在 prepareRoundStart 内** → 「只在回合开始时重算」成立，头描述准确；且**未加豁免**（判据一行未放宽），回放仍 pass=8 fail=0。附一条口径提示写入本轮结论：该成因在**回放里不可复现**（回放注入 Buff 的时机恒在 prepareRoundStart 之前），所以这条规则"回放全绿"不等于"覆盖到了"——它的失败只可能来自浏览器真跑。
- 顺手补上 122 的 V6.1.15 头顶行缺失的**体积声明**（项目惯例 `Vx | ~N bytes | 日期 描述`，该行原缺中段与会话日期），`~27700 bytes`（实测 27732）。

### 跑了什么（改前基线，全部可复跑）

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| 静态快检 | `node tests/123static-scan.js` | 79 文件 / 158ms，三类 issue 全 0，退出码 0 |
| 规则回放（改前） | `node tests/rules-replay.mjs` | 无失败规则；**恒 skip 4 条**（129/134/141/143）；`butterflyNoHost 缺字段 sisterUid` 警告 2 条 |
| DEAD 红线 | `DEAD=1 node tests/rules-replay.mjs` | 退出码 1（严格模式生效） |
| 基线比对 | `node tests/140-baseline.js --check` | `BASELINE-MATCH 18 场`，退出码 0 |
| 冒烟 | `node tests/smoke-headless.mjs` | 全过（20 局 + FACT_SPECS 74 条 + player/47 导出齐全） |
| 关键字探针 | `KEYWORDS='九阳神功\|近身\|严阵以待\|如沐春风\|生生不息\|飞天\|免疫'` | 九阳神功/近身/飞天/免疫 **顶层全 0**；严阵以待 239、生生不息 674 —— 与"4 条恒 skip"直接矛盾 |
| 逐条取证探针 | `tests/_probe-{skip,dump,dead,yz,array,synth,registry}.mjs` × 7 | 见下（已全部删除） |
| 语法 | `node --check` × 121/122/123/124/140-baseline/rules-replay/smoke + health-rules 全 24 条 | 全过 |

**跑不动/没跑的**：`120test-runner.html` 及 121 的 UI 检查链路（checkBuffIcons / checkFxOrphans / runUIChecks / runSettleChecks）属浏览器端，**本轮回放无法覆盖**，只做了静态通读与调用点核对；`tools/health_auto_check.py` 本轮未跑（无 Python 侧改动）。

### 发现的问题（本轮修 3 个，同一根因族：回放数据模型 ↔ 规则扫描口径不一致）

**问题 1 —— 回放器把"渲染结果数组"整条吞掉（假绿放大器，本轮主因）**

根因三行：
- 根因：`tests/rules-replay.mjs` L141 旧实现 `const e = renderLog(f.factType, f.data); if (e) log.push(e);` 只认单对象；而 render/30 有渲染器**返回条目数组**（`renderZhangSwitchFact` 一次返回「切换形态 + 语音」两条），生产侧 `player/42player-core.js` L316-319 是用 `log.splice(i, 1, ...rendered)` **展开**入日志的 —— 两边口径不一致
- 证据：`_probe-array.mjs` 统计 120 场中 `renderLog` 返回数组的 factType **只有 zhangSwitch，共 58 次**（58 场各 1 次），全部以"数组对象"形态躺在 log 里（`log 总条目 13608，其中是数组的 58`）；该对象既无 `text` 也无 `entries`，任何按文本/子条目扫描的规则都看不见它 → **张无忌在 58 场里切了近战形态，日志里一次都查不到**
- 影响范围：① `134 张无忌近身切换时机` 恒 skip=120（假绿；`isZhangSwitch` 与「张无忌切换近战形态」文本双重判据全失效）；② `143` 的"变身前条目跳过百分比比较"判据失效（switchIdx 恒 -1）→ 实测**误报 29 条**（如 seed1 stage4 `九阳神功回复2 ≠ floor(180×12%)=21`，真因是**当时**血上限仅 90、`floor(90×12%)=10` 对得上，180 是切换后的终局值）；③ 结构性隐患：**任何多条目渲染的 fact 在回放里都不可见**

**问题 2 —— 129 扫错条目层级（恒空转）**

根因三行：
- 根因：规则逐条扫**顶层** `e.text` 找「🕷️ 飞天」，但飞天是 `attack-group` 的 `entries` 子条目（`render/30` L628 `renderSpiderFlyFact` → `{type:'info', spiderAction:'fly', spiderUid, text:'…剩余次数：N'}`），顶层 attack-group 自身没有 text
- 证据：`_probe-{skip,dead}.mjs` 120 场 —— 顶层命中 0，子条目命中 85 条 / 59 场；`spiderAction`/`spiderUid` 字段全在子条目上
- 影响范围：三道判据（每单位上限 3 次、剩余次数不得回退、不得停留不降）自登记以来**一次未跑**；`render/30` L548 的「蛛落」同为 spiderAction 但值为 `'return'`，不能误当飞天信号

**问题 3 —— 143 扫错条目层级（恒空转）**

根因三行：
- 根因：同上；九阳回复是子条目（`render/30` L587 `renderNineYangHealFact` → `{type:'info', text:'☀️ 九阳神功回复+…，…→…', isHealEntry, healAmount, healUnitUid}`），旧版只扫顶层 `e.text`
- 证据：120 场顶层 `九阳神功回复+` 命中 **0**、子条目命中 **899** 条；且九阳走 `core/15` L164 的 `data.declarations` 通道，`NINE_YANG_HEAL` **从不进 step.log** → 回放器的"零产出 factType"直方图把它误列为数据源缺失
- 影响范围：12% / 10% / 8% 三档回退检测 + 血线同步 + 非正回复，四条判据全部空转；并**推翻第 5 轮与复盘报告"143 属数据源缺失"的归因**（同 127 一样，是规则侧扫错口径，不是业务侧没产出）

### 改了什么

| 文件 | 改动 | 理由 |
| --- | --- | --- |
| `tests/rules-replay.mjs`（V6.1.17 → V6.1.22） | 渲染结果改为**数组则逐条展开**入日志（`if (Array.isArray(e)) for (const one of e) log.push(one)`），与生产侧 `player/42` L316-319 同口径；文件头补本次修复说明 | 修主因：数组型 fact 被整条吞掉 → 134 假绿 + 143 误报 29 条 |
| `tests/health-rules/129-spider-fly-count.js`（V6.1.10 → V6.1.22） | 新增 `collectFlyEntries()`：先把**顶层 + entries 子条目**的飞天条目收齐（判据字段 `spiderAction==='fly'`，文本仅兜底），再走原有三层断言；文件头补【数据契约】块 | 修恒空转真因（扫错层级）；三道判据首次真正生效 |
| `tests/health-rules/143-jiuyang-heal-pct.js`（V6.1.11 → V6.1.22） | ① 新增 `collectNineYangHeals()` 从子条目取九阳回复（判别位＝文本「九阳神功回复+」，**不可**用 `isHealEntry`——它被嗜血/热血/韦一笑/幻影/白骨爪**所有回血共用**）；② 归属单位改用契约字段 `healUnitUid` 定位（不再靠"扫 isZhang"猜），非张无忌的回复不属本规则口径；③ 血上限增益/变身等守卫改为**顶层+子条目两层都扫**（守卫面取超集＝只减少误报）；④ 新增 `checked` 计数：**一条都没断言到 → skip 而非 pass**（防空转假绿）；⑤ 文件头补【数据契约】块 | 修恒空转真因；同时修掉"无可判定条目却报 pass"的假绿 |
| `tests/122health-utils.js`（V6.1.15 行内） | 仅为 V6.1.15 头顶行补上缺失的体积声明 `~27700 bytes`（未改任何代码/判据） | 项目惯例 `Vx | ~N bytes | 日期 描述`，该行原缺中段与日期 |

未动任何 `core/ ui/ player/ fx/ modules/ infra/ render/ content/ assets/ scripts/ 展示与CG/` 业务代码，未动 `tools/` 下任何工具（含 `tools/health_auto_check.py`）。7 个临时探针已全部删除，未入仓。

### 验证结果

- **合成负向/正向测试 25/25 全过**（129：同单位 4 次超限 / 剩余次数回退 / 停留不降 / 负数 4 负 + 正常递减 / 两单位各 3 次（上限按单位算，不误报）/ 蛛落 return 不计入 / 无飞天 skip / 顶层直挂兜底 5 正；143：回退 10% / 回退 8% / 改成 15% / 非正回复 / 血线不同步 / 同场第二条错不提前放行 / 变身后仍回退 7 负 + 12% 正常 / 回满条目（整型与小数）/ carry 抬高血上限降级 / 变身前条目跳过 4 正 + 无九阳条目 / 归属单位无法定位 / uid 指向非张无忌 3 skip）——确认**没有变成"一刀切不报"**
  - ⚠ 合成测试首轮 2 条不过，其中 1 条暴露的是**我新写的代码自身的假绿**（九阳条目找不到归属单位时 `continue` 后直接 `return {fail:false}` = 未断言却报 pass），已修为一律 `return 'skip'`；另 1 条是测试用例自身写错（血上限取 180 却按 200 期望）。此处按"证据优先"记录，不掩过。
- 复跑 `node tests/rules-replay.mjs`（本期最终态）：`RESULT: 无失败规则；恒 skip(空转)规则 1 条`，其中 ——
  - `134 张无忌近身切换时机`：**skip=120 → pass=76 fail=0 skip=44**（假绿消除，首次真实断言）
  - `143 张无忌九阳神功回复量`：**skip=120 → pass=120 fail=0 skip=0**（29 条误报随主因修复全部消失，12% 口径与 content 的 `pct=0.12`／`healPct=12` 对得上）
  - `129 小昭妹飞天免疫次数超限`：**skip=120 → pass=61 fail=0 skip=59**
  - 恒 skip 名单 **4 → 1 条**（剩 `141 张三丰核心机制回归`）
  - 未波及其他 21 条：坚盾/分隔符/破防/小昭可选期等 4 条仍 pass=120 fail=0，其余 pass 数与改前一致（136 pass=65、146 pass=43、133 pass=93、123 pass=20、132 pass=20、127/128 pass=48、140 pass=8）
- 静态快检 79 文件三类 issue 全 0、退出码 0；基线 `BASELINE-MATCH 18 场`、退出码 0；冒烟全过
- `DEAD=1 node tests/rules-replay.mjs` 退出码 **1**（红线仍生效，剩余 1 条属结构性空转，见下轮目标）
- 语法：121/122/123/124/140-baseline/rules-replay/smoke + health-rules 全 24 条 `node --check` **0 报错**
- 体检体检（四表注册一致性）：health-rules 文件名 24 ↔ 121 `import rule70-93` 24 ↔ 121 `allRules` 24 ↔ 124 `RULE_META` 24 key（逐名与各规则导出 `name` 完全一致）↔ 123 `SCAN_FILES` 24 项，**探针逐名 diff 0 问题**，编号 123–146 无缺登、无多登、无撞号；版本头体积：121 `~42400`/实测 42459、140-baseline `~7600`/7497、133 `~6.3KB`/6451 均符，122 原缺已补

### 下轮目标

1. **`141 张三丰核心机制回归` —— 全条结构性空转，本轮已定性、未修（唯一剩项）**。根因：它**三处判据全读 `e.factType` / `e.data`**，而这两个字段在 battleLog 里**根本不存在** —— 生产侧 `player/42` L316-324 渲染后**显式剥离** `factType`/`data`（`extra` 循环里 `if (k !== 'factType' && k !== 'data')`），回放侧同样只存渲染条目。于是：① 「第 6 回合是否已开始」用 `e.factType === 'roundStart'` 数 → maxRound 恒 0 → 恒走 `maxRound < 6` 分支 → **120 场全 skip**（真实模型是 `e.type === 'round-start'` + 文本「第N回合」；探针实测 **120/120 场都有**、且 120 场 maxRound ≥ 6）；② 严阵以待用 `buffSummary + data.buff.name + data.allyTeamUids` → 真实模型是 `{type:'buff-summary', text:'🛡️ 严阵以待：张三丰 防御+50% 反弹50%'}`（20 场有张三丰、**20 场都有该摘要**）；③ 生生不息溢出用 `factType==='endlessBreath' + data.heal/overflow` → 真实模型是 `{type:'info', text:'☯ 生生不息：张三丰 生命已满，溢出27点转给X（其已满血，未生效）'}`（20 场有）。
   **为何本轮不顺手改（已取证的阻塞点，下轮不必重查）**：判据①③的文本契约干净可直接改；**判据②的"严阵以待"文案存在歧义** —— `render/30` L314-326 的 FORTIFY 分支对**两个阵营**都渲染成同一文案「🛡️ 严阵以待：{存活防战名} …」，而单机生产**只给明教选海克斯**（`player/42` L456 调 `handleBuffSelection(c, nextActiveBuffs)` 不传 camp → 默认 `ALLY`），故生产侧该文案只可能来自张三丰自身组件（`modules/26` L128-140，`allyTeamUids:[zhang.uid]`）；**但回放器 `tickAndPickBuffs` 给双方都注入**（`sides` 含 `CAMP_TYPES.ENEMY`），张三丰恰是六大派唯一防战 → 一旦回放替六大派抽到 fortify，文案与自身组件**完全同形**，判据②就会假绿。已取证：stage2 × 20 seed 下严阵以待摘要条数**恰等于 `终结回合数 − 5`**（seed1 10 回合×5、seed8 16 回合×11、seed2 14 回合×9 …）→ 当前批次确实只由自身组件产出，歧义尚未发作但**结构上存在**。
   下轮两条路：**(A)** 先修回放器的阵营保真度（单机只给明教注入，与 `player/42` 同口径）再落地文本判据；**(B)** 用"条数 = 终结回合数 − 5"这类更强的一致性判据替代"存在性"判据（更强，但对回放其余 buff 组合更敏感）。**不选折中口径、不加豁免**。
2. **回放器"零产出 factType"直方图的归因精度**（本轮副产物）：走 `data.declarations` 通道的 fact（如 `nineYangHeal`）从不进 `step.log`，却被直方图列为"数据源缺失"，容易把规则侧扫错层级误判成业务侧没产出（本轮 143、第 5 轮 127 都是这么被误归因的）。下轮考虑给直方图加标注或改为读渲染后的条目。
3. **业务侧待修清单（体检侧无动作，仅跟进/移交）**：① `butterflyNoHost` 产出点补 `sisterUid`（`modules/27` L273，契约 `infra/58` L97 + 渲染 `render/30` L617 都要；回放 stderr 每轮仍刷 2 条）；② `fortifyRebound`（严阵以待反弹）零产出：`modules/26` L150+ 有产出点、`render/30` L661-663 有渲染器，但 120 场一次未落日志 → 建议核查（**这是判据②之外更硬的"严阵以待真实生效"信号，若修好可一并解掉 141 的歧义**）；③ 孤儿回血定性（seed1 j=88/89、seed10 j=53/54，已挂 3 轮）；④ `zhangTaunt` 零产出（`modules/27` L50 走顶层 log、L93/95 走 `group.data.entries`，两条通道都没进日志）。
4. `140-wei` 的回放盲区已在第 6 轮注明：其失败成因只可能在浏览器真跑复现，回放 pass 不代表覆盖 —— 后续如做浏览器端冒烟，优先补这条。

### 推送

- 只提交体检侧：`git add tests/ "记录-更改履历.md"`（工作区里 `player/42`、`player/49`、`ui/65` 三处 **V6.1.21 业务侧**改动原样保留，不 add、不 stash）
- commit 主题：`test(体检): 回放器展开数组型渲染条目 + 129/143 补 entries 层级扫描（恒 skip 4→1）`（7 文件，+336/−38）
- **推送失败（两端均失败，如实记录）**：
  - `GIT_TERMINAL_PROMPT=0 timeout 180 git push gitee HEAD:workbuddy/main-87959581` → `fatal: could not read Username for 'https://gitee.com': terminal prompts disabled`（凭据缺失，退出码 124 由 timeout 收口）
  - `GIT_TERMINAL_PROMPT=0 timeout 150 git push origin HEAD:workbuddy/main-87959581` → `fatal: unable to access 'https://github.com/luzhigui/new-acode-repo.git/': Empty reply from server`（网络不可达，退出码 128）
  - **本地提交 `ad2e9f4` 待推**。未强推、未推 main、未动他人分支、未改 remote 地址、未写入任何 token。

---

## 复盘员修剪记录 — 2026-09-22 10:25（第 3 轮复盘）

> 非迭代轮，由「体检代码复盘」自动化执行，覆盖第 5 轮（08:5x）与第 6 轮（10:0x~10:1x）。**全量回归复跑全绿**：冒烟 / 基线 `BASELINE-MATCH 18 场` / 回放 **0 fail、恒 skip 1 条（141）** / `DEAD=1` 退出码 1 / 静态快检 79 文件全 0 / `node --check` 121/122/123/124/140-baseline/rules-replay/smoke + health-rules 全 24 条 + `py_compile` 全过。**第 5、6 轮除"129/134/143 被误判为业务侧数据源缺失"这条叙事结论外，其余日志声称的数字（127 pass=48 / 129 pass=61 / 134 pass=76 / 143 pass=120 / 恒 skip 4→1）逐条独立复跑命中，无虚报、无需回滚。** 完整结论见 `tests/体检复盘报告.md` 第 3 轮。

**本轮修剪 2 个文件（另清 1 个缓存目录）**：

| 文件 | 改动 | 理由 |
| --- | --- | --- |
| `tests/rules-replay.mjs`（V6.1.22 → V6.1.23） | 修正"零产出 factType"的归因文案 3 处（V6.1.17 文件头段 / 直方图注释 / 输出文本）：删掉"依赖其的规则必然空转 · 数据源缺失 · 改规则没用"的断言，改为列出四类真实成因（① `data.declarations` 等非 step.log 通道 ② 嵌在 `group.entries` 子层级 ③ 规则扫错层级 / 读了被剥离的 `e.factType`、`e.data` ④ 业务侧真未产出），并要求先取证后归因、勿直接移交业务侧 | **已被实测证伪的事实错误**：`nineYangHeal`/`spiderFly` 就在零产出清单里，但 129/143 规则已 pass=61/120，数据一直在。该文案已连续误导第 4/5 两轮，把 3 条规则错判成业务侧 bug |
| `tests/120test-runner.html`（V6.0.0 → V6.0.1） | ① 清理文件头 **30 个连续冗余 BOM**（`efbbbf`×30 = 90 字节纯垃圾）；② 体积声明 `~6000 bytes` → `~14700 bytes`（实测 14794） | 垃圾清理 + 120-149 段位头/码一致性（原声明低估 146%） |
| `tools/__pycache__/` | 删除 | 本轮 `py_compile` 校验产生的缓存目录，非源文件，不入仓 |

**明确不改（留给迭代轮，附理由）**：`tests/health-rules/141-zhangsanfeng-fortify-round.js` 三处判据全读 `e.factType`/`e.data`（渲染后被 player/42 剥离）→ 全条结构性空转、120 场全 skip。**本轮刻意不动它**：① 只修契约干净的判据 ①③ 而留下无法断言的判据 ②，会让规则"看起来 pass"而 ② 静默不覆盖 = 假绿，违反"不选折中口径、不加豁免"铁律；② 判据 ② 需先做第 6 轮给出的 A/B 决策（A 修回放阵营保真度 / B 更强一致性判据）。请迭代轮按 A/B 决策**一次性修透三处**。

**给迭代轮的四个提醒（本轮系统性发现，详见复盘报告第五节）**：

1. **归因方法的根因**：零产出直方图统计的是**渲染前**的原始 `factType`，而规则消费的是**渲染后**的条目，两者不是同一个数据模型 —— 这是三轮误判的共同根因。根治建议：回放时给渲染产出打**仅回放侧**的诊断标签 `_factType`，直方图改按它统计，误判从结构上消失。
2. **同题材规则数据模型互斥**：141 与 142 同批产出，**142 的文件头已写明**"渲染层已把 factType 抹掉，规则只能靠 type+text 识别"，141 却用了被剥离的 `e.factType`/`e.data`。建议把这段行规提升为 `health-rules/` 共用约定，并 grep 排查存量 24 条是否还有同类读法。
3. **文本探针是启发式、会假阳性**：`KEYWORDS='反弹'` 与 `KEYWORDS='严阵以待'` 命中**同为 239** —— 全部来自 buff-summary 摘要文本「🛡️ 严阵以待：… 防御+50% 反弹50%」，独立的 `renderFortifyReboundFact`（render/30 L660）一次未触发。用"反弹"当探针键会得出相反结论。探针结论必须附"该文本是否被其它机制共用"的检查。
4. **未记档改动第二次复发**（第 4 轮 → 第 6 轮）：122 V6.1.15 + 140-wei V6.0.1 又是一轮没写日志的改动（mtime 09:56）。第 2 轮复盘提的"每趟无论有无改动都记一行"至今未落实（同轮的"每轮 commit"建议已落实）。这条已成僵尸建议，需从**迭代自动化 prompt 层面**固化，靠自觉治不好。

**另需跨文档更正（本轮无权限，仅声明）**：第 6 轮已把 129/143/134 从"业务侧数据源缺失"撤出，但 `记录-更改履历.md` 的 V6.1.20 条目仍写着旧的错误归因 —— 请业务侧/迭代轮在 V6.1.22+ 条目里同步更正，否则后续照旧清单去 `core/` 查产出点就是白跑。

---
---

## 第 7 轮 — 2026-09-22 14:05（141 一次性修透 + 回放阵营保真度，恒 skip 归零 / DEAD 红线首次转绿）

### 跑了什么（开场全量基线）

- `node tests/123static-scan.js` → 79 文件三类 issue 全 0，退出码 0
- `node tests/rules-replay.mjs` → RESULT: 无失败规则；恒 skip 1 条（141）
- `DEAD=1 node tests/rules-replay.mjs` → 退出码 1（141 空转触发红线）
- `node tests/140-baseline.js --check` → BASELINE-MATCH 18 场，退出码 0
- `node tests/smoke-headless.mjs` → 全过
- 开场工作区状态：含复盘员 10:25 修剪轮的未提交改动（rules-replay V6.1.23 归因文案、120test-runner V6.0.1 BOM/体积清理、两份日志），本轮随第 7 轮一并归档提交。

### 本轮主修（第 6 轮遗留的 141 A/B 决策 → 选 A 落地）

**问题 1：回放器 Buff 注入阵营保真度缺失（A 路线的前提）**

- 根因：`tickAndPickBuffs` 给明教/六大派**双方**各补选一个，但生产单机口径是只给明教注入（player/49battle-flow.js L19 `handleBuffSelection` 默认 `camp=CAMP_TYPES.ALLY`，player/42 L455-456 调用不传 camp；双方各选是联网 PVP 专属 `handlePvpBuffSelection`）。旧注释声称口径来自 player/42，与事实不符。
- 证据：render/30 L325 的 FORTIFY 摘要对任意阵营渲染同形文案「🛡️ 严阵以待：{存活防战名} 防御+50% 反弹50%」，张三丰是六大派唯一防战 → 回放替六大派注入 FORTIFY 时，摘要与张三丰自身组件（modules/26）产出完全同形，141 的存在性判据无法区分来源（第 6 轮已取证，本轮在 ally-only 批次复核）。
- 影响范围：回放会制造生产单机不存在的战斗场景（六大派持海克斯），既污染 141 判据又虚增部分规则覆盖。
- 改动：`tests/rules-replay.mjs`（V6.1.23 → V6.1.24）`sides` 只留 ALLY；顺带清掉签名与两处调用点的 `enemy` 死参数、改写口径注释。
- **过程事故（如实记录）**：签名/调用点编辑因文件锁（EBUSY）**静默丢失一半**，导致回放器一度处于"6 参签名 + 5 参调用"的错位状态——从第 2 回合起 `pickNew=undefined`，回合 3/6/9 补选全部静默失效，140 覆盖归零、129/134/136/146 等计数全被扭曲。靠自制双模式探针（both/ally）+ 逐条目 diff 定位到分叉点后才发现。教训：**Edit 工具报成功≠落盘，关键编辑后必须 grep 复核**；本轮回放器三处已用 python 整写并验证。这也意味着本轮开场跑的那次"ally 模式下 140 pass=0"是**假信号**，修正签名后 140 pass=10（与探针 ally 模式完全一致）。

**问题 2：141 张三丰核心机制回归全条结构性空转（第 5/6 轮遗留，本轮一次性修透三处）**

- 根因：三处判据全读 battleLog 里不存在的 `e.factType` / `e.data`（生产侧 player/42 L316-324 渲染后显式剥离；142 头里早写明该行规，141 未遵守）→ 判据① roundStart 恒 0 → 恒走 `maxRound<6` → 120 场全 skip。
- 证据：判据①真实模型 = `{type:'round-start', text:'———— 第N回合开始 ————'}`（render/30 L697）；判据②真实模型 = `{type:'buff-summary', text:'…严阵以待：张三丰 防御+50%…'}`（render/30 L325，且 ally-only 注入后产出点唯一=张三丰自身组件）；判据③真实模型 = `☯ 生生不息：张三丰 …` 文本行（render/30 L668-683，三种溢出文案全带「溢出」，纯回血回退后满血只渲染"生命已满"无尾巴）。ally-only 批次实测：张三丰存活且 ≥6 回合的 19 场，严阵以待首现回合 **19/19 全= 6** → 时机断言可落地。
- 影响范围：该规则自登记以来一次断言都没跑过（假绿），V6.1.6/V6.1.7 的三处张三丰回退全部无监控。
- 改动：`tests/health-rules/141-zhangsanfeng-fortify-round.js`（V6.1.10 → V6.1.24）整体重写，只认渲染后条目（type+text）：① 存在性 + **首现回合必须恰为 6**（比原"存在性"更强：提前=触发点被改小，推迟=round 回退成 10，都能抓）；② 如沐春风文本出现即 fail（原有判据保留）；③「生命已满」且无「溢出」即 fail；冒充源排查：「严阵以待反弹N给X」（fortifyRebound 行，无冒号、type=info）与己方 FORTIFY 摘要（不含张三丰名）均不干扰；他人"转给张三丰"行必带「溢出」，不误判。

### 验证结果

- 合成负向/正向 **15/15 全符合预期**（5 负：无摘要/推迟到 8/提前到 5/如沐春风重现/满血无溢出；7 正：三种溢出文案/反弹行不冒充/他人转给不误判/重复摘要首现仍=6/无满血行；3 skip：无张三丰/阵亡/第 5 回合结束）——确认没有变成"一刀切不报"
- 复跑回放：**`141 pass=0 skip=120 → pass=19 fail=0`**（与 142 的 pass=19 精确互证）；**恒 skip 名单 1 → 0 条，`DEAD=1` 退出码 1 → 0，空转红线首次全绿**
- 阵营保真度对覆盖的影响（both → ally，120 场）：宋青书未命中 11→8、死亡特效 93→85、134 76→46、136 65→39、146 43→29、129 61→53、143 120→119、145 20→31、140 8→10 —— 全部可用"敌方不再持 buff + 战局位移"解释，无规则归零、无新增 fail
- 静态快检 79 文件全 0；基线 **BASELINE-MATCH 18 场**（140-baseline 独立重放恒 `activeBuffs:[]`，不走 tickAndPickBuffs，基线不受注入口径影响）；冒烟全过
- 语法：121/122/123/124/140-baseline/rules-replay/smoke + health-rules 全 24 条 `node --check` 0 报错
- 探针 3 个（_probe-140141 / _probe-copy / _probe-141-cases）+ 临时 stderr 文件用完即删，不入仓

### 下轮目标

1. **给回放器渲染条目打 `_factType` 回放侧诊断标签**（复盘报告第 3 轮的优先级最高建议，本轮未做）：直方图改按标签统计，"零产出"清单只剩"渲染器都没产出"一类，四类人肉归因退役。
2. 覆盖面跟进：ally-only 后 136（65→39）、146（43→29）等覆盖下降是口径保真的**真实代价**（敌方持 buff 场景生产单机不存在）；若想补回覆盖，方向是把"补选节奏/种子轮转"参数化，而不是恢复双方注入。
3. 业务侧待修清单跟进：butterflyNoHost 补 sisterUid（回放 stderr 每轮仍刷 2 条契约警告）、fortifyRebound 零产出、孤儿回血、zhangTaunt 零产出 —— 均维持既往结论。**注意：履历 V6.1.20 里"129/134/141/143 属业务侧数据源缺失"的旧归因已被第 6/7 轮实测推翻，业务侧请勿按旧清单去 core/ 查产出点（本轮已在履历 V6.1.24 条目内声明更正）。**
4. `140-wei` 的"末回合中途授予"回放盲区维持第 6 轮结论：回放 pass=10 ≠ 覆盖到该成因，仍只能靠浏览器真跑暴露。
### 推送

- `GIT_TERMINAL_PROMPT=0 timeout 180 git push gitee HEAD:workbuddy/main-87959581` → `fatal: could not read Username for 'https://gitee.com': terminal prompts disabled`（凭据缺失）
- `GIT_TERMINAL_PROMPT=0 timeout 180 git push origin HEAD:workbuddy/main-87959581` → `fatal: unable to access 'https://github.com/luzhigui/new-acode-repo.git/': Recv failure: Connection was reset`（网络不可达）
- **推送失败：两端均失败（gitee 无凭据 / github 连接重置）；本地提交 `ca82a83`、`ad2e9f4`、`1bfd688`、`e40b84a` 待推**。未强推、未推 main、未动他人分支、未改 remote 地址、未写入任何 token。

---

## 第 8 轮 — 2026-09-22 15:0x（fact 覆盖直方图计数口径修正：嵌套子 fact 盲区）

### 跑了什么（开场全量基线，改前）

- `node tests/123static-scan.js` → 79 文件三类 issue 全 0，退出码 0
- `node tests/rules-replay.mjs` → `RESULT: 无失败规则；恒 skip(空转)规则 0 条`；fact 覆盖 **产出 41 种 / 登记 74 种，零产出 33 种**
- `DEAD=1 node tests/rules-replay.mjs` → 恒 skip 0 条，退出码 0（空转红线自第 7 轮起保持全绿）
- `node tests/140-baseline.js --check` → `BASELINE-MATCH 18 场与基线一致`，退出码 0
- `node tests/smoke-headless.mjs` → 全过（20 局跑完、20/20 分胜负、FACT_SPECS 74 条、player/47 的 12 导出齐全）
- 开场工作区：`player/42`、`player/49`、`ui/65` 三处 V6.1.21 业务侧改动仍未提交 —— 本轮**原样保留**，未 add、未 stash、未碰
- 未跑：`tests/120test-runner.html`（浏览器侧，node 不做端到端），只做静态分析

### 发现的问题

**问题 1（本轮主修）—— fact 覆盖直方图的计数口径漏掉整类"嵌套子 fact"**

根因三行：
- 根因：直方图只按 `step.log` **顶层** fact 的 `f.factType` 计数（rules-replay L169-170），但引擎还把大量 fact 作为**子 fact** 嵌进父 fact 的 data 里（`data.entries[]` / `data.phantomFact` / `data.dmgCalc.bonusEntries[]` / `data.dmgResult.bonusEntries[]`），render/30 L200-204 对这些子条目逐条 `projectFactEntry` 渲染进 `attack-group.entries` —— 规则消费的正是这些条目，两边不是同一个数据模型
- 证据：`_probe-factnest.mjs` 递归统计（30 场无 buff 注入）—— 顶层产出 32 种、嵌套产出 16 种，**16 种全部 top=0**（fortifyRebound ×21、zhangTaunt ×36、nineYangHeal ×216、clawHit ×98、fortifyShield ×563、qianKunUpgraded ×1104…）；带 buff 注入的 120 场完整口径下"仅嵌套出现"共 **23 种**，条数见下方验证结果
- 影响范围：① 直方图"零产出 33 种"里 **16 种是假报**（修正后 33→10）；② **已造成业务侧待修清单错项** —— `fortifyRebound`、`zhangTaunt` 被当成"业务侧真未产出"写进履历 V6.1.20 / V6.1.24 与复盘报告第 1~3 轮，业务侧照单去 `core/` 查产出点必然白跑（实测产出点健在：fortifyRebound = core/12 L378 + modules/26 L157；zhangTaunt = modules/27 L50 / L93 / L95）；③ 这是复盘报告第 3 轮"问题 A"（三轮误判共同根因）里尚未被结构性堵住的那一半

**问题 2 —— 137 / 142 仍读渲染后已被剥离的 `e.factType` / `e.data`（死分支 + 注释与实现不符）**

根因三行：
- 根因：生产侧 player/42 L316-324 渲染时显式剥离 `factType` / `data`，规则只能靠 `type` + `text` 识别（142 文件头早写明该行规、141 V6.1.24 已按此重写）；137 与 142 各留了 1~2 处读 `e.factType === 'roundStart'` / `e.data.round` 的分支，恒不成立
- 证据：全仓 grep `tests/health-rules` 的 `.factType` / `.data` → 命中恰好只剩 137（L30-31）、142（L45、L65）；137 文件头还把渲染口径标成"render/30 第466行"，实测该行是 `renderKuLianPriorityFact`（苦练勤学），真正的"苦练强化"在 **L471**
- 影响范围：纯死代码，不影响当前判据（回合号都能从 `type='round-start'` + 文本兜底取到，实跑 pass=20 / pass=19），但注释向后来者承诺了不存在的兼容性（"兼容 factType='roundStart'(可无 text)"），且正是复盘报告第 3 轮"问题 B"点名要清的存量；137 的过期行号属"规则引用已改掉的代码路径"

**问题 3（仅记录，不改）—— 业务侧清单里 `butterflyNoHost` 的证据行已过期**

- 履历 V6.1.20 / V6.1.24 与复盘报告记的证据是"回放 stderr 每轮仍刷 2 条契约警告"。本轮把回放 stderr 全量落盘统计：**契约警告 0 条**（只有 4 行 node 的 MODULE_TYPELESS 警告）。`butterflyNoHost` 在 120 场里顶层与嵌套均 0 产出（"无宿主"分支 modules/27 L271-274 要求己方除小昭外无存活非拒马单位，本批次从未触发）
- **结论仍成立**：产出点 modules/27 L273 的 `data: { unitName: sister.name }` 与契约 `requiredFields: ['sisterUid']`（infra/58 L97）静态不符，属业务侧产出 bug，只是"场景未覆盖"而非"每轮回放都在报"。改的是证据口径，不是结论

**问题 4（仅记录，不改）—— 剩余 10 条零产出逐条定位产出点**

xiaoZhaoHorse（modules/27 L537）、spiderFly（modules/27 L604）、spiderDoubleStrike（modules/27 L559）、spiderDeadTarget（modules/20 L169）、xinHunDeath（core/15 L454）、qianKunDerived（modules/27 L227）、butterflyNoHost（modules/27 L273）、butterflyHostDead（modules/27 L334）、flySkip（core/10 L35）—— 9 条都有产出点，属"守卫/分支本批次未触发"；**`meteorSplashGrowth` 只在 infra/56 L30 与 infra/58 L111 登记，全仓无产出点 → 报业务侧待修**（唯一一条真·零产出）

### 改了什么

| 文件 | 改动 | 理由 |
| --- | --- | --- |
| `tests/rules-replay.mjs`（V6.1.24 → **V6.1.25**） | ① 新增 `collectNestedFacts()` 递归收集父 fact 内嵌的子 fact，`factHist` 改为"顶层 + 嵌套"统计，另设 `factHistTop` / `factHistNested`；② 输出新增"计数口径"行与"仅嵌套出现（N 种，带条数）"清单；③ 零产出清单文案由"成因四类"改为"剩余三类"（原成因 ② 已被计数口径消化）；④ 文件头补 V6.1.25 段 + "成因 ② 后注"；⑤ 补体积声明 `~24400 bytes`（原缺） | 修主因：23 类嵌套子 fact 被误报零产出，并直接造成 fortifyRebound / zhangTaunt 被错移交业务侧 |
| `tests/health-rules/137-kulian-prompt.js`（V6.1.11 → **V6.1.25**） | 删掉恒不成立的 `e.factType === 'roundStart' && e.data.round` 分支（回合只按 `type='round-start'` + 文本识别）；文件头补 V6.1.25 清理说明；修正渲染口径行号 `第466行` → `L471` | 死代码 + 引用过期；与 141 / 142 统一到"只读渲染后条目"行规 |
| `tests/health-rules/142-zhangsanfeng-endless-roundstart.js`（V6.1.9 → **V6.1.25**） | `isRoundStart()` 去掉 `|| e.factType === 'roundStart'`；回合号改只从文本取（去掉 `e.data.round` 死分支）；文件头补 V6.1.25 清理说明 | 同上；判据与口径一字未动 |

未动任何 `core/ ui/ player/ fx/ modules/ infra/ render/ content/ assets/ scripts/ 展示与CG/` 业务代码，未动 `tools/` 下任何工具（含 `tools/health_auto_check.py`）。

### 验证结果

- **守卫必要性对照实验**（复制实现成 3 个变体，逐条 diff 计数）：去掉 WeakSet 环路守卫 → `qianKunUpgraded 1531→4593`、`qianKunBasic 6→18`（同一子 fact 从多条路径可达被重复计数）→ **守卫必需**；去掉 `log` 键跳过 / 去掉 depth≤8 → 23 项计数**逐条完全一致**、耗时 1.58s vs 1.50s → 两者在本批次**未生效**，但保留并在注释里写明理由（`data.declarations` 会带声明对象进 fact data（core/04 L194/L200），而声明对象里直接挂着 step.log 本体引用（modules/27 L256 / L485），不跳会把整条 step.log 重复计入）。三条守卫的必要性/实测结论都写进了代码注释，避免后人当成无理由的防御代码删掉
- 合成负向/正向 **14/14 全符合预期**（137：+0攻占位 fail / 自身三倍 fail / 数值≠1/2/5 fail / 同回合 2 次 fail 且指名第4回合 / 全场 25 次 fail / 每回合 1 次 pass / 无苦练 skip / 嵌套形态 skip；142：3 回合齐全 pass / 第2回合缺 fail 且指名第2回合 / 全缺 fail / 无张三丰 skip / 阵亡 skip）—— 确认没有变成"一刀切不报"。**其中 1 条是我探针自身的假设错**（预设"苦练只出现在 entries 子条目里应 pass"），按证据（core/15 L418 苦练强化 fact 直接 push 顶层 log）改为期望 skip 并注明，不掩过
- 复跑回放（120 场，改后）：`RESULT: 无失败规则；恒 skip(空转)规则 0 条`，24 条规则 pass/fail 数与改前**逐条一致**（无规则受影响）；fact 覆盖 **产出 41 → 64 种 / 登记 74 种**，零产出 **33 → 10 种**；新增"仅嵌套出现 23 种"清单：qianKunUpgraded ×1531、rangedGrowth ×1521、fortifyShield ×3161、nineYangHeal ×797、weiLeech ×883、breakDef ×1045、fortifyRebound ×190、zhangTaunt ×80、clawHit ×421、clawExecute ×23、phantomDisguiseHeal ×145、phantomConfuse ×148、phantomReveal ×110、rongHuiBonus ×99、warriorExecute ×102、horseRebound ×144、clawHeal ×43、hotBloodHeal ×333、meteorShowerMain ×136、meteorShowerSplash ×115、qianKunBasic ×6、windAssaultSplash ×100、bloodthirstLeech ×79
- 静态快检 79 文件三类 issue 全 0；基线 `BASELINE-MATCH 18 场`；冒烟全过；`DEAD=1` 退出码 0
- 语法：121 / 122 / 123 / 124 / 140-baseline / rules-replay / smoke + health-rules 全 24 条 `node --check` **0 报错**
- 体检体检（四表注册一致性，程序化逐项 diff）：health-rules **24** 文件（编号 123–146 连续、0 重复 0 跳号）↔ 121 引用 24 个 + `allRules` 24 条 ↔ 124 RULE_META 24 个 name 全登记 ↔ 123 SCAN_FILES 24 项 —— **四项互缺登均为空**；rules-replay 版本头体积 `~24400` / 实测 24451
- 取证探针 6 个（_probe-factnest / _probe-r8 / _probe-reg / _probe-v-nologskip / _probe-v-nocycle / _probe-v-minimal）用完即删，不入仓

### 下轮目标

1. **`_factType` 回放侧诊断标签**（复盘报告第 3 轮"问题 A"的另一半，仍未做）：本轮的嵌套计数解决的是"引擎有没有产出 fact"，还没解决"渲染器有没有把它变成规则看得见的条目"。可在 `renderLog` 返回后按顶层 factType 给渲染条目打**仅回放侧**标签，直方图增列"产出但渲染 0 条目"一类
2. **`meteorSplashGrowth` 报业务侧**：infra/56 L30 + infra/58 L111 有登记、全仓无产出点，是唯一一条真·零产出
3. **业务侧清单更正**：`fortifyRebound`、`zhangTaunt` 应从"业务侧零产出"**撤出**（本轮实测有产出 190 / 80 条）；`butterflyNoHost` 保留结论、**改证据口径**（不是"stderr 每轮刷警告"，是"静态契约不符 + 无宿主分支未触发"）。清单现剩：butterflyNoHost 补 sisterUid、meteorSplashGrowth 无产出点、孤儿回血定性、zhangTaunt 顶层通道（modules/27 L50）零命中
4. `140-wei` 的"末回合中途授予"回放盲区维持第 6 轮结论：回放 pass ≠ 覆盖该成因，只能靠浏览器真跑暴露

### 推送

- 只提交体检侧：`git add tests/ "记录-更改履历.md"`（工作区 `player/42`、`player/49`、`ui/65` 三处 V6.1.21 业务侧改动原样保留，未 add、未 stash）
- commit 主题：`test(体检): fact覆盖直方图补嵌套子fact口径(零产出33→10) + 137/142清死分支`（5 文件，+192/−25，本地 `2e27e9e`）
- **推送失败（两端均失败，如实记录）**：
  - `GIT_TERMINAL_PROMPT=0 timeout 180 git push gitee HEAD:workbuddy/main-87959581` → `fatal: could not read Username for 'https://gitee.com': terminal prompts disabled`（凭据缺失，退出码 124 由 timeout 收口）
  - `GIT_TERMINAL_PROMPT=0 timeout 170 git push origin HEAD:workbuddy/main-87959581` → `fatal: could not read Username for 'https://github.com': terminal prompts disabled`（凭据缺失，退出码 124）
  - 两个远端 URL 均为 https 且未配凭据（`gitee` = gitee.com/luzhigui/new-acode-repo.git，`origin` = github.com/luzhigui/new-acode-repo.git）；未 fetch 到 `workbuddy/main-87959581` 跟踪 ref，无法判断远端是否落后
  - **本地 6 个提交待推**：`ca82a83`、`ad2e9f4`、`1bfd688`、`e40b84a`、`0750d1b`、`2e27e9e`。未强推、未推 main、未动他人分支、未改 remote 地址、未写入任何 token。

---

## 第 9 轮 — 2026-09-22 15:5x~16:0x（零产出清单改为机检归因：补上"本批次未触发"这一类）

> 本轮起**改由当前对话手动迭代**（不再走定时自动化：每次触发会新建会话、非 hy4 模型、额外计费）。
> 原定目标 141 已在第 7 轮（`e40b84a`，V6.1.24 按渲染后模型重写）修透，本轮换靶。

### 跑了什么

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| 静态快检 | `node tests/123static-scan.js` | 79 文件 / 1785ms，三类 issue 全 0，退出码 0 |
| 规则回放（改前） | `node tests/rules-replay.mjs` | 120 场 24 规则：**无失败规则、恒 skip 0 条**；fact 覆盖产出 64 / 登记 74，**零产出 10 种全堆在一个 ⚠ 清单里** |
| DEAD 红线 | `DEAD=1 node tests/rules-replay.mjs` | 退出码 0（第 7 轮起恒 skip 归零后红钱持续转绿） |
| 基线比对 | `node tests/140-baseline.js --check` | `BASELINE-MATCH 18 场`，退出码 0 |
| 冒烟 | `node tests/smoke-headless.mjs` | 全过（20/20 局分出胜负，player/47 导出齐全） |

### 发现的问题（本轮修 1 个）

**问题 1 —— 零产出清单缺了占比最高的一类成因，且归因全靠人工（主修）**

根因三行：
- 根因：V6.1.23 起附的"四类成因自查"文字只列了 ①③④，**漏了"本批次回放没跑到该分支"**这一类；清单本身也不区分个案属哪一类，定性全靠人工重跑 KEYWORDS 探针
- 证据：对 10 项逐个取证——`xinHunDeath` ← `core/15` L454、`flySkip` ← `core/10` L35、`spiderFly` ← `modules/27` L604、`butterflyNoHost` ← `modules/27` L273、`butterflyHostDead` ← L334、`xiaoZhaoHorse` ← L537、`spiderDoubleStrike` ← L559、`spiderDeadTarget` ← `modules/20` L169、`qianKunDerived` ← `modules/27` L227 —— **9 项都有明确 emit 点**，只是 120 场的阵容/条件没触发；仅 `meteorSplashGrowth` 全仓无 emit 点（唯一的孤儿登记，结论与第 8 轮一致）
- 影响范围：这个缺口 + 人工定性，正是第 4/5 轮把 129/134/141/143 错判成"业务侧数据源缺失"写进业务侧待修清单的直接来源；不机检化则每轮重犯

**连带的方法论坑（写进判据注释，防后人重踩）**：按**裸值**（`'clawHit'`）检索会大面积漏判——代码里写的是**常量名**（`FACT_TYPES.CLAW_HIT`）；反之若放宽成"任何出现 `FACT_TYPES.KEY` 的行都算"，`render/31` 的渲染映射（如 `[FACT_TYPES.METEOR_SPLASH_GROWTH]: (data,index)=>…`）会把孤儿登记误判成"有产出点"，孤儿类永远查不出来。

### 改了什么

| 文件 | 改动 | 理由 |
| --- | --- | --- |
| `tests/rules-replay.mjs`（V6.1.25 → **V6.1.26**） | ① 新增 `collectEmitSites()`：反查 `FACT_TYPES`/`FLY_MODE_TYPES` 常量名 → 只认 `factType: FACT_TYPES.KEY` 形态的 **emit 赋值**（排除枚举定义、契约登记、渲染映射）→ 返回 `file:line`；② 零产出输出改为**机检分两类**：【本批次未触发】附 emit 点 + 处置指引（补 SEEDS/STAGES 或构造场景，勿移交业务侧）/【孤儿登记】（ infra/58 数据卫生，由 infra 侧增删）；③ 源码读不到时如实打印"取证跳过"，不猜；④ 新增 `PROBE_ZERO=a,b` 取证自检开关（可传合成名做负向测试）；⑤ 同步清掉旧注释里那段已被取代的"四类成因自查"文字与过时的 V6.1.17/1.23 口径描述 | 让"零产出"清单从"待人工排查的疑点"变成"自带证据的结论"，并给判据留长期自检入口 |
| `tests/_probe-factsites.mjs` | **已删除**（取证用临时探针，不入仓） | 探针不入仓惯例 |

未动任何 `core/ ui/ player/ fx/ modules/ infra/ render/ tools/` 代码。

### 验证结果

- **正对照 64/64**：本批次有产出的 64 个 factType 全部检出 emit 点，**0 假阴性** —— 确认判据不是"一律判无产出"
- **负向测试**：① 合成名 `totallyFakeFactName` → 落入【孤儿登记】✅（不是"一律判有产出点"）；② `meteorSplashGrowth`（只有 `render/31` L230 渲染映射、无 emit）→ 落入【孤儿登记】✅（证明渲染映射被正确排除）；③ 正向 `clawHit` → 落回【本批次未触发】且指到 `core/15:365` ✅
- 复跑 `node tests/rules-replay.mjs`：**【本批次未触发】9 种**（附 emit 点）/ **【孤儿登记】1 种**（meteorSplashGrowth）；`RESULT: 无失败规则；恒 skip 0 条`
- 静态快检 79 文件全 0、`BASELINE-MATCH 18 场`、冒烟全过、`DEAD=1` 退出码 0、`node --check` 通过

### 下轮目标

1. **`_factType` 回放侧诊断标签**（第 8 轮遗留 #1）：本轮解决的是"引擎有没有 emit 点"，还没解决"渲染器有没有把它变成规则看得见的条目"；给渲染条目打仅回放侧标签后可增列"有产出但渲染 0 条目"一类
2. **`air cover` 补覆盖率（对应新分类的处置）**：9 项"本批次未触发"里挑 2~3 项按 emit 点条件构造场景（如 `xinHunDeath` 需要新婚+周芷若阵亡、`flySkip` 需要飞行单位行动），把零产出数压下来并顺带给相关规则真覆盖
3. **孤儿登记移交 infra 侧**：`meteorSplashGrowth` 在 `infra/56` L30 + `infra/58` L111 有登记、全仓无 emit 点，需 decide：删登记 vs 补产出（`core/16` L167 现只当 addMod group 用）
4. 推送仍待解决凭据（见下）

### 推送

- 只 add 体检侧：`tests/rules-replay.mjs`、`tests/体检迭代日志.md`、`记录-更改履历.md`（工作区 `player/42`、`player/49`、`ui/65` 三处业务侧改动原样保留，未 add、未 stash）
- ⚠ **本轮踩到一个 git 事故并已修复（记此防复发）**：第一次 `git add tests/ "记录-更改履历.md"` + commit 后 `git show --stat` 显示 **151 个文件被记为新增**（README/assets/ui 全在里面）。取证：`git log --format="%h parents=[%p]"` 显示该 commit `parents=[]` —— 是**根提交**，说明分支在我提交前已被重置成"无父"状态（reflog 显示 HEAD@{1} 是另一会话的 `199ca18`，正常链 `71275f6 → 199ca18`）。文件内容与 blob 均正确，只是父链断开、diff 无法对账。
- 修复：`git reset --mixed 199ca18`（**不动工作区**）→ 分支指回正常父、`git status` 恢复为预期的少量改动 → 精确 `git add` 三个体检侧文件重新提交 = `4430d12`（parents=[199ca18]，3 文件 +178/−13）
- **教训：提交后必须至少校验一次 `git log --format="%h parents=[%p]"` 与提交文件数**，只看"commit 成功"会漏掉父链断裂这类静默事故
- ✅ **推送本轮成功（两端都通了）**：
  - `GIT_TERMINAL_PROMPT=0 timeout 100 git push gitee HEAD:workbuddy/main-87959581` → `* [new branch] HEAD -> workbuddy/main-87959581`，退出码 0
  - `GIT_TERMINAL_PROMPT=0 timeout 100 git push origin HEAD:workbuddy/main-87959581` → `* [new branch] HEAD -> workbuddy/main-87959581`，退出码 0
  - 即**积压 8 个本地提交（ca82a83…4430d12）一次性推送到两个远端**，此前"gitee 无凭据 / github 不可达"的现象消失（凭据已可用）
  - 未强推、未推 main、未动他人分支、未改 remote 地址、未写入任何 token；多人/多会话共用一个 worktree 时尤其要查


---

## 第 10 轮 — 2026-09-22 21:2x~09-23 07:5x（fact 覆盖补「渲染产出」口径 + 声明≠fact 判据修正）

> 开场读档：第 9 轮已把零产出清单改成机检归因（【本批次未触发】9 /【孤儿登记】1），并把积压 8 个提交一次性推通两端。
> 本轮接的是**复盘报告第 3 轮问题 A 的另一半**（连续挂了 3 轮的 `_factType` 标签议题）。

### 跑了什么

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| 静态快检 | `node tests/123static-scan.js` | 79 文件 / 289ms，三类 issue 全 0，退出码 0 |
| 规则回放（改前） | `node tests/rules-replay.mjs` | 120 场 24 规则：无失败规则、恒 skip 0 条；零产出 10 种（9 未触发 + 1 孤儿） |
| DEAD 红线 | `DEAD=1 node tests/rules-replay.mjs` | 退出码 0 |
| 基线比对 | `node tests/140-baseline.js --check` | `BASELINE-MATCH 18 场`，退出码 0 |
| 冒烟 | `node tests/smoke-headless.mjs` | 全过（20/20 局分出胜负） |
| 语法 | `node --check` × 31 文件（7 个体检 + 24 条规则） | 0 报错 |

浏览器端（`120test-runner.html` / 121 监控面板）本轮**未跑**，仅静态分析（不在 node 侧可执行范围），如实记录。

### 发现的问题（本轮修 1 个主问题 + 1 个连带口径 bug）

**问题 1 —— fact 覆盖直方图缺「渲染产出」这一层（主修，复盘问题 A 的另一半）**

- 根因：V6.1.25/1.26 把口径修到「引擎有没有把 fact 写进 step.log」，但规则消费的既不是原始 fact 也不是 fact 树，而是 **render/30 渲染出来的条目**。中间还隔着一道 `renderLog`：未知 factType 直接 `console.error` + `return null`（render/30 L828-830）、渲染器缺失 throw（L834）、渲染器自身也可能返回 null/空数组 —— 这几种情况**引擎明明产出了、规则一条都看不见**，表现和"业务侧没产出"完全一样。
- 证据：V6.1.23 起的"四类成因自查"里，成因 ①（渲染器不给条目）从未被机器检出过，只能靠人工 KEYWORDS 探针；本轮接上口径后立刻现形 3 种（mindControlBanner 160/160 恒空、spiderStrike 68/68 恒空、buffSummary 284/1258 条件性空）。
- 影响范围：这正是第 4/5 轮把 129/134/141/143 误判成"业务侧数据源缺失"、写进业务侧待修清单让业务侧白跑的最后一环；不机检化则每轮重犯。

**问题 2 —— 声明（declaration）被当成 fact 重复计数（连带的口径 bug，V6.1.25 遗留）**

- 根因：`collectNestedFacts` 的判据是 `typeof node.factType === 'string'`，把**声明**也算成 fact。声明的形状是 `{ type: EFFECT_TYPES.REBOUND, ..., factType, factData }`（core/12 L349-380），字段叫 **factData 不是 data**，由 core/10 L194-196 转成真 fact `{ factType, data: decl.factData }` 后才进 `group.data.entries`。
- 证据：120 场路径探针实测 —— `horseRebound` 144 = `<attack>.entries[*]` 72（真 fact，有 data）+ `<attack>.dmgResult.horseReboundDeclarations[*]` 72（声明，无 data）；`fortifyRebound` 190 = 160 + 30。全批次取证：**无 data 的 factType 节点只有这两类声明**，没有真 fact 被误伤。
- 影响范围：① 引擎产出数虚高（144/190 实为 72/160）；② 更严重的是给新口径制造**假红**——声明没 data，`renderLog(t, undefined)` 必抛 `Cannot read properties of undefined (reading 'unitName')`，本轮首跑就报出 horseRebound 抛错 72 / fortifyRebound 抛错 30 的假象；③ 反向漏报：若某个 factType 只以声明形态存在、从不被转成 fact，旧口径会把它算成"有产出"，零产出清单漏报。

### 改了什么

| 文件 | 改动 | 理由 |
| --- | --- | --- |
| `tests/rules-replay.mjs`（V6.1.26 → **V6.1.27**，30508 → 43284 bytes） | ① 新增 `probeRender(t, data)`：对**每个** fact 节点（顶层 + 嵌套）单独调 `renderLog` 取证，分记 `renderOk` / `renderNull` / `renderErr` / `renderErrMsg`；顶层复用其返回值拼日志（不重复渲染） | 把"引擎产出"与"规则看得见"拆成两个口径，两者不等号才说明渲染器吞了 |
| 同上 | ② 新增【渲染未产出条目】清单，并用 `collectRenderSilence()` 反查 `FACT_SPECS[t].renderFn` → 在 render/ 定位函数体 → 看有没有显式 `return null`，自动区分**恒静音（有意不落日志，规则永久盲区）**与**条件性落空（默认分支/适用单位为空，属正常）** | 光报"渲染没给条目"等于把皮球踢回人工 —— 第 4~6 轮就是这么栽的，清单必须自带证据 |
| 同上 | ③ `collectNestedFacts` 判据收紧：**有 `data` 才算 fact**（声明有 `factData` 无 `data`，不进 renderLog）；新增 `onFact` 回调把节点交给 `probeRender` | 修问题 2 的重复计数 / 假红 / 反向漏报 |
| 同上 | ④ 新增 `PROBE_RENDER=a,b` 自检开关（每个名字探 `{}` 与 `undefined` 两种 data），用法行与版本头同步 | 与 `PROBE_ZERO` 对称，给渲染口径留长期自检入口 |
| `tests/_probe-renderpath.mjs` | **已删除**（路径取证用临时探针，不入仓） | 探针不入仓惯例 |

**为什么没按复盘原建议给条目打 `_factType` 标签**（已写进代码注释，防后人重复提议）：

1. 嵌套子条目由 render/30 内部 `projectFactEntry`（L200-204）渲染，回放侧拿不到「子条目 ↔ 子 fact」的对应关系，只能拿父 factType 顶包 → 归因反而更假；
2. 给条目加字段等于给规则开一个**生产环境不存在**的依赖口子，规则一旦读了它，浏览器里恒不命中 → 制造新的恒 skip。

按 fact 节点逐个探测则两者都避开，且**覆盖更全**（含嵌套子 fact，标签方案覆盖不到）。

### 验证结果

- **与 HEAD 版本对拍**：导出 `git show HEAD:tests/rules-replay.mjs` 同参数跑一遍，24 条规则的 `✅/❌/⏭` 行 + `RESULT` 行**逐条完全一致**（25 行 diff 为空）→ 战报内容与规则判定零影响，本次改动纯诊断层。
- **负向测试（三分支全通，证明不是一刀切）**：`PROBE_RENDER=__totallyFake__,clawHit,horseRebound`
  - 伪造名 `__totallyFake__` → 【返回空】✅（不是"一律判有产出"）
  - `clawHit` / `horseRebound` 配 `{}` → 【渲染出条目】✅（不是"一律判空"）
  - 配 `data=undefined`（声明形态）→ 【抛错】且落进 `renderErr` ✅（抛错分支本身是通的，不是被吞掉的死代码）
- **新口径产物**：【渲染未产出条目】3 种 —— `mindControlBanner`（恒空 160/160，render/30 L502 显式 `return null`）、`spiderStrike`（恒空 68/68，L552 显式 `return null`，"由导演 stageAction 驱动特效"）、`buffSummary`（条件性 284/1258 = 23%，L368 兜底分支，属正常）。**三者均定性为"有意不落日志 / 条件性落空"，不是渲染侧 bug。**
- **口径修正后的计数变化**（体检侧修正，非业务产出变化）：`fortifyRebound` 190 → **160**、`horseRebound` 144 → **72**；零产出清单仍为 10 种不变。
- 全套复检：静态 79 文件全 0、回放 0 fail / 恒 skip 0、`DEAD=1` 退出码 0、`BASELINE-MATCH 18 场`、冒烟全过、`node --check` 31 文件 0 报错。

### 下轮目标

1. **9 项【本批次未触发】补覆盖**（第 9 轮遗留 #2）：按 emit 点条件构造场景，挑 `xinHunDeath`（新婚+周芷若阵亡）、`flySkip`（飞行单位行动）、`spiderFly` 先做，把零产出数压下来并顺带给相关规则真覆盖
2. **规则侧覆盖盲区登记**：`mindControlBanner` / `spiderStrike` 已确认日志里永远没有条目，任何基于它们的规则都会恒 skip —— 后续若有人要写这两块的规则，先看这条再动手
3. `meteorSplashGrowth` 孤儿登记移交 infra 侧（删登记 vs 补产出）
4. 业务侧清单维持：butterflyNoHost 补 sisterUid、孤儿回血定性、zhangTaunt 顶层通道（modules/27 L50）零命中

### 推送

- 只 add 体检侧：`tests/rules-replay.mjs`、`tests/体检迭代日志.md`、`记录-更改履历.md`（工作区 `player/42`、`player/49`、`ui/65` 三处业务侧改动原样保留，未 add、未 stash）
- ⚠ **本轮又踩到一次分支引用事故（与第 9 轮同源，但表现不同，记此防复发）**：`git add` 后准备 commit 时，`git log` 报 `your current branch 'workbuddy/main-87959581' does not have any commits yet`，`git for-each-ref refs/heads` 里该分支**整个消失**（对象仍在，`git cat-file -t dbd70ab` = commit）。即分支引用被别的会话/进程删掉了，HEAD 的 symbolic-ref 还指着它 → 又变成"无父"状态。
- **本环境无法重建两级 ref**：`git branch workbuddy/main-87959581 <sha>` / `git update-ref refs/heads/workbuddy/main-87959581 <sha>` 均**返回 0 且不报错、但文件系统里不产生该 ref**；实测 `git branch foo/bar` 同样失败，而 `git branch zz-probe-ref`（扁平名）成功 → 结论是**本环境下含斜杠的两级 ref 一律创建不了**（`refs/heads/feat/miejue-stage3-rotation` 是早前别的上下文建的）。用 node 直接 mkdir + 写 loose ref 文件也会被回滚。
- **绕过办法（不改远端、不强推、不动他人分支）**：`git update-ref --no-deref HEAD dbd70ab` 把 HEAD **detach** 到第 9 轮 tip → `git reset --mixed HEAD` 同步索引（不动工作区）→ 精确 add 三个体检侧文件 → commit = **`4fb5aaa`（parents=[dbd70ab]，3 文件 +268/−10）** → 用**显式 refspec** 推送 → 最后 `git symbolic-ref HEAD refs/heads/workbuddy/main-87959581` 恢复原指向，并用扁平分支 `round10-health` 给本次提交留本地 bookmark（防 GC）。
- ✅ **推送成功（两端都通）**：
  - `GIT_TERMINAL_PROMPT=0 git push gitee HEAD:workbuddy/main-87959581` → `dbd70ab..4fb5aaa  HEAD -> workbuddy/main-87959581`，退出码 0
  - `GIT_TERMINAL_PROMPT=0 git push origin HEAD:workbuddy/main-87959581` → `dbd70ab..4fb5aaa  HEAD -> workbuddy/main-87959581`，退出码 0
  - 注：`timeout` 在本环境解析到了 Windows 的 `TIMEOUT.EXE`（`错误: 无效语法`），故未加 timeout，凭据可用、推送秒回
- 未强推、未推 main、未动他人分支、未改 remote 地址、未写入任何 token；诊断用的两个临时分支 `workbuddy-test` / `zz-probe-ref` 已删除

- ⚠ **补记：第二笔提交 `bc0c9fb`（"补记推送结果"）的推送结果不一致**
  - gitee：`GIT_TERMINAL_PROMPT=0 git push gitee HEAD:workbuddy/main-87959581` → `4fb5aaa..bc0c9fb  HEAD -> workbuddy/main-87959581`，退出码 0 ✅
  - github（origin）：连试 3 次均失败 —— 第 1 次 `Empty reply from server`(128)，第 2、3 次 `CONNECT tunnel failed, response 502`(128)
  - **根因**：出网代理不通（CONNECT 隧道 502），不是凭据问题（凭据 2026-09-22 16:00 起可用，gitee 同凭据秒通），也不是本仓库状态问题
  - **影响范围**：仅"体检日志补记"这一笔文档提交暂未同步到 github；`4fb5aaa`（代码改动）已在两端；gitee 分支为最新
  - **遗留**：**推送失败：github 代理 502（CONNECT 隧道建不起来）；本地提交 `bc0c9fb` 待推**。下轮起手先 `GIT_TERMINAL_PROMPT=0 git push origin HEAD:workbuddy/main-87959581` 补推这一笔即可，无需任何 rebase/强推

- ⚠ **HEAD 最终状态（重要，防下轮再踩）**
  - 现象：`git branch -f workbuddy/main-87959581 89ab7a2` 退出码 0、无报错，但 `git for-each-ref refs/heads` **再次确认创建不出来**（只剩 `feat/miejue-stage3-rotation`、`friend-dev`、`main`、`main-backup-20260920`、`round10-health`）→ 本环境"含斜杠的两级 ref 建不了"是**稳定复现**的，不是偶发
  - **根因**：本 worktree 环境对两级 ref 的创建被静默吞掉（写 loose ref 不落盘）
  - **影响范围**：如果按第 9 轮的做法把 `symbolic-ref HEAD` 指回 `refs/heads/workbuddy/main-87959581`，HEAD 会指向一个**不存在的 ref** → 下一次 commit 又变成无父 root commit（第 9、10 轮两次事故同一形态）
  - **本轮决定**：**不再把 HEAD 指回该分支名**，保持 HEAD **detached 在 `89ab7a2`**（指向真实存在的 commit），并用扁平分支 `round10-health`（已更新到 `89ab7a2`）做本地 bookmark 防 GC
  - **下轮起手动作**：`git log --format="%h parents=[%p]" -1` 确认父链；若 HEAD 又变成 detached 或非预期，用 `git update-ref --no-deref HEAD <上轮 tip>` + `git reset --mixed HEAD` 复位后再精确 add 体检侧文件；推送一律用显式 refspec `git push gitee|origin HEAD:workbuddy/main-87959581`

---

## 第 11 轮 — 2026-09-23 18:0x（零产出清单补第三级【不可达 emit 点】）

> 开场读档：第 10 轮把 fact 覆盖补齐了「渲染产出」口径，遗留目标是"9 项【本批次未触发】补覆盖"。
> 本轮从这条入手，结果**推翻了其中 1 项的性质**——它压根不是覆盖不足。

### 本轮深度判断：A（全流程）

- 依据：`git log --since="2026-09-23 07:00"` 只有 4 笔体检侧提交（4fb5aaa / bc0c9fb / 89ab7a2 / fe57a33），按规则属 C 类；
- 但 `git status` 显示工作区有**未提交的主代码改动** `player/42`（V6.8.1）、`player/49`（V6.3.1）、`ui/65`（V6.0.1）——
  全自动档飞行方向弹窗默认向左 + 3 秒倒计时 + 兜底方向改左。回放器跑的是当前工作区，故**升级为 A 类全流程**。

### 跑了什么

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| 静态快检 | `node tests/123static-scan.js` | 79 文件，三类 issue 全 0，退出码 0 |
| 规则回放 | `node tests/rules-replay.mjs` | 120 场 24 规则：0 fail、恒 skip 0 条 |
| DEAD 红线 | `DEAD=1 node tests/rules-replay.mjs` | 退出码 0 |
| 基线比对 | `node tests/140-baseline.js --check` | `BASELINE-MATCH 18 场`，退出码 0 |
| 冒烟 | `node tests/smoke-headless.mjs` | 全过（20/20 局分出胜负） |
| 体检的体检 | 程序化四方对齐（health-rules / 121 / 124 / 123） | 各 24 项，互缺互多均为空，全对齐；121 按文件路径动态装载（不写规则名），属正常 |
| 对拍 | HEAD 版 vs 新版规则输出 | 24 条规则行 + `RESULT` 行**逐字节相同**（25 行 diff 空） |

浏览器端（`120test-runner.html` / 121 监控面板）本轮**未跑**，仅静态分析，如实记录。

### 发现的问题（P0 一条，在体检侧自己身上）

**问题 1 —— 【本批次未触发】的归因不验证 emit 点是否还活着，给出永远做不到的补救（主修）**

- ① 根因：V6.1.26 的机检把"全仓有 `factType: FACT_TYPES.X` 赋值"直接等同于"有产出点"，进而归入【本批次未触发】并建议"加 SEEDS/STAGES 或针对该分支构造场景"。但**"有赋值"只说明代码里写了这行，不说明这行还活着**。
- ② 证据：`spiderFly` 的 emit 点在 `modules/27elite-mingjiao.js:604`，所在函数 `executeFly` 全仓唯一调用者是 `core/11battle-round.js:323`，被 `} else if (decl.type === 'spiderFly') {` 门控；而全仓（含 `content/*.json`）**没有任何位置**产出 `type: 'spiderFly'` —— `_pendingStateTransitions` 的写入点只有 core/11 的 331/445 两条 delayed 通道，类型固定为 `butterflyReturn`/`spiderDescend`。同时实测 120 场里飞天机制**跑了 53 场**（129 号规则 pass=53，条目由 `modules/27:487` 的免疫声明 `{immune:true, flyData}` 经 `render/30:264 renderSpiderFlyFact(fact.flyData)` 渲染），即**机制活着、fact 通道死了**。
- ③ 影响范围：清单对 `spiderFly` 给出的"加 SEEDS/STAGES"是**不可能完成**的补救（加多少种子都碰不到）；这与第 4~6 轮"把体检侧问题甩给业务侧"是同一失效形态，且更隐蔽——建议本身看起来可执行。同时掩盖了一条真实的业务侧死代码（core/11:323 + modules/27:586-607 整段 `executeFly` 不可达）。

**连带：我自己的一处误判被证据推翻（记录在案，不掩过）**

- 本轮开场假设 `butterflyNoHost` 与 `spiderFly` 同为死通道（判据：`_executeAttach` ← core/11:321 被 `decl.type === 'butterflyAttach'` 门控，而该 type 全仓无产出方）。
- 机检落地后实测推翻：`_executeAttach` 另有**可达调用点** `modules/27elite-mingjiao.js:162`（`comp._executeAttach(...)`），故 `butterflyNoHost` 仍是【本批次未触发】，只有 `spiderFly` 一条死通道。**业务侧待修清单里挂了 5 轮的 `butterflyNoHost 补 sisterUid` 因此仍然有效**，不因本轮改动撤下。

### 改了什么

| 文件 | 改动 | 理由 |
| --- | --- | --- |
| `tests/rules-replay.mjs`（V6.1.27 → **V6.1.28**，43284 → 54737 bytes） | 新增 emit 点**可达性分析**：`enclosingFnName`（回溯所在函数）/ `findCallSites`（查全仓调用点，同时试原名与去下划线名）/ `gateLiteralAbove`（向上取最近一条 `type === '字面量'` 门控）/ `hasTypeProducer`（查该 type 有无产出方） | 把"有 emit 赋值"细分成"可达"与"不可达"，死通道不再伪装成覆盖不足 |
| 同上 | 零产出清单由两级改**三级**：【不可达 emit 点】/【本批次未触发】/【孤儿登记】，不可达项打印函数名 + 调用点 file:line + 门控字面量 | 只报结论不给证据等于把皮球踢回人工（第 4~6 轮教训），清单必须自带可复核的证据 |
| 同上 | `loadSourceTexts()` 抽出缓存，供 emit 点检索与可达性分析共用 | 避免同一批 79 文件读两遍、两处口径漂移 |
| 同上 | `hasTypeProducer` 增加 `content/*.json` 检索口径（`"type": "字面量"`） | **修掉本轮首跑的假阳性**（见下） |

**为什么不做完整调用图**：完整静态可达性在 JS 上无解（高阶函数、事件总线 `eventBus.on` 动态派发），硬做会引入大量假阳性、反而制造新的假红。这三级判据的共同点是**证据可打印**——输出里带函数名 / 调用点 file:line / 门控字面量，人工一眼可复核，不靠猜。也不因"某函数没被 import"就判死：`import()` 与 eventBus 注册都是合法可达路径，误判代价高于漏判。

### 验证结果

- **首跑假阳性 → 已修**：第一版 `hasTypeProducer` 只搜 JS 的 `type: '字面量'`，把实产 **421 条**的 `clawHit`、20 场 pass 的 `xinHunDeath` 全误判成"不可达"。取证后发现**技能声明的 `type` 是配置数据**：`core/15:390` / `:466` 消费的 `chainClaw` / `xinHun` 声明来自 `content/200game-data.json`（`"type": "chainClaw"` / `"type": "xinHun"`，各 1 处）。补上 JSON 口径后二者正确回落【本批次未触发】。**这正是"负向测试必须双向都跑"的价值——只跑"应 fail"那份会漏掉这次假红。**
- **负向测试（三分支全通）**：`PROBE_ZERO=spiderFly,butterflyNoHost,clawHit,xinHunDeath,__totallyFake__`
  - `spiderFly` → 【不可达 emit 点】✅（不是"一律判可达"）
  - `clawHit` / `xinHunDeath` / `butterflyNoHost` → 【本批次未触发】✅（不是"一律判不可达"）
  - `__totallyFake__` → 【孤儿登记】✅（无 emit 点分支仍通）
- **新分类结果**：10 项零产出 = 【不可达 emit 点】1（`spiderFly`）+【本批次未触发】8 +【孤儿登记】1（`meteorSplashGrowth`）。
- **对拍**：与 HEAD 版 24 条规则行 + `RESULT` 行逐字节相同 → 本次改动纯诊断层，不影响任何规则判定。
- 全套复检：静态 79 文件全 0、回放 0 fail / 恒 skip 0、`DEAD=1` 退出码 0、`BASELINE-MATCH 18 场`、冒烟全过。
- 临时文件 `tests/_tmp-head-replay.mjs` 已删（对拍用，不入仓）。

### 报业务侧 / 工具侧待修（本轮不动手）

1. **【新】`spiderFly` fact 通道死代码**：`core/11battle-round.js:323` 的 `brotherComp.executeFly(...)` 与 `modules/27elite-mingjiao.js:586-607` 整段 `executeFly` 在现行调用链上不可达（门控 `decl.type === 'spiderFly'` 无产出方）。飞天机制本身走的是 `modules/27:487` 的免疫声明通道（120 场跑通 53 场）。**二选一**：给 `_pendingStateTransitions` 补 `type:'spiderFly'` 产出方，或删掉 `executeFly` + `FACT_SPECS.spiderFly` 登记与其渲染映射。
2. **`meteorSplashGrowth` 孤儿登记**（infra 侧，第 9 轮遗留）：`infra/56 L30` + `infra/58 L111` 有登记、全仓无 emit 点。
3. 业务侧清单维持：`butterflyNoHost` 补 `sisterUid`（**经本轮复核仍然有效**，通道可达）、孤儿回血定性、`zhangTaunt` 顶层通道（`modules/27 L50`）零命中。

### 下轮目标

1. **【本批次未触发】8 项补覆盖**（第 10 轮遗留 #1，本轮收窄为 8 项）：优先 `xinHunDeath`（宋青书 + 周芷若残血）、`flySkip`（飞天单位轮到行动）、`spiderDeadTarget`（注意：`modules/20:169` 的 `if (!target.alive)` 与上一行 `aliveEnemies = enemySide.filter(u => u.alive)` 存在**条件矛盾**，需先确认是业务侧死分支还是取证口径问题，不要直接当覆盖不足）
2. `spiderDeadTarget` 的条件矛盾单独定性（疑为业务侧死分支）
3. 补推 github（`bc0c9fb` 起 3 笔，上轮代理 502）

### 推送

- 只 add 体检侧：`tests/rules-replay.mjs`、`tests/体检迭代日志.md`、`记录-更改履历.md`
  （工作区 `player/42` V6.8.1、`player/49` V6.3.1、`ui/65` V6.0.1 三处主代码改动原样保留，未 add、未 stash）
- 提交 **`2ea6612`（parents=[fe57a33]，3 文件 +305/−13）**，父链已按惯例校验（`git log --format="%h parents=[%p]" -1`）
- ✅ **两端推送全部成功**：
  - gitee：`fe57a33..2ea6612  HEAD -> workbuddy/main-87959581`，退出码 0
  - github（origin）：`4fb5aaa..2ea6612  HEAD -> workbuddy/main-87959581`，退出码 0 —— **上轮因代理 502 积压的 3 笔（bc0c9fb / 89ab7a2 / fe57a33）本次一并带上**，github 已追平
- 未强推、未推 main、未动他人分支、未改 remote 地址、未写入任何 token；HEAD 继续保持 detached（第 10 轮定的规矩，本环境建不出含斜杠的两级 ref）

---

## 第 12 轮 — 2026-09-23 20:0x~20:5x（同步轮：本地 test 分支合入主线，主线 main 不动）

> 本轮由主线侧工程师执行（非 workbuddy worktree）。起因：体检线与主线自分叉点 94e4a42（09-21）互不可见——主线进了 29 个提交（规则 147~152、渲染拆分、新角色、新精英），体检线 11 轮成果也传不回主线。用户决定：在本地仓建 `test` 分支合流，改完推 gitee 的 test 分支，main 永不被自动碰。

### 跑了什么

| 检查项 | 合并前（main 旧体检） | 合并后（test 分支） |
| --- | --- | --- |
| 静态扫描 | 静默假绿（0 输出、exit 0） | 103 文件，0 条（清 3 条注释假阳性后） |
| 规则回放 | V6.1.11，红 2 条 + 恒 skip 2 | V6.1.28，红 2 条 + **恒 skip 0** |
| DEAD 红线 | 红 5 条 | 红 2 条（同回放） |
| 基线 --check | V1.0.0 直接崩（Windows 双盘符 ENOENT） | V1.2.0，重录后 MATCH 18 场 |
| 冒烟 | 全过 | 全过 |
| 登记一致性 | 规则 30 条（主线侧） | **30 文件 = 121 imports = allRules = META = SCAN_FILES，五方对齐** |

### 发现的问题与裁决（8 处冲突）

1. **rules-replay.mjs**：主线 fork 后的唯一改动（数组摊平）与体检线第 6 轮是同一修复的独立实现 → 取体检线 V6.1.28（超集）。
2. **123/129/133/143 四条规则**：两边对同一批恒 skip 各自修了一遍，语义收敛（摊平扫描）→ 取体检线版（经 11 轮 + 3 轮复盘独立复跑验证）。
3. **132 规则**：取体检线版 + 主线发现的字段更名适配。**本轮踩了一次"适配未验前提"**：V6.1.20 照主线第 7 趟思路把判据 2 接回 `hpAfter`，实跑炸 16 条假红——取证 core/15 L401-413 发现现行 isExecute 语义是"打到斩杀线(≤15%)未归零"，hpAfter>0 合法；V6.1.21 按现行契约重写为"hit 标记 isExecute → 同组必须存在处决条目"（core/15 L407-413 保证 executeInfo 必然同组产出）。负向测试 4 例全过（无处决条目 FAIL / 现行合法形态 PASS / 递增+处决 PASS / 递减 FAIL）。
4. **tools/106**：主线 V6.0.8 已含体检线补登的 3 个运行文件 → 取主线版 V6.0.11（超集）。
5. **履历**：两侧是两个独立序列（业务 V7.x / 体检 V6.1.x），拼接保留，加序列说明行。

### 顺手修的两处（合流后首次暴露）

- **123static-scan V1.1.0→V1.1.1**：infra/56 文档注释「eventBus.on」、core/06 注释、health-rules/149 头注释「BUFF_TYPES.HOLY_FLAME」被符号检测当真使用 → 3 条纯注释假阳性。修法：检测前剥离注释行（stripCommentLines，枚举/共享符号/形参排除四处口径统一）。负向测试 6 例全过。
- **基线重录**：14/18 场差异、winner 全部不变（rounds/facts 变化），与履历 V7.0.x~V7.2.0 的有意变更（新角色/阵容独立化/三技能改版/新精英）吻合 → 按工具自身口径重录，复检 MATCH。属"业务有意变更后重录"，非为变绿改基线。

### 报业务侧待修（本轮不动手）

1. **146 概率连击**（fail 6）：新精英「金毛狮王谢逊」召唤物场景，宣告者与触发者不一致（宣告[幼狮]实际[母狮/雄狮]，seed=1/3/10 跨 stage 复现）——合并前主线即红，指向业务侧连击归属逻辑，待定性。
2. **148 carry 上界**（fail 3）：张无忌 carry 攻/防/血上限超上界（seed=9/15/16 stage6）——与 V7.x 新内容相关，待业务侧确认上界口径是否更新。
3. 既有清单维持：spiderFly 死通道 / meteorSplashGrowth 孤儿登记 / butterflyNoHost 缺 sisterUid / zhangTaunt 零命中 / 孤儿回血定性（详见 tests/体检更改总览.md 第六节）。

### 下轮目标

1. 146/148 两条红先取证后定性（新精英连击归属 / carry 上界口径），真 bug 报业务侧，口径过期则修判据。
2. 恢复复盘节奏（原每 2 小时复盘自 09-22 10:25 后停更，第 7~11 轮无复盘覆盖）。
3. 两份主线速记文档（体检更改总览.md / 体检收获与方法.md）已随本轮入 test 分支，作为行规手册维护。

---

## 第 13 轮 — 2026-09-24 12:0x~12:5x（同步轮 + 第 4 轮复盘小节）

> 开场读档：第 12 轮（同步轮）尾部无"待推"字样（两端均推送成功），无需补推；下轮目标是"146/148 两条红先取证后定性 + 恢复复盘节奏"。
> 触发条件：自第 3 轮复盘（09-22 10:25）起新增第 7~12 轮共 **6 轮** → 满额，**本轮先做复盘小节再开新活**（第 4 轮复盘已记入 `tests/体检复盘报告.md`）。

### 本轮深度判断：A（全流程）

- 依据：三条安全检查通过后 `git checkout test`，`git log test..main` 有 **12 笔**主线新提交，最新两笔 `8230284`（灭绝师太+谢逊：出手计数含被动出手、召唤改阵亡触发、**狮群机制重做**，2026-09-24 11:30）与 `56b7bb1`（tools 侧）改动面覆盖 `core/10`、`core/12`、`core/17`、`content/200game-data.json`、`fx/87~91`、`infra/50`、`infra/55`、`modules/22`、`modules/26`、`modules/27`、`render/32` → **游戏代码有变，五件套全跑**。
- 合并：`git merge main --no-edit` **一次通过、0 冲突**（与第 12 轮的 8 处冲突裁决不同——本轮主线只动业务代码，未再碰 `tests/`），合并后 `grep '<<<<<<<' tests/ "记录-更改履历.md"` 无残留。体检侧在本次 merge 中**零改动**（`git diff --stat HEAD~1 HEAD -- tests/` 为空），只有履历被自动合入 +50 行。

### 跑了什么

| 检查项 | 命令 | 结果 |
|---|---|---|
| 静态快检 | `node tests/123static-scan.js` | **103 文件 / 0 条**，退出码 0 |
| 规则回放 | `node tests/rules-replay.mjs` | 120 场 × 30 规则：**3 fail / 恒 skip 0** |
| DEAD 红线 | `DEAD=1 node tests/rules-replay.mjs` | 恒 skip 0（DEAD 本体绿），退出码 1（因 3 条红） |
| 基线比对 | `node tests/140-baseline.js --check` | 先 **DIFF 9 场**（winner 全不变）→ 重录 → **MATCH**，退出码 0 |
| 冒烟 | `node tests/smoke-headless.mjs` | 全过（20/20 局分出胜负，FACT_SPECS 77 条映射完整） |
| 体检的体检 | 四方登记脚本 + 版本头 + 孤儿文件 + 符号存活 | 30/30/30/30/30 缺失 0；头记 3 处失真已修；孤儿 0；死符号 0 |

### 发现的问题

**【P0-③ 引用已不存在的路径】140-wei 的复发归因整体过期（主修）**

- ① 根因：`e266abc`（V6.1.0 妆造字段剥离）把 `u.buffDodgeBonus = stats.dodgeBonus` 的 **3 处写入**（core/11 A 段、B 段、事件载荷）一并删除后，**live 源码已无任何写入点**；而消费方 `render/32-grid-render.js:97-99`（`getDodgeBreakdown` 的「流云身法 +x%」）与 `ui/62:162`（5v5 面板）照旧读该字段 → 闪避面板数据源被抽干。
- ② 证据：全仓（排除 `web/` 部署副本与 tests/）`buffDodgeBonus` 只剩 render/32 三处**读**点、**0 处写**点（`git log -S "buffDodgeBonus" -- core/11battle-round.js` 定位删除点 = `e266abc`）；`computeBuffStats` 仍返回 `dodgeBonus`（core/04:46-55），core/11:187/207 调完后 `stats` / `bStats` 变成**只赋值不使用的死变量**（全文件 grep 仅此两行）→ 佐证写入确已断链、不是挪到别处。
- ③ 影响范围：规则头注释自第 5 轮起挂着"仅 prepareRoundStart 重算、末回合中途授予无重算机会"，**8 轮未随被测代码复核**，据此给业务侧的修法建议（"授予即重算"）是错的方向；本条性质由"末回合时序性红"升级为**结构性恒红**（只要我方流云在场必红，`pass=0 fail=3 skip=117`）。

**【P2 文档与代码脱节】版本头 ~bytes 与实际不符 3 处**（复盘小节核对发现，明细见复盘报告第五节）。

**未发现**：恒 skip 空转 0 条；规则活动代码中无 `e.factType` / `e.data` / `clawTargetHpAfter` 引用（全仓 6 处命中全在注释里）；规则 import 符号 0 个死符号。

### 改了什么

| 文件 | 改动 | 理由 |
|---|---|---|
| `tests/health-rules/140-wei-dodge-cloud.js`（**V6.0.2**） | 头注释「已知成因」整段重写为现行取证结论（写点删除 + `e266abc` 出处 + 死变量佐证），fail 文案同步；**判据一字未动、未加任何豁免** | 归因注释过期会误导业务侧修错方向；修文案不改判据，不碰"不许放宽"红线 |
| `tests/rules-replay.mjs`（V6.1.28→**V6.1.29**，头记 54700→**55500**，实际 55512） | 仅版本头 bytes 与版本号 | 复盘核对出头记失真 812 |
| `tests/121health-monitor.js`（V6.1.0→**V6.1.1**，42400→**42900**） | 仅版本头 | 失真 503 |
| `tests/122health-utils.js`（V6.1.15→**V6.1.16**，27700→**27760**） | 仅版本头 | 失真 58 |
| `tests/baselines/baseline-v1.json` | 重录 18 场 | 见下「基线」 |

**负向测试（10 例全通，证明 140-wei 不是一刀切必红）**：构造合成战报——应 FAIL 3 例（流云 ally 且存活队友 bonus 全 0 / 含韦一笑 / cloudBody 无 target）→ 全部 FAIL；应 PASS 2 例（有队友 bonus>0 / 韦一笑 bonus>0）→ 全部 pass；应 SKIP 5 例（无流云文本 / 流云挂敌方 target=enemy / 无 cloudBody / 我方全灭 / 空战报）→ 全部 skip。临时探针 `tests/_tmp-neg140.mjs` 跑完即删。

### 验证结果

- `node --check` 四个改动文件全过；改后复跑回放：**3 fail / 恒 skip 0，与改前逐条一致**（无回归）。
- 五件套终态：静态 103 文件 0 条、回放 3 fail / 恒 skip 0、`DEAD=1` 恒 skip 0、基线 **MATCH 18 场（退出码 0）**、冒烟全过。
- 基线重录合规性：DIFF 的 9 场 **winner 全部不变**（明教→明教 / 六大派→六大派），仅 rounds/facts 变化；依据 main 的 `8230284`（狮群机制重做）+ 履历 V7.2.0 / V7.3.0 均为业务有意变更 → 按规则允许重录；**未为让退出码变 0 而改基线**。

### 报业务侧 / 工具侧待修（本轮不动手）

1. **【新·P0】`unit.buffDodgeBonus` 无写入源**：`e266abc` 删掉 core/11 三处写入后，`render/32:97-99` 与 `ui/62:162` 仍读 → 闪避面板恒不显示流云身法加成。二选一：恢复写入（授予/每回合重算即写回 unit），或把 `getDodgeBreakdown` 改为实时从 `activeBuffs` 计算。
2. **【新·P2】`core/11:187` `stats` 与 `:207` `bStats` 为死变量**（同上笔删除的残留）。
3. **【新·P2】`8230284`（狮群机制重做）在 `记录-更改履历.md` 无对应条目**，履历顶部仍为 V7.3.0 / 2026-09-23，建议补 V7.4.0。
4. `127 小昭妹/姐不可选期被打`（fail 1，seed=6 stage=3）：合流前主线即红，未定性。
5. `146 概率连击`（fail 2，seed=3 stage=4 / seed=9 stage=2）：宣告[幼狮]、实际触发者[雄狮]；狮群重做后由 6 → 2，**残留未清**。
6. 既有清单维持：`spiderFly` 死通道、`meteorSplashGrowth` 孤儿登记、`butterflyNoHost` 缺 `sisterUid`、`zhangTaunt` 零命中、孤儿回血定性。

### 下轮目标

1. **140-wei 保持红、不许加豁免**；若下轮它变成 skip，先查是不是被人加了跳过条件（那是掩盖真信号）。真正的退出条件是业务侧修好写入 → 转 **pass**。
2. `127` / `146` 两条红取证定性（真 bug 报业务侧；口径过期才动判据）。
3. `spiderStrike` 是否退出【渲染未产出条目】：**待确认**（FACT_SPECS 仍有登记 infra/56:71）。
4. 第 4 轮复盘遗留：核对本轮修订的成因注释是否再次过期。

### 推送

- 只 add 体检侧：`tests/` 与 `记录-更改履历.md`（工作区无业务侧未提交改动，本次 merge 后工作区干净）
- 提交后 `GIT_TERMINAL_PROMPT=0 git push gitee HEAD:test`，失败改试 `origin HEAD:test`；两者皆败则记"推送失败：<原因>；本地提交 <sha> 待推"
- 未强推、未推 main、未动他人分支、未改 remote、未写 token；收尾 `git checkout main` 还原工作区

---

## 第 14 轮 — 2026-09-24 13:2x~13:4x（WIP 态全量轮 + 146 假红定性修复）

> 开场读档：第 13 轮尾部无"待推"字样（gitee 推送成功 `68d4589..a213fe3`），无需补推；下轮目标是"140-wei 保持红 / 127·146 取证定性 / 第 4 轮复盘遗留复核"。
> 复盘触发？第 4 轮复盘在本序列第 13 轮，本轮为其后第 1 轮（未满 6），**不做复盘小节**。

### 本轮深度判断：A（五件套全跑；结果属"WIP 态"）

- 三条安全检查通过：`git branch --show-current` = **main** ✓；`git status --porcelain -- tests/ "记录-更改履历.md"` **空** ✓；工作区另有业务侧未提交改动，一律原样保留（不 add / 不 stash / 不 checkout）。
- `git checkout test` → `git merge main --no-edit` 报 **Already up to date**（`git rev-list --left-right --count main...test` = 0 / 21，test 已含 main 全部提交）——**本轮没有新提交进来**。
- 但工作区有未提交的业务改动 10 个文件（`content/200game-data.json`、`fx/80`、`fx/87`、`fx/89`、`infra/55`、`modules/26`、`modules/27`、`player/48`、`render/32`、`render/39`，+114 / −57）。体检跑的就是这批代码 → 按 A 全跑，并在全篇标注**结果只代表这批改动生效后的状态**，不作为主线结论。
- **补记（收尾复核时发现）**：跑到一半时这批改动被用户在 **test 分支**上提交成了 `08567d4`（feat(战斗): 狮子进战报/灭绝计数繁体飘字+金名M115/幼狮召唤30%/雄狮振奋锚在出手帧/胖远桥加强，10 文件 +114/−57）。体检侧全程未 add / 未 stash / 未 checkout 过业务文件，也未改动该提交一字。**该提交缺履历条目**（`记录-更改履历.md` 顶部仍是 V7.3.0 / 2026-09-23）。它与 main 的关系属用户分支管理范畴，体检侧只报不处理。

### 跑了什么

| 检查项 | 命令 | 结果 |
|---|---|---|
| 静态快检 | `node tests/123static-scan.js` | **103 文件 / 0 条**，退出码 0 |
| 规则回放（改前） | `node tests/rules-replay.mjs` | 120 场 × 30 规则：**2 fail**（140-wei fail 9 / 146 fail 2）、恒 skip 0 |
| 规则回放（改后） | 同上 | **1 fail**（140-wei fail 9）、恒 skip 0 |
| DEAD 红线 | `DEAD=1 node tests/rules-replay.mjs` | 改后退出码 1（仅 140-wei），**恒 skip 0 → DEAD 本体绿** |
| 基线比对 | `node tests/140-baseline.js --check` | **DIFF 9 场**（winner 全不变），退出码 1 → **本轮不重录** |
| 冒烟 | `node tests/smoke-headless.mjs` | 全过（20/20 局分出胜负，FACT_SPECS 77 条映射完整） |
| 体检的体检 | 四方登记脚本 + bytes 头 + 孤儿文件 | 30/30/30/30/30 缺失 0 多余 0；bytes 头 2 处失真已修；孤儿 0 |

### 发现的问题

**【P1 误报】146 概率连击的两条红全是「幼狮成长」造成的身份误判（主修）**

- ① 根因：规则拿**显示名**做单位身份比对，而幼狮成长是**同一 unit 对象原地改名**（`modules/27elite-mingjiao.js:665` `cub.name = spec.name`，uid / pos 都不变，只有 `isLionCub` 被显式清掉）；连击登记走 uid（`core/11battle-round.js:61` `chosen.uid` → `core/03battle-utils.js:371` `unit.uid !== doubleStrikeUnitUid`），所以「回合开始宣告[幼狮] → 成长 → 触发者[雄狮]」是**同一个单位**，不是越界。
- ② 证据：战报 dump（临时探针）——seed=3 stage=4：idx54 宣告「⚡ 概率连击：幼狮」→ idx57「🦁 幼狮成长为雄狮（3 号位）」→ idx61 雄狮(攻24 血71) 出手 → idx62 DSB → idx63 雄狮(攻26 血71，+2 来自雄狮振奋) 额外攻击；seed=10 stage=2 同形态（idx106 宣告 → idx108「成长为雄狮（6 号位）」→ idx122 DSB）。
- ③ 影响范围：146 的 **fail 2 → 0**；判据本身没坏，**不是放宽**——真越界（宣告张无忌却由雄狮出手、宣告幼狮却由母狮出手、跨回合借用别名）照样报。

**【P2】版本头 ~bytes 自引用失真 2 处**：`rules-replay.mjs` / `121health-monitor.js` 第 13 轮填的是**写头之前**的实测值，新写的头两行本身又占字节 → 写完即失真（271 / 249）。附带结论：Windows `core.autocrlf=true` 工作区是 CRLF，比仓库 blob 大「行数」个字节（rules-replay：blob 54994 / 工作区 55771，差 777 = 行数），**跨换行符口径核对要按 stat，不按 blob 大小**。

**基线 DIFF 9 场（winner 全不变）—— 本轮不重录**：差异源是上面那批业务改动（现为 `08567d4`），**但它在 `记录-更改履历.md` 里没有任何对应条目**（顶部仍是 V7.3.0 / 2026-09-23）。基线保护红线的重录前提是「履历里存在可依的业务有意变更条目」，本轮不满足 → 只报不录；等补上 V7.4.0 条目后再重录（重录前必看 winner 是否翻转——本轮 9 场 winner **全部不变**，符合"有意变更"特征，不是回归）。

**未发现 / 顺带结案**：恒 skip 0；140-wei 仍是结构性恒红（pass=0 fail=9 skip=111，条数比上轮 3 条多系 WIP 下更多场次触发流云，**性质不变**），未被加任何豁免；127 由 fail 1 → 0（WIP 态下消失，非本轮改动所致）；`spiderStrike` 已不在【渲染未产出条目】清单里 —— 第 13 轮留下的"待确认"**本轮结案**。

### 改了什么

| 文件 | 改动 | 理由 |
|---|---|---|
| `tests/health-rules/146-double-strike.js`（V6.1.12 → **V6.1.13**） | 新增 `grownNameOf`（解析「X成长为Y（N 号位）」）+ `sameNameAfterRename`（别名双向比对），三条复发信号全部改走别名比对；头注释「误报规避」补上原地改名的取证（modules/27:665 + core/11:61 + core/03:371） | 修掉狮群成长的假红；只补"身份延续"，判据强度不动 |
| `tests/rules-replay.mjs`（V6.1.29 → **V6.1.30**，~55500 → **~56000**） | 仅版本头与 ~bytes | 自引用失真 271 |
| `tests/121health-monitor.js`（V6.1.1 → **V6.1.2**，~42900 → **~43200**） | 仅版本头与 ~bytes | 自引用失真 249 |

**负向测试（11 例全通）**：应 FAIL 5 例（真越界：宣告张无忌/出手雄狮且无成长 · 失败文案写未宣告者 · 别名到别的形态「幼狮→雄狮却由母狮出手」· 同回合同人两次连击 · 跨回合借别名）→ 全部 fail；应 PASS 4 例（幼狮→雄狮 · 幼狮→母狮 · 普通常态 · 成长后失败文案用新名）→ 全部 pass；应 SKIP 2 例（空战报 / 无连击内容）→ 全部 skip。
首版 3 例不过：正则 `(.+?)成长为` 把 🦁 前缀与空格一起吃进名字，别名键变成「🦁 幼狮」，永远对不上宣告名「幼狮」；改名按"不含空白的一段"取（`/([^\s]+?)成长为([^\s（]+)（\s*(\d+)\s*号位/`）后全通——这条正好再次验证"只测一边必漏"。
临时探针 `tests/_tmp-probe146.mjs`（战报 dump）与 `tests/_tmp-neg146.mjs`（负向）**跑完即删**。

### 验证结果

- `node --check` 三个改动文件全过；改后复跑回放：**2 fail → 1 fail**，其余 29 条结果与改前逐条一致（无回归）；`DEAD=1` 恒 skip 仍 0。
- 五件套终态（WIP 态）：静态 103 文件 0 条、回放 **1 fail / 恒 skip 0**、DEAD 恒 skip 0、基线 **DIFF 9 场（未重录）**、冒烟全过。
- 登记一致性：规则文件 30 = 121 imports 30 = 123 SCAN 30 = 124 META 30 = 规则名 30，缺失 0 多余 0；孤儿文件 0。

### 报业务侧 / 工具侧待修（本轮不动手）

1. **【P0·维持】`unit.buffDodgeBonus` 无写入源**：140-wei 结构性恒红（pass=0 fail=9 skip=111），退出条件是业务侧恢复写入或改实时计算。
2. **【新·待确认】连击名额可能白占**：`core/11battle-round.js:59` 挑概率连击单位只过滤 `isHorse`，未过滤 `isLionCub`；幼狮 atk=0（`content/200game-data.json` 幼狮 `"atk": 0`）按 `core/11:331` 走「幼狮休息」不出手，本回合未成长则该连击名额空转（成长后正常，非本轮两条红的成因）。
3. **【P2·维持】**`core/11:187` `stats` / `:207` `bStats` 死变量；`8230284`（狮群机制重做）与 **`08567d4`（狮子进战报 / 幼狮召唤 30% / 雄狮振奋锚出手帧 / 胖远桥加强）** 在履历均无对应条目，顶部仍为 V7.3.0 / 2026-09-23 —— 建议各补一条（V7.4.0 / V7.5.0），补完即可合规重录基线 18 场。
4. 清单维持：`spiderFly` 死通道、`meteorSplashGrowth` 孤儿登记、`butterflyNoHost` 缺 `sisterUid`、`zhangTaunt` 零命中、孤儿回血定性。

### 下轮目标

1. **下轮 merge 预警**：同一批业务改动被提交了两次——test 上是 `08567d4`、main 上是 `f31b9cf`（两次 `git diff` 补丁**逐字节一致**）。下轮 `git merge main --no-edit` 大概率按"两边做了相同改动"直接合并成功；万一冲突，按"内容相同取其一"裁决，别当成回归。
2. 基线：等 `8230284` 与 `08567d4` / `f31b9cf` 补上履历条目（建议 V7.4.0 / V7.5.0）后再重录 18 场；在此之前 DIFF 9 场**继续只报不重录**（本轮 9 场 winner 全不变，符合有意变更特征，不是回归）。
2. 140-wei 保持红、不许加豁免；若它变 skip，先查是不是被人加了跳过条件。
3. 复核本轮新增的"成长别名"判据在后续狮群内容（母狮随动 / 多只幼狮同回合成长）下是否仍成立。

### 推送

- 只 add 体检侧：`tests/` 与 `记录-更改履历.md`（业务侧文件一律不 add、不 stash、不 checkout）
- 提交后 `GIT_TERMINAL_PROMPT=0 git push gitee HEAD:test`，失败改试 `origin HEAD:test`；两者皆败则记"推送失败：<原因>；本地提交 <sha> 待推"
- 未强推、未推 main、未动他人分支、未改 remote、未写 token；收尾 `git checkout main` 还原工作区
- **实测结果**：提交 `5e66776`（7 文件 +156/−10），`git push gitee HEAD:test` 成功 `a213fe3..5e66776`。
  ⚠️ 该区间含用户自己的业务提交 `08567d4`（体检侧未碰它一个字，但它在 test 分支上，随本次推送一起到了远端 test）。
  用户若本意是落在 main，可 `git cherry-pick 08567d4` 自行搬回（提交内容完好，未做任何改写）。

---

## 第 15 轮 · 2026-09-24 14:51

### 开场

- 三条安全检查通过：在 `main`、`git status --porcelain -- tests/ "记录-更改履历.md"` 干净（仅 `.workbuddy/` 未跟踪，与你无关）。
- 上轮日志尾部**无待推**（`5e66776`/`2b3c1ae`/`c5bfbcb` 已推 gitee test）→ 无需补推，直接开新活。
- `git checkout test` → `git merge main --no-edit`：**Merge made by 'ort'，零冲突且零内容变化**——上轮预警的 `08567d4`(test) / `f31b9cf`(main) 同内容双提交被 git 判为「两边做了相同改动」，`git diff c5bfbcb HEAD` **空**。

### 深度判断：**D 档**（无改动 → 只做「体检的体检」）

主线相对上轮末没有带来任何新代码，所以不按 A 档跑全套归因；但五件套仍逐条实跑留证（下面「验证结果」），不是只信 merge 结果。

### 跑了什么

| 命令 | 结果 |
|---|---|
| `node tests/123static-scan.js` | 扫 103 文件 / 213ms，枚举缺失 0、共享符号 0、引用断裂 0 → **无致命结构问题** |
| `node tests/rules-replay.mjs` | **1 条失败（140-wei 流云身法 pass=0 fail=9 skip=111）；恒 skip 0** |
| `DEAD=1 node tests/rules-replay.mjs` | 恒 skip 0（红线仍绿） |
| `node tests/140-baseline.js --check` | **DIFF 9 场**（seed=1/42/999/777/88888 各若干 stage），**winner 全部不变**；退出码 **1** |
| `node tests/smoke-headless.mjs` | 20/20 局分出胜负（最长 19 回合）、播放层 12 导出齐全、3 个已删死函数确认不存在 → **全过** |

### 发现的问题（根因三行）

**【P2·已修】122 版本头 ~bytes 自引用失真 +152**

- ① 根因：`tests/122health-utils.js` 头记 `~27760`，取自**写新头之前**的实测 27758，而新头那一行自身又占 154 字节 → 写完即失真。这是行规手册第 11 条警告的同一陷阱；**第 13/14 轮只在 `rules-replay` 与 `121` 上修了，122 漏网**（第 13 轮复盘报告里还写着「已对齐」，实则没落上）。
- ② 证据：`tests/122health-utils.js:1` 头记 27760；`wc -c` 实测 **27912**（差 +152）；blob 27371、CRLF 差 541 = 行数，**换行符口径解释不了这 152**；本文件自 `a213fe3` 起再无提交改动 → 27758 只可能是写头前的旧值。
- ③ 影响范围：仅文档口径失真，不涉任何判据与执行结果；但它污染「体检的体检」里 ~bytes 核对项本身的可信度（复盘靠这一项兜底）。

**【P0·维持，不修】140-wei 流云身法结构性恒红**：`pass=0 fail=9 skip=111`，与上轮同形态（条数差系随机性）。成因是 `unit.buffDodgeBonus` **全库无写入源**（第 13 轮重新取证结论），退出条件在业务侧。**本轮未加任何豁免**，也未放宽判据。

**【红线·维持】基线 DIFF 9 场（winner 全不变）继续只报不重录**：差异源是 `8230284`（狮群机制重做）与 `08567d4`/`f31b9cf`（狮子进战报 / 幼狮召唤 30% / 雄狮振奋锚出手帧 / 胖远桥加强），**`记录-更改履历.md` 顶部仍是 V7.3.0 / 2026-09-23，三者在履历均无条目** → 不满足「有可依的业务有意变更条目」这一重录前置条件。9 场 winner 全不变（符合有意变更特征、不是回归），但**没条目就是不许录**。

### 改了什么

| 文件 | 改动 | 理由 |
|---|---|---|
| `tests/122health-utils.js`（V6.1.16 → **V6.1.17**，~27760 → **~28100**） | 仅版本头与 ~bytes | 自引用失真 +152（见上根因三行） |

- **负向测试（本改动为纯注释，判据零改动）**：不适用判据级负向测试；改后 `node --check` 过，且**复跑回放 / 冒烟 / 静态三项结果与改前逐条一致**（1 fail / 全过 / 0 条），证明无回归。~bytes 用「写完再测」的两遍法收敛：第一遍写入后实测 28099，第二遍把标注改成 28100（位数相同，不再移动字节）→ 终差 **−1**。

### 验证结果

- 五件套终态：静态 **103 文件 0 条** / 回放 **1 fail（140-wei）、恒 skip 0** / DEAD 恒 skip 0 / 冒烟 **全过** / 基线 **DIFF 9 场（未重录，退出码 1）**。
- **体检的体检（D 档主体）**：
  - 四方登记 **30 全齐** —— 规则文件 30 = 121 allRules 30 = 123 SCAN_FILES 30 = 124 RULE_META 30。本轮特意做了**双向 + 双口径**：既按文件号比（121/123），也按**规则名**比（124 的键是规则名，按文件号取会全空——首版探针就踩了，返回 0 条假阳性）；双向均「缺 0 / 多 0」。
  - 版本头 ~bytes 全量核对：121 −48 / **122 −1** / 140 −2 / rules-replay −24，全部落在「最近百位取整」的残差内；`123` / `124` / `smoke-headless.mjs` 本就无 ~bytes 标注（123 用 `VER` 串，非缺陷）。
  - 孤儿文件 0：本轮 2 个探针 `tests/_tmp-regcheck.mjs`、`tests/_tmp-regcheck2.mjs` 跑完即删。
  - 规则 import 符号完整性由 123 覆盖：引用断裂 0 条。
- 复盘触发检查：上次复盘为第 13 轮（第 4 轮复盘），本轮距之仅 **1 轮**（14→15）→ **不触发**复盘小节。

### 报业务侧 / 工具侧待修（本轮不动手）

1. **【P0·维持】`unit.buffDodgeBonus` 无写入源** → 140-wei 恒红（pass=0 fail=9 skip=111）。退出条件是业务侧恢复写入或改实时计算。
2. **【P2·维持】履历缺条目**：`8230284` 与 `08567d4` / `f31b9cf` 在 `记录-更改履历.md` 均无条目，顶部仍 V7.3.0 / 2026-09-23 —— 建议补 V7.4.0 / V7.5.0；**补完即可合规重录基线 18 场**（9 场 winner 全不变，符合有意变更特征）。
3. **【待确认·维持】连击名额可能白占**：`core/11battle-round.js:59` 挑概率连击单位只过滤 `isHorse`，未过滤 `isLionCub`；幼狮 `atk=0`（`content/200game-data.json`）按 `core/11:331` 走「幼狮休息」不出手，本回合未成长则名额空转。
4. 清单维持：`spiderFly` 死通道、`meteorSplashGrowth` 孤儿登记、`butterflyNoHost` 缺 `sisterUid`、`zhangTaunt` 零命中（本轮 fact 直方图 `zhangTaunt ×133`，非零，登记口径待核）。

### 下轮目标

1. **merge 预期**：本轮已把 main 合平（test 领先 main 一个 merge 提交 + 体检提交），下轮 `git merge main` 大概率 `Already up to date`；真有新提交就按 A 档跑全套。
2. 基线：继续等履历补 V7.4.0 / V7.5.0；在此之前 DIFF 9 场**只报不重录**，且每轮重查 winner 是否出现翻转（一旦翻转立即按回归报红）。
3. 140-wei 保持红、不许加豁免；若它变成 skip，先查是不是有人给它加了跳过条件。
4. `zhangTaunt` 在 fact 直方图有 133 条产出，但遗留清单仍记「零命中」—— 下轮核实是清单过期还是规则侧口径错位（属 P1 漏报嫌疑，需先探针取证）。

### 推送

- 只 add 体检侧：`tests/` 与 `记录-更改履历.md`（业务侧文件一律不 add / 不 stash / 不 checkout）
- `GIT_TERMINAL_PROMPT=0 git push gitee HEAD:test`，失败改试 `origin HEAD:test`；两者皆败则记「推送失败：<原因>；本地提交 <sha> 待推」
- 未强推、未推 main、未动他人分支、未改 remote、未写 token；收尾 `git checkout main` 还原工作区

---

## 第 18 轮 · 2026-09-24 18:07~18:23

### 开场与上下文继承

- **第 16 / 17 轮的缺口先补记**（两轮都没在本文留痕，工作区却留下了状态，本轮先收拾）：
  - 第 16 轮（15:55）`checkout test` + `merge main` 生成 `9d81339` 后**中断**，留下探针 `tests/_tmp-probe139.mjs`（16:01）未删、工作区停在 `test`；
  - 第 17 轮（17:02）因此**两条安全检查全中**（分支非 main + tests/ 脏）→ 整轮零动作跳过；
  - 本轮开工即删掉该孤儿探针（属"探针跑完即删"纪律，非用户改动），`git status --porcelain -- tests/ "记录-更改履历.md"` 归零后再开新活。
- 上轮（第 15 轮）日志尾部无待推，但第 16 轮的 `9d81339` 实际**未推 gitee**（gitee/test 停在 `4dfa2a8`）——本轮第一件事补推：`4dfa2a8..9d81339` 成功。
- 三条安全检查通过：开工在 `main`、tests/ 干净（删探针后）、收尾回 `main`。
- `git checkout test` → `git merge main --no-edit`：**Merge made by 'ort'，零冲突**，带入主线 1 个新提交 `1a6e035`（溅射逐目标广播 `SIGNAL_TYPES.SPLASH_DAMAGED` / 胖远桥莽撞吃溅射 + 拒马消散走普通死亡管线 / 韦一笑闪避吸血飘字 / 远程闪避弧线 / 无锚点行打字提速），8 文件 +69/−31。

### 深度判断：**A 档**（游戏代码有变 → 五件套全跑）

### 跑了什么

| 命令 | 结果 |
|---|---|
| `node tests/123static-scan.js` | 103 文件 / ~195ms，枚举缺失 0、共享符号 0、引用断裂 0 → 无致命结构问题 |
| `node tests/rules-replay.mjs` | 改前 **3 条失败**：139（fail=1）、140-wei（fail=8，维持）、**141（fail=20，新增）**；恒 skip 0 |
| `DEAD=1 node tests/rules-replay.mjs` | 恒 skip 0 |
| `node tests/140-baseline.js --check` | **DIFF 14 场（上轮 9 场）→ 退出码 1**，且**出现 winner 翻转 1 场** |
| `node tests/smoke-headless.mjs` | 全过（20 局分出胜负、FACT_SPECS 77 条完整、播放层 12 导出齐全、3 个死函数确认已删） |

### 发现的问题（根因三行）

**【P1 误报·已修】139 小昭妹/姐不可选期被打 —— 漏认「宿主阵亡」这条窗口关闭分支**

- ① 根因：附身窗口有**两个**关闭点，规则只认其中一个。正常飞回 `renderButterflyReturnFact`（`render/35-facts-effect.js:360-362`）带 `butterflyAction:'return'`；宿主阵亡被迫返回 `renderButterflyHostDeadFact`（`render/35-facts-effect.js:363-365`）**不带 `butterflyAction`**（只有 `uidD=sisterUid` + `isDead` + 固定文本「🦋 蝶变：宿主已阵亡，X 被迫返回！」）。旧判据只认前者 → 宿主一死窗口永不关闭，姐姐此后正常挨的每一次打都被判"附身期间被打"。
- ② 证据：探针 `tests/_tmp-probe139.mjs` 实跑 seed=17 stage=3 —— 0-based **198** attach（`sisterUid='u2'`）→ **202**「🦋 蝶变：宿主已阵亡，小昭·姊 被迫返回！」（标记命中表里**没有**这条 return，末条只到 i=198 的 attach）→ **206** `attack-group uidA=u9 uidD=u2` 被报复发。语义上宿主一死姐姐即恢复原形、重新可选，206 那条是**合法挨打**。
- ③ 影响范围：仅 139 一条规则；本批次 1/120 场误报（pass=119 fail=1）。不改业务语义，只补识别分支。

**【P1 误报·已修】141 张三丰核心机制 —— 严阵以待触发回合硬编码 6，与配置脱钩**

- ① 根因：规则把「严阵以待首现回合」写死为 6，而现行实现由配置门控：`modules/26elite-sixsects.js:43-44` 取 `getSkillParams('张三丰','tenRoundFortify')`，L128-133 在 `ON_ROUND_START` 里 `_roundCountForFortify > tf.round` 才触发并 push 摘要 →「首现回合 = `tf.round + 1`」。主线 `89baff6` 把 `round` 由 10 改成 3（首现 6 → 4），规则未同步。
- ② 证据：`content/200game-data.json:332-336` `tenRoundFortify.params.round = 3`；`modules/26` 注释「技能5：第 3 回合结束后仍未分胜负（即第 4 回合开始）」；回放 20 场失败消息**全部同一形态**「首次出现在第4回合」（无一场是 6，排除随机性）。
- ③ 影响范围：仅 141；20/120 场假红，且会一直红到有人再改硬编码。**判据跟配置走，不跟历史值走**。

**【红线·报红，不重录】基线 DIFF 14 场，其中 1 场 winner 翻转**

- ① 事实：`node tests/140-baseline.js --check` 退出码 **1**；DIFF 由 9 → **14 场**；`seed=999 stage=3` 为 `winner=明教->六大派`（基线 `明教` / 15 回合 / 142 facts，当前 `六大派` / 15 回合 / 133 facts），**winner 翻转**。单场复跑两次均得「六大派 15 回合 133 facts」，确定性无抖动。
- ② 归因（`git archive 9d81339` 取合并前树独立复跑）：合并前那棵树**已经是 14 场 DIFF 且同一场翻转** → 翻转与 `1a6e035` 无关，来自 `89baff6`（张三丰严阵以待提前到第 3 回合 / 胖远桥嘲讽永久+40 防 / 韦一笑吸血 5%→50%）或 `f31b9cf`（狮群机制 / 幼狮召唤 30%）。**两笔在 `记录-更改履历.md` 均无条目**（顶部仍 V7.3.0 / 2026-09-23）。
- ③ 处置：按红线「winner 翻转不是有意变更、是回归」，**报红并严禁重录**；同时因缺履历条目，本就连"有意变更"这一前置条件也不成立。请业务侧确认该翻转是否为预期（最可能的通道是严阵以待提前到第 3 回合让六大派前中期变硬），并补 V7.4.0 / V7.5.0 条目。

**【P0·维持，不修】140-wei 流云身法结构性恒红**：`pass=0 fail=8 skip=112`，与上轮同形态（条数差系随机性）。成因 `unit.buffDodgeBonus` 全库无写入源（第 13 轮取证结论），退出条件在业务侧。**本轮未加任何豁免、未放宽判据**。

### 改了什么

| 文件 | 改动 | 理由 |
|---|---|---|
| `tests/health-rules/139-spider-butterfly-target.js`（V6.0.0 → **V6.1.26**） | 新增 `isHostDeadReturn(e)`（文本含「蝶变」+「宿主已阵亡」），与主循环里 `findMarker(e,'butterflyAction','return')` **并列**为附身窗口关闭点；文件头补 V6.1.26 三段式说明 | 补上第二个关闭分支，消除误报（判据语义一字未动：窗口内被打仍报） |
| `tests/health-rules/141-zhangsanfeng-fortify-round.js`（V6.1.24 → **V6.1.26**） | 新增 `import { getSkillParams }`；期望回合改为配置推导 `tf.round + 1`（取不到配置 → skip）；`maxRound < 6` → `maxRound < expectedRound`；两处失败消息带上配置值 | 判据与配置脱钩，不再随业务调阈值就假红 |

- **双向负向测试（`tests/_tmp-neg.mjs`，跑完即删）21 例全通**：
  - 139 十例：附身窗口内被打 FAIL / **宿主阵亡后被打 PASS（本轮新增分支）** / 正常飞回后被打 PASS / 窗口内打的是别人 PASS / 空战报 SKIP / 只有 attach 无攻击 PASS / 宿主阵亡后先打别人再打姐姐 PASS / 二次附身第二次窗口内被打 FAIL / 飞天窗口内被打 FAIL / 蛛落后被打 PASS。
  - 141 十一例：首现恰第 4 回合 PASS / 推迟到第 5 回合 FAIL / 提前到第 2 回合 FAIL / 打满 6 回合零触发 FAIL / 只打到第 3 回合 SKIP / 无张三丰 SKIP / 张三丰阵亡 SKIP / 他人同名摘要（小昭·姊）不算 FAIL / 出现「如沐春风」FAIL / 生生不息满血有溢出 PASS / 满血无溢出 FAIL。
  - **踩坑（已写进行规手册第 13 条）**：合成用例必须给 `beforeE` 带上攻击者 uid，否则 139 末尾「`enemyAttacks===0` → skip」会让所有"应 pass"用例一律变 skip（首版 6 例假失败全是这个）。

### 验证结果

- 五件套终态：静态 **103 文件 0 条** / 回放 **1 fail（140-wei）、恒 skip 0** / DEAD **恒 skip 0** / 冒烟 **全过** / 基线 **DIFF 14 场、1 场 winner 翻转（未重录，退出码 1）**。
- 逐条对照：139 **pass=119 fail=1 → pass=120 fail=0**；141 **pass=0 fail=20 skip=100 → pass=20 fail=0 skip=100**；140-wei 维持 fail=8；其余 27 条 pass/fail/skip 与改前**逐条一致**（无回归）。
- **首版翻车记录**：141 首版写成 `tf.params.round`（`getSkillParams` 返回的**就是** params 对象本身，见 `core/01config-5v5-test.js:24-28`），取不到值 → 全场 `skip=120`，被 DEAD=1 当场逮到（恒 skip 由 0 变 1）。已按 `modules/26:43-44` 的 `tf.round` 同款写法修正并写进文件头注释。
- 体检的体检：四方登记 **30 全齐**（规则文件 30 / 121 import 30 / 123 SCAN_FILES 30 / 124 RULE_META 按规则名 30）；本轮未新增规则，无需补登；孤儿探针 **0**（`_tmp-probe139.mjs`、`_tmp-neg.mjs` 跑完即删）。
- 复盘触发检查：上次复盘为第 13 轮（第 4 轮复盘），距本轮 **5 轮**（13→18）→ 尚未满 6 轮，**不触发**；下轮（第 19 轮）满 6 轮，开场先做复盘小节。

### 报业务侧 / 工具侧待修（本轮不动手）

1. **【红线·回归】基线 `seed=999 stage=3` winner 翻转：明教 → 六大派**（15 回合 142 facts → 15 回合 133 facts）。归因指向 `89baff6` / `f31b9cf`，最可能通道是「张三丰严阵以待由第 6 回合提前到第 3 回合」。请确认是否预期；**在确认前基线不重录**。
2. **【P2·维持】履历缺条目**：`8230284`、`f31b9cf`/`08567d4`、`89baff6`、`1a6e035` 四笔在 `记录-更改履历.md` 均无条目，顶部仍 V7.3.0 / 2026-09-23 —— 建议补 V7.4.0 / V7.5.0；补完才谈重录基线 18 场。
3. **【P0·维持】`unit.buffDodgeBonus` 无写入源** → 140-wei 恒红（pass=0 fail=8 skip=112）。退出条件是业务侧恢复写入或改实时计算；**不许加豁免**。
4. **【工具侧·观察】** `renderButterflyHostDeadFact`（`render/35:363-365`）是唯一不带 `butterflyAction` 的蝶变条目，与本族另外三条不同形。规则侧已按文本兜住；若业务侧愿意补一个 `butterflyAction:'return'`，139 可去掉文本识别（属业务侧改动，红线内不碰）。
5. 清单维持：`spiderFly` 死通道、`meteorSplashGrowth` 孤儿登记、`butterflyNoHost` 缺 `sisterUid`（本轮核过：`render/35:358` **带** sisterUid，该条清单条目口径待核）、`zhangTaunt` 直方图 134 条。

### 下轮目标

1. **下轮（第 19 轮）开场先做复盘小节**：自第 13 轮复盘起本轮已满 6 轮 —— 独立复跑近 3 轮（15/18）声称的全部数字对账、修剪日志冗余、核对版本头、整理遗留清单，记入 `tests/体检复盘报告.md`。
2. 140-wei 保持红、不许加豁免；若它变 skip，先查是不是被人加了跳过条件。
3. 基线继续只报不重录；每轮重查 winner 是否再出现翻转（本轮已翻 1 场，若再增即升级为 P0 回归单独立项）。
4. `zhangTaunt` 遗留清单「零命中」与 fact 直方图 134 条矛盾 —— 下轮核实是清单过期还是规则侧口径错位（P1 漏报嫌疑，先探针取证）。

### 推送

- 只 add 体检侧：`tests/` 与 `记录-更改履历.md`（业务侧文件一律不 add / 不 stash / 不 checkout）
- 补推先于新活：`9d81339` 已推 gitee test（`4dfa2a8..9d81339`）；本轮提交随后 `GIT_TERMINAL_PROMPT=0 git push gitee HEAD:test`，失败改试 `origin HEAD:test`；两者皆败则记「推送失败：原因；本地提交 <sha> 待推」
- 未强推、未推 main、未动他人分支、未改 remote、未写 token；收尾 `git checkout main` 还原工作区

- **实测结果**：提交 `1c64c3c`（6 文件 +174/−12，仅体检侧），`git push gitee HEAD:test` 成功 `9d81339..1c64c3c`。

---

## 第 19 轮 · 2026-09-24 22:45~23:05（交互式：用户持浏览器体检结论来问 + 提默认参数需求）

### 触发（用户原话要点）

浏览器跑完体检报 4 条，问"是合理的吗"，并要求"体检改成从第二关开始默认 4 倍速"：

1. 关卡3 规则 — 流云身法已生效，但 5 名存活队友 `buffDodgeBonus` 全为 0
2. 关卡3 / 关卡6 规则 — 九阳神功回复 24 ≠ floor(血上限 112.5 × 12% = 13)
3. 关卡4 规则 — 快乐回血 58 点，但血量 180→182 既非 +58 也非回满截断
4. 关卡5 UI — 战斗结束后 75 个特效未清理

### 深度判断：**C 档起手，实为 A 档**（回放器与浏览器结论相反 → 先做数据源对账，不是先看数值）

### 跑了什么

| 命令 | 结果 |
|---|---|
| `SEEDS=1..60 STAGES=3,4,6 node tests/rules-replay.mjs` | 180 场：143 **pass=162 fail=0**；144 **pass=58 fail=0**；140-wei fail=16；其余全绿 |
| 探针双喂（同一批战报，真终局 vs 开战副本冒充终局） | 143 真终局 fail=**0** / 冒充 fail=**40**；144 真终局 fail=**0** / 冒充 fail=**25** |
| 单位 camp 字段探针 | `ally=["ally"×5]`、`enemy=["enemy"×5]`、`uid=u1..u5 / u6..u10` → 分组依据成立 |
| 五件套（改后复跑） | 123 **0 条**；replay **1 fail（140-wei 维持）**；DEAD **恒 skip 0**；smoke **全过**；baseline **exit=1（14 场 DIFF 维持，未重录）** |

### 发现的问题（根因三行）

**【P0 体检假红·已修】143 / 144 误报 —— 121 把「开战副本」当「终局快照」喂给规则**

- ① 根因：`runBattleRules` 取 `ctx.UI.allyTeam` 同时充当 beforeA 与 afterA，而 `ui/65main-battle.js:27` 只在**开战** clone 一次、战斗全程不再更新；2026-09-14「状态三轨收敛」后单位真值的唯一账本已迁到 battleStore（`ui/62ui-render-5v5-test.js:36` 明写「战斗期一律读 battleStore，不再读 c.UI 的冗余拷贝」）。**本文件头 V6.1.0 注释其实早就记着「UI.allyTeam 是开战 clone 副本 alive 恒真」，只是这条事实从未传到规则侧。**
- ② 证据：同一批 180 场战报双喂 —— 真终局 fail 0/0；冒充后 143 误报 40 场、144 误报 25 场，样例「九阳神功回复4≠floor(血上限**112.5**×12%=13)」「快乐回血**58**点，但血量161→**182**」与用户报的 112.5 / 58 / 182 **数值完全吻合**（第 5 关张无忌开战 maxHp 就是 112.5）。
- ③ 影响范围：所有靠终局值反推/兜底的判据（143 血上限、144 回满截断、140 buff 面板字段）。回放器用真终局所以全绿 —— 与浏览器结论相反这件事本身就是最强证据。

**【P0 业务侧真 Bug·报红不豁免】140-wei 流云身法 —— 字段被架构收敛删了，读取端没跟着删**

- ① 根因：`buffDodgeBonus` 在**真终局单位对象上和 battleStore.units 上都是 0/11**（探针旁证），即这个字段根本不存在于单位上；`modules/24battle-store.js:1` 版本头自陈「V6.1.0 妆造字段剥离……**删 buffXxxBonus 顶层字段**」，`UNIT_TOP_FIELDS`（24:11-16）也不含它，而 `render/32-grid-render.js:96-98` 仍在读 `unit.buffDodgeBonus` → 恒 `undefined` → 闪避面板永远显示不了流云加成。
- ② 证据：`git log -S buffDodgeBonus` 指向 `e266abc`（妆造字段剥离）；探针实测终局单位 + store 两侧均 0/11。
- ③ 影响范围：业务侧渲染一条。旧结论「业务侧缺写入源」精确化为「**字段被 e266abc 删除、读取端未同步删**」。**体检不加豁免、不放宽判据。**

**【待确认·倾向误报】特效残留 75 个**：判据 `122health-utils.js:87-93` 是 `doc.querySelectorAll('[data-fx="temporary"]').length > 5` 即报，而特效池有 3500ms 存活期；一键体检原 speed=100（8x）下状态切换极快，GAMEOVER 时大量特效仍在存活窗口内。**本轮不动**（需浏览器实测，且用户没要求改它）。下轮用 4x + 延迟采样复测，若仍是 75 个再定性为真泄漏。

### 改了什么

| 文件 | 改动 | 理由 |
|---|---|---|
| `tests/121health-monitor.js`（V6.1.2 → **V6.1.3**） | 新增 `finalTeamsFromStore()`：`afterA/afterE` 改从 `battleStore.getState().units` 按 `camp` 分组（无 camp 时退回按开战副本 uid 集合归类），取不到 store 再退回 UI 快照；`beforeA/beforeE` 维持用 `ctx.UI.allyTeam`（它本就是开战 clone，语义正确） | 修 143/144 数据源错位；退回路径保证取不到真值时体检照样跑，不伪造数据 |
| `tests/120test-runner.html`（V6.0.1 → **V6.0.2**） | 两处一键体检默认参数：`start` 3→**2**、`speed` 100→**300** | 用户要求。档位口径 `ui/68ui-controls.js:166 attachSpeedButton('btnSpeed4x', 300)`：**100=8x / 300=4x / 500=默认**，故本次实为**放慢一档**（换快进下 UI 检查少滞后误报），已写进文件头注释 |

### 验证结果

- `node --check tests/121health-monitor.js` OK；五件套终态：123 **0 条** / replay **1 fail（140-wei）** / DEAD **恒 skip 0** / smoke **全过** / baseline **exit=1（14 场 DIFF，未重录）** —— 与第 18 轮逐项一致，无回归。
- 单位 `camp` 字段探针确认分组依据成立（ally 全 'ally'、enemy 全 'enemy'）。
- **踩坑（已加进行规手册）**：Git Bash 的 `sed -i` 会把 CRLF 全改成 LF（121 一次掉 840 字节，等于每行一个 `\r`）。已用 node 统一转回 CRLF，`git diff --stat` 确认只剩 55 行真实改动。**以后在体检文件上改文本一律用 Edit 工具或 node 脚本，不要用 sed。**

### 复盘小节（距第 13 轮第 4 次复盘已满 6 轮，本轮补做）

- **数字对账（声称为证、复跑为准）**：第 15/16/18 轮声称的关键数字全部复跑 —— 123「0 条」✓、replay「1 fail（140-wei）恒 skip 0」✓、DEAD「恒 skip 0」✓、smoke「全过」✓、baseline「14 场 DIFF 含 1 场 winner 翻转，未重录」✓。**无一条对不上。**
- **版本头 ~bytes 核对**：121 声明 ~46300 / 实测 46263；120 声明 ~15300 / 实测 15314；均在 `~` 容差内且为"写完之后的实测值"。
- **遗留风险清单**（详见 `tests/体检复盘报告.md`）：`runEngineChecks` / `runUIChecks` 同用开战副本（同根因假绿，本轮未动）；140-wei 业务侧字段缺失；基线 14 场 DIFF 待履历补条目；`butterflyNoHost 缺 sisterUid` 经本轮探针复核**确为真实现象**（清单没过期）；`spiderFly` 死通道、`meteorSplashGrowth` 孤儿登记维持。

### 报业务侧 / 工具侧待修（本轮不动手）

1. **【P0】`unit.buffDodgeBonus` 被 `e266abc` 删除但 `render/32:96-98` 仍在读** → 流云身法闪避面板恒不显示。请业务侧二选一：恢复字段，或删掉读取端。
2. **【P2】履历仍缺条目**：`89baff6` / `f31b9cf` / `1a6e035` 在 `记录-更改履历.md` 无条目（顶部仍 V7.3.0），基线 14 场 DIFF 的重录前置条件不成立。
3. **【工具侧·观察】** `renderButterflyHostDeadFact`（`render/35:363-365`）仍是唯一不带 `butterflyAction` 的蝶变条目，139 靠文本兜底；补上字段后 139 可去掉文本识别。

### 下轮目标

1. 请用户在浏览器复跑一次：143/144 应消失；若仍在，说明 `battleStore` 路径在真实页面取不到（本轮已加退回路径，需实测）。
2. `runEngineChecks` / `runUIChecks` 同样吃 `ctx.UI.allyTeam`（开战副本）——**同根因假绿**，下轮改透，别留尾巴。
3. 特效残留用 4x + 延迟采样复测定性。
4. 140-wei 保持红不许加豁免；基线继续只报不重录。

### 推送

- 本轮在 `git worktree` 里改（主工作区有用户未提交的 `fx/88fx-trigger.js`、`render/39-actions-defs.js`，一律不碰、不 add、不 stash）
- 只 add 体检侧：`tests/` 与 `记录-更改履历.md`；提交后 `GIT_TERMINAL_PROMPT=0 timeout 180 git push gitee HEAD:test`
- 未强推、未推 main、未动他人分支、未改 remote、未写 token

---

## 第 20 轮 · 2026-09-25 13:12~（**改在 main 分支直接进行**，用户授权「直接接进 main，不动主代码」）

### 开场：本轮工作模式变更

- 用户指令：「优化体检相关，直接接进 main 分支，不动主代码就行，有主代码配合需求先申请，其他的你充分思考后优化」。
  → 本轮**不再走 test 分支 + worktree 隔离**，直接在 main 上改 tests/；主代码（core/ui/player/render/fx/modules…）一行未改。
- 工作区当时有用户自己的未提交改动：`render/32-grid-render.js`（V6.4.1 闪避面板改走 computeBuffStats）
  与 `tests/health-rules/140-wei-dodge-cloud.js`（V6.1.0 规则重写）。**前者属主代码，本轮未 add、未 stash、未 checkout，原样保留**；
  后者属体检侧且是本轮方向③的正解，已随 tests/ 一并入库。
- 发现 main 与 test 已分叉：main 有 7 个 test 没有的提交（`2a5f68d` 架构重构、`8194e77` 幼狮 30%→50% + 明教固定位恒为 3、
  `1a6e035` 拒马消散走普通死亡管线等）；test 有第 14/18/19 轮的体检修复 main 没有。
- main 的 `tests/` 里**完全没有**体检记档文档（迭代日志/总览/行规手册/复盘报告只在 test 上），本轮一并带入，避免主线失去连续性。
- main 的 `140-baseline.js` 是 V1.0.0，**根本没有 `--check` 模式**（第 18 轮那套 diff 逻辑只在 test 上），本轮带入 V1.2.0 后再改。

### 深度判断：**A 档**（游戏代码有变 → 五件套全跑）

### 跑了什么（改前基线）

| 命令 | 改前结果 |
|---|---|
| `node tests/123static-scan.js` | 通过 |
| `node tests/rules-replay.mjs` | **4 条失败**（139 fail=1、概率连击 fail=3、拒马阵 fail=4、carry fail=1）+ 恒 skip 2 条（姐姐附身叠加、张三丰核心机制） |
| `node tests/140-baseline.js --check` | main 上**无法运行**（无 check 模式） |
| `node tests/smoke-headless.mjs` | 全过 |

### 改了什么

**【移植】test 分支上 main 缺失的三条规则修复**（`git show test:...` 直接取）

| 文件 | 修复内容 | 效果 |
|---|---|---|
| `139-spider-butterfly-target.js` | 补「宿主已阵亡」附身窗口关闭分支 | fail 1 → **0** |
| `141-zhangsanfeng-fortify-round.js` | 严阵以待首现回合改由配置推导（原硬编码 6） | 恒 skip 120 → 正常参与判定 |
| `146-double-strike.js` | 幼狮原地改名别名比对（母狮/雄狮） | fail 3 → **0** |

**【方向①】真值源统一 —— `121health-monitor.js` V6.1.0 → V6.2.0**

- 新增 `teamsFromStore(ctx)`：返回**调用时刻** battleStore 的真值（取不到退回 UI 快照并报一次"真值源降级"，绝不静默伪造）。
- 引擎 / UI / 规则 / 结算**四组全部**改走它（原四组全吃 `ctx.UI.allyTeam` 开战副本＝系统性假绿）；
  `runRuleChecks` 的 before 保留开战副本（语义本就如此），只把 after 换成真值。
- 关键：这四组在**采样循环中途**就被调用，故取到的是**中期真值** —— 「中期溢出后被修回」这类漂移终局快照抓不到，改后能抓到。

**【方向②】不变量套件 —— `rules-replay.mjs` V6.1.11 → V6.2.0**

- 新增逐步（每 step）断言：`hp∈[0,maxHp]`、`maxHp>0`、同阵营 `pos` 唯一、factType 是否注册渲染器。
- 首版两条判据**全部假阳性、已作废**：① 断言 hp 为整数 → 引擎 hp/maxHp 本就是浮点，误报 **11901 条**；
  ② 用"本次渲染有无产出"判映射缺口 → 误报 **929 条**（两个类型都有注册渲染器，只是无条件时合法为空）。
  正解改为查注册与否（`getFactRenderer`）。详见行规手册第四条踩坑。

**【方向③】规则不许复制产品实现 —— 由用户完成，本轮只做验证**：`render/32` V6.4.1 改走 `computeBuffStats().dodgeBonus`、
规则 140 改为直接调 `getDodgeBreakdown` 断言面板输出。实测**规则 140 由恒红（fail=8）转为 pass=4 fail=0**。
（我上轮提议的正是这个方向，用户已落地；本轮确认 `render/32:469` 确实导出了该函数，Node 回放侧可正常 import。）

**【方向④】基线二分 —— `140-baseline.js` V1.2.0 → V1.3.0**

- 新增 `commitsSince()`：取基线 `generatedAt` 之后的提交说明。
- DIFF 二分：winner 翻转 → **回归**（硬失败、报红不重录）；winner 未变 + 基线后有业务提交 → **预期变更**（单列、
  打印提交清单供人工核对、不硬失败）。只有回归才退码 1。

### 验证结果

| 项 | 结果 |
|---|---|
| `node tests/123static-scan.js` | 通过 |
| `node tests/rules-replay.mjs` | **2 条失败**（拒马阵 fail=4、carry fail=1）、恒 skip 1 条、**不变量违规 26 类** |
| `node tests/smoke-headless.mjs` | 全过 |
| `node tests/140-baseline.js --check` | **退码 1**：预期变更 15 场 / **回归 1 场**（seed=42 stage=3 winner 六大派→明教 **翻转**） |

### 报业务侧 / 待确认（本轮不动手，需主代码配合的先申请）

1. **【新发现·不变量】同阵营双单位占同一格，26 例**：如 seed=6 stage=4 雄狮与小昭·姊同占格 4（round 4~8 持续）、
   seed=9 stage=1 明教弟子3 与张无忌同占格 8。渲染层 `render/32:224-226` 按格只取第一个单位 → 另一个**渲染不出来**。
   限定条件：**仅在团队 Buff 注入生效时出现**（无 Buff 探针零冲突），且 `pos` 唯一性有 `modules/29` 的 `takenPos` 佐证。
   请确认是某个 Buff 机制（换位/击退/召唤）写坏了占位，还是合成 Buff 组合所特有。
2. **【回归·红线】基线 seed=42 stage=3 winner 六大派→明教**（rounds 11→14）。红线：报红、**未重录**。
3. **【待确认】拒马阵 fail=4**：「第 N 回合 X 号位拒马被清算两次」。疑似 `1a6e035`「拒马消散走普通死亡管线」
   引入的双重清算：原有消散路径 + 新的死亡管线各记一次。请确认是真重复清算（应去重）还是规则判据需跟上新管线。
4. **【待确认】carry 五号位 fail=1**：攻+40 超上界（上界 31）。该上界**是配置推导**（`sumAtk × cfg.atkBonus × deathMultiplier × 2` 余量）
   非硬编码，超 2 倍余量说明实际计算口径与规则假设不一致；`8194e77` 改了明教固定位，可能影响"五号位/全队之和"的构成。
5. 用户未提交的 `render/32-grid-render.js`（V6.4.1）本轮**未代为提交**，请自行入库，否则该修复未落盘。

### 下轮目标

1. 口径收紧：方向④目前"基线后有业务提交"这个判据太松（基线生成于 2026-08-28，之后已有 **254 条**提交），
   导致非 winner 差异几乎一律归预期变更。建议人工核对后**重录基线**，把时钟拨正，此后二分才有意义。
2. 拒马阵 / carry / pos 三条待确认项，等业务侧答复后决定是改规则还是改判据。
3. 「姐姐附身叠加」恒 skip 120 —— 按 P0 两类性质先分辨：机制本场未触发（合理，不动）还是数据源错位（改扫描层级）。

### 推送

- 本轮按用户指令在 **main 分支直接提交**（未走 test、未建 worktree）；未强推、未动他人分支、未改 remote、未写 token。
- 只 add 体检侧（tests/ + 记档文档）；用户未提交的 `render/32-grid-render.js` **未 add**。

---

## 第 21 轮 · 2026-09-25 21:27~22:20（**体检模块全盘优化**，main 分支，主代码零改动）

### 开场：本轮任务形态变了

用户指令：「把体检模块分析叠带一下，按照一直要求的，先分析之前的记录，然后阅读全盘代码，优化。可以删减无效的，增加有必要的，修改需要调整的。但是不要乱动主代码，主代码需要调整时提前申请。」

→ 本轮不是"修某条规则"，是**全盘体检**。按指令分三段走：① 先复盘历史记录 ② 再通读体检模块全盘代码 ③ 最后动刀。两路并行取证（一路专啃四份记档文档、一路清点 tests/ 全盘代码），再由我自己**逐条复核**二手结论后才动手。

### 一、复盘历史记录得到什么

- **最活跃根因仍是「规则依赖产品实现细节」，历史复发 6 次**（127 `_baseAtk` / 123 引用已删守卫 / 137 行号失效 / 132 `hpAfter` / 140 `buffDodgeBonus` 挂 15 轮 / 141 硬编码 6）。140 已于第 20 轮闭环，但**该根因仍活着**（本轮 132、142 又是同一形态）。
- 已闭环：终局快照 vs 真值（第 20 轮四组改透）。**未闭环**：基线时钟停摆、恒 skip 残 1 条、孤儿回血挂 16 轮、zhangTaunt 口径挂 5 轮。
- 文档自身也过期：手册有两个"第 9 条"（后续序号整体错位）、头部仍写"11 轮 + 3 次复盘"；总览 ⚡ 块时序倒挂、§2 文件数 79→103、§7 声称已删的文件在 main 上仍在。

### 二、全盘代码清点得到什么（复核后采信的部分）

- 死代码：`TAG_PREFIX`(124:39)、`ctx._doc = doc`(121，全库无规则读)、`getRuleGroups` 未使用的 `prefix`。
- 三个被取代的临时 runner `baseline-check/diff/entries.mjs` 在 **main 上仍在**（自述"跑完即删"，能力已被 `140-baseline` V1.2.0 收编）。
- 覆盖缺口：134/139 依赖 `ctx.snapshot`，而回放构造的 ctx 没有它 → 那几条判据在回放里**静默不执行**（回放侧假绿）。
- 不变量双份实现且口径不一致：`122 checkUnitHpValidity` 多一条「`_baseMaxHp×2.5` 膨胀」判据（含 isWei 豁免），回放侧没有 → 同一批单位在浏览器体检和回放里两套结论，违反第 20 轮刚立的四原则第 2 条。
- 缺 VER：`124` / `rules-replay` / `smoke` 三个文件没有 `export const VER`，版本头对账会漏。

### 三、改了什么

**【删减·无效】**

| 对象 | 理由 |
|---|---|
| `baseline-check.mjs` / `baseline-diff.mjs` / `baseline-entries.mjs` | 自述"临时 runner…跑完即删"，`140-baseline.js` V1.2.0 已明写"吸收并取代"。已用 `git rm` 删除 |
| `TAG_PREFIX`（124） | 定义后全库零引用（分组逻辑用的是内联三元） |
| `ctx._doc = doc`（121） | 全库无任何规则读 `ctx._doc`，纯死赋值 |
| `getRuleGroups` 的 `prefix` 变量 | 计算出来后未使用 |
| 132 的判据2 | **伪判据**，详见下方踩坑 |

**【修复·复活血判据】**

| 文件 | 旧 → 新 | 效果 |
|---|---|---|
| `132-claw-damage.js` | 读已删字段 `clawTargetHpAfter` → 按现行 `hpAfter`（`render/35:478`，值来自 `modules/26:534` 的 `simulatedTargetHp`） | 不只判据2，连**判据3 连锁调血校验**也一起复活 —— 此前 hpAfter 恒 undefined，NaN 参与等值校验使其全程空转 |
| `142-zhangsanfeng-endless-roundstart.js` | 删 `\|\| e.factType === 'roundStart'` 与 `e.data.round` 两个死分支 | `factType`/`data` 已被 renderLog 剥离（141:5-6 明写此约定），只认 `type` + `text` |

**【补覆盖缺口】** `rules-replay.mjs` 的 ctx 补 `snapshot`。

- 语义取证（关键，差点搞反）：`ui/61main-5v5-test.js:392-393` 注释明写「战斗进行中 snapshot **不反映当前态**」「snapshot.enemy 是 Object.freeze 的定稿」→ snapshot 是**开战定稿**，故必须喂 `beforeA/beforeE`；**若喂终局单位反而会伪造口径**。
- 效果：134/139 里依赖它的判据从"回放里静默不执行"变为真正运行。

**【统一·单一真值源】** 血量不变量：回放删掉自己那份实现，改为直接复用 `122 checkUnitHpValidity`；同时给 122 补上「maxHp 自身须合法（非数值/≤0）」这条（原缺失，会让钳制判据失去意义）。

**【补齐】** `124` / `rules-replay` / `smoke-headless` 三个文件补 `export const VER`。

**【文档与参数】** 手册重排错位编号（原有两个"第9条"，后续 10~17 整体错位，已重排为 1~18）+ 更新过期头部；体检页面 `120test-runner.html` 清 **30 个冗余 BOM**、修体积声明（原写 ~6000、实测 14779）、并把「第 2 关起 / 4 倍速」默认参数同步到 main（此前只落在 test 分支）。

### 四、踩坑（本轮最重要的一条）

**「复活血判据」必须同步验证判据的语义是否还成立，否则复活的是假红。**

- 132 判据2 原文：标记斩杀 → 目标血量应为 0。按现行字段复活后**立刻报 15 条**。
- 取证（`modules/26elite-sixsects.js:534`）：`isExecute = !isDeadByHit && hpPctAfter <= execThreshold && simulatedTargetHp > 0` —— **斩杀的定义就是"没被打死但血量跌破阈值、且 hp 仍 > 0"**。所以 isExecute 为真时 `hpAfter ≠ 0` 是**设计使然**，该判据自相矛盾、一复活必然假红。
- 处置：**删除而非放宽**（放宽就是为了让数字变绿的折中）。删掉并在文件头写清证据。斩杀真正应保证什么（例如"该连锁序列内最终致死"）属业务语义，不猜，记待确认。

另一条：本轮**你在并发提交主代码**（`5bef450`、`28d0388`、`31e781f`），导致回放数字在取样间漂移。用 `git archive HEAD` 取原始树做干净归因才拿到可信的净效果；并已验证回放本身**确定且与时间无关**（连跑两次一致、间隔 45 秒仍一致）。

### 五、验证结果（干净归因：工作树 vs HEAD 原始树）

| 项 | HEAD | 本轮工作树 |
|---|---|---|
| 回放 | 3 条失败 / 恒 skip 1 / 不变量 0 | **4 条失败 / 恒 skip 1 / 不变量 0** |
| 唯一净变化 | — | **134 张无忌近身切换时机：pass=81 fail=0 → pass=74 fail=7**（snapshot 缺口补上后暴露） |
| 静态扫描 | 通过 | 通过 |
| 冒烟 | 全过 | 全过 |
| 基线 `--check` | 退码 1 | 退码 1（预期变更/回归二分，未重录） |

### 六、特别记录：不变量套件首次抓到并证实为真 Bug

第 20 轮我用新增的 pos 不变量报了「同阵营双单位占同一格 26 例」。本轮你的提交 `5bef450` 正是 **「乘风突袭击退取错队——敌方飞行单位触发时把两个阵营的人对调，造成同阵营双人同格」** —— 与该发现完全对应，修复后不变量由 26 归零。**这是不变量套件上线以来第一次抓到真 Bug 并被业务侧修复证实**，验证了第 20 轮方向②的价值。

### 七、报业务侧 / 待确认（本轮未动）

1. **134 张无忌近身切换时机 fail=7**：「前排队友第 N 回合阵亡，张无忌第 M 回合才切换近战（切换时机过晚）」。该判据此前在回放里从不执行，是本轮补 snapshot 后才首次跑起来 —— 未经验证过的新信号，请确认是真时机回归还是判据预期已过时。
2. **斩杀语义待确认**：132 判据2 已删。请确认「斩杀」真正应保证什么（如"该连锁序列内最终致死"），确认后我按你的口径补一条正确的判据。
3. **基线时钟仍未拨正**：`generatedAt=2026-08-28`、之后 254 条提交 → 非 winner 差异几乎一律被判为"预期变更"，回归红线实际被架空。解开的前提是 winner 翻转项有结论（红线：翻转=回归，未重录）。
4. 历史遗留仍挂：`zhangTaunt` 零命中 vs 直方图 134 条（挂 5 轮）、孤儿回血定性（挂 16 轮）、`spiderFly` 死通道、`meteorSplashGrowth` 孤儿登记。
5. 工具侧待登（未动 tools）：`tools/106` 未登记 `rules-replay.mjs` / `smoke-headless.mjs`（两个活跃 runner 不随包），且 121/122 在清单里重复登记两次。

### 八、下轮目标

1. 等业务侧对 134 与斩杀语义的答复，再决定是改规则还是改判据。
2. 基线：一旦 winner 翻转有结论，就重录基线把时钟拨正，二分才有意义。
3. 恒 skip 残 1 条（姐姐附身叠加）：按两分法定性（机制未触发 vs 数据源错位）。

### 推送

本轮在 main 直接提交（`d8b9aab`），只 add 体检侧；未强推、未推远端、未动他人分支、未改 remote。主代码一行未改（你的 `5bef450`/`28d0388`/`31e781f` 均为你自己的提交，本轮未触碰）。

---

## 第 22 轮 · 2026-09-25 22:57~（**P0 收口 + 误报清零**，main 分支）

### 开场

延续第 21 轮的全盘优化，本轮按协议优先处理 **P0**：① 唯一那条恒 skip 空转 ② 假绿。主代码零改动（遇需主代码配合的走"申请"）。

### 一、恒 skip 空转定性并修复：127「姐姐附身叠加」

- **定性**：不是"机制未触发"，是**机制在场但 0 命中**的数据源错位 → 属 P0，必须改扫描层级。
- **取证（探针，用完即删）**：48/120 场确有「化为蝴蝶附身」文本且存在 `isXiaoZhaoSister`，其中
  **顶层 `_baseAtk` 有值 0 场、`state._baseAtk` 有值 48 场**（`core/02unit.js:177` 写的是
  `this.state._baseAtk`，`core/13:89` 取用时也是 `unit.state._baseAtk`）。规则读顶层 → 恒 undefined → 120 场全 skip。
- **改法**：`sister._baseAtk` → `sister.state._baseAtk`（并抽出 `baseAtk/baseDef` 局部变量）。
- **效果**：skip=120 → **pass=48 fail=0 skip=72**；**恒 skip 空转规则由 1 条 → 0 条**（首次归零）。
- 顺带印证判据本身是对的：样本 `state._baseAtk=27 → 攻+13 = floor(27/2)`。

### 二、假绿收口：131「回合分隔符重复」

- 原实现**没有任何 skip 出口** —— 一场战报连一个回合分隔符都没有时循环零命中、直接 `return {fail:false}`，
  被记成 pass，等于"什么都没验证却算通过"。
- 加前提守卫：本场无任何回合分隔符 → `skip`。实测仍 pass=120（分隔符本就恒有），守卫只在真空场景下生效，防的是假绿。

### 三、两条"疑似问题"经取证排除（记此免得以后反复怀疑）

- **129 的 `rem < 0`**：看似永假（`Math.max(0,...)` 兜底），但文件自述是**有意保留的防御性断言**
  —— 一旦有人撤掉钳制它立刻能抓到。**不动**（盲删会削覆盖）。
- **147/148/149 不下钻 `entries`**（清点报告列为嫌疑，且"条目挂在 attack-group.entries 而规则只扫顶层"正是
  历史最高发根因）：探针实测它们的目标文本**顶层命中 250/300/155、嵌套 entries 命中 0**
  —— 这些 fact 本就只在顶层，**不是漏报**。已排除，不改。

### 四、147「拒马阵」误报：我先翻车、后由主代码给出正解

- **我的误判**：先以为是"按号位去重误伤两只同格"，改成「累计判定数 ≤ 累计召唤数」。结果
  **fail 由 3 暴涨到 32** —— 我漏了拒马的**第二个召唤来源**（小昭·妹，文本是「在N号位出现」
  而非「出现在N号位」），召唤数被严重低估。该版作废。
- **正解来自主代码**：`render/35-facts-effect.js` 当天升到 **V1.0.6**，头部明写「拒马『未消散』条目补
  `horseUid`（此前只有消散条目带，规则只能按号位去重 → 跨阵营同号位假阳性）」。
  现两个分支（render/35:19 / :26）**都带 `horseUid`** → 可直接按个体去重。
- **用户已按此重写 147**（优先 `horseUid`、缺失才退回号位），**以用户版本为准**（我后两次编辑因文件被
  并发修改而失败，遂不再争抢）。结果 **fail 3 → 0**。
- 教训：① 改判据前必须枚举**全部**数据来源（同源不同文案是常见坑）；② 用户在并发编辑时，编辑前先重读文件。

### 五、本轮的并发改动（均为用户所为，我一并纳入提交）

| 文件 | 内容 |
|---|---|
| `134-zhang-switch.js` V6.2.0 | 修两处规则侧误报：死亡判定只认 `uidD`（原把"前排击杀敌人"误记成前排阵亡）；张无忌被换位时放弃时机判定 |
| `136-meteor-atk.js` V6.1.12 | growth 正则锚定 `⚡` 段，不再吃进「胖远桥莽撞」尾随的"攻击+2" |
| `147-horse-destroy.js` | 按 `horseUid` 去重（见上） |
| `148-carry-bonus.js` | carry 相关修正 |
| `rules-replay.mjs` | 清掉遗留的 `PROBE=` / `DUMPALL=` 调试探针块（-13 行） |
| `render/35-facts-effect.js` V1.0.6（**主代码**） | 未消散条目补 `horseUid` —— **未提交，我不代提交** |

### 六、验证结果（五件套）

| 项 | 结果 |
|---|---|
| `node tests/rules-replay.mjs` | ✅ **无失败规则；恒 skip 0；不变量违规 0** |
| `DEAD=1 node tests/rules-replay.mjs` | ✅ 同上（严格模式一致） |
| `node tests/123static-scan.js` | 通过 |
| `node tests/smoke-headless.mjs` | 全过 |
| `node tests/140-baseline.js --check` | 退码 1（时钟未拨正，见下） |

**里程碑：回放首次"零失败 + 零恒 skip + 零不变量违规"。**

### 七、报业务侧 / 待确认

1. **`render/35-facts-effect.js`（V1.0.6）你尚未提交** —— 147 的 uid 去重依赖它带的 `horseUid`；
   未提交状态下 147 会退回按号位去重（有跨阵营同号位假阳性风险）。**请一并提交**（我不动主代码）。
2. **基线时钟仍未拨正**：`generatedAt=2026-08-28`、之后 254+ 提交 → 非 winner 差异几乎一律判为"预期变更"。
   需 winner 翻转项有结论后才能重录。
3. **巨马阵 buff 图标**：`122 isBenefited` 对 `BUFF_TYPES.HORSE_FORMATION` 恒 `return false`，
   于是 `BUFF_ICONS['horseFormation']` 那条永不可达。是"本就无受益单位"（则图标条目该删）
   还是"漏了受益判定"（则是漏报）？属业务语义，请定。
4. **斩杀语义**（第 21 轮遗留）：132 判据2 已删，"斩杀"究竟该保证什么请给口径，我再补正确判据。

### 八、下轮目标

1. 等上述 3、4 两项业务口径，再动手。
2. 基线重录（需先解 winner 翻转）。
3. 可继续的纯体检项：`collectNodes` 摊平函数在 7 个规则里各复制一份（P2 重复代码，
   但 147/148/149 与其余口径不同——只做一层不下钻，故不能盲目合并，需逐个确认语义后收敛）。

### 推送

本轮提交落在 main（仅 `tests/`，**未 add 主代码 `render/35`**）；未强推、未推远端、未动他人分支。

---

## 第 23 轮 · 2026-09-25 23:32~（**修工具自身缺陷 + 登记补登 + 不变量扩覆盖**，main 分支，主代码零改动）

### 开场

延续全盘优化。上一轮已把回放打到"零失败 + 零恒 skip"，本轮不再追绿，改做三件更有长期价值的事：
① 修我在第 21 轮给基线工具埋的**判据缺陷**；② 补登记一致性；③ 按方向②再扩不变量覆盖，并**先证后立**。

### 一、修自家工具的缺陷：基线"预期变更"判据形同虚设

- **问题（我自己引入的）**：`140-baseline.js` 的 `commitsSince()` 取的是**全部**提交，于是我的
  `test(体检)` / `docs` / `chore` 提交也被算成"战斗 DIFF 属预期变更"的依据 —— **等于用体检提交给
  战斗结果变化发通行证**。基线后共 266 条提交，该判据恒真，"预期变更"这一栏没有任何鉴别力。
- **改法**：只取触达**战斗代码**的提交（pathspec 限定 `content core modules render fx player infra`），
  并单独统计其中的**数值平衡类**（`content/`，即游戏数据）—— 后者才是胜率/回合数漂移最常见的正当来源。
- **效果**：依据提交由 **266 → 136 条**，且不再被体检侧提交污染。分类仍保留"winner 翻转 = 回归"的硬红线。

### 二、登记一致性补登

- `123static-scan.js` 的 `SCAN_FILES` 补入 **`./rules-replay.mjs` 与 `./smoke-headless.mjs`** ——
  两个活跃 runner 同样 import 主代码符号，改名/删除会断裂，此前漏登导致扫不到。
- `tools/106` 打包清单仍未登记这两个（属工具侧，**只报不动**）；另 121/122 在该清单里重复登记两次。

### 三、不变量扩覆盖（方向②）：两条候选，一条作废、一条留观

**① 累计统计"单调不减" —— 首版误报 44 条，已作废（判据不成立，不是引擎问题）**

- 取证：`core/13battle-shared.js:55-59` 有 `case 'immuneRollback'`（免疫回退：承伤已记、只退输出），
  会**刻意**把 `dmgDealt` 用 `Math.max(0, x - amount)` 减回去。所以 dmgDealt 本就不是单调的。
- 处置：**删除而非放宽**；改成真正成立且仍有价值的不变量 —— **累计统计不得为负**
  （作者既然用 `Math.max(0,..)` 兜底，兜底一旦被撤就会漏出负值，这条抓得住）。

**② 存活 ⟺ hp>0 —— 保留，8 条待你裁定（有字段也无从干净豁免，按纪律不弱化）**

| 现象 | 场次 | 我的判断 |
|---|---|---|
| **小昭·姊 存活但 hp=0** | seed=8:3、13:2、13:4、18:2（4 类） | 疑为**附身/蝶变中**的合法特例（附身时她没有独立血量）。但全库 grep 不到 `isAttached`/`butterflyHostUid` 一类字段，无从在规则里干净豁免 |
| **峨眉弟子 已阵亡但 hp=0.8** | seed=13:5（round 5/6/7，同一单位） | 疑为**斩杀（execute）**路径：按 `modules/26` 斩杀的定义就是"hp 仍 >0 时触发击杀"，故击杀后残留血量属设计使然 —— 恰好绕回第 21 轮那个未决的**斩杀语义**问题 |

两条都属业务语义，我不猜，记待确认。**不弱化判据、不为了转绿而豁免**（第 20 轮起立的规矩）。

### 四、验证结果（五件套）

| 项 | 结果 |
|---|---|
| `node tests/123static-scan.js` | exit 0（补登后无新问题） |
| `node tests/smoke-headless.mjs` | exit 0 |
| `node tests/rules-replay.mjs` | **无失败规则；恒 skip 0**；不变量违规 **8 类**（上述待确认） |
| `DEAD=1 node tests/rules-replay.mjs` | 同上 |
| `node tests/140-baseline.js --check` | 退码 1：预期变更 15 场 / **回归 1 场**（seed=42 stage=3 winner 六大派→明教，红线未重录） |

### 五、报业务侧 / 待确认

1. **小昭·姊 存活但 hp=0**（4 类）是否为附身中的合法状态？若是，请给一个可判别的字段（如 `butterflyHostUid`），
   我据此精确豁免；若否，则是真回归。
2. **峨眉弟子 已阵亡但 hp=0.8** —— 与**斩杀语义**（第 21 轮遗留）是同一个问题，请一并给口径。
3. **基线时钟仍未拨正**：`generatedAt=2026-08-28`，之后 136 条战斗相关提交。唯一堵点是
   **seed=42 stage=3 的 winner 翻转** —— 你近期一直在做平衡（`平衡三改`/`平衡两改`/伤害波动等），
   这一次翻转很可能是有意平衡的结果。**请明确一句"是预期还是回归"**：是预期我就重录基线把时钟拨正
   （此后二分才有意义）；是回归我就立案查。
4. 工具侧（未动）：`tools/106` 未登记 `rules-replay.mjs` / `smoke-headless.mjs`，且 121/122 重复登记两次。

### 六、下轮目标

1. 等你裁定上述 1~3 项，尤其是**基线 winner 翻转**（拨时钟的前提）。
2. 纯体检侧可做：`collectNodes` 摊平函数在 7 个规则里各复制一份（P2 重复代码；147/148/149 与其余
   口径不同——只做一层不下钻，第 22 轮已证它们的 fact 本就只在顶层，故需逐个确认语义后才能收敛）。

### 备注

- 你已提交 `render/35-facts-effect.js`（`670a8bb` 拒马未消散条目补 `horseUid`）—— 147 的 uid 去重依赖它，已闭环。
- 工作区根目录有未跟踪的 `demo-fx-death.html`（你的），本轮未动。

### 推送

本轮提交落在 main（仅 `tests/` + 记档文档）；未强推、未推远端、未动他人分支、未 add 主代码。

---

## 第 24 轮 — 2026-09-26（GMT+8）

> 承接第 23 轮的三项遗留：①不变量 5 类违规待定性 ②基线时钟拨正后的状态 ③静态快检"全绿"是否可信。
> 结论：**第 3 项是真 ,P0** —— 静态快检这段时间根本没在跑。

### 一、P0：静态快检长期假绿（`123static-scan.js` V1.0.1 → **V1.2.0**）

- **现象**：`node tests/123static-scan.js` **零输出、退出码 0**，被当成"静态全绿"。
- **根因**：main 上这份实例停在 **V1.0.1**，是纯 `export` 模块——**没有任何执行入口**。历史教训
  （V1.1.0 的 IS_NODE 判定 + fetch 垫片 + CLI 自举，"此前 node 下零输出假绿"）**从未并进 main**。
  即：106 个文件的三类结构检查这段时间一次都没真正执行过。
- **改法**：补 Node 自举分支——垫 `file://` fetch（含剥 `?t=` 缓存串）+ `window.location`（指向 tests/ 本目录，
  `SCAN_FILES` 的相对路径正是相对它解析），跑完全量打印，**且有 issue 必须退出码 1**（不设退出码＝换个姿势继续假绿）。
  浏览器侧不受影响：整段包在 `isNode` 分支内、用 async IIFE 而非顶层 await。

### 二、自举恢复后立刻报出 10 条 → 验明全是误报，暴露扫描器两个缺陷

恢复后有输出了，**马上证明它之前在偷懒**：报 10 条。逐条 grep 核实全部为假阳性，成因两类：

| 成因 | 实例 | 处置 |
|---|---|---|
| **不剥注释**：注释里提一句符号也算"使用" | `core/06` 4 处全在 `//` `/** */`、`infra/56:134`、`health-rules/149:9` | 新增 `stripComments()`（逐字符跟踪引号，不误伤字符串里的 `//`，如 URL） |
| **不认解构取值**：只认 `import {} from` | `smoke-headless.mjs:83` `const { CAMP_TYPES, ROLE_TYPES } = mods.enums;`；`rules-replay.mjs:40-41` `const [{ renderLog }, { GlobalStore }, { STORE_ACTION_TYPES, CAMP_TYPES, BUFF_TYPES }] = await Promise.all([...])` | `collectImports` 增补对象/数组解构的名收集（`{orig: local}` 记 local、跳 `...rest`、去掉默认值） |

改完：**106 文件 / 0 条 / 退出码 0**。

### 三、负向测试（证明工具没被削牙）

给 `health-rules/149` 末行注入一句**真实**（非注释）的未 import 使用 `const _negProbe = BUFF_TYPES.HOLY_FLAME + GlobalStore;`
→ 扫描器立即报 1 条；`git checkout` 还原 → 回到 0 条。
（注：`GlobalStore` 未被判缺失是正确行为——它作**裸标识符**出现，用法正则只认 `.`/`(` 形式。）

### 四、不变量 5 类清零：`_pendingDeath` 是设计内中间态（`rules-replay.mjs` V6.2.1 → **V6.3.0**）

- **取证**（复制回放器为 `_tmp-pending.mjs` 探针）：5 例违规**全部** `state._pendingDeath === true`。
- **根因**：业务侧 V7.4.5 / `core/10` V6.3.3 起，致死不再当场 `alive=false`，统一挂 `_pendingDeath`
  交 `core/12:413 resolveDeaths` 结算（修「带血尸体」hp 不清零 + DEATH 信号不发两个洞）。
  逐步断言恰恰落在"已判死、未结算"这一瞬 → 是**正常窗口，不是缺陷**。
- **改法（对齐契约，非放宽）**：红线收窄为 `alive && hp<=0 && !state._pendingDeath`（＝结算断链）。
  与引擎自身读法同源（`core/03:63/290`、`core/10:59/88`、`modules/27:573/737` 皆用 `alive && !state._pendingDeath`），不另立口径。
- **补回覆盖面**：新增「回合末仍存活但血空」兜底断言（`<battleEnd>` 有胜者时跳过——胜负已分即战斗终止，
  最后一击的待死单位本就不再结算），专为"标记了却没人结算"一类留一条线。
- **负向测试**：抹掉 `_pendingDeath` 后同样 5 例**立刻复报**（还多抓一条 seed=10 stage=6 round=11），证明判据仍有牙。
- **顺带修正头/码矛盾**：文件头仍写着"hp 整数"这条**早已因 11901 条假阳性被删除**的断言，一并订正。
- 结果：不变量 **5 类 → 0 类**，且回合末新断言零误报（说明结算确实兜住了）。

### 五、基线按登记流程再录一轮

新一轮业务提交（详见下表）导致 DIFF 8 场，**winner 全部未变、无回归**，归入「预期变更」→ 重录并复检。

| 提交 | 内容 |
|---|---|
| `01ffc66` | balance(config)：防战留存系数顶级档 6.66 → 2.66 |
| `d72b69b` | balance(谢逊)：三狮改「基础值+职业加成」口径，振奋 2→1；补履历 V7.4.6 |
| `268f82f` / `e172dc1` | feat(fx)：胖远桥两技能特效接入（新建 `fx/92`） |
| `555a928` | fix(replay)：战报录制 state 共享引用污染 |

### 六、验证（全量）

| 检查项 | 结果 |
|---|---|
| 静态快检 | ✅ 106 文件 / 0 条 / 退出码 0（**且经负向测试确认真的在跑**） |
| 规则回放 | ✅ 120 场 / 30 条规则：0 fail、恒 skip 0、**不变量 0 类** |
| 基线 `--check` | ✅ BASELINE-MATCH 18 场 |
| 引擎冒烟 | ✅ 全通过 |

### 七、文档盘点（本轮另做）

- 补一份 `tests/体检使用说明.md` —— `tests/` 目录内的**现场使用说明**（命令速查 + 红线注意点），
  方便在体检代码旁直接翻。**它不叫 README、不取代体检正式协议**：
  正式协议一直是 `文件汇总20260730/README-体检.md`（根目录 `README.md` 钦定的四大专项衍生版之一）。
  教训：动笔前先查根目录 `README.md` 的文档体系，别在钦定文档族里另起同名文件。
- 其余 5 份逐份核对并更新状态；`夜间体检日志.md` 自 2026-09-21 起已不再追加，标注为历史记录。

### 八、下轮目标 / 遗留

1. `collectNodes` 摊平函数在 7 个规则里各复制一份（P2 重复代码）——**需在逐个确认语义后收敛**，
   147/148/149 只做一层不下钻是有原因的（第 22 轮已证其 fact 本就只在顶层）。
2. 工具侧（未动）：`tools/106` 仍未登记 `rules-replay.mjs` / `smoke-headless.mjs`，且 121/122 重复登记两次。
3. "标记了 `_pendingDeath` 却永远不被结算"目前仅由回合末断言覆盖；若后续出现跨回合残留，需再补**跨回合**断言。

---

## 第 25 轮 — 2026-09-26（GMT+8）

> 承接第 24 轮遗留：①跨回合/存活态的待死标记覆盖 ②四方登记核对常驻化 ③`collectNodes` 重复（本轮**只取证不动手**）。

### 一、补强 `_pendingDeath` 断言：从"血空"扩到"人还活着却被标记待死"

- **动机**：第 24 轮的回合末断言只管「存活且血空」。但更危险的一类是**血还在、却挂着 `_pendingDeath`** ——
  下一轮 `resolveDeaths`（`core/12:413` 过滤 `_pendingDeath && alive`）会把**已经救回来的人再杀一次**。
- **首版写宽了，被实测打回**：不加 `alive===true` 限定 → 120 场误报一大片
  `拒马 / 少林弟子 alive=false hp=0 仍挂 _pendingDeath`，即**已死单位**上的标记残留。
  已死单位不在 `resolveDeaths` 的过滤里，残留**无后果**，属无害脏数据 → 断言收窄为 `pend && alive===true`。
- **负向测试**：强制视作所有单位都挂标记后，断言立刻报（且只打存活单位，连 hp=186 的张无忌都抓到）→ 判据有牙。
  探针 `_tmp-neg2.mjs` 跑完即删。
- 结果：不变量 **0 类**（新增断言零误报）。

### 二、新增常驻检查器 `tests/registration-check.mjs`（V1.0.0）

- **根因**：历次复盘的「四方登记」都是 `tests/_tmp-check.mjs`**现写、跑完即删** —— 结论随探针一起消失，
  下一轮重新踩同样的坑（147~152 曾整批漏登打包清单、本轮又发现 121/122 重复登记）。
- **做什么**：一条命令核对 30 条规则在 **121（import）/ 124（RULE_META）/ 123（SCAN_FILES）/ 106（打包清单）** 四处是否齐全。
- **退出码二分**（与基线同思路，不让"我无权修的事"把红线恒红）：
  - tests/ 侧缺失 → **硬失败 exit 1**（体检侧自修）；
  - tools/106 侧缺失/重复 → 单列「工具侧待办」，**不计硬失败**（官方协议明写"不修改 tools/ 下任何文件"）。
- **本轮实测输出**：121/123/124 **三处齐全 ✅**；工具侧待办 2 项——`rules-replay.mjs` / `smoke-headless.mjs`
  未登记打包清单、`121health-monitor.js` / `122health-utils.js` 各**重复登记两次**。
- **负向测试**：塞一个未登记的假规则文件 → 三处全部报出 + 退 1；删掉后恢复 ✅。
- 已把该检查器本身加入 `123static-scan.js` 的 SCAN_FILES（静态 106→**107** 文件，仍 0 条）。

### 三、`collectNodes` 重复：本轮只取证，未动手（留作下轮）

7 个规则各复制一份摊平函数。第 22 轮已证 **147/148/149 的 fact 本就只在顶层**（故它们"只做一层不下钻"是对的），
与其余规则的"要下钻 entries"语义**不同** → 收敛前必须逐个确认语义，不允许一刀切改写。本轮未改。

### 四、验证（五件套）

| 检查项 | 结果 |
|---|---|
| 静态快检 | ✅ 107 文件 / 0 条 / 退 0 |
| 登记一致性 | ✅ 121 / 123 / 124 齐全（工具侧 2 项待办，不计失败） |
| 规则回放 | ✅ 0 fail、恒 skip 0、**不变量 0 类** |
| 基线 `--check` | ✅ MATCH 18 场 |
| 引擎冒烟 | ✅ 全通过 |

### 五、下轮目标

1. `collectNodes` 按语义逐个确认后收敛（147/148/149 与其余分组处理）。
2. 工具侧 2 项待办（`tools/106` 补登两个 `.mjs`、去掉 121/122 重复）——**需你授权改 tools/，否则只能一直挂着**。
3. 考虑把「回合末存活却挂 `_pendingDeath`」的经验反哺：检查 `core/12` 结算后是否该统一清标记（业务侧建议，不改）。

---

## 第 26 轮 — 2026-09-26（GMT+8）

> 承接第 25 轮「`collectNodes` 七份重复，只取证未动手」——本轮按语义确认后收敛。

### 一、取证：七份实现归一化比对（结论：三种形态，不是七种）

| 形态 | 文件 | 说明 |
|---|---|---|
| 扁平 `push(node)` | **129 / 133 / 150 / 151 / 152** | 归一化后**逐字完全相同**（md5 同组） |
| `push({e, gi, i})` | **123** | `.i` **判据真在用**（L85-86 同组内比先后 `cn.i > hn.i`），不是冗余字段 |
| `push({e, gi})` | **132** | 不带 `i`（多余字段对其无害） |

→ 收敛方案：`122health-utils.js` 导出唯一实现 `collectNodes`（扁平）与 `collectNodesGrouped`（`{e, gi, i}`），
七个规则改为 import，本地副本删除。**净删约 117 行重复代码。**

### 二、本轮踩坑（最重要）：改名漏改调用点，120 场全红

- **现象**：第一版把 123/132 写成 `import { collectNodesGrouped }`，但**调用点仍叫 `collectNodes(log)`**
  → 运行时 `collectNodes is not defined` → 两条规则 `pass=0 fail=120 skip=0`（从"只在该机制出现时跑"变成"每场都报错"）。
- **为什么没第一时间发现**：`node --check` **只验语法，不验标识符是否真的存在** —— 语法全过，照样全红。
- **修法**：`import { collectNodesGrouped as collectNodes }` —— 用 `as` 保留原调用名，**调用点一行不改**。
- **教训（务必沿用）**：① 抽公共函数时优先用 `as` 保留原名，别顺手改调用点；
  ② **验证必须真跑回放、逐条对数字**，语法检查通过 ≠ 没坏。本轮正是靠"改动前后 7 条规则 diff"抓到的。

### 三、验证：零行为变化（逐条对数字）

改动前后各跑一次全量回放，取 7 条相关规则比对 —— **pass / fail / skip 三项逐条完全一致**：
123(20/0/100) · 129(28/0/92) · 132(20/0/100) · 133(111/0/9) · 150(66/0/54) · 151(51/0/69) · 152(20/0/100)。

### 四、⚠ 发现并行改动（本轮未提交该文件）

- `tests/rules-replay.mjs` 出现**非本轮**的改动：**V6.4.0**（2026-09-26）新增 fact 契约警告收集器
  （劫持 `console.error` 收 `[fact契约]` 前缀，去重后计入不变量与退出码）。
- 后果（**不是我引入的**）：RESULT 行多出「`fact 契约违规 1 类`」= `butterflyNoHost 缺 sisterUid`（×5 次）——
  这是挂了多轮的**业务侧产出 bug**，此前只是控制台噪声、现在被计入。
- **取证方法**：`git worktree add ../_wt-head HEAD` 在改动前状态另开工作区跑一次 → 该状态 RESULT **无**此条，
  坐实是 V6.4.0 收集器带来的，与本轮 `collectNodes` 收敛无关。临时工作区已 `git worktree remove` 清理。
- **处置**：本轮**只提交自己改的文件**，`rules-replay.mjs` 原样留在工作区不动。
- 另发现两个他人在用的 worktree：`main-87959581`、`gmd-test-wt` → 后续每轮动手前先 `git status` / `git diff`
  确认哪些改动不是自己的，避免互相覆盖。

### 五、验证（五件套）

| 检查项 | 结果 |
|---|---|
| 静态快检 | ✅ 107 文件 / 0 条 |
| 登记一致性 | ✅ 121 / 123 / 124 齐全（工具侧 2 项待办不计失败） |
| 规则回放 | ✅ 0 fail、恒 skip 0、不变量 0 类（fact 契约 1 类来自他人 V6.4.0，见上） |
| 基线 `--check` | ✅ MATCH 18 场 |
| 引擎冒烟 | ✅ 全通过 |

### 六、下轮目标

1. 工具侧 2 项待办（`tools/106` 补登两个 `.mjs`、去掉 121/122 重复）——**仍需你授权改 tools/**。
2. `butterflyNoHost 缺 sisterUid`：现已被 V6.4.0 计入红线，属业务侧产出 bug，建议正式立案（我不动主代码）。
   → **已由豆包修掉**：`fd19d9f fix(mingjiao): butterflyNoHost 产出点补 sisterUid`，本轮复跑该红线已消失。

---

## 第 27 轮 — 2026-09-27（GMT+8）

> 两件事：①按**一次性授权**修改 `tools/106`（此前只能干报告）；②继续收敛跨规则重复函数。

### 一、工具模块 `tools/106-ai-pack-config.js`（V6.0.15 → **V6.0.16**，用户授权一次）

- 依据 `tests/registration-check.mjs` 的机器核对结果改，不靠手摸：
  ①**补登 3 个 node 直跑入口** —— `tests/rules-replay.mjs`（规则回放主力）、`tests/smoke-headless.mjs`（引擎冒烟）、
  `tests/registration-check.mjs`（登记核对器）。三者长期漏登，一直没随包发出；
  ②**删除 `121health-monitor.js` / `122health-utils.js` 的重复登记行**（上方 tests 段已各登记过一次）。
- 验证：登记核对的「工具侧待办」**整段清零**；tests 条目 39 → 40；106 模块加载正常；五件套不受影响。
- 顺带把核对器自己也纳入被核 runner 名单（否则它自己漏登没人管）。

### 二、继续收敛跨规则重复函数（`plain` / `entryTexts` / `maxHpOf`）

扫描 30 个规则文件的顶层函数定义，发现除 `collectNodes` 外还有 3 个重名函数，且**各自只有一种实现**
（归一化比对确认，`plain` ×5、`entryTexts` ×2、`maxHpOf` ×2 内部完全相同）→ 一并收到 `122health-utils.js`：

| 函数 | 原副本 | 作用 |
|---|---|---|
| `plain` | 146/148/149/150/151（×5） | 去 HTML 标签取纯文本 |
| `entryTexts` | 136/144（×2） | 取条目自身文本 + entries 子条目文本 |
| `maxHpOf` | 150/151（×2） | 按名字在终局两队查 maxHp（回血/吸血判据用上限夹） |

- **名字本来就一致 → import 不需 `as`**，比第 26 轮那次（改名漏改调用点）风险低一档。
- 150/151 已有 122 的 import 行，新名字**合并进同一行**，不另起第二条。
- **验证**：改动前后各跑全量回放、取 29 条规则数字 diff —— **逐条完全一致，零行为变化**。

### 三、协作边界（豆包在并行跑并负责审核）

- 已确认 `tests/rules-replay.mjs` 是豆包在改（V6.4.0 fact 契约收集器，未提交），本轮**继续不动它**。
- 豆包提交 `fd19d9f` 修掉 `butterflyNoHost 缺 sisterUid`（挂 5+ 轮的业务侧红线）→ 本轮复跑该红线已消失，闭环。
- 纪律：每轮动手前 `git status` / `git diff` 分清哪些改动不是自己的，**只提交自己的文件**。

### 四、验证（五件套）

| 检查项 | 结果 |
|---|---|
| 静态快检 | ✅ 107 文件 / 0 条 |
| 登记一致性 | ✅ 121/123/124 齐全，**工具侧待办清零** |
| 规则回放 | ✅ 0 fail、恒 skip 0、不变量 0 类（fact 契约红线已被豆包修复而消失） |
| 基线 `--check` | ✅ MATCH 18 场 |
| 引擎冒烟 | ✅ 全通过 |

### 五、下轮目标

1. 覆盖度体检：个别规则 120 场里只触发个位数（流云 6、苦练 13、未命中重试 11），
   需先取证是"机制本就罕见"还是"数据源/覆盖矩阵有缺口"，再决定是否调整 SEEDS/STAGES。
2. 122 已是共用工具的家，建议把 121 的部分检查也下沉复用（当前 121 的 runEngineChecks 仍偏长）。不改）。
