# 体检更改总览（主线侧快照）

> **生成**：2026-09-23，由主线侧整理。数据源：`workbuddy/main-87959581` 分支的 git 历史 + 分支上的 `tests/体检迭代日志.md` 与 `tests/体检复盘报告.md`。
> **用途**：本文档是体检迭代成果的浓缩速记。详细逐轮记录见 `tests/体检迭代日志.md`，方法论见 `tests/体检收获与方法.md`。
>
> **⚡ 2026-09-23 同步轮已完成（见迭代日志第 12 轮 / 履历 V6.1.30）**：体检线 16 提交已在本地 `test` 分支与主线合流，五方登记 30 条全对齐、恒 skip 0、静态假阳性清零、基线重录 MATCH；剩 2 条红（146 概率连击 / 148 carry 上界）待定性。第五~七节的"分叉现状"描述是同步前的历史快照，保留不改。后续体检迭代直接在 `test` 分支上进行、推 gitee 的 test。
>
> **⚡ 2026-09-24 第 19 轮（见迭代日志第 19 轮 / 履历 V6.1.35）**：用户持浏览器体检结论来问「这 4 条合理吗」+ 要求体检改「从第 2 关开始 + 默认 4 倍速」。回放器 180 场（seed 1~60 × stage 3/4/6）里 143/144 **全绿**，浏览器却成片红 → 反过来说明是**数据源差异而非数值回归**。根因：**`121` 把 `ctx.UI.allyTeam` 当终局快照喂规则，而它只在 `ui/65main-battle.js:27` 开战 clone 一次、战斗全程不更新**（真值账本 2026-09-14 起已迁到 battleStore，`ui/62ui-render:36` 明写「不再读 c.UI 的冗余拷贝」）。双喂探针实测：真终局 fail 0/0，冒充后 **143 误报 40 场、144 误报 25 场**，消息里的 112.5 / 58 / 182 与用户报的数值完全吻合。改法：新增 `finalTeamsFromStore()` 从 `battleStore.getState().units` 按 `camp` 分组取终局（取不到退回 UI 快照），before 仍用开战副本。**140-wei 经重新定性是业务侧真 Bug**：`buffDodgeBonus` 被 `e266abc`（妆造字段剥离）从单位上删了，`render/32:96-98` 仍在读 → 恒 undefined；体检**不加豁免**。默认参数：`120test-runner.html` 两处 `start` 3→**2**、`speed` 100→**300**（300=4x / 100=8x，见 `ui/68:166`，本次实为放慢一档）。终态与第 18 轮逐项一致：静态 0 条、回放 1 fail、恒 skip 0、冒烟全过、基线 exit=1 未重录。同步补做**第 5 次复盘**（数字对账无一条对不上）。**最新完整状态以 `tests/体检迭代日志.md` 第 19 轮为准。**

> **⚡ 2026-09-26 第 24 轮（见迭代日志第 24 轮）**：本轮最大收获是拆掉一颗长期哑弹——`123static-scan.js`
> 在 main 上的实例停在 **V1.0.1、无 Node 执行入口**，`node` 跑它零输出、退出码 0，等于这段时间
> **"静态快检通过"从未真正发生过**（106 个文件的三类检查一次没跑；历史 V1.1.0 的 CLI 自举没并进 main）。
> 补回自举后立刻报出 10 条，验真发现**全是误报**，反手修掉扫描器两个真缺陷：不剥注释（`core/06` 4 处、
> `infra/56`、`149` 命中均在注释里）与不认解构取值（`const {X}=mods.enums`、`const [{…},{…}]=await Promise.all([…])`）。
> 同轮另一项：不变量遗留的 5 类「小昭·姊 存活但 hp≤0」经探针取证确认**全部** `state._pendingDeath===true`，
> 即引擎 V7.4.5 起「致死改挂 `_pendingDeath` 交 `resolveDeaths` 结算」的**设计内中间态**；判据对齐为引擎同源的
> `alive && !state._pendingDeath`，并补「回合末仍存活但血空」兜底断言（有胜者跳过），负向测试确认判据仍有牙。
> 基线因新一轮业务提交（`01ffc66` 防战系数 6.66→2.66、`d72b69b` 谢逊口径、`268f82f`/`e172dc1` fx/92、
> `555a928` 战报录制）产生 8 场 DIFF，**winner 全部未变、无回归**，已按登记流程重录。
> **终态：静态 106 文件/0 条/退 0 · 回放 120 场 0 fail 恒 skip 0 · 不变量 0 类 · 基线 MATCH 18 场 · 冒烟全过。**
> 另在 `tests/` 补一份**现场使用说明** `tests/体检使用说明.md`（命令速查 + 踩过的坑）。
> 注意：它不是、也不取代体检的正式协议——后者一直是 `文件汇总20260730/README-体检.md`
> （根目录 `README.md` 钦定的四大专项衍生版之一）。另盘点其余 5 份文档。
> **最新完整状态以 `tests/体检迭代日志.md` 第 24 轮为准。**

> **⚡ 2026-09-26 第 25 轮（见迭代日志第 25 轮）**：①新增常驻检查器 `tests/registration-check.mjs` —— 把历次
> 复盘靠临时探针「现写现删」的**四方登记**核对（121/124/123/106）固化为一条命令，退出码二分：tests 侧缺失硬失败，
> tools/106 侧只提示（官方协议禁止改 tools/）。实测 121/123/124 齐全；工具侧待办 2 项：两个 `.mjs` runner 未登记、
> 121/122 各重复登记两次。负向测试（塞假规则）三处全报出且退 1。②不变量再补强：新增「**回合末仍存活却挂着
> `_pendingDeath`**」断言（这类最危险——下轮 `resolveDeaths` 会把已救回的人再杀一次）；首版不限 `alive` 被实测打回
> （误报一片「已死单位标记残留」，无害），收窄后 0 误报，负向测试确认有牙。③`collectNodes` 七份重复本轮**只取证未动**
> （147/148/149 只做一层是有原因的）。终态五件套：**静态 107/0 · 登记齐全 · 回放 0fail/恒skip0/不变量0类 · 基线 MATCH · 冒烟全过**。
> **最新完整状态以 `tests/体检迭代日志.md` 第 28 轮为准。**

---

> **⚡ 2026-09-27 第 28 轮（见迭代日志第 28 轮）**：覆盖度体检 + 常驻化。取证：跑 120 场 vs 240 场（seed 1~40）对照，触发次数随样本量**线性增长**（流云 6→15、未命中重试 11→20、苦练 13→20、生生不息 15→32）→ **机制本身罕见（随机阵容决定），不是规则条件坏了**，不调种子/关卡。新建 `tests/coverage-report.mjs` V1.0.0：进程内引回放器解析 pass/fail/skip，按触发率排序，<10% 标偏薄、**零触发=空转硬失败退 1**；负向测试（STAGES=1）8 条零触发、退 1。123 SCAN_FILES 补登（108 文件/0 条）；registration-check / 使用说明同步。待办：`coverage-report.mjs` 未进 `tools/106`，用户此前只授权改 106 一次（已落 353d778），本轮不擅动。终态：**静态 108/0 · 登记齐全(工具侧待办 1 项) · 回放 0fail/恒skip0/不变量0类 · 基线 MATCH18 · 冒烟全过**。

> **⚡ 2026-09-26 第 27 轮（见迭代日志第 27 轮）**：`plain`/`entryTexts`/`maxHpOf` 九份副本收敛入 `122`（零行为变化）。扫描 30 个规则顶层函数，归一化比对确认各自只有一种实现后收进 `122`，9 份本地副本删改 import；名字本就一致无需 `as`，150/151 合并进已有 import 行。验证：前后各跑全量回放取 29 条数字 diff，**逐条完全一致**。本轮按用户一次性授权改完 `tools/106`（353d778，补登 3 个 runner + 删 121/122 重复登记）。豆包 `fd19d9f` 修掉 `butterflyNoHost 缺 sisterUid`（挂 5+ 轮的业务侧红线），复跑该红线已消失。`rules-replay.mjs` 仍为豆包并行改（V6.4.0），不动不提交。终态五件套全绿。

> **⚡ 2026-09-26 第 26 轮（见迭代日志第 26 轮）**：`collectNodes` 七份重复收敛入 `122`（零行为变化）。取证归一化逐字比对：129/133/150/151/152 五份完全相同；123 为 `{e,gi,i}` 且判据真用 `.i` 做同组内排序；132 为 `{e,gi}`。122 导出 `collectNodes` + `collectNodesGrouped`，七规则删本地副本改 import，净删 ~117 行。踩坑：首版 import 名 `collectNodesGrouped` 但调用点仍 `collectNodes` → 运行时未定义 → 两条规则 120 场全红，`node --check` 查不出；修用 `as collectNodes`。验证：前后数字 diff 逐条一致。**另发现有人在并行改 `tests/rules-replay.mjs`（V6.4.0 fact 契约收集器，非本轮引入），用 HEAD 工作区对照确认 `butterflyNoHost 缺 sisterUid` 红线非本轮引入；只提交自己文件，那份原样留工作区。**
>
> **⚡ 2026-09-25 第 22 轮（见迭代日志第 22 轮 / 履历 V6.1.38）—— 🟢 回放首次全套转绿**：`RESULT: 无失败规则；恒 skip 0；不变量违规 0`，`DEAD=1` 严格模式同结果，静态 + 冒烟通过。**① 恒 skip 归零**：127「姐姐附身叠加」是**数据源错位不是机制未触发** —— `_baseAtk/_baseDef` 不在单位顶层而在 `unit.state` 下（`core/02unit.js:177`），探针取证 48/120 场有附身文本与小昭·姊但顶层有值 **0 场**、state 下有值 **48 场**；改读 `sister.state._baseAtk` 后 **skip=120 → pass=48**，恒 skip 由 1 条→**0 条**。② **假绿守卫**：131 原无 skip 出口（无分隔符也记 pass＝没验证算通过），加前提守卫。③ **取证排除两条嫌疑**（记此免反复怀疑）：129 的 `rem<0` 是**有意保留的防御性断言**不动；147/148/149 不下钻 entries **不是漏报**（目标文本顶层命中 250/300/155、嵌套 0）。④ **147 拒马阵 fail 3→0**：我首改「累计判定数≤累计召唤数」**翻车**（fail 3→32，漏了小昭·妹第二召唤源，文案「在N号位出现」≠「出现在N号位」）已作废；**正解来自主代码 `render/35` V1.0.6**——销毁条目消散/未消散两分支**都带 `horseUid`**，按个体去重，用户据此重写 147，以其版本为准。⑤ 用户并发改动一并纳入：134 V6.2.0、136 V6.1.12、148、`rules-replay` 清调试探针块。⚠ **`render/35-facts-effect.js`（V1.0.6，主代码）你尚未提交**——147 的 uid 去重依赖它，请自行提交（本轮未 add 主代码）。
>
> **⚡ 2026-09-25 第 21 轮（见迭代日志第 21 轮 / 履历 V6.1.37）**：按「先分析记录 → 再通读全盘代码 → 最后动刀」做了一次体检模块全盘优化（主代码零改动）。**删减**：3 个被取代的临时 runner（`baseline-check/diff/entries.mjs`，自述"跑完即删"、能力已被 `140-baseline` V1.2.0 收编）；死代码 `TAG_PREFIX`(124)、`ctx._doc`(121，全库无规则读它)、`getRuleGroups` 未使用的 `prefix`；132 的伪判据2。**修复（复活血判据）**：132 原读已删字段 `clawTargetHpAfter` → 按现行 `hpAfter`（`render/35:478`）重写，连带让**判据3 连锁调血校验真正生效**（此前 hpAfter 恒 undefined，该校验全程空转）；142 删掉读已剥离 `factType`/`data` 的死分支。**补覆盖缺口**：回放 ctx 补 `snapshot`（语义据 `ui/61:392-393`「战斗进行中 snapshot 不反映当前态」→ 必须喂**开战定稿**而非终局），此前 134/139 依赖它的判据在回放里**静默不执行**（回放侧假绿），补上后 134 暴露 7 条待确认。**统一**：血量不变量改单一真值源（回放直接复用 122 的 `checkUnitHpValidity`），消除"两处各写一份、口径不一致（122 多膨胀判据）"。**补齐**：124 / rules-replay / smoke 补 `export const VER`（版本头对账不再漏）。**文档**：手册重排错位编号（原有两个"第9条"，后续整体错位）+ 更新过期头部；体检页面清 30 个冗余 BOM、修体积声明（原写 ~6000 实测 14.8KB）、并把「第 2 关起 / 4 倍速」默认参数同步到 main（此前只落在 test 分支）。**⚠ 重要背景**：本轮你在并发提交主代码（`5bef450` 修「乘风突袭击退取错队→同阵营双人同格」、`28d0388`、`31e781f`）—— **第 20 轮报的 pos 同格 26 例是真 Bug，已被 `5bef450` 修复，不变量随之由 26 归零**，这是不变量套件首次抓到并被证实为真 Bug 的用例。
>
> **⚡ 2026-09-25 第 20 轮（见迭代日志第 20 轮 / 履历 V6.1.36）**：**工作模式变更 —— 按用户指令改在 main 分支直接进行，不再走 test 分支**（主代码一行未改；用户未提交的 `render/32` V6.4.1 原样保留、未代提交）。按用户定的四方向推进：① **真值源统一** —— `121` 新增 `teamsFromStore()`，引擎/UI/规则/结算**四组**全改取 battleStore 真值（原四组全吃 `ctx.UI.allyTeam` 开战副本＝系统性假绿），采样循环中途调用＝**中期真值**，故"中期溢出后被修回"也能抓到；② **不变量套件** —— `rules-replay` 逐步断言 hp∈\[0,maxHp\] / maxHp>0 / 同阵营 pos 唯一 / fact 是否注册渲染器，**首版两条判据假阳性已作废**（hp 整数误报 **11901 条**、按"渲染有无产出"判映射缺口误报 **929 条**，正解是查注册与否）；③ **规则脱钩产品实现** —— 由用户在主仓完成（`render/32` V6.4.1 走 `computeBuffStats` + 规则 140 直接调 `getDodgeBreakdown` 断言面板输出），规则 140 由恒红转 **pass=4 fail=0**；④ **基线二分** —— `--check` 的 DIFF 分「回归」（winner 翻转，硬失败、报红不重录）与「预期变更」（单列打印提交清单、不硬失败），不再一律退码 1。另移植 test 上 main 缺失的 `139`/`141`/`146` 三条修复 → 回放 **fail 4→2、恒 skip 2→1**；并带入 main 此前完全没有的四份体检记档文档。终态：静态通过、冒烟全过、基线 **退码 1（预期变更 15 场 / 回归 1 场：seed=42 stage=3 winner 六大派→明教 翻转）**。报业务侧待确认：**pos 同格冲突 26 例**（渲染按格只取第一个，另一个渲染不出来）、拒马阵双重清算 fail=4、carry 超配置推导上界 fail=1。**最新完整状态以 `tests/体检迭代日志.md` 第 20 轮为准。**
>
> **⚡ 2026-09-24 第 18 轮（见迭代日志第 18 轮 / 履历 V6.1.34）**：先补推第 16 轮遗留的 `9d81339`（`4dfa2a8..9d81339`）并清掉它留下的孤儿探针，再 `git merge main --no-edit` 零冲突合入 `1a6e035`（溅射逐目标广播 `SPLASH_DAMAGED` / 胖远桥莽撞吃溅射 / 拒马消散走普通死亡管线 / 韦一笑闪避吸血飘字）→ 判 **A 档**跑五件套。修 **2 处 P1 误报**：① `139` 漏认「宿主已阵亡」这条附身窗口关闭分支（`render/35:363-365` 的 `renderButterflyHostDeadFact` **不带** `butterflyAction`，窗口永不关闭 → 姐姐此后正常挨打全被判复发），补 `isHostDeadReturn` 并列识别后 **fail 1 → 0**；② `141` 把严阵以待首现回合硬编码为 6，而业务已按 `tenRoundFortify.round` 由 10 改 3（首现 6 → 4），改为配置推导 `tf.round + 1` 后 **fail 20 → 0**。双向负向测试 **21 例全通**。终态：静态 103 文件 0 条、回放 **1 fail（140-wei）/ 恒 skip 0**、DEAD 恒 skip 0、冒烟全过。**⚠ 基线 DIFF 由 9 → 14 场且出现 1 场 winner 翻转（`seed=999 stage=3` 明教 → 六大派）→ 按红线报红、不重录**（合并前树独立复跑已确认翻转与 `1a6e035` 无关，源自 `89baff6`/`f31b9cf`，二者在履历均无条目）。**最新完整状态以 `tests/体检迭代日志.md` 第 18 轮为准。**
>
> **⚡ 2026-09-24 第 15 轮（见迭代日志第 15 轮 / 履历 V6.1.33）**：`git merge main --no-edit` 成功但**零内容变化**（main `f31b9cf` 与 test `08567d4` 同内容双提交，`git diff c5bfbcb HEAD` 空）→ 判 **D 档**，只做「体检的体检」。五件套复跑：静态 103 文件 0 条、回放 **1 fail（140-wei）/ 恒 skip 0**、DEAD 恒 skip 0、冒烟全过、**基线 DIFF 9 场（winner 全不变，缺履历条目 → 继续只报不重录）**。修 **1 处 P2**：`tests/122health-utils.js` 版本头 ~bytes 填的是「写新头之前」的 27758（自引用失真 +152，行规手册第 11 条同一陷阱，第 13/14 轮漏了它）→ V6.1.17 / ~28100。**体检的体检结论**：四方登记 **30 全齐**（按文件号与按规则名双向核对，124 用规则名键，按文件号取会全空）、版本头残差 ≤48、孤儿探针 0。**最新完整状态以 `tests/体检迭代日志.md` 第 15 轮为准。**
>
> **⚡ 2026-09-24 第 14 轮（见迭代日志第 14 轮 / 履历 V6.1.32）**：`git merge main` 报 Already up to date（无新提交），但工作区有 10 个业务文件改动（跑到一半被用户在 test 上提交成 `08567d4`）→ 五件套全跑，结果代表**这批改动生效后的状态**。主修 **146 概率连击假红**：幼狮成长是同一 unit 原地改名（`modules/27:665`），规则按显示名比对会误判"连击越界"，新增别名比对后 **fail 2 → 0**；另修 2 处版本头 ~bytes 自引用失真。基线 **DIFF 9 场（winner 全不变）但本轮不重录**（差异源是未提交 WIP，无履历条目可依）。终态：静态 103 文件 0 条、回放 **1 fail / 恒 skip 0**、冒烟全过，登记 30 方齐。**最新完整状态以 `tests/体检迭代日志.md` 第 14 轮为准。**
>
> **⚡ 2026-09-24 第 13 轮（见迭代日志第 13 轮 / 履历 V6.1.31）**：二次合入 main @ `60dd2a0`（含 `8230284` 狮群机制重做），**0 冲突**；同时触发**第 4 轮复盘**（自第 3 轮起满 6 轮）。当前状态：静态 103 文件 0 条、回放 30 规则 **3 fail / 恒 skip 0**、基线重录后 MATCH、冒烟全过。红条构成已变：148 carry 上界 **已归零**、146 由 6→2，新增 **140-wei 流云身法（fail 3，P0：字段无写入源）** 与 127（fail 1）。**最新完整状态以 `tests/体检迭代日志.md` 第 13 轮与 `tests/体检复盘报告.md` 第 4 轮为准。**

---

## 一、体检线是怎么运作的

- WorkBuddy 体检 agent 不直接改主仓，而是在 `C:\Users\luzhigui\WorkBuddy\Worktrees\new-acode-repo\main-87959581` 开了独立 git worktree，分支 `workbuddy/main-87959581`，推送到 gitee 与 origin 双远端（均已推齐至 `2931c6b`）。
- 权限边界：只许改 tests/ 体检代码、三份体检文档，以及在 `记录-更改履历.md` 顶部追加条目；主代码（core/render/ui/player/fx/modules/infra/content/assets/scripts 等）与 tools/ 一律不动，发现的问题只登记为"业务侧/工具侧待修"。
- 实际调度（迭代日志自述，头行口径已修正）：迭代每 **10 分钟**一趟，复盘每 **2 小时**一轮。
- **分叉点**：`94e4a42`（2026-09-21，第 3 趟夜间体检刚结束）。此后主线自己又前进了 **29 个提交**（夜间体检第 4~9 趟 + 灭绝师太/谢逊/胖远桥等新内容），两边互不可见。

## 二、体检系统是什么（命令速查）

| 命令 | 作用 |
| --- | --- |
| `node tests/123static-scan.js` | 静态结构快检（79 文件） |
| `node tests/rules-replay.mjs` | 120 场回放 × 全部规则 |
| `DEAD=1 node tests/rules-replay.mjs` | 严格模式：恒 skip 空转规则当失败（CI 红线） |
| `node tests/140-baseline.js --check` | 18 场确定性基线比对（回归红线，差异即退出码 1） |
| `node tests/smoke-headless.mjs` | 引擎无头冒烟（20 局 + 播放层导出齐全性） |
| `tests/120test-runner.html` | 浏览器端体检入口（node 跑不了） |

## 三、11 轮迭代逐轮记录（2026-09-21 23:05 → 09-23 18:0x）

| 轮 | 时间 | 主题 | 关键改动与结果 |
| --- | --- | --- | --- |
| 1 | 09-21 23:05 | 静态快检 node 化 + DEAD 红线 | `123static-scan.js` 加 IS_NODE 判定 + node fetch 垫片 + CLI 自举——修掉 **node 下零输出假绿**；`scanSharedSymbolImport` 加形参/解构排除（消 26elite-sixsects 的 eventBus 误报）；replay 加恒 skip 完整名单与 `DEAD=1` 严格模式。恒 skip 8 条首次可见 |
| 2 | 09-22 00:08 | 基线回归红线修复 | `140-baseline.js` V1.0.0→V1.2.0：修 Windows `fileURLToPath` 路径 + 新增 `--check` 模式（旧 baseline-check.mjs 打印完 DIFF 不设退出码，18/18 场不同照样绿）；按当前行为重录 `baseline-v1.json`；删除 3 个临时 runner（baseline-check/diff/entries.mjs，功能收编） |
| 3 | 09-22 00:2x | 133 死代码/漏报修复 | `133-death-effect.js` V6.1.17：修 `!e.text` 守卫漏报（恒 skip→pass 93）、修 `saw` 死代码、修 isDodge+isDead 误报、拆战士斩杀/白骨爪双通道；replay 加 fact 覆盖直方图。恒 skip 8→7 |
| 4 | 09-22 07:4x | 123/132 假红修复 | 132 连锁递减 `prev` 下沉到组内（4 假红→0）；123「夹在中间」判据改组内轴（11 假红→0）；补档一轮未记档改动（V6.1.18→V6.1.19）。恒 skip 7→5 |
| 5 | 09-22 08:5x | 127 恒空转真因 | 基础值路径 `sister._baseAtk` 应为 `sister.state._baseAtk`（core/02 L157、modules/29 为准），守卫恒真导致自登记以来 skip=120；修复后 pass=48。恒 skip 5→4 |
| 6 | 09-22 10:0x | 回放器吞数组 + 子层级盲区 | replay V6.1.22：旧 `if (e) log.push(e)` 只认单对象，**数组型渲染条目被整条吞掉**（一处同时解掉 134 假绿与 143 的 29 条误报）；129/143 改 collectNodes 摊平 + 契约字段归属 + `checked` 计数防空转假绿。恒 skip 4→1 |
| 7 | 09-22 14:05 | 141 修透 + 阵营保真 | 141 三处判据按渲染后模型（type+text，factType 已被剥离）整体重写，不许只修两处留静默不覆盖；回放器补阵营保真度。**恒 skip 归零、DEAD 红线首次全绿** |
| 8 | 09-22 15:0x | fact 直方图口径 | 嵌套子 fact 计入统计，零产出 33→10；137/142 清死分支 |
| 9 | 09-22 15:5x | 零产出机检归因 | 零产出清单从人肉归因改机检（9 项覆盖不足 / 1 项孤儿登记）+ `PROBE_ZERO` 自检开关；补登 tools/106 打包清单 3 个漏登文件（履历 V6.1.26）；同轮处理了一次 git 父链断裂事故并记档 |
| 10 | 09-22 21:2x~09-23 | 渲染产出口径 | fact 覆盖补【渲染未产出条目】机检清单；声明≠fact 判据修正；期间 github 推送遇代理 502 积压 3 笔、worktree 分支引用被删改用 detached HEAD 绕过（均记档） |
| 11 | 09-23 18:0x | 三级零产出分类 | replay V6.1.28：emit 点**可达性分析**（函数回溯 + 全仓调用点 + 门控字面量），零产出分三级【不可达 emit 点 / 本批次未触发 / 孤儿登记】；产出方检索补 content JSON 口径，修掉把实产 421 条的 clawHit 误判"不可达"的首跑假阳性；github 推送追平，双端一致 |

外加 **3 轮复盘修剪**（09-22 01:05 / 07:55 / 10:25）：独立复跑核对每轮声称的数字、修剪版本头字节数与孤儿文件、推翻"零产出=数据源缺失"的错误归因文案（该结论曾连续误导两轮）、推动"每轮必 commit / 每趟必记一行日志"落地。

## 四、关键成果（体检能力净变化）

1. **假绿清剿**：node 下静态扫描零输出假绿、基线比对不设退出码假绿、恒 skip 静默假绿——三处全灭；`DEAD=1` 成为可直接当 CI 红线的严格模式。
2. **规则复活**：133/127/129/143/134/141 六条规则从恒 skip 120 场（从未断言过任何东西）复活为真实断言；恒 skip 总数 8→0。
3. **误报清剿**：123 的 11 条、132 的 4 条、143 的 29 条、133 的白骨爪混判 11 条全部归零，且都做了双向负向测试防"一刀切不报"。
4. **诊断体系**：fact 覆盖直方图 → 渲染产出口径 → 三级零产出分类，归因从人肉猜演进为结构化机检、每条结论自带可复核证据（函数名 / file:line / 门控字面量）。
5. **工具收编**：3 个自封"临时"的 runner 删除、140-baseline 升 V1.2.0 修 Windows 路径，基线红线 18 场恢复可用。

## 五、当前状态：分支全绿，但两边互相看不见

分支侧（截至第 11 轮，gitee/origin 双端已推齐）：回放 0 fail / 恒 skip 0、DEAD=1 绿、静态 79 文件全 0、基线 18 场 MATCH、冒烟全过。

分叉的代价（2026-09-23 在主线 main 实测）：

| 检查 | 迭代线（分支） | 主线 main |
| --- | --- | --- |
| 静态扫描 | V1.1.0 真跑 79 文件全 0 | **静默假绿**：0 输出、退出码 0（缺 node 自举，即第 1 轮修掉的那个病） |
| 规则回放 | V6.1.28，24 条全绿 | V6.1.11，30 条，**红 2 条**（127 姐姐附身期间被攻击 ×3 场、147 拒马同体双重清算 ×3 场）；DEAD=1 下共 5 条（另三条：乘风击退换位、概率连击"宣告者≠触发者"×3 个 seed、carry 加成超上界） |
| 基线 --check | V1.2.0，18 场 MATCH | **跑不起来**：V1.0.0 的 Windows 双盘符路径 bug（ENOENT） |
| 规则集 | 24 条（fork 时点） | 30 条（主线第 4~9 趟新增 147~152 共 6 条，迭代线没见过） |

- 主线 5 个新角色/演示提交（灭绝师太、金毛狮王谢逊、胖远桥阵容、第三关独立化、demo 实战化）完全不在体检视野内；主线那 5 条红强烈指向新内容（连击越界的 3 个 seed 全是谢逊召唤物场景）。
- 复盘自动化停在第 3 轮（09-22 10:25），第 7~11 轮迭代没有复盘覆盖，原因待查。

## 六、业务侧待修清单（体检线登记在案、未动手）

1. **spiderFly fact 通道死代码**：`core/11battle-round.js:323` 的 `executeFly` 调用与 `modules/27elite-mingjiao.js:586-607` 整段在现行调用链上不可达（门控 `decl.type === 'spiderFly'` 全仓无产出方；飞天机制实际走 `modules/27:487` 的免疫声明通道）。二选一：给 `_pendingStateTransitions` 补 `type:'spiderFly'` 产出方，或删掉 executeFly + `FACT_SPECS.spiderFly` 登记与渲染映射。
2. **meteorSplashGrowth 孤儿登记**：`infra/56 L30` + `infra/58 L111` 有登记、全仓无 emit 点。
3. **butterflyNoHost 缺 sisterUid 字段**：产出点 `modules/27 L273` 只写 `{unitName}`，而契约 `infra/58 L97` 与渲染 `render/30 L617` 都需要 sisterUid（挂了 5+ 轮，通道经复核可达，定性成立）。
4. **zhangTaunt 顶层通道零命中**（`modules/27 L50`）。
5. **孤儿回血定性**：seed1/seed10 存在组内 0 条 clawHit 却发「宋青书已满血，白骨爪未能回复生命」的条目。
6. **【主线新红，迭代线尚未见过】** 127/147/乘风击退/概率连击/carry 超上界——待两边同步后 triage（真 bug 报业务侧，规则未适配新角色的修判据）。

## 七、文件与版本对照（分支 fork 后的净变化）

- 版本推进：`rules-replay.mjs` V6.1.11→**V6.1.28**；`140-baseline.js` V1.0.0→**V1.2.0**；`123static-scan.js` →V1.1.0；`122health-utils.js` →V6.1.15；`120test-runner.html` →V6.0.1（清理 30 个冗余 BOM）；规则文件 133→V6.1.17、127/129/143→V6.1.2x 等。
- 规模：23 个文件 +2863/−948 行；删除 baseline-check/diff/entries.mjs 与 `_baseline-result.json`；重录 `baselines/baseline-v1.json`。
- 新增三份文档（只在分支上）：`tests/体检迭代日志.md`、`tests/体检复盘报告.md`、`记录-更改履历.md` 的多条 V6.1.x 条目。

## 八、以后想合并回主线（备查）

- 命令：`git fetch gitee && git merge gitee/workbuddy/main-87959581`（在 main 上执行）。
- **预期冲突不小**：`rules-replay.mjs`、`123static-scan.js` 与规则文件 123/129/132/133/143 两侧都改过（分支是深度重构版，主线第 4~9 趟是针对最新游戏代码的适配版），`记录-更改履历.md` 顶部两侧各自追加过条目。语义合并需逐处判断，不建议人工硬合。
- **推荐路径**：交给体检线自己跑一轮"同步 main"（它有全部上下文与回归工具）：在其分支上 merge main → 逐个裁决冲突 → 全量回归 → 把主线 30 条规则与 5 个新提交纳入体检视野，顺手 triage 主线那 5 条红。
