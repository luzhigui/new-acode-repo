# 夜间体检日志

> 每趟夜间体检的成果与分析都追加在文件末尾，随代码一起推送，下一趟靠本文件继承上下文。
> 改动红线：只动 `tests/` 下的体检代码与本文件；战斗逻辑与数值（core/ infra/ player/ ui/ fx/ modules/ content/）一律不动，作者本人的 `记录-更改履历.md` 也不动。

---

## 2026-09-21 05:30~05:42（第 1 趟 · 本文件创建）

### 一、上下文继承

- 仓库此前**没有**本日志文件（首趟留痕），无上一趟结论可直接继承。
- 从最新提交与规则文件头推断出的"上一趟状态"：最新提交为 `新增体检项 143-张无忌九阳神功回复量`（覆盖 V6.1.8 九阳 8%→10%），规则编号已排到 143；`137/141` 标到 V6.1.11、`129/132/143` 标到 V6.1.10、`142` 标到 V6.1.9，其余 12 条仍停在 V6.0.0。
- **并发提醒（下一趟注意）**：本趟进行期间远端又落了两个体检提交——`2d10164`（V6.1.12：修 121 的 getCtx 挂载点让实时体检真正跑起来、122 补 getUnitCol/Row 的 import、141 改扫敌方 afterE、140 补阵营口径）与 `c3b5bbf`（142/143 配方登记 + 静态扫描覆盖）。本趟已 rebase 到它们之上，二者与 130/144 无重叠。若下一趟也遇到"别人同时在改 tests/"，push 前记得重新 pull --rebase。

### 二、分析结论（对照 记录-更改履历.md V6.1.6~V6.1.10）

本趟先搭了可复用的回放自检工具 `tests/rules-replay.mjs`（真跑引擎 → 渲染战报 → 跑全部规则），用 120 场对局（20 种子 × 1~6 关）实测每条规则，得到三档现状：

1. **真跑得起来且全绿**：125 坚盾时序、131 分隔符、135 破防、139 小昭不可选期（各 120/120）；126 玄冥、128 姐姐飞回、137 苦练、141/142 张三丰、124 宋青书未命中（按阵容命中 10~48 场）→ 0 失败。
2. **恒 skip 的空转规则 10 条**（120 场一次都没跑到）：123 白骨爪回血、127 姐姐附身、129 小昭妹飞天、132 九阴白骨爪、133 死亡特效、134 张无忌切换、136 流星赶月、138 乘风击退、140 流云身法、143 九阳神功。其中 **136 流星赶月** 的问题不只是"没碰到"——它按当前版本（加攻属小昭姊海克斯增强专属，普通溅射本就不加攻，`core/16` 的 growth 由 `xiaoHexEnhance.atkPerSplash` 门控）的判据已经过时，**一旦碰到就是误报**，下一趟优先处理它。
3. **数据源错位**：`130 坚盾叠加超上限` 120/120 恒 skip，但战报里明明有坚盾"已叠X/Y"——因为它只扫顶层 `e.text`，而该文本嵌在 attack-group 的 `entries` 子条目里（同源的 125 就是扫 entries 才跑得起来）。→ 本趟挑它优化。

### 三、优化：130-坚盾叠加超上限（数据源修正，规则复活）

- 改动：`130-fortify-overflow.js` 新增 `fortifyTexts()`，改为「顶层 text + attack-group entries 子条目」双层扫描（与 139 同款），同一条战报只取首个 `已叠X/Y` 匹配避免重复计数；判据本身（X > Y 即复发）不动，保持"只改一处"。VER 提到 V6.1.11。
- 效果：由 **skip 120/120 → pass 120/120**（从整条空转变成真正生效的上限防线）；负向用例（造"已叠5/4"）确认能报错。

### 四、新增：144-宋青书新婚快乐链路（此前零覆盖）

- 文件：`tests/health-rules/144-xinhun-kuaile.js`（rule91），已接入 `121health-monitor.js` 的规则数组与 `124rule-recipes.js` 的配方表（tag `enemy:宋青书`）。
- 覆盖机制：新婚扣血（content `宋青书.xinHun.hpDeduct = 1`）、快乐新层百分比（healLevels[0] = 16%，序列 `[0.16, 0.10, 0.06, 0.03]`）、性奋代价与新婚 1:1 配对且 penalty 每次 +1、快乐回血层数不超过累计叠加次数 + 回写 `hpAfter = hpBefore + heal`。
- 误报规避：性奋代价有 `unit.maxHp > 1` 保底，宋青书上限被扣到 ≤2 时跳过配对校验；本场无新婚条目直接 skip。
- 实测：**pass 20 / skip 100**（120 场中 20 场宋青书+周芷若同场），0 误报；负向用例 6 类（扣血漂移 / 新层百分比错用衰减值 / 性奋漏扣 / 性奋步长退化 / 快乐层数虚高 / 上限见底不误报）全部符合预期。

### 五、体检当前状态

| 项 | 结果 |
| --- | --- |
| 规则回放自检（120 场 × 22 条） | **0 失败**；10 条恒 skip（机制未出场，见上） |
| 改动后即时比对 | 130 由 skip120 → pass120；144 pass20/skip100，均未影响其余 20 条 |
| `node tests/smoke-headless.mjs`（引擎冒烟） | 全绿（20 局跑完、分出胜负、播放层导出齐全） |
| `node tests/baseline-check.mjs`（基线比对） | 18/18 场与 `baselines/baseline-v1.json` 不同 —— **改动前后一致**，属历史基线过期（游戏已迭代多版），非本趟引入 |
| 语法检查 | 121/124/130/144 四个文件 `node --check` 通过 |

### 六、下一趟建议（交接话）

1. **优先修 136 流星赶月**：判据仍停在"普通溅射必须加攻"的旧口径，与现行设计（加攻 = 小昭姊海克斯增强专属，普通版无加攻）相反，属"碰到就误报"。建议改成校验加攻量 = 溅射命中人数 × `atkPerSplash(2)`，并去掉对普通版的强制要求。难点：120 场里该机制一次都没碰到，改完需用构造战报验证。
2. **其次查 140 流云身法**：本趟 120 场它一次都没跑到（战报里"流云身法"出现 0 次），需确认是"流云本就没出场"还是数据源（`unit.buffDodgeBonus`）已随 V6.1.1 词条化改道。注：并发提交 `2d10164` 刚给它补了阵营口径（流云挂敌方时我方闪避本就该为 0），下一趟请在**那版之后**重新实测，别按本趟结论重复改。
3. 恒 skip 名单里还有 123/127/129/132/133/134/138/143 八条：本趟未逐一核实是"阵容没碰到"还是"数据源错位"。建议下一趟挑其中 1 条做 130 同款核查（先看它锚定的文本到底在顶层还是 entries）。
4. 值得考虑的新覆盖点（仍无规则）：**叛逆突袭真伤 12%**、**成昆幻影伪装**、**连击（doubleStrike）**、**拒马（巨马 0/5/25）**、**V6.1.10 飞行未命中光环**（`6% + 残血×6% − 空列×12%`，下限 0）。其中飞行光环最新但战报只渲染"未命中"三字、不带数值，若要覆盖得另找 fact/面板通道，别硬凑。
5. 工具沿用：`node tests/rules-replay.mjs`（可用 `SEEDS=` / `STAGES=` / `KEYWORDS=` 调参）就是本趟的自检手段，改规则前后各跑一次对比 pass/fail/skip 即可；新增规则文件放进 `tests/health-rules/` 会被它自动装载，不用改 runner。

---

## 2026-09-21 06:00~06:12（第 2 趟）

### 一、上下文继承

- 上一趟（第 1 趟）结论：改了 `130 坚盾叠加超上限`（entries 双层扫描，skip120→pass120）、新增 `144 宋青书新婚快乐链路`（pass20/skip100）；体检 0 失败、11 条恒 skip。
- 上一趟交接话里排第一的就是 **136 流星赶月**（判据过时、碰到就误报），排第二是 **140 流云身法**，并点名"飞行未命中光环"是还没人盯的新覆盖点。本趟按这个顺序做。
- 拉取时远端已是最新（`Current branch main is up to date`），无并发冲突；仅有第 1 趟遗留的一笔日志改动未提交，已先落一个 commit 保住再 rebase。

### 二、分析结论（对照 记录-更改履历.md V6.1.6~V6.1.10）

1. **136 流星赶月**：现行设计（core/16 SPLASH 分支 + content 小昭 `hexEnhance.params.meteorShower.atkPerSplash = 2`）是「加攻 = 小昭·姊海克斯增强专属，普通 ☄️ 版本就不加攻，小昭·弟永久版标签为 🦋 蝶星」。旧规则却强制要求普通版必须带"攻击+"，口径**正好反了**，只要碰到就是误报 → 本趟挑它优化。
2. **飞行未命中光环（V6.1.10，最新一版改动）**：`core/03 getMissBreakdown` 是未命中率的唯一算法源，公式 `飞行基础 6% + 残血数×6% − 空列数×12%`，下限 0。上一趟判断"战报只渲染『未命中！』三字、没有数值通道，别硬凑"——本趟复核确认渲染侧确实无数值，但**函数侧有干净通道**：`getMissBreakdown(unit, allySide, enemySide)` 直接返回 `{ total, sources }`，可以按同一份快照重算公式做闭环比对。→ 本趟新增覆盖。
3. **恒 skip 11 条的根因（新发现，重要）**：`KEYWORDS` 实测 120 场里"流星赶月 / 溅射 / 蝶星 / 海克斯"全部命中 0 次。团队海克斯是在 `player/49battle-flow.js` 的回合间选 Buff 流程里注入 `activeBuffs` 的，**`tests/rules-replay.mjs` 从不注入**，所以 `state.activeBuffs = []` → 所有"靠 Buff 触发"的机制（流星赶月、乘风、流云、拒马、连击…）在回放里永远不可能出现。也就是说 136/138/140 这批不是"数据源错位"，是**回放器缺一层 Buff 注入**。本趟未动它（留给下一趟，见第六节）。

### 三、优化：136-流星赶月（判据 V6.1 化，消除反向误报）

- 文件：`tests/health-rules/136-meteor-atk.js`，VER → V6.1.11；规则名 `流星赶月普通溅射加攻缺失(回归)` → `流星赶月溅射加攻量(回归)`（名字与新口径对齐，同步改 `124rule-recipes.js` 的 key）。
- 改动（只改判据这一处）：
  - 不再要求普通版必须加攻（旧口径 = 反向误报源）；改成**一旦出现"攻击+N"，N 必须 = 溅射命中人数 × atkPerSplash(2)**，命中人数从文本"溅射：甲、乙，"的顿号名单里解析。
  - 新增**反向兜底**：本场阵容里完全没有小昭·姊（无任何海克斯增强来源）却出现加攻 → 判定"加攻被误接到普通路径，攻击数值膨胀"。有小昭·姊时不做该判定（她可能中途阵亡，终局阵容反推不出来），避免误报。
  - 覆盖范围顺带从"☄️ 流星赶月"扩到"🦋 蝶星"（同一次 SPLASH 结算的两套标签），并按 130/144 同款做「顶层 text + attack-group entries」双层扫描。
- 验证：构造战报 7 例全部符合预期——2人+4/有小昭姊 pass、2人+2 fail（成长量错）、2人无加攻/无小昭姊 pass（**旧口径会误报，新口径不报**）、2人+4/无小昭姊 fail、蝶星3人+6 pass、单人+2 pass、无流星条目 skip。

### 四、新增：145-飞行未命中光环数值（V6.1.10，此前零覆盖）

- 文件：`tests/health-rules/145-fly-miss-aura.js`（rule92），已接入 `121health-monitor.js` 规则数组、`124rule-recipes.js` 配方表（tag `mechanic:飞行未命中`）、`123static-scan.js` 扫描清单。
- 覆盖机制：`6% + 存活残血(<40%)数×6% − 敌方空列数×12%`，下限 0；三条复发信号 = 合计值不符 / sources 明细的项数或单条数值不符 / 光环越界出现在非飞行单位。
- 数据源：直接调 `core/03 getMissBreakdown`（引擎唯一算法源）+ `countEnemyEmptyCols`，按同一份单位快照重算公式做闭环比对 —— 绕开"战报不带数值"的限制。
- 误报规避：`_neverMiss` 必中单位跳过公式校验；只算存活单位（与引擎口径一致）；本场无存活飞行单位直接 skip。
- 实测：**pass 28 / skip 92**（120 场中 28 场终局有存活飞行单位），0 误报；负向用例 7 例全部符合预期——把 `FLY_MISS_LOWHP_BONUS` 改回旧值 12 或 `FLY_MISS_EMPTYCOL_REDUCE` 改回旧值 6 都会当场报错，满血/无空列/空列3（下限0）/无飞行/必中飞行均不误报。

### 五、体检当前状态

| 项 | 结果 |
| --- | --- |
| 规则回放自检（120 场 × 23 条） | **0 失败**；恒 skip 11 条（根因见第二节第 3 点：回放器不注入团队 Buff） |
| 改动后即时比对 | 145 新增跑起来（pass28）；136 仍 skip120（Buff 未注入所致，非规则问题）；其余 21 条 pass/fail/skip 与改动前完全一致 |
| 构造用例验证 | 136 七例、145 七例，正向/负向/边界全部符合预期 |
| `node tests/smoke-headless.mjs` | 全绿（20 局跑完、分出胜负、播放层导出齐全） |
| `node tests/baseline-check.mjs` | 18/18 场与 `baselines/baseline-v1.json` 不同 —— 与第 1 趟结论一致，属历史基线过期（游戏已迭代多版），非本趟引入 |
| 语法检查 | 121/123/124/136/145 五个文件 `node --check` 通过 |

### 六、下一趟建议（交接话）

1. **最高优先：给 `tests/rules-replay.mjs` 注入团队海克斯 Buff**（参照 `player/49battle-flow.js` 的选 Buff 流程 / `tools/101auto-battle-utils.js` 的 `nextBuffs` 写法，按 seed 轮转 `CONFIG.BUFFS` 的 key 生成 `{ key, target, remaining, name }` 塞进 `battleState.activeBuffs`）。**这一改能把 136/138/140 以及 123/127/129/132/133/134/143 里的 Buff 触发型规则一次性激活**，是本仓库体检目前最大的一处空转。改完先单跑一轮确认 pass/fail 不炸，再逐条复查。
2. 做完第 1 条后，**用真战报复验 136**（本趟只有构造用例，缺真实命中人数文本样本）。
3. **140 流云身法**仍在恒 skip 名单里，Buff 注入后如它还是 0 命中，再去查 `unit.buffDodgeBonus` 是否已随 V6.1.1 词条化改道。
4. 仍无覆盖的候选点（按新鲜度排）：**叛逆突袭真伤 12%**、**成昆幻影伪装**、**连击（doubleStrike）**、**拒马（巨马 0/5/25）**。注意前三个同样依赖 Buff/精英随机出阵，建议先把第 1 条做掉再挑。
5. 小遗留：`tests/health-rules/144-xinhun-kuaile.js` 第 1 趟新增时漏登进 `123static-scan.js` 的 SCAN_FILES（本趟只补了 145），下一趟顺手补上即可。

---

## 2026-09-21 06:30~06:39（第 3 趟）

### 一、上下文继承

- 上一趟（第 2 趟）结论：优化 `136 流星赶月`（判据 V6.1 化，消除反向误报）、新增 `145 飞行未命中光环数值`（pass28/skip92）；体检 0 失败、**恒 skip 11 条**。
- 上一趟交接话排第一的是：**「给 `tests/rules-replay.mjs` 注入团队海克斯 Buff」**——它点出 11 条恒 skip 的根因不是"数据源错位"，而是**回放器从不注入 `activeBuffs`**，于是所有靠 Buff 门控的机制（流星赶月/乘风突袭/流云身法/概率连击/拒马…）在回放里永远不可能出场。本趟照这条做。
- 拉取时远端已是最新（`Already up to date`），无并发冲突。`记录-更改履历.md` 最新一版仍是 **V6.1.10**（2026-09-20 飞行未命中光环），与上一趟对照基线一致，本趟沿用同一套数值口径。

### 二、分析结论

1. **先做交接话第 1 条（注入 Buff）**：照 `tools/116-role-balance-worker.js` 的跑批口径 + `player/49battle-flow.js` 全自动选 Buff 口径实现——每回合 `remaining−1` 淘汰过期、每 3 回合（`round % 3 === 0`，与 `player/42` 一致）为明教/六大派各补选一个，过滤已有 key 与 `BUFF_ROLE_REQUIREMENTS` 职业要求；**用 seed/round 确定性轮转，不消耗战斗 RNG**，避免改变战斗随机序列、破坏可复现性。第 1 回合额外预注入一轮（真实流程要等第 3 回合才选，体检为覆盖机制提前一拍）。
2. **注入效果（实测 120 场）**：关键字"流星赶月/溅射 147、流云 73、乘风 407、连击 230、嗜血 83、热血 122、圣火 172、carry 267、心智 522"全部从 0 变正；**恒 skip 由 11 条降到 8 条**——`136 流星赶月` skip120→**pass65**、`138 乘风击退` skip120→**pass47**、 `140 流云身法` skip120→**pass7**，三条规则同时复活。
3. **由此暴露出 138 的误报**（本趟真正的优化落点）：`138 乘风击退换位异常` 复活后立刻报 1 例（seed=20 stage=4）。用新增的 `DUMP=seed:stage` 单场调试打开发现：`44 [push] 张无忌 5→8、宋青书 8→5` 之后，紧邻的 `47 [atk] u6@8 D=u1@5` 快照**仍是旧位**，但 `57`、`64` 两条快照已在宣告位（5/8）——即**位置确实生效了，只是 fxSnapshot 有一拍时滞**。旧判据"只看击退后第一条快照"过严 → 误报。

### 三、优化：138-乘风击退换位（判据容忍一拍时滞，消除误报）

- 文件：`tests/health-rules/138-wind-push.js`，VER → V6.1.12；只改"位置一致性"这一处。
- 改动：由「击退后**第一条** attack-group 快照必须等于宣告位置」改为「在被下一次 `buff-push`/`buff-swap` 打断之前，**任意**一条快照落到宣告位置即视为生效；只有存在后续快照却全部不等于宣告位置，才算真未生效」。击退距离 +3、换位文案两项判据不动。
- 效果：138 由 **fail1/pass47 → pass48/fail0**（120 场全绿），且负向用例"全部快照都是旧位"仍会报错——真 bug 没被放宽掉。
- 配套工具改动（同属 tests/，为上述两步提供前提与证据）：`tests/rules-replay.mjs` 加 Buff 注入 + `NOBUFFS=1` 开关 + `DUMP=seed:stage` 单场调试输出。

### 四、新增：146-概率连击触发越界/重复（此前零覆盖）

- 文件：`tests/health-rules/146-double-strike.js`（rule93），已接入 `121health-monitor.js` 规则数组、`124rule-recipes.js` 配方表（tag `mechanic:概率连击`）、`123static-scan.js` 扫描清单（**顺手补登上一趟漏掉的 144**）。
- 覆盖机制：`core/11` 每回合为持 Buff 方各挑一人登记 `doubleStrikeUnitUid`（明教/六大派各一）+ `core/03 registerDoubleStrike`（80% 再打一次）；把「回合开始宣告名单」（buff-summary）与「实际触发者」（banner 后第一条 attack-group 的攻击者名）对齐比对。
- 三条复发信号：① 同回合同一单位触发连击 ≥2 次（额外攻击被重复登记 → 攻击次数膨胀）；② 触发者不在本回合宣告名单里（连击越界）；③ 「触发失败」文案里的单位同样必须出自宣告名单。附带一条上限校验：单回合触发 >2 次（双方各一人，上限应为 2）。
- 误报规避：只有本场真出现连击宣告/触发才校验；触发者名字解析不到就跳过该条（不猜）；本回合无宣告时不做事后比对。
- 实测：**pass 43 / skip 77**（120 场），0 误报；构造用例 8 例（正常/重复触发/越界/失败文案越界/双方各一次/单回合3次/无条目）**全部符合预期**。

### 五、体检当前状态

| 项 | 结果 |
| --- | --- |
| 规则回放自检（120 场 × 24 条） | **0 失败**；恒 skip 由 11 条 → **8 条** |
| 复活情况 | 136 pass65（原 skip120）、138 pass48（原 skip120，且已修掉误报）、140 pass7（原 skip120）、146 新增 pass43 |
| 构造用例验证 | 138 四例 + 146 八例，正向/负向/边界**全部符合预期** |
| `node tests/smoke-headless.mjs` | 全绿（20 局跑完、分出胜负、播放层导出齐全） |
| `node tests/baseline-check.mjs` | 18/18 场与 `baselines/baseline-v1.json` 不同 —— 与前两趟结论一致，属历史基线过期，非本趟引入 |
| 静态扫描（node 等效版，79 文件） | import 引用断裂 **0**、枚举 import 缺失 **0**；仅 3 条共享符号提示且均落在引擎自导出文件（50-event-bus/54-global-store/26elite-sixsects），脚本未实现原版的"自导出排除"规则，属脚本简化噪音 |
| 语法检查 | 121/123/124/138/146/rules-replay 六个文件 `node --check` 通过 |

### 六、下一趟建议（交接话）

1. **剩余 8 条恒 skip 里挑 1 条核查**（本趟 Buff 注入已排除"Buff 未注入"这一公共根因，剩下的多半是"精英随机出阵碰不到"或"数据源错位"）：`123 白骨爪回血`、`127 姐姐附身`、`129 小昭妹飞天`、`132 九阴白骨爪`、`133 死亡特效`、`134 张无忌近身切换`、`141 张三丰核心机制`、`143 九阳神功`。**优先看 134/143**（张无忌是明教核心，不该 120 场一次都碰不到，疑似数据源错位）。方法同 130：先确认它锚定的文本在顶层还是 entries 子条目。
2. **136 已能跑真战报（pass65）**：用 `DUMP` 抽几场核对"溅射：甲、乙，"名单解析是否与实际命中人数一致（上一趟只有构造用例，缺真实样本）。另外 55 场 skip 值得确认是"没选到流星赶月"还是"选到但没溅射"。
3. **拒马阵仍是零覆盖**（`horseFormation`：hp25 / atk0 / def5，回合开始生成、回合结束 50% 销毁）。注：战报文本写的是「**拒马**」不是「巨马」，用 `KEYWORDS=拒马` 统计，别用错关键字。render 侧有 `buff-summon`（horsePos/horseUid）与 `buff-destroy` 两条 fact 通道，可校验"生成位置不覆盖已有单位 + 属性符合配置"。
4. 其余仍无覆盖的候选（都已有干净 fact/数值通道）：**嗜血狂刀吸血 80%**（`renderBloodthirstLeechFact` 带 `leechVal`）、**热血奋战**（15% 已损失生命、每第 3 次翻倍；`renderHotBloodHealFact` 带 `leech`）、**carry 五号位加成**（攻/防/血 +8%/+8%/+10%，死亡队友双倍；`renderCarryApplyFact` 带 atk/def/hp）、**圣火令**（随机两列 +30% 攻、两行 +30% 防）。
5. 工具沿用：`node tests/rules-replay.mjs`；新开关 `NOBUFFS=1`（关注入做前后对比）、`DUMP=seed:stage`（单场打印 push/swap/atk 位置链）、`KEYWORDS=甲|乙`（统计战报文本命中）、`SEEDS=`/`STAGES=`。

---

## 2026-09-21 23:00~23:20（第 4 趟）

### 一、上下文继承

- 上一趟（第 3 趟）结论：给 `tests/rules-replay.mjs` 加了团队海克斯 Buff 注入，恒 skip 由 11 条降到 **8 条**；优化 `138 乘风击退换位`（容忍 fxSnapshot 一拍时滞）、新增 `146 概率连击触发越界/重复`。
- 上一趟交接话排第一的是：「**剩余 8 条恒 skip 里挑 1 条核查，优先看 134/143**（张无忌是明教核心，不该 120 场一次都碰不到，疑似数据源错位）」。本趟照这条做，并按上一趟给的口径「先看它锚定的文本到底在顶层还是 entries」逐个确认。
- **本趟拉取时远端有大批并发改动**（74 个文件）：`记录-更改履历.md` 从上一趟的 V6.1.10 一路加到 **V6.1.15**（V6.1.12 九阳 10%→12%、V6.1.13 render/30 计算行取整口径对齐伤害行、V6.1.14 体检两项误报修复 + 精英评测、V6.1.15 精英评测撤 force）。另有别人同期改的 tests/：`e4a860a`（143 九阳常量收口 12% + 三档回退识别）、`ed51817`（体检两项误报修复）、`389aa43`（静态扫描清单补登）。**143 已被别人更新到 12%（V6.1.11），但其恒 skip 问题任何人都没解决** —— 本趟在其基础上接着修，不重复改数值口径。

### 二、分析结论（本次两个"数据源错位"的实锤）

先用关键字扫全部 120 场，把"是机制没出场"和"是规则扫错地方"彻底分开：

| 关键字 | 120 场命中 | 结论 |
| --- | --- | --- |
| 张无忌 | 1435 | 张无忌 **120/120 场都出阵**，不存在"英雄碰不到" |
| 九阳 / 切换近战 | **0 / 0** | 出阵了却一条都没扫到 → **确定是数据源错位** |
| 拒马 / carry / 连击 / 圣火 / 热血 / 嗜血 | 460 / 227 / 280 / 167 / 145 / 55 | 拒马样本充足，且**此前零体检覆盖** |

进一步用一次性探针挖到两处不同的错位形态（都不是"机制没触发"）：

1. **143 的锚点在 attack-group 的 entries 子条目里**（`render/30 renderNineYangHealFact` 的结果被挂在攻击组里，实测单局 120 场共 **926 条**九阳回血文本，顶层一条都没有）；「张无忌切换近战形态」则更特殊——`renderZhangSwitchFact` 是全仓库**唯一返回数组**的渲染函数（切换行 + 台词行两件套），旧规则里 `e.isZhangSwitch` 和 `e.text` 在这种元素上双双取不到。→ 与第 1 趟 130 完全同源，130 的解法（双层扫描）可直接复用，故本趟挑 **143** 优化。
2. **另一处是 replay 工具层**：`rules-replay.mjs` 第 131 行 `log.push(renderLog(...))` 直接把渲染结果当单条目压入，**从不摊平数组**。这才是 134（以及 143 的变身检测）躺平的根因，属 replay 工具层缺陷。本趟刻意**没有**顺手改它（改一次会影响全部 25 条规则的可见条目），留给下一趟单独做并做全量回归，见第六节第 1 条。

### 三、优化：143-张无忌九阳神功回复量（数据源修正，规则复活）

- 文件：`tests/health-rules/143-jiuyang-heal-pct.js`，VER → V6.1.15；**只改"战报扫描"这一处**，判据（12% / 10% / 8% 三档回退识别 + 回血写回一致性 + 变身/背负/蝶变附身自动降级）一字未动。
- 改动：新增 `nineYangTexts(e)`，按「**顶层 text + attack-group entries 子条目 + 数组元素递归摊平**」三层收集文本（保留原战报下标，使"变身 vs 回血"的前后位置比较仍有效），146 行在原位改一次内层循环即可。
- 效果：**skip 120/120 → pass 120/120**（0 误报）。覆盖深度实测（复用同一口径另跑一轮统计）：926 条九阳回血里 **197 条真正走到严格 12% 比例比对**（有牙齿），140 条回满截断、138 条变身前、451 条降级为仅查算术——全部按设计分级，不是靠放水通过。
- 构造用例 12 例**全部符合预期**：12% 正确 pass（含 entries 子条目形状）、回退 10%/8%、改错比例 15%、血线不同步、回复量非正 → 均报错；无条目 skip、顶层 text 旧形状仍兼容、回满截断不误报、变身前跳过比例、carry 抬血上限降级 → 均不误报。

### 四、新增：147-拒马阵生成销毁判据（此前零覆盖）

- 文件：`tests/health-rules/147-horse-destroy.js`（rule94），已接入 `121health-monitor.js` 的规则数组、`124rule-recipes.js` 配方表（tag `mechanic:拒马阵`）、`123static-scan.js` 扫描清单。
- 覆盖机制：`core/05battle-horse.js spawnHorse / destroyHorse`（core/11 L46/L50 生成、L509 销毁）——回合结束按 `pos 降序` 逐只判定，`prob` 起 Config.BUFFS.horseFormation.destroyProb×100，成功（消散）后砍半、失败后重置回基值，`roll ≤ prob` 等号算消散。
- 四条复发信号：① **判据反向/边界写反**（roll 与 success 不符，含 `roll == prob` 的等号边界）；② **roll 越界**（不在 1~100）；③ **prob 既非基值也不在逐次减半序列 {50,25,12,6,3,1}**（递减链断点、或被硬编码吃掉——V6.1.3 履历就记过"原先硬编码 50，改配置不生效"）；④ **同回合同号位清算两次**；外加站位越界（非 1~9）兜底。
- **误报规避（关键取舍）**：`destroyHorse(A); destroyHorse(B);` 同回合会连跑两批，而战报里没有任何字段区分这两批，故**刻意只选"与分批无关"的判据**，不去反推批内递减链，避免双阵营同时持 Buff 时误判。`baseProb()` 用 try/catch 现读配置（`CONFIG.BUFFS` 是 getter，游戏数据未就绪时会抛），拿不到则退回 0.5 —— 绝不让体检规则自己在浏览器里炸掉（开发时已踩到一次，当场修掉）。
- 实测：**pass 38 / skip 82**（120 场，拒马只在这己方抽到「巨马阵」团队 Buff 时才生成），0 误报；构造用例 **16 例全部符合预期**（正常/roll 边界 50/两种判据写反/两个 roll 越界/递减 25/失败重置 50/断点 49/硬编码 60/同回合重复/跨回合合法/站位越界/无条目）。

### 五、体检当前状态

| 项 | 结果 |
| --- | --- |
| 规则回放自检（120 场 × 25 条） | **0 失败**；恒 skip 由 8 条 → **7 条** |
| 143 | **pass 120**（原 skip 120，规则复活） |
| 147（新增） | **pass 38 / skip 82** |
| 其余 23 条 | pass/fail/skip 与本趟改动前**逐条一致**（无回归） |
| 构造用例 | 143 十二例 + 147 十六例，正/负/边界**全部符合预期** |
| `node tests/smoke-headless.mjs` | 全绿（gameData 加载、74 条 FACT_SPECS、20 局跑完分出胜负、播放层导出齐全） |
| 静态结构快检（node 等效版） | SCAN_FILES 93 个文件缺失 0；25 条规则 import 路径断裂 0、枚举漏 import 0 |
| `node tests/baseline-check.mjs` | 18/18 场与 `baselines/baseline-v1.json` 不同 —— 与前 3 趟结论一致，属历史基线过期（游戏已迭代多版），本趟只动 tests/ 规则，不可能影响战斗结果 |
| 语法检查 | 143/147/121/123/124 五个文件 `node --check` 通过 |

### 六、下一趟建议（交接话）

1. **最高优先：`tests/rules-replay.mjs` 第 131 行不摊平渲染数组**（本趟实锤的根因，见第二节第 2 点）。改法一行：`const r = renderLog(...); if (Array.isArray(r)) log.push(...r); else if (r) log.push(r);`。改完**必须全量重跑 120 场做回归**（所有规则会看到更多条目，134 预计直接复活），出现新 fail 先确认是真 bug 还是规则过时。`renderZhangSwitchFact` 目前是全仓库唯一返回数组的渲染函数，若哪天新增第二处，这笔改动同样受益。
2. 第 1 条做完后，**134 张无忌近身切换时机**（仍 V6.0.0）就能拿到真样本，届时别忘了把判据对齐现有实现——现在 `modules/27elite-mingjiao.js` 的切换还过了一道 FSM 守卫（`fsm.is('ranged')` + `state._zhangSwitched`），旧写的"每场仅 1 次"口径未必还成立。
3. 剩余 7 条恒 skip（本趟 Buff 注入 + 143 已排查过一轮）：`123 白骨爪回血`、`127 姐姐附身`、`129 小昭妹飞天`、`132 九阴白骨爪`、`133 死亡特效`、`141 张三丰核心机制`、以及仍未复活的 134。注意 **`122health-utils.js V6.1.14` 有意放过了"名字≥4字且图标>2"的长名单位**（避免渲染截断误报），123/133 这类"刷屏/特效"规则若恒 skip，先确认是不是这个取舍连带导致的，别急着加断言。
4. 仍无覆盖的候选（都已有干净的数值/事实通道，按本趟实测战报命中数排）：**carry 五号位加成 227**（攻/防/血 +8%/+8%/+10%，死亡队友双倍；`renderCarryApplyFact` 带数值）、**圣火令 167**（随机两列 +30% 攻、两行 +30% 防）、**热血奋战 145**（15% 已损失生命、每第 3 次翻倍；`renderHotBloodHealFact`）、**嗜血狂刀吸血 80% 55**（`renderBloodthirstLeechFact`）。再下一趟建议挑 carry（样本最大、公式最直白）。
5. **V6.1.13「计算行取整口径对齐伤害行」**目前也没人盯，但别直接拿"计算行 vs 造成行"做等值断言——实测两者会被乾坤大挪移减伤、坚盾等加成/减伤层合法拉开（如 `计算：... = 23` 对 `造成 16`）。要做的话得先把"同一攻击组内的倍率/减伤项"从 entries 里解析出来再相减，否则一定天天误报。
6. 工具沿用：`node tests/rules-replay.mjs`，注意环境变量要写在 node 命令前（见本趟开头的提醒）；本趟所有一次性探针/验证脚本已在提交前删除，`tests/` 目录保持干净。

---

## 2026-09-21 23:30~23:5x（第 5 趟）

### 一、上下文继承

- 接住第 4 趟的改动（143 优化 + 147 新增），本趟开局先补跑验证：**143 pass120、147 pass38、0 失败、恒 skip 7 条**，确认第 4 趟改动没把体检弄坏，可直接继承。
- **并发情况（重要，本趟踩到）**：本趟开局 `git pull --rebase` 时远端还是 `4f32d48`（`Already up to date`），但**跑到一半时另一个夜间体检会话把第 4 趟提交并推送了**（`3a6f347`，23:32:08，含 143/147/121/123/124 + 第 4 趟日志）。它写在同一台机器的同一工作目录里，于是本趟改为：`git stash` → `pull --rebase` → `stash pop` 接住自己的改动，并把日志里已与 `3a6f347` 重复的第 4 趟补记**删掉**、把被插到文件中间的第 5 趟记录**移到末尾**，保证日志按 1→5 趟顺序排列。**下一趟请注意：日志是多人/多会话共享文件，写完先 `git pull --rebase` 再提交，且不要假设自己是唯一写入方。**
- 第 4 趟交接话第 1 条建议「改 `rules-replay.mjs` 第 131 行摊平渲染数组，让 134 复活」。本趟走的是**另一条路**：直接把摊平逻辑写进 134 规则内部（不改 runner）。两条路不冲突——runner 摊平后 134 的 `switchTexts` 依旧能命中（顶层就是单条），不会重复计数；但 runner 摊平会让**所有**规则看到更多条目，动静更大，仍建议单独一趟做全量回归（见第六节）。
- 第 3 趟交接话里「134/143 疑似数据源错位」这条，第 4 趟只做了 143，**134 同病未治**（两条共用同一个锚点文本）——本趟接着做 134。
- 第 4 趟交接话第 4 条按战报命中数排的零覆盖候选里，**carry 227 次**排第一（圣火 167、热血 145、嗜血 55），且公式最直白 —— 本趟新增它。
- 拉取时远端已是最新（`Already up to date`），`记录-更改履历.md` 最新版仍是 **V6.1.15**（精英评测口径变更），战斗数值口径与上一趟一致，本趟未涉及引擎数值改动。

### 二、分析结论

1. **134 张无忌近身切换（本趟优化落点）**：`render/30 renderZhangSwitchFact` 返回的是 `[切换行, 台词行]` 数组，数组自身既无 `.text` 也无 `.isZhangSwitch`，而 134 只认顶层 → **120 场恒 skip**。第 4 趟 143 用同一套双层扫描从 skip120 变 pass120，直接佐证「张无忌切换近战形态」这条文本确实活在数组里，134 只是没去取。属于"数据源错位"而非"阵容碰不到"，修它的性价比最高（张无忌是明教核心，切换时机这条防线不该空转）。
2. **carry 五号位加成（本趟新增落点）**：公式在 `core/14` 两处重复实现（Normal 版 5 号位 / Sister 版 4~6 号位），数值完全走文本通道（`renderCarryApplyFact` 带 `攻+N 防+M 血上限+K`）。手工验算与引擎一致（seed=1/stage=3 首回合 张无忌 攻+8 防+5 血上限+51，按 `Σfloor(队友属性×比例×mult)` 逐项对上）。
   **最值得盯的是 hp 分支**：它读 `unit.state._baseMaxHp` 且带 `? … : 0` 兜底，而 V6.1.1 做过「属性词条化 + state/双源清理」——一旦这个字段在后续重构里改道，血上限加成会**静默归零**：不报错、不改文案、战报上照样有「👑 carry」这行，肉眼完全看不出来。这与本仓库已出过多次的"静默失效"是同一类病灶。

### 三、优化：134-张无忌近身切换时机（数据源 V6.1 化，整条规则此前恒空转）

- 文件：`tests/health-rules/134-zhang-switch.js`，VER V6.0.0 → **V6.1.15**（顺带把停更很久的版本号对齐到当前版本）。
- 改动（只改数据源这一处，**时机过早/过晚/多次切换三条判据一字未动**）：新增 `switchTexts(e)` 递归摊平（数组元素 + attack-group entries + 顶层）与 `isSwitchEntry(e)`；扫描循环条件由 `e.isZhangSwitch || e.text…` 换成 `isSwitchEntry(e)`。
- 误报规避：切换统计按**战报下标**循环，一个下标只 push 一次，故 `[切换行, 台词行]` 两件套不会被误判成"切换两次"。
- 效果：**skip120 → pass78 / skip42**（恒 skip 由 7 → 6），0 失败。
- 构造用例 8 例全部符合预期：数组形态@第1回合前排存活 → FAIL（**旧版抓不到**）、顶层形态@第1回合 → FAIL（回归旧能力）、entries 内形态 → FAIL、切换两次 → FAIL；第3回合切、前排阵亡后切、无 ctx 快照 → pass；无切换条目 → skip。

### 四、新增：148-carry 五号位队友加成量（此前零覆盖）

- 文件：`tests/health-rules/148-carry-bonus.js`（rule95），已接入 `121health-monitor.js` 规则数组、`124rule-recipes.js` 配方表（tag `mechanic:carry`）、`123static-scan.js` 扫描清单。
- 覆盖机制：门控（存活 / 非拒马 / **非小昭·姊弟** / 5 号位，有小昭·姊放宽到 4~6）+ 公式 `Σfloor(队友atk×0.08 / def×0.08 / _baseMaxHp×0.1 × mult)`，`mult = 存活?1:deathMultiplier(2)`。
- 五条复发信号：① 同回合同单位被应用两次（属性翻倍）；② 加成出现负数或小数（floor 累加必为非负整数）；③ **静默归零**——最弱队友按比例 floor ≥1 却加成为 0（含 hp 分支读不到 `_baseMaxHp` 这一类）；④ 门控失效——小昭·姊/弟拿到 carry；⑤ 数值膨胀——超出「全队属性之和 × 比例 × 死亡倍率 × 2 倍余量」上界。
- 误报规避（关键取舍）：**刻意不做精确重算**——战报不带当期队友属性快照，硬凑反推只会造误报；只选"用文本自身 + 终局快照量级就能定性"的判据。归零判据取"除自己外**最弱**队友"作门槛（最保守）、拒马不计入队友、拿不到池就跳过量级判据；配置比例现读 `CONFIG.BUFFS.carry` 且包 try/catch，读不到只跑结构判据。
- 实测：**pass 32 / skip 88**（carry 是团队 Buff，需本局选到才出场，skip 占多数属正常），0 误报。
- 构造用例 14 例全部符合预期：真值 8/5/51 → pass；跨回合重复应用 → pass（不误报）；队友极弱致全 0 → pass；只有拒马队友 → pass；同回合两次 / 小数 / 负数 / 血上限归零 / 攻归零 / 小昭妹受益 / 数值膨胀 → 全部 FAIL；无 carry 条目 / 仅无数值 buff-summary / 空战报 → skip。

### 五、体检当前状态

| 项 | 结果 |
| --- | --- |
| 规则回放自检（120 场 × **26** 条） | **0 失败**；恒 skip 由 7 条 → **6 条** |
| 本趟复活 / 新增 | 134 skip120→**pass78**；148 新增 **pass32** |
| 第 4 趟成果复核 | 143 pass120、147 pass38，均无失败 |
| 构造用例验证 | 134 八例、148 十四例，正向/负向/边界**全部符合预期** |
| `node tests/smoke-headless.mjs` | 全绿（20 局跑完、分出胜负、FACT_SPECS 74 条映射完整、播放层导出齐全） |
| `node tests/baseline-check.mjs` | 18/18 场与 `baselines/baseline-v1.json` 不同 —— 与前三趟结论一致，属历史基线过期（游戏已迭代多版），非本趟引入 |
| 语法检查 | 121/123/124/134/148 五个文件 `node --check` 通过 |
| 残留清理 | 本趟用的一次性探测脚本 `tests/_tmp-probe-carry.mjs` 已删除（未留在仓库里） |

> 当前恒 skip 6 条：`123 白骨爪回血`、`127 姐姐附身`、`129 小昭妹飞天`、`132 九阴白骨爪`、`133 死亡特效`、`141 张三丰核心机制`。

### 六、下一趟建议（交接话）

1. **恒 skip 6 条里继续挑**：上一趟已排除"Buff 未注入"这个公共根因，剩下多半是**精英随机出阵碰不到**或**数据源错位**。建议下一趟先查 **133 死亡特效** —— 它与本趟修的 134 是同款病（134 的"前排阵亡"检测 `f.isDead` 也只看顶层，若死亡条目同样是数组，133 必然空转），修法可直接复用 `switchTexts` 那套摊平函数；**132 九阴白骨爪**（明教主力技能，不该 120 场 0 命中）次之。
2. **134 顺手可再进一步**（本趟刻意没做，避免一次改两处）：它的"前排阵亡"检测 `f.isDead === true` 仍只扫顶层，同样可能漏检；漏检会让 `frontDead` 恒为 null，从而只靠"第1回合切换"这一条判据在守。改完需重跑 120 场确认不引入新误报（阵亡检测变灵敏后，"切换过晚"判据会首次真正参与）。
3. **148 的量级判据可升级**：现在的上界留了 2 倍余量，只能抓"比例/倍率被写大 10 倍"这类粗错。若想抓 2~3 倍的偏差，得先给回放器补一条"每回合单位快照"通道（现在 `ctx` 只有终局 `UI.allyTeam`），到时就能按当期属性精确重算 `Σfloor(...)`。**注意：这条属于改 runner，动静比改规则大，建议单独一趟做。**
4. 仍无覆盖的候选（都还有干净数值通道）：**嗜血狂刀吸血**（`renderBloodthirstLeechFact` 带 `leechVal`）、**热血奋战**（15% 已损失生命、每第 3 次翻倍，`renderHotBloodHealFact` 带 `leech`）、**圣火令**（随机两列 +30% 攻、两行 +30% 防，`render/30` L350 带 atkPct/defPct）、**叛逆突袭真伤 12%**、**成昆幻影伪装**。前三个同样依赖团队 Buff，回放器已能注入，直接写规则即可。
5. 工具沿用：`node tests/rules-replay.mjs`；`KEYWORDS=xxx`（统计战报文本命中数，挑新覆盖点前先数一下出场率，避免又写一条恒 skip）、`DUMP=seed:stage`、`NOBUFFS=1`、`SEEDS=`/`STAGES=`。新增规则放进 `tests/health-rules/` 会被自动装载，**但必须手工补登 `121` 的 import+数组、`123` 的 SCAN_FILES、`124` 的 RULE_META 三处**（`123` 历史上漏登过 144，别再犯）。
6. **134 有一条判据口径待复核**（第 4 趟交接话第 2 条提出，本趟刻意没动，避免一次改两处）：它仍断言「每场仅切换 1 次」，但 `modules/27elite-mingjiao.js` 现在的切换多了一道 FSM 守卫（`fsm.is('ranged')` + `state._zhangSwitched`），旧口径未必还成立。本趟 120 场实测 **0 失败**（说明当前确实每场只切一次，或该分支没被走到），但**没有真样本验证过"切两次"的情况**——下一趟若要动它，建议先构造"FSM 允许二次切换"的用例确认引擎真实行为，再决定是放宽判据还是修引擎。

> ⚠ 环境变量必须用 `KEYWORDS='甲|乙' node tests/rules-replay.mjs` 这种「环境变量在前」的写法；
>   写成 `node tests/rules-replay.mjs 'KEYWORDS=甲|乙'` 会被当成脚本参数，env 取不到（第 4 趟踩过一次，白跑一轮；本趟沿用此写法，确认有效）。


---

## 2026-09-22 00:30~01:0x（第 6 趟）

### 一、上下文继承

- 本趟开局 `git pull --rebase` 时**工作区不干净**：上一趟（第 6 趟）会话中断在工作目录里，留下 `tests/health-rules/133-death-effect.js` 的**未提交半成品**和 3 个一次性探针 `tests/_tmp-probe6*.mjs`。按红线「绝不 reset --hard 抹掉本地改动」，走的是：`rm` 探针 → `git stash push` 133 → `pull --rebase`（远端 `Already up to date`，仍是 `d12b880`）→ `stash pop` 接住半成品。**本趟的「优化」就是把这个半成品做完（补最后一条判据 + 自检 + 留痕 + 推送）**，不重复劳动。
- 接住后先跑基线自检：`node tests/rules-replay.mjs` → **133 由恒 skip120 变成 pass82 / fail11**，即数据源修复生效了、但**冒出 11 条新 fail**。这 11 条是本趟的真正工作量（见第二节），属于「规则复活后暴露的误报」，必须当场修，否则等于把体检弄坏。
- 第 5 趟交接话排第一的是「恒 skip 6 条里先查 **133 死亡特效**，它与 134 同款数据源病」。本趟照做并收尾；**134 已由第 5 趟复活，本趟未再动它**。
- 远端 `记录-更改履历.md` 最新版仍是 **V6.1.15**（精英评测撤 force），战斗数值口径与第 5 趟一致，本趟未涉及引擎数值改动。

### 二、分析结论（11 条新 fail 的定性：是误报，不是真 bug）

逐条 dump 战报（`seed=2/3/5 stage=4`）比对源码后确认，**11 条全部是同一形态**：攻击组 entries 里挂着「🐾 九阴白骨爪斩杀！…」，而攻击组自身 `isDead=false`、`hpAfter=61 alive=true`。

追到三条源码事实，结论是「设计如此」而非缺陷：

| 事实 | 出处 |
| --- | --- |
| 白骨爪斩杀走 `EFFECT_TYPES.CLAW_CHAIN`，**不是** `EXECUTE` | `core/15-skill-mechanisms.js` L373 |
| `executeKill` 只认 `EFFECT_TYPES.EXECUTE` → 白骨爪斩杀**不会**置 `attack-group.isDead` | `core/10battle-attack.js` L210-214 |
| 白骨爪斩杀有**独立**的死亡通道：`FACT_TYPES.CLAW_EXECUTE` → `kind:EXECUTE, dead:true` 的 stage action | `render/31-stage-actions.js` L117-125 |

也就是说：战士斩杀复用攻击组的 `isDead`（fx/88 L71 的 DEAD flash、player/46 L137 的战斗结束判定都靠它），白骨爪斩杀则走自己的 stage action，**两条通道互不相干**。旧版把「文本含『斩杀』」一刀切，把白骨爪也算进攻击组判据，必然天天假红。

顺带纠正一个可能的误判：`applyHolyFlame_Brother`（小昭·弟版圣火令）无条件全加 atk/def、不看 cols/rows，与 `_Normal` 的按列/按行判定不一致 —— 看着像 bug，但那是**角色专属实现**，体检不该断言，本趟未动。

### 三、优化：133-死亡特效缺失（把路径1 的判据收窄到「战士斩杀」，消除 11 场误报）

- 文件：`tests/health-rules/133-death-effect.js`（VER 保持 V6.1.15）。**只改路径1 的匹配关键字这一处**，路径2/路径3 与数据源扫描（上一趟半成品已改好）一字未动。
- 改动：路径1 的两处 `en.text.indexOf('斩杀')` → `indexOf('战士斩杀')`，并在注释里写清「白骨爪斩杀为什么不能算进来」（走 CLAW_CHAIN + 独立 CLAW_EXECUTE stage action，混判必误报）。
- 效果：**pass82/fail11/skip27 → pass93/fail0/skip27**，恒 skip 由 6 条 → **5 条**（133 彻底复活且 0 误报）。
- 构造用例 **11 例**：战士斩杀 + isDead=false → fail（仍抓得住）、战士斩杀 + isDead=true → pass、**白骨爪斩杀 + 攻击组 isDead=false → pass（旧版会假红，本趟修掉）**、白骨爪缺 isExecute / 缺 clawTargetUid → fail、闪避反击 isDead 与 hpAfter 四种组合 → 全部符合预期。
- 一处刻意保留的行为（不是缺陷）：闪避反击**未致死**时 `saw` 不置 true → 该场仍记 skip。口径是「saw = 本场真有致死事件发生、这条防线才算验证过」，未致死的闪避不算验证。用例里我一开始期望 pass，核对后确认 skip 才是设计意图，故**没有**为凑 pass 去改规则（避免一次改两处）。

### 四、新增：149-圣火令行列选取（此前零覆盖）

- 文件：`tests/health-rules/149-holy-flame-cols-rows.js`（rule96），已接入 `121` import+规则数组、`123` SCAN_FILES、`124` RULE_META（tag `mechanic:圣火令`）。
- 覆盖机制：`core/11battle-round.js` L86-103 每回合重掷 cols/rows（`holyColCount` = 小昭·姊在场 ? 强化值 : **1**；`holyRowCount` = 强化值 : **2**；`rng.nextInt(1,3)` + `!includes` 去重 + `sort()` 升序）→ `core/14buff-effects.js` L27-48 按列 +30% 攻、按行 +30% 防 → `render/30` L339-354 渲染 buff-summary「🔥 圣火令：攻击第X列 +30%，防御第Y行 +30%」。
- 六条复发信号：① 列/行号越界（不在 1~3，rng 边界写反 → 命中不到任何单位，**静默失效**）；② 数组内重复（去重守卫被删 → 文案显示两列实际只加成一列，**静默缩水**）；③ 未升序（`sort()` 被删）；④ 百分比与 `CONFIG.BUFFS.holyFlame.*Bonus` 不符（渲染被写死 / 配置未收口，V6.1.3 类病灶复发）；⑤ cols 与 rows 同时为空（渲染守卫失效 → 打出「攻击无 +30%」的空转条目）；⑥ 同回合 ≥3 条摘要（重复登记回归，`core/11` V6.1.0 注释里修过）。
- 误报规避（关键取舍）：**刻意不反推受益单位** —— 战报不带当期站位快照，硬凑必误报，只选"文本自身能定性"的结构判据；配置现读且包 try/catch，读不到就只跳过判据④；文案形态变了匹配不上就 skip，绝不硬报；判据⑥门槛取 3（实测 120 场每个回合恒 1 条），只当回归哨兵，不会误伤双阵营同时持 Buff 的情况。
- 实测：**pass 55 / skip 65**（圣火令是团队 Buff，需本局选到才出场），0 误报；构造用例 **18 例全部符合预期**（正常单列/两列/三列/跨回合/只有列/边界 1 与 3 → pass；越界 0 与 4、列重复、行重复、乱序、攻+40%、防+50%、行列皆空、同回合三条 → 全部 fail；无条目、只有「圣火令掉落」消息、空战报 → skip）。

### 五、体检当前状态

| 项 | 结果 |
| --- | --- |
| 规则回放自检（120 场 × **27** 条） | **0 失败**；恒 skip 由 6 条 → **5 条** |
| 133（本趟优化） | **pass93 / fail0 / skip27**（原恒 skip120 → 修数据源后 pass82/fail11 → 收窄判据后 pass93/fail0） |
| 149（本趟新增） | **pass55 / skip65** |
| 其余 25 条 | pass/fail/skip 与本趟改动前**逐条一致**（无回归） |
| 构造用例 | 133 十一例（10 例符合 + 1 例口径澄清）、149 十八例，正/负/边界**全部符合预期** |
| `node tests/smoke-headless.mjs` | 全绿（gameData 20 明教/10 角色、FACT_SPECS 74 条、20 局跑完并分出胜负、播放层导出齐全） |
| 静态结构快检（node 等效版） | SCAN_FILES **95** 个文件缺失 **0**；规则目录 27 个文件**全部**登记进 `123`、全部被 `121` import、全部在 `124` RULE_META 有条目（三处补登无遗漏） |
| `node tests/baseline-check.mjs` | 18/18 场与 `baselines/baseline-v1.json` 不同 —— 与前 4 趟结论一致，属历史基线过期（游戏已迭代多版），本趟只动 tests/ 规则，不可能影响战斗结果 |
| 语法检查 | 133/149/121/123/124 五个文件 `node --check` 通过 |

> 当前恒 skip 5 条：`123 白骨爪回血`、`127 姐姐附身`、`129 小昭妹飞天`、`132 九阴白骨爪`、`141 张三丰核心机制`。

### 六、下一趟建议（交接话）

1. **仍是最高优先：`tests/rules-replay.mjs` 第 131 行不摊平渲染数组**（第 4 趟实锤的根因，连续三趟都在交接话里排第一，至今没人做）。改法一行：`const r = renderLog(...); if (Array.isArray(r)) log.push(...r); else if (r) log.push(r);`。**改完必须全量重跑 120 场回归**（所有规则会看到更多条目，可能集体翻红），出现新 fail 先按本趟第二节的办法定性：dump 战报 + 追源码，分清"真 bug"还是"规则过时"，别急着改引擎。
2. **恒 skip 5 条继续挑**：本趟确认 133 的根因是"数据源 + 判据双过时"，剩下 5 条大概率同类。建议下一趟查 **132 九阴白骨爪伤害**（明教主力技能，不该 120 场 0 命中；它与本趟刚理清的 CLAW_CHAIN 链路同源，可复用本趟第二节的三条源码事实）。**123 白骨爪回血**次之，注意它可能是 `122health-utils.js V6.1.14` 那个"长名单位放过"的取舍连带导致，别急着加断言。
3. **149 可升级方向**：现在只校验 cols/rows 的"结构不变量"，没校验"**列数是否等于应有的 holyColCount**"（基础 1 列、小昭·姊在场按 `小昭.hexEnhance.holyFlame.atkCols`）。要做的话得把 `ctx.activeBuffs` 或快照里"小昭·姊是否在场"接进规则——**注意 `ctx` 现在只有终局 `UI.allyTeam`，且拒马/阵亡单位混在里面**，先确认口径再动手，否则一定误报。
4. 仍无覆盖的候选（按本趟实测战报顶层命中数排）：**性奋 186**（`renderXingFenGrantFact`，宋青书受周芷若激励、本回合每次攻击后可再攻击 —— 与 144 新婚快乐是两条不同链路）、**热血奋战 145**（15% 已损失生命、每第 3 次翻倍，`renderHotBloodHealFact` 带数值）、**嗜血狂刀吸血 55**（`renderBloodthirstLeechFact` 带 `leechVal`）。性奋样本最大但要注意它和 144 的边界别写重。
5. **V6.1.13「计算行取整口径对齐伤害行」依旧没人盯**，但别直接拿"计算行 vs 造成行"做等值断言 —— 实测两者会被乾坤大挪移减伤、坚盾等加/减伤层合法拉开。要做的话得先把"同一攻击组内的倍率/减伤项"从 entries 里解析出来再相减。
6. 工具沿用：`node tests/rules-replay.mjs`；`KEYWORDS='甲|乙'`（**环境变量必须写在 node 命令前**）、`DUMP=seed:stage`、`NOBUFFS=1`、`SEEDS=`/`STAGES=`。新增规则必须手工补登 `121` import+数组、`123` SCAN_FILES、`124` RULE_META 三处（本趟用 `/tmp` 下的脚本做了三处补登自动核对，建议沿用；一次性脚本别留在 `tests/` 里）。

> ⚠ 本趟开局遇到"工作区不干净导致 `pull --rebase` 失败"。正确姿势是 `git stash push <文件>` → `pull --rebase` → `stash pop`，**不要用 `reset --hard`**（红线：保留本地未推送的提交）。另外日志是多会话共享文件，写完先 `pull --rebase` 再提交。
