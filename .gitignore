# Trae IDE 分享预览自动生成的 zip 包，不入库
.trae-html-share-packages/

# 文档（本地可见，不入库不推送）
/文件汇总20260730/

# DSH 技能说明书（本地可见，不入库不推送）
/.dsh/

# 展示与CG（独立演示页与立绘素材，本地可见，不入库不推送）
/展示与CG/

# APK 打包产物（由 GitHub Actions 生成，不入库）
web/
android/
node_modules/
package-lock.json
