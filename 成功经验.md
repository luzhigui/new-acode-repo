# 成功经验 - 文件复制器序列发送优化

## 问题描述

文件复制器点击"序列发送"后，弹窗显示的包序号始终比当前复制的包多一个（显示的是下一个包的信息）。

## 根因分析

`nextBatch()` 函数逻辑顺序错误：

```
复制当前包 → sendIndex++ → 显示弹窗  // 错误：显示的是 sendIndex++ 后的下一个包
```

## 正确方案

### 优化一：显示当前操作内容，而非下一个

复制后先显示"已复制当前包"，再 ++ 切换到下一包，等待下一次操作：

```
复制当前包 → 显示"已复制当前包" → sendIndex++ → 延迟 0.8s → 显示下一包等待复制
```

### 优化二：一键开始，立即复制第一个包

点击"序列发送"后，不要等用户再按一次 Enter，**直接复制第一个包并显示"已复制"**。后续每个包按 Enter 复制。

```
点击序列发送 → 复制第 1 包 → 显示"✅ 已复制 全局 1/22 ..." → 按 Enter → 复制第 2 包
```

**关键点**：`renderSenderBar(sendIndex, 'copied')` 中，`sendIndex` 必须在复制后立即显示，不要 ++ 后再显示，否则面板会显示下一个包，导致用户困惑。

## 关键代码

### renderSenderBar 新增 status 参数

```javascript
function renderSenderBar(showIndex, status) {
    // ...
    if (status === 'copied') {
        // 绿色提示"已复制"，不含按钮，不响应 Enter
        senderBar.innerHTML = `✅ 已复制 全局 ${displayIndex}/${totalBatches} · 【${gn}】 组内 ${groupBatchIndex}/${groupBatchTotal}，准备下一包...`;
    } else {
        // 等待状态：显示包信息 + 按钮，响应 Enter
        senderBar.innerHTML = `📦 全局 ${displayIndex}/${totalBatches} · 【${gn}】 组内 ${groupBatchIndex}/${groupBatchTotal}（${chars} 字符）→ 按 Enter 复制当前包`;
    }
}
```

### nextBatch 先显示已复制，再切换

```javascript
async function nextBatch() {
    const fullText = getBatchText(sendBatches[sendIndex]);
    await navigator.clipboard.writeText(fullText);
    renderSenderBar(sendIndex, 'copied');  // 先显示已复制
    sendIndex++;                            // 再切换
    setTimeout(() => {
        renderSenderBar(sendIndex);         // 延迟后显示下一包
    }, 800);
}
```

### 点击事件：直接复制第一个包

```javascript
sendBtn.addEventListener('click', () => {
    sendBatches = Array.from(cards); sendIndex = 0; sendCancelled = false;
    addSenderKeyListener();
    setTimeout(() => nextBatch(), 100); // 直接复制第一个包，无需用户再点一次
});
```

## 核心原则

**弹窗显示的是用户当前正在操作的内容，不是下一个要操作的内容。** 任何时候，UI 展示的信息必须与用户当前的操作状态一致。
*******************************************************************************
## 格子飞撞（`showMeleeCrash`）修复后完整逻辑

---

### 一、飞撞动画流程（不变）

飞撞动画本身没有改动，仍然是三阶段：

```
蓄力放大 → 克隆体飞出撞击 → 弹回原位消失
 800ms       900ms            800ms
```

总时长 = `2500ms × speedFactor`

两个模式通过 `window._crashMode` 切换：
- `'ghost'`（虚影）：原地留半透明蓝色虚影
- `'fly'`（飞走）：原地完全空

---

### 二、修复后的数据流（与旧框架的核心差异）

**旧框架**：特效文件直接操作 DOM（`cellA.style.visibility = 'hidden'`、`cellA.style.opacity = '0.5'`），依赖时序控制——飞撞动画必须在两次 `updateUI` 之间完整跑完。一旦播放器的 `atkTimer` 在动画期间触发 `updateUI`，格子就被重绘出来，造成残留。

**新框架**：不再依赖 DOM 操作隐藏格子，改为**数据驱动渲染**。

具体流程：

1. **飞撞开始**：`showMeleeCrash` 通过 Store dispatch `SET_VISUAL`，给单位设置 `_flyMode = 'ghost'` 或 `'fly'`，同时设 `_acted = true`。然后调 `updateUI` 触发一次重绘。

2. **重绘阶段**：`renderGrid` 遇到 `unit._flyMode` 不为空，**跳过正常渲染**，直接画占位格：
   - 飞走模式：空白 `<div>`，无任何内容
   - 虚影模式：带角色名、图标、攻防血的半透明蓝色格子（`opacity: 0.5`）

3. **飞撞动画期间**：克隆体在 `body` 上飞行，与 grid 内的占位格互不干扰。**无论播放器何时调用 `updateUI`，`renderGrid` 始终读到 `_flyMode`，始终画占位格，不会出现残留。**

4. **飞撞结束**：`finishCrash` 执行：
   - 移除克隆体
   - Dispatch `SET_VISUAL`：清空 `_flyMode`，设 `_acted = true`
   - Dispatch `SET_FLASH`：重新设 `flash = 'attack'`
   - 调 `updateUI` 触发重绘，格子恢复正常蓝色闪光

5. **蓝色闪光保留**：播放器 `atkTimer` 到时间后正常清除 `_flash`，格子变回普通状态。

---

### 三、播放器侧的保护

播放器 `handleAttackGroup` 末尾有两处同步清除闪光的代码，都加了 `_flyMode` 检查：

- 兜底清除闪光：`if (!su || !su._flyMode)` 才执行 `CLEAR_UNIT_FLASH`
- 遮挡状态遍历：飞走单位跳过 `SET_VISUAL` dispatch

这样飞走期间播放器的同步代码不会误清闪光或触发额外重绘。

---

### 四、与旧框架的不一致点总结

| | 旧框架 | 新框架 |
|---|---|---|
| 隐藏方式 | CSS 操作（`visibility: hidden`） | 数据标记（`_flyMode`）驱动渲染占位格 |
| 重绘风险 | 依赖时序，重绘即残留 | `renderGrid` 始终画占位格，重绘安全 |
| 闪光恢复 | `finishCrash` 不碰闪光，依赖播放器 | `finishCrash` 主动 dispatch `SET_FLASH: 'attack'` |
| 播放器保护 | 无 | 末尾同步清除加 `_flyMode` 检查 |

****************************************************************
## 闪避反击（子弹时间）修复后完整逻辑

---

### 一、触发条件与流程

闪避反击发生在**防御者成功闪避**时。播放器 `handleAttackGroup` 检测到 `entry.isDodge === true` 且开启了华丽模式（`c.dodgeEffectEnabled`），调用 `showDodgeBulletTime(defender, attacker, reboundDmg)`。

整个特效约 8-10 秒，分为六个阶段：

```
闪电(400ms) → 黑幕降临(200ms) → 屏息凝视(3000ms) → 飞撞+碰撞(4000ms) → 气泡+飞走(2000ms) → 清理恢复(200ms)
```

---

### 二、修复后的数据流

**核心原则**：所有动画都在 `cloneNode` 生成的克隆体上执行，**原始格子只做隐藏/恢复**，不参与动画。

1. **特效开始前**：
   - 保存 `aCell`（攻击者）和 `dCell`（反击者）的全部样式快照
   - 设 `ctx.isPaused = true` 暂停战斗流程

2. **黑幕降临后**：
   - 原始 `aCell` 和 `dCell` 均设 `opacity = '0'` 隐藏
   - 生成 `cloneA`（攻击者克隆体，蓝色）和 `cloneD`（反击者克隆体，金色）
   - 克隆体挂到 `body` 上，`position: fixed`，不受 grid 层叠上下文限制
   - `cloneD` 的 `z-index: 10050`，在黑幕（`9999`）之上

3. **动画阶段**：
   - 所有飞行、碰撞、震退动画都在克隆体上执行
   - 原始格子始终保持隐藏状态

4. **cleanup 恢复**：
   - 移除所有克隆体和临时元素
   - 调用 `restoreCellStyles` 将原始格子的全部样式恢复
   - 通过 Store dispatch `CLEAR_UNIT_FLASH` 清除双方闪光
   - 调 `ctx.updateUI()` 触发重绘，格子恢复正常

---

### 三、与旧框架的不一致点

| | 旧框架 | 新框架 |
|---|---|---|
| **格子高亮方式** | 直接修改 `dCell` 内联样式，设 `position: fixed` 但仍是 grid 子节点 | 隐藏原始格子，生成独立克隆体挂到 `body`，完全脱离 grid 层叠上下文 |
| **反击者可见性** | 被黑幕压制，用户反映"看不清" | `cloneD` 设 `z-index: 10050` 浮在黑幕（`9999`）之上，清晰可见 |
| **闪光颜色分配** | 播放器把 `unitA`（反击者）误设为蓝色 `attack` | 修复为：闪避事件中 `unitA` 设黄色 `defend`，`unitD` 设蓝色 `attack` |
| **恢复机制** | 直接调 `dCell.style.xxx = ''` 逐个清空，容易遗漏 | `saveCellStyles` / `restoreCellStyles` 完整保存和恢复全部样式 |
| **图层管理** | `dCell` 是 grid 子元素，`z-index` 被 grid 层叠上下文锁死 | 克隆体直接挂 `body`，`z-index` 自由控制 |
| **风暴圈位置** | 偏矮，被"开打开打"气泡遮挡 | 从 `y - 60` 调整为 `y - 80`，位于气泡和格子之间 |
| **cleanup 安全性** | `dCell` 在 `try` 内声明，`cleanup` 函数访问时报 `ReferenceError` | `dCell` 提到 `try` 外声明为 `let dCell = null`，`cleanup` 安全访问 |

---

### 四、一句话总结

**旧框架**：直接操作原始格子 DOM，被 grid 层叠上下文限制，反击者看不清。  
**新框架**：隐藏原始格子，生成独立克隆体到 `body` 做动画，恢复时完整还原样式，反击者清晰可见。
****************************************************************************
## 弹幕/特效消失问题

### 问题现象

第一关正常，第二关及之后所有弹幕（嘲讽台词）、伤害飘字、治疗数字、闪避气泡全部不显示。

### 根源

项目使用对象池机制复用 DOM 元素。弹幕、飘字等元素在创建时被标记了 `data-fx="temporary"` 属性：

```javascript
// 四个对象池创建函数，每个都加了这行
b.setAttribute('data-fx', 'temporary');
```

关卡切换时调用 `clearAllEffects`，它会删除页面上所有带 `[data-fx="temporary"]` 属性的元素。对象池中的弹幕、飘字元素就这样被从 DOM 中移除了。后续 `acquireFromPool` 虽然能拿到 JS 引用，但元素已脱离 DOM，设置 `display` 和定位都不可见。第一关正常只是因为那时对象池刚初始化，还没被清过。

### 修复

去掉四个对象池创建函数中的 `setAttribute('data-fx', 'temporary')`：

| 函数 | 池类型 | 用途 |
|------|--------|------|
| `createDanmakuEl` | danmaku | 弹幕气泡 |
| `createDmgFloatEl` | dmgFloat | 伤害飘字 |
| `createHealFloatEl` | healFloat | 治疗数字 |
| `createDodgeBubbleEl` | dodge | 闪避气泡 |

这些对象池元素是长期复用的，不应被 `clearAllEffects` 清除。

---

## 倍速按钮逻辑

### 问题现象

- 点击高亮的 2x 按钮无法取消高亮，恢复常速
- 点击 7x 高亮后滚动日志，恢复时变成 2x
- 日志自然滚动也会清除倍速高亮

### 根源

`44ui-controls.js` 和 `39main-state.js` 各自维护了一套同名变量：

```
44ui-controls.js:        39main-state.js:
let manualSpeedLock      export let manualSpeedLock
let manualSpeedValue     export let manualSpeedValue
let slideSpeedActive     export let slideSpeedActive
```

两个文件互不通信。44 改了 `manualSpeedLock = true`，39 里的同名变量纹丝不动。`updateSpeedButtons` 通过 `getState.speed()` 读速度值（来自 39），但如果播放器或其他模块通过 39 的 setter 改了 `speed`，44 里保存的 `manualSpeedValue` 就和新速度不匹配，高亮按钮跳到错误的位置。

### 修复路径（最终方案）

**第一步**：把 39 里的倍速相关变量删掉（`manualSpeedLock`、`manualSpeedValue`、`slideSpeedActive`），所有状态统一在 44 中维护。

**第二步**：在 44 中实现完整的倍速逻辑：

**变量设计**：

| 变量 | 作用 |
|------|------|
| `manualSpeedLock` | 是否有手动锁定的倍速 |
| `manualSpeedValue` | 锁定倍速的数值（500/143/250/1800） |
| `slideSpeedActive` | 是否正常播放（`false` = 滚动慢放中） |
| `preScrollSpeed` | 滚动前保存的速度值 |
| `preManualSpeedLock` | 滚动前保存的锁状态 |
| `preManualSpeedValue` | 滚动前保存的锁数值 |

**状态机逻辑**：

```
用户点击倍速按钮：
  ├─ 已高亮 → 取消锁定，恢复 speed=500，所有按钮不高亮
  └─ 未高亮 → 锁定新速度，该按钮高亮

用户上滑日志（触发慢放）：
  └─ 保存当前锁状态和速度 → 0.5x 高亮 + 原倍速半高亮

用户回到底部（退出慢放）：
  ├─ 有手动锁 → 恢复原倍速，该按钮重新高亮
  └─ 无手动锁 → 恢复常速 500，所有按钮不高亮

用户点右下角箭头（回到底部）：
  └─ 同上

日志自然滚动到底部（非用户操作）：
  └─ 不触发任何倍速变化
```

**`restoreSpeedFromScroll` 的关键修复**：

```javascript
// 如果 slideSpeedActive 已经是 true，说明没有进入过慢放状态，不需要恢复
if (slideSpeedActive) return;
```

这一行防止了日志自然滚动时误触恢复逻辑，导致高亮消失。
******************************************
## 闪避反击（子弹时间）修复后完整逻辑

---

### 一、触发条件与流程

闪避反击发生在**防御者成功闪避**时。播放器 `handleAttackGroup` 检测到 `entry.isDodge === true` 且开启了华丽模式（`c.dodgeEffectEnabled`），调用 `showDodgeBulletTime(defender, attacker, reboundDmg)`。

整个特效约 8-10 秒，分为六个阶段：

```
闪电(400ms) → 黑幕降临(200ms) → 屏息凝视(3000ms) → 飞撞+碰撞(4000ms) → 气泡+飞走(2000ms) → 清理恢复(200ms)
```

---

### 二、修复后的数据流

**核心原则**：所有动画都在 `cloneNode` 生成的克隆体上执行，**原始格子只做隐藏/恢复**，不参与动画。

1. **特效开始前**：
   - 保存 `aCell`（攻击者）和 `dCell`（反击者）的全部样式快照
   - 设 `ctx.isPaused = true` 暂停战斗流程

2. **黑幕降临后**：
   - 原始 `aCell` 和 `dCell` 均设 `opacity = '0'` 隐藏
   - 生成 `cloneA`（攻击者克隆体，蓝色）和 `cloneD`（反击者克隆体，金色）
   - 克隆体挂到 `body` 上，`position: fixed`，不受 grid 层叠上下文限制
   - `cloneD` 的 `z-index: 10050`，在黑幕（`9999`）之上

3. **动画阶段**：
   - 所有飞行、碰撞、震退动画都在克隆体上执行
   - 原始格子始终保持隐藏状态

4. **cleanup 恢复**：
   - 移除所有克隆体和临时元素
   - 调用 `restoreCellStyles` 将原始格子的全部样式恢复
   - 通过 Store dispatch `CLEAR_UNIT_FLASH` 清除双方闪光
   - 调 `ctx.updateUI()` 触发重绘，格子恢复正常

---

### 三、与旧框架的不一致点

| | 旧框架 | 新框架 |
|---|---|---|
| **格子高亮方式** | 直接修改 `dCell` 内联样式，设 `position: fixed` 但仍是 grid 子节点 | 隐藏原始格子，生成独立克隆体挂到 `body`，完全脱离 grid 层叠上下文 |
| **反击者可见性** | 被黑幕压制，用户反映"看不清" | `cloneD` 设 `z-index: 10050` 浮在黑幕（`9999`）之上，清晰可见 |
| **闪光颜色分配** | 播放器把 `unitA`（反击者）误设为蓝色 `attack` | 修复为：闪避事件中 `unitA` 设黄色 `defend`，`unitD` 设蓝色 `attack` |
| **恢复机制** | 直接调 `dCell.style.xxx = ''` 逐个清空，容易遗漏 | `saveCellStyles` / `restoreCellStyles` 完整保存和恢复全部样式 |
| **图层管理** | `dCell` 是 grid 子元素，`z-index` 被 grid 层叠上下文锁死 | 克隆体直接挂 `body`，`z-index` 自由控制 |
| **风暴圈位置** | 偏矮，被"开打开打"气泡遮挡 | 从 `y - 60` 调整为 `y - 80`，位于气泡和格子之间 |
| **cleanup 安全性** | `dCell` 在 `try` 内声明，`cleanup` 函数访问时报 `ReferenceError` | `dCell` 提到 `try` 外声明为 `let dCell = null`，`cleanup` 安全访问 |

---

### 四、一句话总结

**旧框架**：直接操作原始格子 DOM，被 grid 层叠上下文限制，反击者看不清。  
**新框架**：隐藏原始格子，生成独立克隆体到 `body` 做动画，恢复时完整还原样式，反击者清晰可见。
*******************************************************
## 格子飞撞（`showMeleeCrash`）修复后完整逻辑

---

### 一、飞撞动画流程（不变）

飞撞动画本身没有改动，仍然是三阶段：

```
蓄力放大 → 克隆体飞出撞击 → 弹回原位消失
 800ms       900ms            800ms
```

总时长 = `2500ms × speedFactor`

两个模式通过 `window._crashMode` 切换：
- `'ghost'`（虚影）：原地留半透明蓝色虚影
- `'fly'`（飞走）：原地完全空

---

### 二、修复后的数据流（与旧框架的核心差异）

**旧框架**：特效文件直接操作 DOM（`cellA.style.visibility = 'hidden'`、`cellA.style.opacity = '0.5'`），依赖时序控制——飞撞动画必须在两次 `updateUI` 之间完整跑完。一旦播放器的 `atkTimer` 在动画期间触发 `updateUI`，格子就被重绘出来，造成残留。

**新框架**：不再依赖 DOM 操作隐藏格子，改为**数据驱动渲染**。

具体流程：

1. **飞撞开始**：`showMeleeCrash` 通过 Store dispatch `SET_VISUAL`，给单位设置 `_flyMode = 'ghost'` 或 `'fly'`，同时设 `_acted = true`。然后调 `updateUI` 触发一次重绘。

2. **重绘阶段**：`renderGrid` 遇到 `unit._flyMode` 不为空，**跳过正常渲染**，直接画占位格：
   - 飞走模式：空白 `<div>`，无任何内容
   - 虚影模式：带角色名、图标、攻防血的半透明蓝色格子（`opacity: 0.5`）

3. **飞撞动画期间**：克隆体在 `body` 上飞行，与 grid 内的占位格互不干扰。**无论播放器何时调用 `updateUI`，`renderGrid` 始终读到 `_flyMode`，始终画占位格，不会出现残留。**

4. **飞撞结束**：`finishCrash` 执行：
   - 移除克隆体
   - Dispatch `SET_VISUAL`：清空 `_flyMode`，设 `_acted = true`
   - Dispatch `SET_FLASH`：重新设 `flash = 'attack'`
   - 调 `updateUI` 触发重绘，格子恢复正常蓝色闪光

5. **蓝色闪光保留**：播放器 `atkTimer` 到时间后正常清除 `_flash`，格子变回普通状态。

---

### 三、播放器侧的保护

播放器 `handleAttackGroup` 末尾有两处同步清除闪光的代码，都加了 `_flyMode` 检查：

- 兜底清除闪光：`if (!su || !su._flyMode)` 才执行 `CLEAR_UNIT_FLASH`
- 遮挡状态遍历：飞走单位跳过 `SET_VISUAL` dispatch

这样飞走期间播放器的同步代码不会误清闪光或触发额外重绘。

---

### 四、与旧框架的不一致点总结

| | 旧框架 | 新框架 |
|---|---|---|
| 隐藏方式 | CSS 操作（`visibility: hidden`） | 数据标记（`_flyMode`）驱动渲染占位格 |
| 重绘风险 | 依赖时序，重绘即残留 | `renderGrid` 始终画占位格，重绘安全 |
| 闪光恢复 | `finishCrash` 不碰闪光，依赖播放器 | `finishCrash` 主动 dispatch `SET_FLASH: 'attack'` |
| 播放器保护 | 无 | 末尾同步清除加 `_flyMode` 检查 |

************************************************************
这是乘风突袭击退换位特效的完整演示代码，一个可直接运行的HTML文件，展示了前排单位被击退后挤开后方单位并完成换位的完整动画效果。

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
  <title>光明顶 · 击退换位特效实验室 (挤开旋转版)</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      background: #f5f0e0; font-family: 'Segoe UI', monospace;
      display: flex; flex-direction: column; height: 100dvh; width: 100vw;
      overflow: hidden; padding: 4px 6px; color: #333; user-select: none;
    }
    .header { display: flex; justify-content: space-between; align-items: center; flex-shrink: 0; height: 5dvh; }
    h2 { color: #b8860b; font-size: 1rem; }

    .upper-area { flex: 0 0 auto; display: flex; flex-direction: column; margin-top: 2px; }
    .battlefield { display: flex; flex-direction: column; justify-content: flex-start; gap: 2px; }
    .team-row { display: flex; align-items: center; gap: 4px; max-width: 500px; margin: 0 auto; width: 100%; }
    .team-label { writing-mode: vertical-lr; font-weight: bold; font-size: 13px; width: 18px; text-align: center; }

    .grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      grid-template-rows: repeat(3, 5.5dvh);
      gap: 2px;
      flex: 1;
      background: #d9d0b5;
      padding: 3px;
      border-radius: 6px;
    }
    .cell {
      background: #e8e6e0;
      border: 2px solid #bbb;
      border-radius: 5px;
      padding: 2px 4px;
      display: flex;
      align-items: center;
      gap: 4px;
      font-size: 9px;
      height: 5.5dvh;
      position: relative;
      cursor: pointer;
    }
    .cell-icon { font-size: 13px; width: 14px; text-align: center; flex-shrink: 0; }
    .cell-info { flex: 1; display: flex; flex-direction: column; justify-content: center; overflow: hidden; }
    .cell-name { font-weight: bold; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; font-size: 9px; }
    .cell-stats { font-size: 7px; color: #555; display: flex; gap: 2px; }
    .hp-bar-wrap { width: 8px; height: 100%; background: #ccc; border-radius: 3px; flex-shrink: 0; align-self: stretch; position: relative; }
    .hp-bar-inner { position: absolute; bottom: 0; width: 100%; border-radius: 3px; transition: height 0.6s ease, background 0.6s ease; }

    .cell.front-unit { border-color: #ff4500 !important; box-shadow: 0 0 12px rgba(255,69,0,0.6); }
    .cell.rear-unit { border-color: #ffd700 !important; box-shadow: 0 0 12px #ffd700; }

    .controls {
      flex: 0 0 auto; background: #f0ead6; border-radius: 8px; padding: 8px; margin: 4px 0;
      max-width: 500px; width: 100%; align-self: center; display: flex; flex-direction: column; gap: 6px;
    }
    .button-row { display: flex; gap: 6px; justify-content: center; flex-wrap: wrap; }
    button {
      padding: 6px 14px; border-radius: 6px; border: 1px solid #8b7355;
      background: #e8d5b0; color: #5c4033; font-weight: bold; cursor: pointer; font-size: 10px;
      transition: all 0.2s;
    }
    button:hover { filter: brightness(0.95); }
    button:disabled { opacity: 0.4; cursor: not-allowed; }
    button.primary { background: #d2691e; color: white; border-color: #a0522d; }
    .info-text { font-size: 9px; color: #666; text-align: center; margin-top: 4px; }
    .log { margin-top: 4px; font-size: 9px; color: #333; text-align: center; }
  </style>
</head>
<body>
  <div class="header"><h2>⚔️ 击退换位特效实验室 (自动判定+旋转挤开)</h2></div>
  <div class="upper-area">
    <div class="battlefield">
      <div class="team-row"><span class="team-label orange">六大派</span><div class="grid" id="enemyGrid"></div></div>
      <div class="team-row"><span class="team-label blue">明 教</span><div class="grid" id="allyGrid"></div></div>
    </div>
    <div class="controls">
      <div class="button-row">
        <button id="pushAllyBtn">💥 明教击退换位</button>
        <button id="pushEnemyBtn">💥 六大派击退换位</button>
        <button id="resetBtn">🔄 重置</button>
      </div>
      <div class="info-text" id="statusText">👆 点击明教/六大派按钮，自动找一组可击退换位的单位</div>
      <div class="log" id="logText"></div>
    </div>
  </div>

  <script>
    (function() {
      // ==================== 单位数据 ====================
      const allyTeam = [
        { name: '张无忌', uid: 'a1', camp: 'ally', role: '战士', pos: 5, alive: true, atk: 50, def: 30, maxHp: 300, hp: 280, isZhang: true, isWei: false },
        { name: '韦一笑', uid: 'a2', camp: 'ally', role: '飞行', pos: 1, alive: true, atk: 35, def: 20, maxHp: 220, hp: 200, isZhang: false, isWei: true },
        { name: '杨逍', uid: 'a3', camp: 'ally', role: '远程', pos: 4, alive: true, atk: 30, def: 18, maxHp: 180, hp: 160, isZhang: false, isWei: false },
        { name: '范遥', uid: 'a4', camp: 'ally', role: '防战', pos: 7, alive: true, atk: 28, def: 25, maxHp: 200, hp: 190, isZhang: false, isWei: false },
        { name: '周颠', uid: 'a5', camp: 'ally', role: '防战', pos: 3, alive: true, atk: 25, def: 30, maxHp: 220, hp: 210, isZhang: false, isWei: false }
      ];
      const enemyTeam = [
        { name: '少林方丈', uid: 'e1', camp: 'enemy', role: '战士', pos: 8, alive: true, atk: 40, def: 25, maxHp: 250, hp: 230, isZhang: false, isWei: false },
        { name: '武当掌门', uid: 'e2', camp: 'enemy', role: '远程', pos: 9, alive: true, atk: 45, def: 15, maxHp: 200, hp: 180, isZhang: false, isWei: false },
        { name: '峨眉剑客', uid: 'e3', camp: 'enemy', role: '防战', pos: 6, alive: true, atk: 30, def: 35, maxHp: 280, hp: 260, isZhang: false, isWei: false },
        { name: '昆仑长老', uid: 'e4', camp: 'enemy', role: '飞行', pos: 4, alive: true, atk: 32, def: 20, maxHp: 210, hp: 190, isZhang: false, isWei: false },
        { name: '华山剑客', uid: 'e5', camp: 'enemy', role: '战士', pos: 2, alive: true, atk: 35, def: 22, maxHp: 230, hp: 200, isZhang: false, isWei: false }
      ];

      let isAnimating = false;
      let highlightedFront = null;
      let highlightedRear = null;

      // ==================== 工具函数 ====================
      function getCellElement(unit) {
        if (!unit || unit.pos == null) return null;
        const grid = document.getElementById(unit.camp === 'ally' ? 'allyGrid' : 'enemyGrid');
        if (!grid) return null;
        const order = unit.camp === 'enemy' ? [7,8,9,4,5,6,1,2,3] : [1,2,3,4,5,6,7,8,9];
        const idx = order.indexOf(unit.pos);
        return idx >= 0 ? grid.children[idx] : null;
      }

      function wait(ms) { return new Promise(r => setTimeout(r, ms)); }

      // 获取某单位正后方的同阵营单位（基于阵营击退方向）
      function getRearUnit(unit, team) {
        const direction = unit.camp === 'ally' ? 1 : -1; // ally向下(+3)，enemy向上(-3)
        const rearPos = unit.pos + direction * 3; // 同一列
        if (rearPos < 1 || rearPos > 9) return null;
        return team.find(u => u.pos === rearPos && u.alive && !u.isZhang && !u.isWei);
      }

      // 查找可触发击退换位的第一组单位
      function findPushPair(team) {
        for (const unit of team) {
          if (!unit.alive) continue;
          const rear = getRearUnit(unit, team);
          if (rear) return { front: unit, rear };
        }
        return null;
      }

      // ==================== 渲染函数 ====================
      function renderGrid(gridId, camp) {
        const grid = document.getElementById(gridId);
        grid.innerHTML = '';
        const displayOrder = camp === 'enemy' ? [7,8,9,4,5,6,1,2,3] : [1,2,3,4,5,6,7,8,9];
        const team = camp === 'ally' ? allyTeam : enemyTeam;

        for (let pos of displayOrder) {
          const unit = team.find(u => u.pos === pos && u.alive);
          const div = document.createElement('div');
          div.className = 'cell';
          div.dataset.camp = camp;
          div.dataset.pos = pos;

          if (highlightedFront && highlightedFront.uid === unit?.uid) div.classList.add('front-unit');
          if (highlightedRear && highlightedRear.uid === unit?.uid) div.classList.add('rear-unit');

          if (unit) {
            const roleIcon = unit.role === '战士' ? '⚔️' : (unit.role === '防战' ? '🛡️' : (unit.role === '远程' ? '🏹' : '🦅'));
            const hpPct = unit.alive ? (unit.hp / unit.maxHp) * 100 : 0;
            const barColor = hpPct > 70 ? '#4caf50' : (hpPct > 40 ? '#ff9800' : '#f44336');
            div.innerHTML = `
              <span class="cell-icon">${roleIcon}</span>
              <div class="cell-info">
                <span class="cell-name ${unit.isZhang ? 'gold' : ''}">${unit.name}</span>
                <span class="cell-stats">攻${Math.floor(unit.atk)} 防${Math.floor(unit.def)} 血${Math.floor(unit.hp)}</span>
              </div>
              <div class="hp-bar-wrap"><div class="hp-bar-inner" style="height:${hpPct}%;background:${barColor};"></div></div>
            `;
          } else {
            div.innerHTML = '<span style="color:#999;font-size:9px;">空</span>';
          }
          grid.appendChild(div);
        }
      }

      function renderAll() { renderGrid('allyGrid', 'ally'); renderGrid('enemyGrid', 'enemy'); }

      // ==================== 击退换位动画（旋转挤开版） ====================
      async function animatePushSwap(frontUnit, rearUnit) {
        const cellF = getCellElement(frontUnit);
        const cellR = getCellElement(rearUnit);
        if (!cellF || !cellR) return;

        isAnimating = true;
        document.getElementById('logText').textContent = `💥 ${frontUnit.name} 被击退，挤开 ${rearUnit.name}！`;

        const posF = frontUnit.pos, posR = rearUnit.pos;
        const rectF = cellF.getBoundingClientRect();
        const rectR = cellR.getBoundingClientRect();
        const dx = rectR.left + rectR.width/2 - (rectF.left + rectF.width/2);
        const dy = rectR.top + rectR.height/2 - (rectF.top + rectR.height/2);

        // ---- 阶段一：前排猛退，后排被挤并旋转 ----
        cellF.style.transition = 'transform 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94)';
        cellF.style.transform = `translate(${dx}px, ${dy}px)`;

        const rotateDir = (frontUnit.camp === 'ally') ? 1 : -1;
        cellR.style.transition = 'transform 0.25s ease-out';
        cellR.style.transform = `translate(${dx * 0.3}px, ${dy * 0.3}px) rotate(${15 * rotateDir}deg)`;

        await wait(250);

        // 后排反向旋转并移动到前排位置
        cellR.style.transition = 'transform 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55)';
        cellR.style.transform = `translate(${-dx * 0.8}px, ${-dy * 0.8}px) rotate(${-10 * rotateDir}deg)`;

        await wait(300);

        // ---- 阶段二：交换闪烁 ----
        cellF.style.transition = 'none';
        cellR.style.transition = 'none';
        for (let i = 0; i < 2; i++) {
          cellF.style.visibility = 'hidden';
          cellR.style.visibility = 'hidden';
          await wait(70);
          cellF.style.visibility = 'visible';
          cellR.style.visibility = 'visible';
          await wait(70);
        }

        // ---- 阶段三：落位，交换坐标 ----
        cellF.style.opacity = '0';
        cellR.style.opacity = '0';
        await wait(150);

        // 交换pos
        frontUnit.pos = posR;
        rearUnit.pos = posF;

        // 清理样式
        cellF.style.transition = '';
        cellF.style.transform = '';
        cellF.style.opacity = '1';
        cellF.style.visibility = 'visible';
        cellR.style.transition = '';
        cellR.style.transform = '';
        cellR.style.opacity = '1';
        cellR.style.visibility = 'visible';

        highlightedFront = null;
        highlightedRear = null;
        renderAll();

        // 落地弹跳
        const newCellF = getCellElement(frontUnit);
        const newCellR = getCellElement(rearUnit);
        if (newCellF) {
          newCellF.style.transition = 'transform 0.2s ease';
          newCellF.style.transform = 'scale(0.8)';
          requestAnimationFrame(() => { newCellF.style.transform = 'scale(1)'; });
        }
        if (newCellR) {
          newCellR.style.transition = 'transform 0.2s ease';
          newCellR.style.transform = 'scale(0.8)';
          requestAnimationFrame(() => { newCellR.style.transform = 'scale(1)'; });
        }

        document.getElementById('logText').textContent = `✅ ${frontUnit.name}(→${posR}) ↔ ${rearUnit.name}(→${posF}) 击退换位完成`;
        isAnimating = false;
      }

      // 执行阵营击退换位
      async function triggerPush(camp) {
        if (isAnimating) return;
        const team = camp === 'ally' ? allyTeam : enemyTeam;
        const pair = findPushPair(team);
        if (!pair) {
          document.getElementById('logText').textContent = `⚠️ ${camp === 'ally' ? '明教' : '六大派'}没有可击退换位的单位（需要前后同列）`;
          return;
        }
        highlightedFront = pair.front;
        highlightedRear = pair.rear;
        renderAll();
        await wait(100);
        await animatePushSwap(pair.front, pair.rear);
      }

      // 按钮事件
      document.getElementById('pushAllyBtn').addEventListener('click', () => triggerPush('ally'));
      document.getElementById('pushEnemyBtn').addEventListener('click', () => triggerPush('enemy'));

      document.getElementById('resetBtn').addEventListener('click', () => {
        if (isAnimating) return;
        allyTeam[0].pos = 5; allyTeam[1].pos = 1; allyTeam[2].pos = 4; allyTeam[3].pos = 7; allyTeam[4].pos = 3;
        enemyTeam[0].pos = 8; enemyTeam[1].pos = 9; enemyTeam[2].pos = 6; enemyTeam[3].pos = 4; enemyTeam[4].pos = 2;
        highlightedFront = null;
        highlightedRear = null;
        renderAll();
        document.getElementById('logText').textContent = '🔄 已重置';
      });

      // 初始渲染
      renderAll();
      document.getElementById('statusText').textContent = '👆 点击按钮自动寻找可击退换位的单位（例：杨逍后方范遥，峨眉后方昆仑长老）';
    })();
  </script>
</body>
</html>
```

---

### 核心逻辑说明

**触发条件**：乘风突袭 Buff 生效时，飞行单位攻击有 40% 概率将目标往后击退一格。如果目标后方有同阵营单位，双方换位。

**自动判定**：
- 明教击退方向：向后（pos + 3，往下）
- 六大派击退方向：向前（pos - 3，往上）
- 只处理同一列（1-4-7、2-5-8、3-6-9）的前后排关系

**三阶段动画**：
1. **猛退挤开（550ms）**：前排向后方平移，后排被挤向一侧旋转 15°，再反向弹回
2. **交换闪烁（280ms）**：两个格子快速交替显隐 2 次
3. **落位弹入（350ms）**：数据层交换 pos，新格子从 scale(0.8) 弹出

**当前演示阵容**：明教 5 人 vs 六大派 5 人，杨逍(4号)后方有范遥(7号)，峨眉剑客(6号)后方有昆仑长老(4号)，两边都有可触发的击退换位组。
**********************************************************