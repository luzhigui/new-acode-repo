# 成功经验 - 文件复制器序列发送优化

## 问题描述

文件复制器点击"序列发送"后，弹窗显示的包序号始终比当前复制的包多一个（显示的是下一个包的信息）。

## 根因分析

`nextBatch()` 函数逻辑顺序错误：

```
复制当前包 → sendIndex++ → 显示弹窗  // 错误：显示的是 sendIndex++ 后的下一个包
```

## 正确方案

### 优化一：显示当前操作内容，而非下一个

复制后先显示"已复制当前包"，再 ++ 切换到下一包，等待下一次操作：

```
复制当前包 → 显示"已复制当前包" → sendIndex++ → 延迟 0.8s → 显示下一包等待复制
```

### 优化二：一键开始，立即复制第一个包

点击"序列发送"后，不要等用户再按一次 Enter，**直接复制第一个包并显示"已复制"**。后续每个包按 Enter 复制。

```
点击序列发送 → 复制第 1 包 → 显示"✅ 已复制 全局 1/22 ..." → 按 Enter → 复制第 2 包
```

**关键点**：`renderSenderBar(sendIndex, 'copied')` 中，`sendIndex` 必须在复制后立即显示，不要 ++ 后再显示，否则面板会显示下一个包，导致用户困惑。

## 关键代码

### renderSenderBar 新增 status 参数

```javascript
function renderSenderBar(showIndex, status) {
    // ...
    if (status === 'copied') {
        // 绿色提示"已复制"，不含按钮，不响应 Enter
        senderBar.innerHTML = `✅ 已复制 全局 ${displayIndex}/${totalBatches} · 【${gn}】 组内 ${groupBatchIndex}/${groupBatchTotal}，准备下一包...`;
    } else {
        // 等待状态：显示包信息 + 按钮，响应 Enter
        senderBar.innerHTML = `📦 全局 ${displayIndex}/${totalBatches} · 【${gn}】 组内 ${groupBatchIndex}/${groupBatchTotal}（${chars} 字符）→ 按 Enter 复制当前包`;
    }
}
```

### nextBatch 先显示已复制，再切换

```javascript
async function nextBatch() {
    const fullText = getBatchText(sendBatches[sendIndex]);
    await navigator.clipboard.writeText(fullText);
    renderSenderBar(sendIndex, 'copied');  // 先显示已复制
    sendIndex++;                            // 再切换
    setTimeout(() => {
        renderSenderBar(sendIndex);         // 延迟后显示下一包
    }, 800);
}
```

### 点击事件：直接复制第一个包

```javascript
sendBtn.addEventListener('click', () => {
    sendBatches = Array.from(cards); sendIndex = 0; sendCancelled = false;
    addSenderKeyListener();
    setTimeout(() => nextBatch(), 100); // 直接复制第一个包，无需用户再点一次
});
```

## 核心原则

**弹窗显示的是用户当前正在操作的内容，不是下一个要操作的内容。** 任何时候，UI 展示的信息必须与用户当前的操作状态一致。