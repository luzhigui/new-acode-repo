# 光明顶 5v5 - 更改履历

## V5.7.11 — 2026-08-24 战报记账口径 v2（承伤=挡刀值 / 治疗记产出者）

### 总述
三项统计重新定义：**输出**不变（clamp 后实际损血）；**承伤=挡刀值**（按来袭全额记，防御挡掉的、溢出的都算，回血不冲减，闪避/免疫吸收也算）；**治疗记产出者**（自己回的算自己，队友奶你的算队友，溢出治疗不记）。变身/苦练/蝶变的血上限变化维持不记账（原本就没记）。

### 改动（3 文件，语法校验通过）
- **core/13battle-shared.js（V5.5.3→V5.6.0）**：`applyStatChange` 记账重写——`dmgTaken += |delta|`（来袭全额，不再 clamp）；`healDone` 记 `(source || target)`（产出者优先）；`dmgDealt` 不变
- **core/12battle-attack-steps.js（V5.6.1→V5.6.2）**：
  - `calcFinalDamage` 新增 `blockValue`：防御减免前来袭总量（普通=atkAct×乘区+加成；防战=atkAct+防档位+血量系数部分），随返回值传出
  - `applyAttackResult` 补差：`dmgTaken += blockValue - dmg`（主攻击承伤凑足防御减免前总量）
  - 闪避分支：闪避者 `dmgTaken += floor(攻击者atk)`（吸收来袭攻击力；反击伤害走 applyStatChange 自动全额记账）
  - 删除 HEAL 的 `dmgTaken -= capped`（回血不再冲减承伤）
- **core/10battle-attack.js（V5.6.0→V5.6.1）**：免疫回退删 `target.dmgTaken -= dmg`（吸收了就保留挡刀值，仅回退输出）

### 连带影响（待跟进）
- `tools/113-stats-check.js` 交叉核对口径基于旧定义（dmgTaken=实际损血），需同步更新
- `tests/health-rules/139`（姐姐零承伤判定）语义随新口径微调：被闪避攻击也会产生承伤

### 验证
- `node --input-type=module --check` 三文件全过
- 浏览器实战验证待用户跑：战报承伤应普遍变大（防高单位尤其明显）、治疗列归产出者

## V5.7.10 — 2026-08-24 坚盾每回合上限 3→4 / 成昆 6→8（文档同步）

> 主代码侧 V5.5.2 已在 `core/01config-5v5-test.js` 把 `FORTIFY_CAP` 3→4，对应 `fortifyIncrementMul:2` 的成昆联动上限 6→8；文档漏跟。

- **设定-职业设定**：坚盾「每回合上限：普通防战 3 点，成昆 6 点」→「普通防战 4 点，成昆 8 点」
- **游戏-玩家指南**：防战职业表「每回合上限 3 点、成昆 6 点」→「4 点、成昆 8 点」
- **设定-游戏设定**：坚盾强化「每回合上限提至 6 点（普通防战 3 点）」→「8 点（4 点）」

## V5.7.9 — 2026-08-23 苦练数值三倍化（主代码）+ 设定文档同步

### 主代码侧（4 处，JSON 与两个 JS 语法校验通过）
- **`content/200game-data.json`**：苦练声明 `hpBonus` 2.5 → 3；技能参数 `hpBonus: 3`，描述「自身翻倍」→「自身三倍」
- **`core/15-skill-mechanisms.js`**：兜底值 `|| 3`；宋青书自身倍率 `mult` 2 → 3（攻/防/血三项三倍）
- **`render/30-fact-renderer.js`**：日志文案同步改为「自身三倍」
- **效果**：普通队友每回合 +1攻+1防+3血上限，宋青书自身 +3攻+3防+9血上限

### 文档侧（数值同步）
- **设定-人物设定**：苦练 `hpBonus` 2.5→3（`1/1/2.5`→`1/1/3`），自身翻倍（+2/+2/+5）→ 自身三倍（+3/+3/+9）
- **设定-游戏设定**：宋青书苦练 +2.5 生命上限、自身翻倍 → +3 生命上限、自身三倍

## V5.7.8 — 2026-08-23 张无忌切换日志丢失 + 融会贯通停摆 + 破防日志位置 + 前三击弹幕

### 总述
四个问题一次修复：张无忌切近战的切换行/台词/弹幕整条丢失（log 数组错位）；融会贯通第 3 次触发后从第 4 次起停摆（FSM 状态判定）；战士破防日志应前置到攻击组开头；张无忌近战前三击台词弹幕被暴击/防御随机台词抢占。

### 张无忌切换日志丢失（modules/27，V5.6.1 → V5.6.2）
- **根因**：组件 register 闭包捕获 prepareRoundStart 的 log 数组（L0）；首个 step yield 后 log 变量重指向新数组（L1）但闭包仍是 L0。onPositionSwap / onUnitDeath 触发的 checkZhangSwitch 把 zhangSwitch fact push 进已消费的 L0 → 随 makeStep 快照永久丢失（切换行 + 台词行 + isZhangSwitch 弹幕三样全丢）。onRoundStart 路径不受影响（emit 时 log 即 L0）
- **修复**：监听器把 `data.log`（emit 时携带的当前 log）传给 `fsm.transition('switching', { log })`，switching.onEnter 优先用 data.log

### 融会贯通停摆（同文件）
- **根因**：onAfterApplyDamage 条件 `fsm.is('near')`——第 3 次攻击后 FSM 转 ronghui 态，is('near') 永远 false；每回合重新 register 保留 current='ronghui'，第 4 次起融会贯通/nearAtkCount 全停
- **修复**：条件改 `fsm.is('near') || fsm.is('ronghui')`（ronghui 是 near 的融会贯通激活态，转入后持续生效）

### 破防日志前置（render/30，V5.7.1 → V5.7.2）
- renderAttackFact 把 fact.entries 中的 breakDef 条目投影后 unshift 到攻击组开头（破防发生在伤害计算之前），末尾循环跳过 breakDef 防重复；executeLog（斩杀）仍留攻击组末尾

### 前三击台词弹幕优先（core/12，V5.5.3 → V5.5.4）
- 12:240 原条件 `if (zt && !waveTaunt)`：本攻击已有暴击/防御随机台词时张无忌近战台词被跳过 → 前三击不保证有弹幕。改为张无忌近战台词优先覆盖（`if (zt) { waveTaunt = zt; waveUnit = unit; }`）

### 遗留观察（未改，待定夺）
伤害弹幕与动画错位的场景识别：① 击杀台词弹幕在 _triggerFX（攻击组开头 setTimeout 0）立即飘出，而死亡红格（V5.7.6 起延迟到日志播完）后置 → 弹幕喊击杀时目标格子还活着；② waveTaunt 弹幕 delay 按伤害分档（0/200/400ms）固定计时，与动画时长（c.speed 可调）不挂钩，远程箭矢场景（动画长、弹幕快）错位最明显

## V5.7.7 — 2026-08-23 战报双弹窗修复（点两次才能关）

- **`ui/64main-dialogs.js`**（V5.5.0 → V5.5.1）：`showBattleReport` 开头加防重复——已存在 `#battleReportOverlay` 直接 return。根因：该函数被调用两次（`player/42` playBattle 结尾一次、`ui/61` playBattle() 返回后一次），两次各自 createElement 叠双层同 id overlay，关闭一层还剩一层。第二次 return 后 61 的 full-auto 3 秒自动移除仍正常工作（该 setTimeout 在调用之后无条件执行）。同日核验：死亡移除后的战报（数据源 `c.snapshot` + `finalStep` 引擎数组）与 carry/蝶变血量计算（引擎侧 `applyStatChange`/`applyMaxHpChange` + 引擎数组）均不经 store，V5.7.6 的 SYNC 尸体过滤不影响两者

## V5.7.6 — 2026-08-23 死亡特效时序修复 + 尸体格循环闪烁修复

### 总述
两个死亡表现问题：击杀时死亡红格抢在攻击动画前显示（时序倒置）；已移除的尸体格被每步 SYNC 反复「消失-重现」无限循环。均为 stageAction 三线架构（V5.7.3）落地后的遗留问题，单文件修复（player/42）。

### 死亡特效时序（V5.7.0 → V5.7.1）
- **根因**：`playStep` 先跑 stageAction 阶段——`applyStageActionToStore` 对击杀攻击（attack/dot/execute/spiderStrike/dodge 的 dead）立即置 `SET_FLASH dead + SET_VISUAL _isDead`，之后才播日志（飞撞动画+攻击文本），死亡格抢跑；88 飞撞回调「动画结束才标死亡」的正确时序被覆盖
- **修复**：dead 标记改为收集 `pendingDeaths`，日志播完后统一落地（幂等去重，88 回调已标则跳过）。新时序：攻击动画/文本 → 死亡特效 → 掉血同步；非近战击杀（远程/dot/处决/闪避反震）也有了统一的死亡时机

### 尸体格循环闪烁
- **根因**：死亡 3 秒定时器 `REMOVE_UNIT` 移除尸体格后，引擎数组仍保留阵亡单位（战报要用），下个攻击步 `SYNC_FULL_UNITS` 的 reducer「补新」逻辑把已移除尸体重新加回 store → 红格重现 → 又开 3 秒定时器 → 又移除 → 无限循环（每个攻击步触发一轮）
- **修复**：`playStep` 的 SYNC 前过滤「引擎已死且 store 已移除」的单位（`syncUnits`：活着、或死了但仍在 store 的 3 秒展示窗口内才保留）；尸体移除后不再复活，拒马销毁（destroy）路径同样受益

## V5.7.5 — 2026-08-23 player→fx 依赖反转（直调改事件订阅）

### 总述
player 层 4 个文件不再 import fx 层任何函数（与 core→render 的 eventBus 拆墙对称作业）：player 只 emit 信号携带数据，fx 侧新建订阅器统一监听转调。信号常量集中放 infra 层，player→infra / fx→infra 均为合法方向，墙真正拆掉而非换砖。

### 新增文件
- **`infra/55-fx-signals.js`**（新建 V5.5.0）：`FX_SIGNALS` 24 个 `fx:` 前缀信号常量（），单一数据源，emit 侧与订阅侧各自 import
- **`fx/89fx-subscriber.js`**（新建 V5.5.0）：模块加载即注册全部监听（priority 50），静态 import 87/86/88 转调原有特效函数；快进判断（fastForwardActive）从 player 调用点归位到订阅器（表现层职责）。未改动 fx/87、86、88 任何函数实现

### player 四文件改造
- **`player/41player-buff-ui.js`**（V5.6.0 → V5.7.0）：拒马阵/拒马销毁/嗜血狂刀 3 处横幅改 `eventBus.emit`；清理死 import `showHealFloat`
- **`player/42player-core.js`**（V5.6.0 → V5.7.0）：删除 87 号 import 与 spiderStrike 分支的动态 `import('../fx/86fx-butterfly-spider.js')`；`applyStageActionToFX` 全部 7 分支（spiderStrike/heal/dodge/attack/dot/execute/splash）改 emit
- **`player/45event-handlers.js`**（V5.6.0 → V5.7.0）：删除 87/88 两个 fx import、`getButterflyFx` 辅助函数（内含第二处动态 import 与 `window[name]` 历史兜底）、`safeShowDanmaku`；蝶变飞出/飞回、蛛变升空/降落、弹幕、爱心、粉闪、攻速飘字、骨爪、画笔渐隐等 13+ 调用点改 emit
- **`player/46attack-group.js`**（V5.6.0 → V5.7.0）：`_triggerFX`（88 确认为 fx 内部派发器）改走通用 `fx:trigger` 信号由订阅器转调；苦练、闪避子弹时间、流星箭雨、骨爪、气泡、残血弹幕等调用点改 emit；`safeShowDanmaku` 的 try-catch 职责由 eventBus.emit 内置异常捕获接管

### 语义保持决策
- await 逐一对应：原带 `await`（横幅/位移/蛛袭/子弹时间）→ `await eventBus.emit`；原同步 fire-and-forget（风爪/飘字/骨爪）→ emit 不加 await
- fastForward 判断归位分两类：单来源信号（HEAL_FLOAT/SPIDER_STRIKE）搬进订阅器；DAMAGE_FLOAT 为多来源混合（导演 stageAction 链路有判断、日志残留链路无），统一搬会改变行为，判断保留 emit 侧、订阅器纯转发，零行为变化

### 接入与登记
- **`mode-5v5-test.html`**：挂载 `<script type="module" src="./fx/89fx-subscriber.js">`（一行）
- **`tools/106-ai-pack-config.js`**：清单登记 infra/55、fx/89 两个新文件

### 回归修复（同日）
- **`infra/50-event-bus.js`**（V5.5.0 → V5.5.1）：`clearAll()` 改为保留 `fx:` 前缀信号。回归根因：`core/11` `prepareRoundStart` 每回合 `clearAll()` 清空全部监听并只重注册战斗监听，fx/89 的页面级一次性订阅第 1 回合即被误杀——症状为飞撞/飞箭等特效全不显示、闪避反击后防守方 'attack' 闪光永久残留（其清除依赖 `showDodgeBulletTime` cleanup 的 `CLEAR_UNIT_FLASH`，子弹时间不播即不清）。修复后 fx: 信号随页面存活、战斗信号维持每回合清空重注册语义，tools/112-115 的 clearAll 调用不受影响（工具页未加载 89 无 fx 监听）

### 验收
`rg "from '\.\./fx/" player/` 与 `rg "import\('\.\./fx/" player/` 零结果；全部改动文件 `node --check` 通过；裸函数名残留扫描仅命中文件头注释；eventBus+FX_SIGNALS 契约接线 node 冒烟通过；clearAll 保留 fx: 信号场景 node 冒烟通过。浏览器全特效实测待人工验收

## V5.7.4 — 2026-08-23 重复 dispatch 收尾 + 文件复制器清单补齐

### 重复 dispatch 收尾（3 处落地）
- **`player/42player-core.js`**：round-start 分支删除两行对全场存活单位的 `SET_VISUAL _acted:false` 重复清标记（导演 roundStart 已处理）
- **`player/45event-handlers.js`**：蛛落 return 分支删除 `SET_VISUAL _flyMode:null,_acted:false` 重复状态清理（导演 flyMode 已处理）
- **`player/46attack-group.js`**：删除攻击者防战「严阵以待」横幅重复调度（handleBuffReboundFortify 负责，注释占位）；另核验该文件 blocked 刷新与 isBlocked import 两处已在此前落地

### 文件复制器清单补齐（tools/106-ai-pack-config.js，V5.6.0 → V5.7.0）
- 补登记 `render/31-stage-actions.js`（舞台动作翻译器，导演调度唯一依据，此前漏登记导致复制包缺失）
- 补登记体检规则 134-140 共 7 个文件（121 已 import rule81-87：zhang-switch/break-def-pos/meteor-atk/kulian-prompt/wind-push/spider-butterfly-target/wei-dodge-cloud）

## V5.7.3 — 2026-08-22 新建准则-舞台动作映射

- **准则-舞台动作映射（新建）**：收录 stageAction kind 定义（22 种：roundStart/attack/dodge/heal/statChange/posSwap/push/summon/destroy/death/dot/execute/transform/flyMode/stun/rest/buffSummary/banner/separator 等，每 kind 对应格子线/特效线/日志线三列职责）、factType → stageAction 映射（回合控制/攻击流程/血量伤害衍生/位置换位击退/属性增益/召唤销毁/变身飞行/技能机制附件，共 8 组）、三线节奏原则（格子线同步优先级最高、特效线异步跟随、日志线最慢只阻塞自己）
- **README-文件**：文档清单登记「准则-舞台动作映射」
- **准则-架构决策**：「引擎与 UI 边界」节新增「stageAction 三线映射（V5.7.3）」指针行

## V5.7.2 — 2026-08-22 播放器状态源收敛 battleStore + tools 页数据加载修复

### 文档侧
- **准则-架构决策**：「引擎与 UI 边界」节新增「battleStore 唯一状态源（V5.7.2）」决策行（findUnitByUid 统一查找入口、c.UI 只读快照整体重建、三层同步错位风险收敛）

### 总述
battleStore 成为播放器动画期间 UI 层唯一状态源，c.UI 退化为每步整体重建的只读快照，降低「引擎 A/B、UI 副本、battleStore」三层同步错位风险；同时修复 tools 页因相对路径 404 导致游戏数据为空的崩溃。

### 数据加载修复
- **`core/01config-5v5-test.js`**：`loadGameData` 改用 `import.meta.url` 解析 `../content/200game-data.json`，修复 tools 页因相对路径 404 导致游戏数据为空、`CONFIG.MING_M` 返回 `{}` 引发崩溃的问题

### UI 层统一查找入口
- **`player/47renderer.js`**：新增 `findUnitByUid(c, uid)`，优先从 battleStore 实时查找单位，store 未就绪时回退 c.UI 快照，作为 UI 层统一查找入口
- **`player/42player-core.js` / `player/45event-handlers.js` / `player/46attack-group.js` / `player/41player-buff-ui.js`**：所有按 uid 查找单位统一改为 `findUnitByUid`，不再直接依赖 `c.UI.allyTeam.concat(c.UI.enemyTeam).find(...)`；按名字查找（宋青书、张无忌）改为从 store 读取

### c.UI 快照化
- **`player/42player-core.js`**：
  - 新增 `rebuildUISnapshotFromStore()`，每步结束后从 store 克隆重建 c.UI 快照
  - 订阅回调移除逐字段同步 c.UI 的逻辑
  - 初始 c.UI 赋值改为调用 `rebuildUISnapshotFromStore()`

## V5.7.1 — 2026-08-22 架构决策文档同步

- **准则-架构决策**：新增「声明化内容管线（V5.7.0）」决策节，覆盖技能装配（installFromGameData + 效果解释器）、Buff 装配（installBuffMechanics）、组件占位化、数据源统一（CONFIG.BUFFS getter / gameData.taunts）、渲染去重（32 收敛三函数、62 转发）；「精英组件结构」「精英技能归属原则」两节补 V5.7.0 现状——朝廷/六派组件占位、机制迁入 gameData + 15 号解释器，小昭姊/妹等专属机制暂留组件待声明化；头部"最后更新"标注 V5.7.0

## V5.7.0 — 2026-08-22 技能/Buff 声明化 + 数据驱动收敛

### 总述
六大派角色技能已声明化，buff 注册链路已声明化，台词/防御台词已数据驱动，渲染重复函数已收敛。主线为内容管线改造：技能与 Buff 机制从命令式手写监听迁移为按 `gameData` 声明自动装配。

### 一、内容管线：技能声明化（15 + game-data）
- **`core/15-skill-mechanisms.js`**（V5.6.0 → V5.7.0）
  - 新增 `installFromGameData`：从 `gameData.characters.*.mechanics` 自动装配技能
  - 新增效果解释器：`phantomDisguise`、`linkAttack`、`chainClaw`、`kuLian`、`xinHun`、`xingFen`
  - 修复 `installXinHun` 漏掉"性奋成本"逻辑
  - 修复 `installXingFen` 中 `processUnitAttack` 的 `B/A` 参数
  - 修复 import 路径：`./20elite-skills.js` → `../modules/20elite-skills.js`
- **`content/200game-data.json`**（5.3.2 → 5.4.0）
  - 新增角色 `mechanics`：韦一笑、张无忌九阳、宋青书叛逆、成昆混元霹雳劲、鹿杖客玄冥神掌、鹤笔翁鹿角杖法、成昆幻影伪装、玄冥联动、宋青书苦练/新婚/性奋、周芷若九阴白骨爪
  - `buffs` 对象扩展数值字段并新增 `effects` 声明

### 二、Buff 声明化
- **`core/04buff-system.js`**：新增 `installBuffMechanics`：按 `gameData.buffs.*.effects` 声明注册处理器；原有 5 个 register 函数保留为处理器，不再在回合循环逐一调用
- **`core/11battle-round.js`**：导入 `installFromGameData`、`installBuffMechanics`、`getGameData`；删除 `registerBloodthirst/HotBlood/WindAssault/MeteorShower/MindControl` 的逐一调用，改为 `installFromGameData` 和 `installBuffMechanics` 调用

### 三、命令式模块收缩
- **`modules/25elite-imperial.js`**：删除成昆/鹿杖客/鹤笔翁手写 `declarations`；删除成昆幻影伪装手写逻辑，成昆组件变为占位；删除 `registerXuanmingLink` 及其注册；清理未使用 import
- **`modules/26elite-sixsects.js`**：删除宋青书/周芷若手写监听，组件变为占位；清理未使用 import
- **`modules/27elite-mingjiao.js`**：删除张无忌、韦一笑手写 `declarations`；清理未使用 import

### 四、数据源统一（部分）
- **`core/01config-5v5-test.js`**：新增 `DEFAULT_BUFFS`；`CONFIG.BUFFS` 改为 getter：运行时优先 `gameData.buffs`，回退 `DEFAULT_BUFFS`
- **`core/03battle-utils.js`**：`getRandomTaunt`、`getKillTaunt`、`getZhangNearTaunt` 改为优先读 `gameData.taunts`
- **`core/12battle-attack-steps.js`**：防御台词 `DT` 读取优先 `gameData.taunts.def`

### 五、渲染去重
- **`render/32-grid-render.js`**：导出 `getBuffStats`、`getDodgeBreakdown`、`isUnitBenefitedByBuff`
- **`ui/62ui-render-5v5-test.js`**：删除与 32 重复的三个函数，从 32 导入并转发导出；清理未使用 import；修复 `updateDetailPopupContent` 中缺失的 `buffStats` 定义

### 六、运行报错修复
- `15-skill-mechanisms.js` 错误引用 `./20elite-skills.js`，已改为 `../modules/20elite-skills.js`
- `62ui-render` 缺少 `isUnitBenefitedByBuff` 转发导出，已补
- `updateDetailPopupContent` 中 `buffStats is not defined`，已补定义

## V5.6.2 — 2026-08-22 文档同步（bug 待修重构 + 开发准则编号更新）

### 总述
文档管家对两份滞后文档做一致性修复，未动任何代码。注：V5.5.5 至 V5.7.0 间的主代码改动（含 V5.6.x 中间版本）由 V5.7.0 条目统一补录；tools/infra/fx 侧若有零星 V5.6.x 改动未涵盖，仍待主代码负责人补充。

### 改动
- **待办-bug待修**：重构为状态表（待修/待讨论/待补信息/已修留档）；修正编号错乱（原有两个"1"）；核对回归规则对应关系——闪避反击击杀死亡特效确认已修（rule80 监控），玄冥联动"吞回合"已修（rule73）但"动变灰"显示问题为另一独立 Bug 仍待修
- **准则-开发准则**（V5.4.0 → V5.6.2）：修正 3 处过时文件编号（16error-capture → modules/21error-capture；15elite-skills 与 20/21/22 精英文件 → 20elite-skills 与 25/26/27）；版本号规则改为"逐文件维护"，删除与现状不符的"当前统一 V5.4.0"表述

## V5.5.5 — 2026-08-22 工具清单同步

### 总述
V5.5.4 拆分产生的新文件未同步进工具清单，本次补录（7 个文件），AI 复制包与函数提取器恢复全量覆盖。

### 清单补录
- **106-ai-pack-config**（V5.5.2）：`ALL_PROJECT_FILES` 补 `core/15-skill-mechanisms.js`、`ui/70buff-dialog.js`、`fx/88fx-trigger.js`、`tools/113-stats-check.js`、`tools/114-baseline-compare.js`、`tools/115-lineup-search.js`、`tests/health-rules/133-death-effect.js`
- **104-toolkit-more**（V5.5.1）：`TARGET_FILES` 同步补录上述 7 个文件
- **README-工具**（V1.1.0）：速查表补 113/114/115，删除已不存在的 `创意-精英战力评测脚本.js` 条目
- 备注：`100build-5v5.TXT` 为已废弃存档，其内部 MODULES 清单指向多个已删除文件，不再维护

## V5.5.4 — 2026-08-21 战斗修复 / 数值与显示 / 架构收口 / player 拆分

### 总述
四线：修复战斗异常与显示问题；清理死代码；infra 合并 + 依赖方向收口；player 拆分。

### 一、修复战斗异常与显示
- **`allySide is not defined`**：`resolveAfterDamageEffects` 补 `allySide`、`unitBuffs` 参数，两处调用传参
- **小昭圣火令额外 30% 遗漏**：`applyHolyFlameBonus` 第二处调用补 `hasSister`
- **伤害显示统一取整**：公式行、击杀行四舍五入，删除旧减伤显示，修复 `32.7`、`减伤-0.7`
- **流云身法姐姐强化**：改为 30% 闪避（`hexEnhance.cloudBody` 配置 + `applyCloudBodyDodge_Sister`）

### 二、死代码
- 删除 `player/46` 的 `isCombo` 死代码

### 三、架构收口
- **infra 小文件合并**：新建 `infra/51-core-utils.js`，合并原 51-fsm / 52-rng / 53-battle-event-store / 55-battle-math，全局替换 import，删除 4 个旧文件
- **工具函数下沉 infra**：`calcDamage` / `getFangLevelPure` / `makeFXSnapshot` / `getUnitRow` / `getUnitCol` / `getAdjacentPositions` / `getAuraBonuses` 等从 core/03 下沉，render 改为直接 import infra，断开 `render → core` 反向依赖
- **window 全局变量收口**：B 类 8 个桥改走 `GlobalStore.setUIHandler`，C 类 3 个死引用删除；修复 4 个 fx 文件 `window._fastForwardActive` 读取（快进失效预存 bug）

### 四、拆 player
- **`_triggerFX` 下沉**：新建 `fx/88fx-trigger.js`，斩断 `player → ui` 反向依赖
- **41 拆分**：新建 `ui/70buff-dialog.js` 放 `showBuffPopup`；`player/41` 只留 4 个 handler

## V5.5.3 — 2026-08-20 规则修复 / 数值收敛 / 精度统一

### 总述
三大线：修复休息回血、拒马、永久海克斯等规则问题；收敛引擎写死数值改读配置；伤害/显示统一 0.1 精度。

### 一、规则修复
- **休息回血**：被遮挡/拒马跳过行动时引擎 +15 血，日志输出，格子显示 Zzz
- **拒马属性统一**：普通 0/5/25、姐姐强化 0/30/30，引擎改读配置，删除播放器写死数据
- **永久海克斯**：选 Buff 后同步到引擎单位，查找统一为 `isXiaoZhaoBrother`，去重
- **张无忌融会贯通**：第三次起每次攻击都触发
- **性奋代价**：从 2 开始递增（第 1 次 -2、第 2 次 -3，跨回合累计）
- **小昭 `_spiderRemaining`**：修复 0 值重置隐患
- **概率连击·姐姐强化**：补"无视遮挡"判定（解决 V5.5.2 存疑项）

### 二、数值收敛
- 引擎不再写死数值：概率连击、嗜血、严阵以待、流云身法、乘风突袭、流星赶月、热血奋战、惑人心智全部改读配置 / hexEnhance
- 未命中三档（远程 3% / 飞行 6% / 地面 1%）进配置，残血扰乱加成进配置
- 九阴白骨爪、成昆、鹤笔翁的百分比统一为小数，`getSkillDesc` 适配小数转百分比
- 圣火令姐姐强化补全：2 列攻 / 2 行防 / 小昭额外 30% 攻防

### 三、精度与显示
- 伤害计算、属性变更、白骨爪伤害、属性生成改为 0.1 步长
- 攻防显示取整数，格子攻防比较基准取整，修复初始假红色/金色

## V5.5.2 — 2026-08-20 文档一致性核对

### 总述
按批次对 MD 文档与主代码逐类核对，修正过时/错误数值，补全遗漏清单。共改 7 份文档，其中 1 份升版本号。

### 设定类
- **设定-游戏设定**：14 处机制/数值对齐代码（含张无忌形态切换加成 ×2→×3、坚盾触发概率、未命中判定、精英出场逻辑等）；版本号 V5.3.2 → V5.4.0
- **设定-职业设定**：3 处数值对齐代码
- **设定-人物设定**：4 处机制描述对齐代码
- **设定-游戏流程**：2 处（我方精英出场掷骰逻辑 5%/15%/60%/20%；未命中判定含全场低血量单位数×3% 加成）

### 游戏类
- **游戏-玩家指南**：3 处（防战攻击坚盾 100%→80%；张无忌每回合回血 5%→每次攻击后回 8%、反弹 15%→减伤 10%+反弹 10%；韦一笑基础闪避 20%→双份 15% 独立判定+残血幻影每损失 1% 生命 +0.7%）

### 清单补全
- **README-文件**：文档清单补登 `README-工具.md`
- **README-工具**：工具速查表补登 106-ai-pack-config / 107-battle-log-viewer / 108-hex-dashboard / 109-role-balance / 110-role-balance-random / 112-elite-eval

### 存疑项（保留待办，未入履历）
- 概率连击·姐姐强化「无视遮挡攻击」、流云身法·姐姐强化「无视行动状态」：代码无此逻辑，待定
- 叛逆突袭真伤比例：已统一为 10%（game-data 改 `currentHpRatio: 0.10`，代码改读 `getSkillParams`）

## V5.5.1 — 2026-08-19

### 总述
- 第一部分：统一"额外攻击"声明制，四路径全部收敛到 `extraRequests`，子级只声明、父级统一执行；`04 ↔ 10` 循环依赖断开
- 第二部分：render 层日志条目补结构化字段，player 端文本嗅探清零
- 第三部分：player/ui 边界收口（战报走 UIHandler 桥），fx 调用集中到 `87fx-manager`

### 一、额外攻击抽象（声明制）

**core/10battle-attack.js**
- `processUnitAttack` 新增 `extraRequests` 收集与统一执行循环（排序、unit 级去重、优先级执行）
- `afterAttack` / `afterDamageApplied` / `hitResult.skipped` 三处接入执行循环
- 支持 `actedMode: 'allow' | 'restore'`（restore 执行后恢复 `actedSnapshot`）
- 清理死字段 `retry` / `retryTargetUid`
- 死 import 清理：`applyBuffEffectsBeforeAttack` / `applyBuffEffectsAfterAttack` 移除，仅保留 `computeBuffStats`

**core/03battle-utils.js**
- `registerDoubleStrike`：不再直接改 `retry` / `_acted`，改为 push 声明 `{ reason:'doubleStrike', actedMode:'allow', priority:10 }`

**core/04buff-system.js**
- 删除 `import { processUnitAttack }`（环断开）
- `registerBloodthirst`：删除 `requestExtraAttack` 监听器；姐姐强化额外攻击改为 push 声明 `{ reason:'bloodthirst', actedMode:'allow', priority:20 }`

**core/12battle-attack-steps.js**
- `resolveAttackHit`：afterMiss 分支改为收集 `extraRequests` 并回传；清理 `retry` / `retryTargetUid`

**modules/26elite-sixsects.js**
- 宋青书性奋 Miss 重试改为 push 声明 `{ reason:'xingFenMiss', actedMode:'allow', priority:30 }`

**modules/27elite-mingjiao.js**
- 小昭·妹永久连击 Miss 改为 push 声明 `{ reason:'xiaoZhaoDoubleMiss', actedMode:'allow', priority:30 }`

**modules/25elite-imperial.js**
- `registerXuanmingLink`：删除直接 `processUnitAttack` 调用；改为 push 声明 `{ unit: partner, reason:'xuanmingLink', actedMode:'restore', actedSnapshot, priority:40 }`；删除 import

**infra/50-event-bus.js**
- 删除死配置 `REQUEST_EXTRA_ATTACK`

### 二、结构化字段替代文本嗅探

**render/30-fact-renderer.js**
- `renderDropFact`：+ `dropKind: 'token' | 'chest'`
- `renderAttackFact`：计算 detail 条目 + `isDamageCalc: true`
- `renderXinHunFact`：+ `hpDeduct`
- `renderButterflyAttachFact`：+ `butterflyAction: 'attach'`、`sisterUid`、`hostUid`
- `renderButterflyReturnFact`：+ `butterflyAction: 'return'`、`sisterUid`、`hostUid`
- `renderButterflyNoHostFact`：+ `butterflyAction: 'noHost'`、`sisterUid`
- `renderSpiderFlyFact`：+ `spiderAction: 'fly'`、`spiderUid`
- `renderSpiderReturnFact`：+ `spiderAction: 'return'`、`spiderUid`

**modules/27elite-mingjiao.js**
- 蝶变附身/飞回渲染调用补传 `sisterUid` / `hostUid`
- 蛛飞渲染调用补传 `spiderUid`

**modules/20elite-skills.js**
- `spiderReturn` 渲染调用补传 `spiderUid`

**core/04buff-system.js**
- `registerHotBlood` 两处吸血 decl + `isDouble: tag.includes('翻倍')`

**core/10battle-attack.js**
- decl → log entry 构建处透传 `isDouble`

**player/42player-core.js**
- 圣火令掉落判断改为 `entry.dropKind === 'token'`
- hotBlood 横幅判断改为 `entry.isDouble`

**player/41player-buff-ui.js**
- `handleBuffLeech` hotBlood 横幅判断改为 `entry.isDouble`

**player/45event-handlers.js**
- 蝶变/蜘蛛特效触发改为消费 `butterflyAction` / `spiderAction` + uid
- 新婚扣血改为消费 `entry.hpDeduct`
- 蛛袭判断改为 `entry.isSpiderStrike`
- 删除重复的乾坤衍生嗅探块和通用"攻击+"嗅探块

**player/46attack-group.js**
- 乾坤衍生特效改为消费 `buffType === 'qiankun_atk'` + `atkTargetUid` + `atkGain`
- 伤害计算飘血改为 `entry2.isDamageCalc`
- 删除休息回复死代码

### 三、边界收口

**player/42player-core.js**
- fx import 集中：从 `87fx-manager` 导入 `showHealFloat` / `showWindClaw` / `showSplashArrows` / `showDamageFloat`
- 胜利结算 `showBattleReport` 改为 `GlobalStore.getUIHandler('showBattleReport')` 调用
- 清理 4 处动态 `import('../fx/80...81...')`，改为直接调用

**ui/63main-state.js**
- import `showBattleReport`
- 注册 `GlobalStore.setUIHandler('showBattleReport', showBattleReport)`

### 备注
- 第二部分未做"特效投影层"和"core 断 render/30 依赖"——经评估，现阶段弊大于利，标记为"等回放/战报导出/跨端等真实需求驱动时再做"
- 验证状态：用户已跑通，无大问题

## V5.5.0 — 2026-08-15 文件序号重排

**目的**：按目录划分段位、整十空位，让后续新增文件无需遍历全组即可确定编号。

**段位总表**（core 不动，其余按目录重排）：
| 段位 | 目录 | 说明 |
|------|------|------|
| 00-19 | core | 引擎，保持 00-14，15-19 留空 |
| 20-39 | modules | 业务，20-29 已用，30-39 留空 |
| 40-59 | player | 播放器，40-47 已用，48-59 留空 |
| 60-79 | ui | 界面，60-69 已用，70-79 留空 |
| 80-99 | fx | 特效，80-87 已用，88-99 留空 |
| 100-119 | tools | 工具，100-106 已用，107-119 留空 |
| 120-149 | tests | 体检，120-133 已用，134-149 留空 |

**核心变更**：
- modules：15-23、35 → 20-29（35battle-init 并入段，消除与 ui/35 的撞号）
- player：24-29 → 40-47（顺带解决 26/26、28/28a 双号，28a 去 a 变 46）
- ui：30-38、68 → 60-69
- fx：39-45、69 → 80-87（39-41 保留 -5v5-test 后缀）
- tools：46-51、71 → 100-106
- tests：55-57、58-70 → 120-133；子目录 `58health-rules` → `health-rules`
- 全部被改名文件版本号统一 V5.5.0，文件头路径前缀修正为实际目录
- 同步改引用：全项目 import/script 路径、`100build-5v5.cjs`、`106-ai-pack-config.js` 清单、`120test-runner.html`、`tools/health_auto_check.py`
- 未动：core 全部、index.html、assets、`tools/health_auto_check.py`（仅改引用）、`tools/创意-精英战力评测脚本.js`
- content：`64game-data.json` → `200game-data.json`（独立数据段，避开与 ui 段号冲突）；同步 `01config-5v5-test.js`、`104-toolkit-more.js`、`106-ai-pack-config.js` 引用

## V5.4.1 — 2026-08-13 ~ 2026-08-15

### 基建与可维护性收尾（继 V5.3.5 的 window 收口 / gs 双轨之后）

**目的**：在 V5.4.0 全文件序号重排与 V5.3.5 window/gs 收口基础上，继续做一轮地基收尾：统一重置入口、统一特效出口、拆分超大播放器文件、拆分回合开始/结束、常量替代裸字符串、补齐关键注释。为后续实际游玩验证和精准修复打底。

**一、重置清理统一 —— `ui/68reset-runtime.js`（新建，V5.4.0）**
- 新增 `resetBattleRuntime({ restoreSpeed })` 统一战斗重置出口
- 收口各重置路径：切关、原班再战、随机重开、强停
- 一次完成全部清理：Store/渲染引用、闪避规则与事件缓冲、全局标记、弹窗与浮动按钮（含弹幕/临时特效 DOM）、单位视觉标记与休息计时、恢复倍速

**二、特效统一出口 —— `fx/69fx-manager.js`（新建，V5.4.1）**
- 集中注册表 `registry` + `playEffect(name, ...args)` 按名调用 + `registerEffect()` 注册新特效
- 统一 re-export 全部特效函数（39fx-common / 40fx-arrows / 41fx-crash / 42fx-position-swap / 43fx-push-back / 44fx-dodge-bullet / 45fx-butterfly-spider）
- 调用方只从 69fx-manager 引入，不再跨文件散引

**三、播放器 28 拆分 —— `player/28a-attack-group.js`（新建，V5.4.1）**
- 从 `player/28event-handlers.js` 拆出 `handleAttackGroup` 到新文件，`26player-core.js` 改为从 `28a-attack-group.js` import
- 拆出后特效统一改为从 `fx/69fx-manager.js` 引入，28event-handlers 体积与职责收敛

**四、core 回合开始/结束拆分 —— `core/11battle-round.js`（V5.4.0）**
- `createRoundStepper` 内的回合开始准备逻辑抽为 `prepareRoundStart()`
- 回合结束收尾逻辑（销毁拒马、递减 Buff、胜负判定、回合结束信号、死亡结算）抽为 `finalizeRoundEnd()`
- 生成器主体只保留调度骨架，逻辑边界更清晰

**五、字符串常量化 —— `core/00-event-bus.js`（V5.4.0）**
- 新增 `EFFECT_TYPES` 枚举（bonusDmg/leech/heal/splash/rebound/statChange/execute/stun/weiHeal/breakDef/ignoreDef/dmgMultiplier/dmgReduction）
- 效果声明对象的 `type` 字段统一使用枚举，禁止裸字符串

**六、关键注释 —— 核心模块（V5.4.0 / V5.4.1）**
- `core/11battle-round.js`：回合生成器/回合开始/回合结束分段注释
- `core/00-event-bus.js`：执行层级与效果类型枚举说明
- `ui/68reset-runtime.js`：重置步骤分段注释
- `fx/69fx-manager.js`：注册表与 playEffect/registerEffect JSDoc

> 注：`resolveStateTransitions`、`resolveActionOrder` 已独立且边界清晰，未再外抽，避免无谓风险。69fx-manager 中蝴蝶/蜘蛛特效仍属裸创建，登记为待办，暂不处理。

### 小昭姐/妹命名区分 —— `core/04buff-system.js`、`modules/22elite-mingjiao.js`（V5.4.1，2026-08-15）

**目的**：玩家可见文本中不再裸写「小昭」，统一为「小昭·姊」/「小昭·妹」，姐是姐、妹是妹。

- `core/04buff-system.js`：
  - 圣火令摘要标签 → `🦋 圣火令（小昭·姊）`
  - 蝶血吸血日志（妹永久嗜血分支）→ `🕷️ 蝶血：${unit.name} 嗜血狂刀吸血`
  - 热血回复日志（妹永久热血分支）→ `🕷️ 热血` 标签 + `${unit.name} 回复`
- `modules/22elite-mingjiao.js`（改动全部落在小昭·妹组件内）：
  - 蝶舞迷心日志 → `被小昭·妹迷惑`（emoji 🦋→🕷️）
  - 拒马召唤日志 → `小昭·妹的拒马`
  - 永久carry 来源标识 → `小昭·妹永久carry`（3 处）
  - 蝶击连击日志 → `小昭·妹永久概率连击`（emoji 🦋→🕷️）

> 全局筛查确认：剩余裸「小昭」均为 `getSkillParams`/`getSkillDesc` 数据键或注释，非玩家可见文本，保持不动。

---

## V5.4.0 — 2026-08-09 ~ 2026-08-12

### 全文件序号系统化重排
- **目的**：全项目统一文件编号，消除各目录编号相互重叠、混乱的问题，让目录结构更清晰、便于检索定位。
- **新编号方案**（按目录全局连续编号，全项目不重复）：

| 目录 | 编号段 | 文件 |
|------|--------|------|
| `core/` | 00–14 | 00-event-bus、01config、02unit、03battle-utils、04buff-system、05battle-horse、06-fsm、07-rng、08-elite-registry、09-battle-event-store、10battle-attack、11battle-round、12battle-attack-steps、13battle-shared、14buff-effects |
| `modules/` | 15–23 | 15elite-skills、16error-capture、17audio-manager、18global-store、19battle-store、20elite-imperial、21elite-sixsects、22elite-mingjiao、23replay |
| `player/` | 24–29 | 24player-text、25player-buff-ui、26player-core、27battle-player-5v5-test、28event-handlers、29renderer |
| `ui/` | 30–38 | 30main-utils、31main-5v5-test、32ui-render-5v5-test、33main-state、34main-dialogs、35main-battle、36audio-control、37fx-trigger、38ui-controls |
| `fx/` | 39–45 | 39fx-common、40fx-arrows、41fx-crash、42fx-position-swap、43fx-push-back、44fx-dodge-bullet、45fx-butterfly-spider |
| `tools/` | 46–54 | 46build-5v5、47auto-battle-utils、48-toolkit.html、49-toolkit、50-toolkit-more、51-shop.html、52-version-calibrator、53-dead-code-scanner、54-filelist-checker |
| `tests/` | 55–63 | 55test-runner.html、56health-monitor、57health-utils、58health-rules/（58–63） |

- **版本对齐**：全项目 JS/HTML 文件版本号统一升级到 V5.4.0（`tools/49`、`tools/50` 升至 V5.4.1）。
- **构建脚本同步**：`tools/46build-5v5.cjs` 的 `MODULES` 列表与 `VER_GLOBAL_MAP` 随新编号同步更新。

---

## V5.3.2 — 2026-08-04

### 一、数据迁移：精英技能参数迁入 game-data.json

**content/64game-data.json**（新建 / 多次扩充）
- 新建文件，迁入周芷若、宋青书、成昆、鹿杖客、鹤笔翁、张无忌、韦一笑、小昭的技能名、描述、参数
- 迁入 Buff 描述文本（11 个）
- 迁入角色台词（TAUNT_LIB、KILL_TAUNT、ZHANG_NEAR_TAUNT、DEF_TAUNT、HP_TAUNT）
- 版本号 V5.3.1 → V5.3.2

**modules/20elite-imperial.js**
- 新增 `getSkillParams` 导入
- 成昆混元霹雳劲读 game-data（`lostHpRatio / 100`）
- 鹿杖客玄冥神掌读 game-data
- 鹤笔翁鹿角杖法读 game-data（`defIgnore/100`、`poisonedBonus/100`）
- 版本号 V5.3.1 → V5.3.2

**modules/15elite-skills.js**
- `tickXuanmingPoison` 读 game-data
- `spiderReturn` 读 game-data（`extraDmgMap`）
- `applyPhantomDisguise` 读 game-data
- `applyDamageModifiers` 读 game-data（升级版乾坤大挪移参数）
- `computeButterflyMastery` 读 game-data
- `spiderTransform` 读 game-data（`hpBonus`）

**modules/22elite-mingjiao.js**
- 新增 `getSkillParams` 导入
- 张无忌九阳神功读 game-data
- 张无忌乾坤大挪移读 game-data
- 韦一笑寒冰吸血/残血闪避读 game-data
- 小昭姐妹全部技能读 game-data
- 版本号 V5.3.1 → V5.3.2

**ui/32ui-render-5v5-test.js**
- 详情弹窗全部精英技能描述改为 `getSkillDesc`
- 版本号 V5.3.2 → V5.3.3

### 二、代码重构

**ui/32ui-render-5v5-test.js**
- 详情弹窗攻防显示 HTML 拼接抽成两个纯函数：`renderAtkDetail(u, buffStats, ctx)` 和 `renderDefDetail(u, buffStats)`
- 详情弹窗内两处立即执行函数替换为函数调用

### 三、Bug 修复

**core/10battle-attack.js**
- 小昭妹妹飞天误触发：`beforeDamageApply` 信号从扣血后移到扣血前发射，新增 `hpBefore` 字段
- 飞天后仍行动：入口拦截 `_spiderFlying` 时增加日志输出
- 概率连击：`afterAttack` 发射后检查 `data.retry`，true 则递归调用 `processUnitAttack`

**core/12battle-attack-steps.js**
- 战士斩杀声明在 `resolveAfterDamageEffects` 中没有被处理，新增 `execute` 类型处理逻辑

**core/04buff-system.js**
- 战士斩杀声明缺少 `target` 和 `source` 字段，补上
- 流星赶月 splash 声明缺少 `attackerUid`、`primaryUid`、`splashUids`、`splashDmg`，补上

**player/26player-core.js**
- 倍速高亮消失：快进恢复时同步写 `GlobalStore.set('speed', restored)` 并调 `updateSpeedButtons`
- 战斗结束后清 `_scheduler`，防止残留 `setSpeed(50)` 影响下一局
- 战报角色不全：改用 `c.snapshot` 初始完整名单合并 `finalStep` 的最终状态
- 保存 `_originalSnapshot` 供原班再战使用

**player/28event-handlers.js**
- 妹妹蛛落日志在特效播放前写入并滚动，不再等特效结束

**ui/38ui-controls.js**
- 全自动模式卡调整站位：`GAMEOVER` 三个重置分支（btnMain下一关、btnNext原班再战、btnSettle随机重开）补 `setState.adjustMode(false)`
- 原班再战改为读 `c._originalSnapshot` 恢复初始阵容
- 滚动降速恢复后强制设 `manualSpeedLock = true, manualSpeedValue = 600`

**ui/33main-state.js**
- `setState.speed` 写入后自动调 `window.updateSpeedButtons()`

### 四、机制调整

**ui/35main-battle.js**
- 精英出场率重做：80% 至少一个精英、30% 双精英、5% 三精英、姐妹互斥
- 精英独立占坑扣预算，剩余坑位按战斗力补普通单位
- 删除废弃的 `forcedElite` 相关代码

**modules/15elite-skills.js**
- 妹妹首回合蛛变：首次变时 `availableRoles` 排除当前职业，不再随机到相同的

---

## V5.3.5 — 2026-08-06

### window 挂载与 gs 双轨收口

**目的**：清理全局 `window` 挂载的临时通道，以及 `GlobalStore` 与旧 `gs` 并存的双轨状态，全部改为显式 import / 统一 `GlobalStore` 读取。

**已完成：**

1. **`window._emitEvent` → 显式 import**
   - modules 层不再读取 `window._emitEvent`，改为直接 `import { emitEvent } from '../core/00-event-bus.js'`

2. **`window._dodgeRules` → 导出函数**
   - 新增 `getDodgeRules()` 导出，调用方直接 import 使用，不再依赖 `window` 挂载

3. **`window._triggerFX` → 显式 import**
   - player 层不再读取 `window._triggerFX`，改为直接 import `_triggerFX`

4. **gs 双轨 → 统一 `GlobalStore`**
   - 所有旧 `gs` 读取点收口为 `GlobalStore`
   - 需要兼容旧代码处统一通过 `getState.gs()` 读取，不再直接访问全局 `gs`

---

## V5.3.4 — 2026-08-06

### 攻击后效果走声明体系、Unit 组件化、FSM、播放器分层、回放系统与 Bug 修复

**一、收口——攻击后效果走声明体系**

| 效果 | 文件 | 改动 |
|------|------|------|
| 远程成长 | `03battle-utils.js` | `applyStatChange` → 提交 `statChange` 声明 |
| 坚盾被攻击 | `03battle-utils.js` | `tryFortify` 新增 `skipStatChange` 参数，被攻击时提交声明 |
| 九阳回血 | `22elite-mingjiao.js` | `applyStatChange` → 提交 `heal` 声明 |
| 融会贯通 | `22elite-mingjiao.js` | `applyStatChange` → 提交 `bonusDmg` 声明 |
| 白骨爪联动回复 | `21elite-sixsects.js` | 函数内建临时声明数组，循环结束统一执行 |
| 闪避反击伤害+眩晕 | `12battle-attack-steps.js` | 直接改字段 → 推入 `dodgeDeclarations`，由 `resolveDodgeEffects` 执行 |

**二、Unit 组件化——运行时状态迁移到 `state` 字典**

| 内容 | 文件 |
|------|------|
| 新增 `this.state = {}` 含 14 个运行时标记 | `02unit.js` |
| `clone()` 改为 `c.state = { ...this.state }` | `02unit.js` |
| 全局替换 `unit._acted` → `unit.state._acted` 等 | 全部核心文件 |

**三、FSM——关键单位显式状态机**

| 内容 | 文件 |
|------|------|
| 新建 `StateMachine` 类 | `06-fsm.js` |
| 张无忌（ranged/switching/near/ronghui） | `22elite-mingjiao.js` |
| 小昭姐（normal/attaching/attached/returning） | `22elite-mingjiao.js` |
| 小昭妹（normal/transforming/flying/descending/dead） | `22elite-mingjiao.js` |
| 引擎层 FSM 状态判断适配 | `11battle-round.js`、`10battle-attack.js`、`14ui-render.js`、`03battle-utils.js`、`19battle-store.js` |

**四、播放器分层——DOM 操作收口到渲染层**

| 内容 | 文件 |
|------|------|
| 新建 `29renderer.js`（8 个渲染函数） | `29renderer.js` |
| `26player-core.js` 所有 DOM 操作改为调 renderer | `26player-core.js` |
| `28event-handlers.js` 所有 DOM 操作改为调 renderer | `28event-handlers.js` |

**五、回放系统——种子模式 + UI 入口**

| 内容 | 文件 |
|------|------|
| 新增 `startRecordingWithSeed`、`replayFromSeed` | `23replay.js` |
| 播放器接入录制 | `26player-core.js` |
| 战报弹窗加"下载回放"按钮 | `34main-dialogs.js` |
| 日志工具面板加"导入回放" | `38ui-controls.js` |
| 删除之前错误添加到主按钮区的导入按钮 | `31main-5v5-test.js` |

**六、Bug 修复**

| Bug | 修复 |
|------|------|
| `resolveAttackHit` 缺少 `rng` | 函数开头加 `const rng = getBattleRng()` |
| `ReplayManager` 未加载 | `26player-core.js` 加 `import '../modules/23replay.js'` |
| Store 写入路径不一致导致飞撞绿格、行动后不回灰 | `battleReducer` 的 `SET_VISUAL`、`hp-change`、`unit-add` 三个写入路径从顶层收口到 `state` 字典 |

**验证方法**：回放、飞撞绿格、行动后灰格、小昭姐妹状态、攻击后效果声明、控制台无报错。

---

## V5.3.3 — 2026-08-06

### 小昭姐妹状态转换纳入声明→裁定模式

**目的**：解决待办 `待办-bug待修.md` 中「小昭妹妹飞天时机不对」「姐姐系列问题」两个 Bug。根因是姐妹的状态转换（附身/飞回/飞天/蛛落）由组件直接执行，时机分散在多个生命周期信号中，缺乏统一裁定。

**架构变化**：新增 `beforeStateTransition` 信号（`core/00-event-bus.js`），姐妹组件各自暴露四个 `execute*` 裁判接口，事件监听器只提交声明不再直接执行，由 `core/11battle-round.js` 的裁判统一收集声明并裁定调用。

**文件改动**：

1. **`core/00-event-bus.js`**
   - `EXECUTION_LAYER` 新增 `BEFORE_STATE_TRANSITION` 层级，含 `BUTTERFLY_ATTACH`、`BUTTERFLY_RETURN`、`SPIDER_FLY`、`SPIDER_DESCEND` 四个信号优先级（均为 10）

2. **`modules/22elite-mingjiao.js`**
   - 姐姐组件：新增 `executeAttach(A, log)`、`executeReturn(sister, A, log)` 方法（从原 `onBeforeFirstAttack`/`onRoundEnd`/文件底部独立函数迁入）。`beforeFirstAllyAttack` 改为提交 `{ type: 'butterflyAttach' }` 声明。`onRoundEnd` 改为提交 `{ type: 'butterflyReturn' }` 声明
   - 妹妹组件：新增 `executeFly(unit, incomingDmg, A, log)`、`executeDescend(unit, A, B, log)` 方法（从原 `onBeforeDeath`/`onRoundEnd` 迁入）。`beforeDamageApply` 飞天触发改为仅提交免疫声明（触发时机判断保留在组件内）。`onRoundEnd` 改为提交 `{ type: 'spiderDescend' }` 声明
   - 删除文件底部独立的 `butterflyReturn` 导出函数

3. **`core/11battle-round.js`**
   - 版本号升级 V5.3.3
   - 组件注册改为单例引用（`sisterComp`/`brotherComp`）
   - `resolveActionOrder` 新增状态转换裁定段：发射 `beforeStateTransition` 信号收集声明，按顺序调用 `executeAttach`/`executeReturn`/`executeFly`/`executeDescend`
   - 回合循环中 `onRoundEnd` 发射后新增声明收集→裁定执行段，处理 `butterflyReturn`/`spiderDescend`

### 状态变更收敛（引擎层）

**新增 `applyStatChange` 统一入口**
- 文件：`core/13battle-shared.js`
- 所有 `hp`、`atk`、`def` 的增量修改统一走此函数，禁止组件直接赋值
- 入口内部负责：变更执行 → 溢出保护 → 死亡标记 → UI 事件推送

**新增 `applyMaxHpChange` 统一入口**
- 文件：`core/13battle-shared.js`
- `maxHp` 变更时自动等比缩放当前血量
- 同理统一推送 UI 事件

**收敛范围**
- 闪避反击伤害、拒马反伤、乾坤反弹自伤/反击、玄冥中毒、白骨爪/斩杀、升级版乾坤反弹、坚盾叠加、苦练加成、远程成长、姐姐附身/飞回、carry 激活/清除、光环加成、流星赶月降防 → `applyStatChange`
- 韦一笑吸血上限、carry 血上限提升/清除/归位、姐姐附身/飞回血上限、苦练血上限、小昭永久 carry 血上限 → `applyMaxHpChange`

**死亡结算统一**
- `resolveDeaths` / `finalizeDeaths` 改用 `applyStatChange` 执行死亡
- 融会贯通击杀 → 走 `_pendingDeath` → `resolveDeaths` 统一裁定
- 姐姐无宿主自杀 → 走 `_pendingDeath` → `resolveDeaths` 统一裁定

### 裁判日志拼接抽离

**抽离范围**
- `resolveAfterDamageEffects`：裁判只执行吸血/回血/溅射/反弹，不再拼日志，调用方拿到执行清单后自己拼
- `resolveAttackHit`：裁判只做未命中/闪避判定，返回纯数据给调用方拼日志
- `applyPostAttackEffects`：裁判只触发 buff 效果，不推 `reboundEntry` 日志，由调用方推
- `buildAttackGroup`：接管 `rawFormula` 伤害公式拼接
- `calcFinalDamage`：不再拼 `rawFormula`，只返回计算数据

**涉及文件**：`core/12battle-attack-steps.js`、`core/10battle-attack.js`、`core/11battle-round.js`

### 执行层级显式化

**新增 `EXECUTION_LAYER` 常量**
- 文件：`core/00-event-bus.js`
- 按生命周期信号（回合开始/攻击前/攻击后/死亡等）和子阶段（吸血/回血/溅射/反弹/斩杀等）定义层级
- 所有文件中 `eventBus.on('signal', 数字, ...)` 的魔法数字替换为 `L.SIGNAL.STAGE` 语义化常量

**涉及文件**：`core/03battle-utils.js`、`core/04buff-system.js`、`core/11battle-round.js`、`modules/20elite-imperial.js`、`modules/21elite-sixsects.js`、`modules/22elite-mingjiao.js`

### 状态转换拦截优化

- 行动调度 `resolveActionOrder` 改为按站位逐个判定，日志顺序和格子顺序一致
- 行动调度新增飞天/附身状态直接排除，不依赖 `_acted` 标记
- 宋青书性奋额外攻击新增递归防护，检查 `_xingFenExtraAttacking` 标记阻止死循环

### 遗留清理

- `selectAttackTarget` 中成昆伪装清除逻辑从裁判迁入成昆组件 `modules/20elite-imperial.js`
- 删除无用别名 `calcAttackDamage`
- 删除 `buildAttackGroup` 中无条件的 `-30%` 伤害公式显示

### 双通道收敛

- 目的：消灭仍绕过 `applyStatChange`/`applyMaxHpChange` 的直接状态赋值，全部统一走裁判入口。
- **文件**：`modules/22elite-mingjiao.js`、`modules/21elite-sixsects.js`
- 已收敛项：
  - 九阳神功回血
  - 白骨爪联动回复
  - 新婚扣血
  - 性奋代价（含重复缩放删除）

### 去名字化

- 目的：引擎层不再硬编码角色名做逻辑判断。
- **文件**：`core/03battle-utils.js`、`core/02unit.js`、`core/11battle-round.js`、`core/01config-5v5-test.js`
- 改动：
  - `03battle-utils.js`：坚盾增量/上限改为读 `_fortifyIncrement`/`_fortifyCap` 字段
  - `02unit.js`：新增两个字段及默认值，`clone()` 同步拷贝
  - `11battle-round.js`：成昆初始化时设置坚盾字段
  - `01config-5v5-test.js`：新增 `FORTIFY_INCREMENT`/`FORTIFY_CAP` 配置项

### 闪避声明化

- 目的：闪避后效果（反击伤害、眩晕、韦一笑吸血）从裁判内联改为声明收集→裁判统一执行。
- **文件**：`core/12battle-attack-steps.js`、`modules/22elite-mingjiao.js`、`core/00-event-bus.js`
- 改动：
  - `12battle-attack-steps.js`：`resolveAttackHit` 改为发射 `onDodge` 信号收集声明，新增 `resolveDodgeEffects` 裁判函数
  - `22elite-mingjiao.js`：韦一笑闪避吸血迁入 `onDodge` 监听器
  - `00-event-bus.js`：`EXECUTION_LAYER` 新增 `ON_DODGE` 层级

### 数值收敛

- 目的：散落各文件的硬编码数值迁入 `CONFIG`。
- **文件**：`core/01config-5v5-test.js`、`core/12battle-attack-steps.js`、`core/03battle-utils.js`
- 新增配置项：
  - `DODGE_REBOUND_RATIO`：闪避反击比例
  - `WARRIOR_BREAK_DEF`：战士破防值
  - `RANGED_GROWTH_ATK`：远程成长值
  - `TOKEN_DROP_RATES`：圣火令掉落率
  - `CHEST_DROP_RATE`：宝箱掉落率
- 已替换位置：
  - `12battle-attack-steps.js`：闪避反击比例、圣火令/宝箱掉落率、拒马反伤日志
  - `03battle-utils.js`：战士破防值、远程成长值

### JSDoc 补充

- 目的：核心引擎函数加类型注释。
- **文件**：`core/12battle-attack-steps.js`、`core/10battle-attack.js`、`core/11battle-round.js`
- 已补充函数：
  - `12battle-attack-steps.js`：`resolveAttackHit`、`calcFinalDamage`、`applyAttackResult`、`buildAttackGroup`、`resolveAfterDamageEffects`、`resolveDodgeEffects`
  - `10battle-attack.js`：`processUnitAttack`
  - `11battle-round.js`：`createRoundStepper`

### 本次 commit message

```
feat: 双通道收敛 + 去名字化 + 闪避声明化 + 数值收敛 + JSDoc

- 双通道收敛：九阳回血/白骨爪联动/新婚扣血/性奋代价改为 applyStatChange/applyMaxHpChange
- 去名字化：坚盾不再硬编码'成昆'，改读 Unit 字段 _fortifyIncrement/_fortifyCap
- 闪避声明化：resolveAttackHit 闪避后效果改为 onDodge 信号收集声明，新增 resolveDodgeEffects 裁判，韦一笑吸血迁入监听器
- 数值收敛：DODGE_REBOUND_RATIO/WARRIOR_BREAK_DEF/RANGED_GROWTH_ATK/TOKEN_DROP_RATES/CHEST_DROP_RATE/FORTIFY_INCREMENT/FORTIFY_CAP 迁入 CONFIG
- JSDoc：12battle-attack-steps.js 六个核心函数 + 10battle-attack.js processUnitAttack + 11battle-round.js createRoundStepper 加类型注释
```

### Bug 修复

- 修复 `carry` 清除时增量丢失（先清零后取负值导致 delta = 0）
- 修复 `buildAttackGroup` 解构遗漏 `raw`、`bonusDmgTotal`、`dmgMultiplier` 导致公式显示 `undefined`
- 修复 `applyStatChange`、`applyMaxHpChange` 重复导出语法错误
- 修复 `20elite-imperial.js`、`22elite-mingjiao.js` 缺少 `EXECUTION_LAYER` 导入
- 修复 `registerRangedGrowth` 函数内误写 `import` 语句
- 修复行动循环 `while` 条件使用 `allyIdx/enemyIdx` 导致第一回合末尾卡死
- 修复回合结束 / 战斗结束产生两次分隔符

### 五、commit message

```
feat: 精英技能迁入game-data + 修复飞天/连击/倍速/战报/原班再战/流星赶月/斩杀等Bug

- 全角色技能参数迁入 content/64game-data.json，97/99/23/14ui 接入 getSkillParams
- 详情弹窗攻防显示抽纯函数 renderAtkDetail/renderDefDetail
- 修复小昭妹妹飞天误触发（beforeDamageApply 移到扣血前）
- 修复小昭妹妹飞天后仍行动（入口加强 _spiderFlying 拦截）
- 修复概率连击：afterAttack 信号增加 retry 递归调用
- 修复战士斩杀未生效（resolveAfterDamageEffects 新增 execute 处理）
- 修复倍速高亮消失：快进恢复/滚动降速/战斗结束多方同步 updateSpeedButtons
- 修复全自动模式卡调整站位：GAMEOVER→IDLE 时 reset adjustMode
- 修复战报角色不全：改用 snapshot 初始名单合并最终状态
- 原班再战恢复初始阵容：播放器保存 _originalSnapshot
- 精英出场率重做：80%至少一出 30%双精英 5%三精英 姐妹互斥
- 妹妹首回合蛛变不再随机到当前职业
- 流星赶月 splash 声明补 attackerUid/primaryUid/splashUids
- 妹妹蛛落日志在特效播放前写入并滚动
```

## V5.3.3 — 2026-08-03

### 裁判组声明→裁定模式全面落地
- **核心思路**：组件只提交声明，裁判统一裁定并执行。组件在整个攻击流程中不再直接修改状态，引擎是唯一的状态写入者。解决组件之间隐式交互、依赖 priority 数字抢跑、多个受击拦截无法裁定谁先生效、死亡立即移除导致后续效果操作已死单位等问题。
- **死亡判定变更**：所有死亡判定从 `alive = false` 改为 `_pendingDeath = true`，伤害应用后不立即判死，所有攻击后效果完成 → 统一扫描 → 发射 `onBeforeDeath` → 执行死亡 → 移除。

### 裁判组架构（七个边裁）
| 边裁 | 函数 | 职责 |
|------|------|------|
| 行动调度 | `resolveActionOrder` | 裁判感知（被遮挡/眩晕/拒马）+ 声明收集（priority/skip/pass）+ 冲突裁决（priority 高优先，同 priority 按 pos 排） |
| 目标选择 | `selectAttackTarget` | 通用规则（近战打前排/远程随机/飞行残血）+ 组件声明（宋青书打最高血/成昆伪装/小昭惑心）+ 裁判校验（目标存活/可选中） |
| 闪避计算 | `_dodgeRules` 规则注册表 | 通用规则（飞行15%/地面3%）引擎初始化注册 + 精英规则（韦一笑动态闪避）回合开始注册 + 攻击时遍历汇总 |
| 伤害计算 | `calcFinalDamage` | 一次性声明收集（破防/忽略防御/附加伤害/增伤倍率）→ 裁判统一结算，不再互相覆盖 |
| 伤害免疫 | `resolveDamageImmune` | 收集免疫声明 `{ immune, reason }` → 裁判裁定（当前规则：任一声明即免疫，预留多声明冲突时按 priority 选） |
| 死亡结算 | `resolveDeaths` | 伤害应用后不立即判死，标记 `_pendingDeath` → 所有攻击后效果完成 → 统一扫描 → 发射 `onBeforeDeath` → 执行死亡 → 移除 |
| 攻击后效果 | `resolveAfterDamageEffects` | 收集吸血/回血/溅射/反弹声明 → 裁判按吸血→回血→溅射→反弹→其他的顺序统一执行 |

### 精英/海克斯声明迁移清单
| 效果 | 来源 | 声明类型 | 接入信号 |
|------|------|----------|----------|
| 战士破防 | 通用 | `breakDef` | `beforeDamageCalc` |
| 战士斩杀 | 通用 | `execute` | `afterDamageApplied` |
| 嗜血狂刀吸血 | 海克斯 | `leech` | `afterDamageApplied` |
| 热血奋战回血 | 海克斯 | `heal` | `afterDamageApplied` |
| 乘风突袭波及 | 海克斯 | `splash` | `afterDamageApplied` |
| 流星赶月溅射 | 海克斯 | `splash` | `afterDamageApplied` |
| 严阵以待反弹 | 海克斯 | `rebound` | `afterDamageApplied` |
| 韦一笑吸血 | 精英 | `leech` | `afterDamageApplied` |
| 韦一笑动态闪避 | 精英 | 规则注册 | `onRoundStart` |
| 小昭妹飞天免疫 | 精英 | `immune` | `beforeDamageApply` |
| 小昭妹跳过行动 | 精英 | `skip` | `beforeActionSelect` |
| 小昭姐跳过行动 | 精英 | `skip` | `beforeActionSelect` |
| 苦练优先行动 | 精英 | `priority` | `beforeActionSelect` |
| 宋青书叛逆目标 | 精英 | `targetResult` | `beforeSelectTarget` |
| 宋青书叛逆真伤 | 精英 | `bonusDmg` | `beforeDamageCalc` |
| 成昆幻影伪装 | 精英 | `targetResult` | `beforeSelectTarget` |
| 成昆霹雳劲 | 精英 | `bonusDmg` | `beforeDamageCalc` |
| 鹤笔翁鹿角杖法 | 精英 | `ignoreDef`/`dmgMultiplier` | `beforeDamageCalc` |
| 小昭永久惑心 | 精英 | `targetResult` | `beforeSelectTarget` |

### 精英技能归属原则（V5.3.3）
- **引擎保留**：通用战场规则——遮挡判定、拒马生成/销毁、属性重置、圣火令行列生成、Buff 统计日志、胜负判定。引擎是裁判，不识别任何具体精英名字。
- **组件声明**：特定精英角色的行为逻辑全部改为监听对应信号提交声明——苦练优先行动、附身/飞天/蛛落、玄冥中毒、快乐回血、性奋授予、九阴白骨爪、玄冥联动、成昆幻影、叛逆突袭——裁判统一裁定，组件不直接修改状态。
- **边裁裁定**：每个攻击环节（行动调度、目标选择、闪避、伤害计算、伤害免疫、死亡结算、攻击后效果）由独立边裁负责，声明收集→冲突裁决→统一执行，组件完全不知道裁决规则。

### 涉及文件
- `core/11battle-round.js` — 新增 `resolveActionOrder` 行动调度边裁，行动循环重构
- `core/10battle-attack.js` — 免疫拦截改为声明收集，攻击后效果统一结算，死亡边裁调用
- `core/12battle-attack-steps.js` — `selectAttackTarget` 重构、`calcFinalDamage` 重构、新增 `resolveDeaths`/`resolveDamageImmune`/`resolveAfterDamageEffects`、闪避规则注册表
- `core/03battle-utils.js` — 战士破防改为提交 `breakDef` 声明
- `core/04buff-system.js` — 嗜血狂刀/热血奋战/乘风突袭/流星赶月改为声明提交，新增斩杀声明
- `modules/22elite-mingjiao.js` — 韦一笑吸血声明+动态闪避注册、乾坤反弹 `_pendingDeath`、小昭妹飞天免疫声明、小昭永久惑心声明
- `modules/21elite-sixsects.js` — 宋青书叛逆目标+真伤声明、新婚/性奋 `_pendingDeath`、白骨爪 `_pendingDeath`
- `modules/20elite-imperial.js` — 成昆霹雳劲声明+伪装目标声明、鹤笔翁鹿角杖法声明

### 补充修复与清理
- **闪避规则不再无限增长**：每回合先清除上一轮注册的精英闪避规则，再重新注册，避免规则重复累积导致闪避无限叠加
- **韦一笑死代码删除**：清理确认无引用的死代码
- **流星赶月主箭迁入声明模式**：主箭增伤迁入 `bonusDmg` 声明
- **成昆回血迁入声明模式**：成昆回血迁入 `heal` 声明
- **resolveDeaths 覆盖所有路径**：回合循环内 + 回合结束兜底双路径，确保 `_pendingDeath` 单位必定结算
- **普通飞行目标选择恢复**："残血优先，否则前排随机"，回归通用层规则
- **光环改为纯函数**：`getAuraBonuses` 实时计算，不再依赖字段存储，避免状态过期
- **carry 逻辑抽离**：`applyCarryBonus` 独立成函数
- **乾坤反弹迁入声明模式**：统一走攻击后效果边裁
- **死 import 清理**：移除未使用的 import 引用
- **保留不做迁移**：小昭姐乾坤衍生、坚盾、远程成长——仅改自身或友方属性，与其他组件无冲突，迁移收益为零

### 后续工作
- **回归测试**：逐关验证基础战斗→海克斯效果→明教精英→六大派精英→全阵容压力测试
- **被动光环拉取化**：被动光环从"引擎推送"改为"引擎拉取"模式
- **裁判校验准则显式化**：输出声明规范文档，明确各边裁的校验与回退准则

---

## V5.3.2 — 2026-08-02

### 战士破防数值调整
- **破防削减值 -3 → -2**：战士破防触发时削弱目标防御从 3 点降为 2 点，概率触发机制（目标当前防御 × 2.5%，封顶 100%）不变
- 涉及文件：`core/03battle-utils.js`（`registerWarriorBreakDefense`）

### 防战坚盾三项 Bug 修复
- **成昆 +2 溢出修复**：坚盾触发前先检查"本次增量 + 本回合已叠加量"是否超限，超限则不触发，解决成昆 +2 跳过上限校验的溢出问题
- **坚盾防御下回合丢失修复**：坚盾增加的防御同步写入 `_baseDef`，确保回合重置后防御值不丢失，坚盾叠加永久保留
- **详情面板显示减半修复**：详情弹窗中坚盾防御加成显示与实际叠加量一致，修复此前显示值仅为实际值一半的问题
- 涉及文件：`core/03battle-utils.js`（`registerFortifyShield`）、`ui/32ui-render-5v5-test.js`

### 防战攻击触发坚盾
- **攻击 100% 触发**：防战每次攻击命中后 100% 概率叠加坚盾，与被攻击时 60% 概率触发共享同一每回合上限（普通防战 3 点）
- 坚盾攻防双通道触发，加快防战叠甲速度
- 涉及文件：`core/03battle-utils.js`（`registerFortifyShield` 中 `afterAttack` 监听）

### 圣火令即时刷新
- **战场格子即时刷新**：选择圣火令 Buff 后立即调用 UI 刷新，战场所有格子的攻防显示实时更新行列加成后的属性，无需等待下一回合
- 涉及文件：`ui/35main-battle.js`、`ui/32ui-render-5v5-test.js`

### 成昆专属坚盾强化
- **坚盾每回合上限提至 6**：成昆的坚盾每回合上限从 3 点提升至 6 点（普通防战仍为 3 点），配合每次 +2 的增量可在一回合内叠加 3 次
- **普攻 100% 触发坚盾**：成昆普攻命中时 100% 触发坚盾（与普通防战一致，作为防战通用机制的一部分）
- 涉及文件：`core/03battle-utils.js`（`registerFortifyShield`）

### 成昆混元霹雳劲伤害调整
- **已损失生命比例 30% → 20%**：成昆精英技能"混元霹雳劲"附加的真实伤害从已损失生命的 30% 降为 20%，降低成昆后期残局威胁
- 涉及文件：`core/01config-5v5-test.js`（`ELITE_SKILLS.phantomThunder.lostHpRatio`）、`ui/32ui-render-5v5-test.js`（技能描述文案）

### 文档同步
- 游戏设定版本 V5.2.1 → V5.3.2，同步破防值、坚盾机制、成昆强化、霹雳伤害比例
- 玩家指南同步四大职业表坚盾描述、新增圣火令即时刷新说明
- 更改履历新增 V5.3.2 条目

### 事件总线迁移完成（commit: feat: 事件总线迁移完成 — 精英技能全面解耦核心引擎）
- **新增 beforeActionSelect 信号**：替代引擎硬编码的苦练优先行动、小昭姐妹跳过、被遮挡/眩晕 pass
- **新增 onRoundStart 信号**：迁入苦练 buff、性奋授予、快乐回血、蛛变、玄冥中毒、永久 carry
- **新增 onRoundEnd 信号**：迁入姐姐飞回、张无忌变身、蛛落
- **新增 beforeFirstAllyAttack 信号**：迁入小昭姐蝶变附身
- **眩晕统一拦截**：47 入口 + 攻击后双点拦截，覆盖所有后续效果（联动/性奋/白骨爪/飞天/伪装）
- **registerEmptyColBonus 加成链改写字段**：不再绕过 computeBuffStats
- **修复 kuLianUnit 删除后阵营切换错误**
- **修复 98 中 emitEvent 重复声明**
- **清理 48 所有精英相关 import**
- 涉及文件：`core/11battle-round.js`、`core/10battle-attack.js`、`core/03battle-utils.js`、`modules/20elite-imperial.js`、`modules/21elite-sixsects.js`、`modules/22elite-mingjiao.js`

---

## V5.3.0 — 2026-07-28

### 事件总线架构（core/00-event-bus.js）
- **新增 EventBus 模块**：提供 `on(event, handler, priority)` / `emit(event, data)` / `clearAll()`，支持优先级排序
- **攻击流程信号化**：定义 9 个攻击生命周期信号——`beforeSelectTarget`、`beforeAttack`、`beforeDamageCalc`、`beforeDamageCalcFinal`、`beforeDamageApply`、`afterDamageApplied`、`allyDamaged`、`afterAttack`、`afterMiss`
- **通用职业效果注册化**：战士破防、远程成长、防战坚盾分别改为 `registerArmorBreak(eventBus)` / `registerRangedGrowth(eventBus)` / `registerFortify(eventBus)` 模式
- **海克斯效果注册化**：嗜血狂刀、热血奋战、乘风突袭、流星赶月、严阵以待反弹、惑人心智全部改为 EventBus 监听器
- **精英技能注册化**：玄冥二老联动、概率连击、宋青书性奋重试、小昭永久连击改为监听 `afterAttack`/`afterMiss` 信号
- 涉及文件：`core/00-event-bus.js`（新增）、`core/10battle-attack.js`、`core/12battle-attack-steps.js`、`core/14buff-effects.js`、`core/04buff-system.js`

### 精英组件按阵营合并
- **删除 9 个旧文件**：`modules/91elite-xiaozhao-brother.js` ~ `modules/99elite-zhangwuji.js`
- **新增 3 个阵营文件**：
  - `modules/22elite-mingjiao.js`：张无忌、韦一笑、小昭姐妹（蝶变附身 + 蛛变飞天）
  - `modules/21elite-sixsects.js`：宋青书、周芷若（苦练/性奋/新婚联动 + 九阴白骨爪）
  - `modules/20elite-imperial.js`：成昆、鹿杖客、鹤笔翁（幻影伪装 + 玄冥神掌联动）
- 每个组件暴露 `register(eventBus, A, B, log)` 函数，注册逻辑从 `11battle-round.js` 下沉
- 涉及文件：`modules/20elite-imperial.js`、`modules/21elite-sixsects.js`、`modules/22elite-mingjiao.js`（新增）、`core/11battle-round.js`

### 攻击管道精简
- **10battle-attack.js**：从 ~214 行缩减到 ~148 行（-31%），只保留五步骨架 + 信号发射
- 删除内联的 `applyRoleGrowth`、`applyExtraAttacks`、硬编码精英组件调用、空列/残血光环刷新
- **12battle-attack-steps.js**：删除硬编码的战士破防、防战坚盾、小昭姐乾坤衍生、乾坤反弹逻辑
- 涉及文件：`core/10battle-attack.js`、`core/12battle-attack-steps.js`

### core 层 window 耦合收敛
- `13battle-shared.js` 新增 `emitCoreEvent` 统一发送事件，优先走 `GlobalStore.pushBattleEvent`
- `10battle-attack.js`、`11battle-round.js`、`12battle-attack-steps.js`、`03battle-utils.js`、`14buff-effects.js`、`13battle-shared.js` 全部改为导入 `emitEvent`，不再直接访问 `window._emitEvent`
- 所有 `window._battleEvents` 直接操作改为 `GlobalStore.flushBattleEvents()`
- 涉及文件：`core/13battle-shared.js`、`core/10battle-attack.js`、`core/11battle-round.js`、`core/12battle-attack-steps.js`、`core/03battle-utils.js`、`core/14buff-effects.js`

### 精英组件通信改走 Unit 字段
- **02unit.js** 新增 `_linkedPartnerUid` 字段，用于精英角色间引用
- 战斗初始化时自动为宋青书↔周芷若、鹿杖客↔鹤笔翁建立引用，替换硬编码 `find(u => u.name === 'xxx')` 搜索
- 涉及文件：`core/02unit.js`、`core/11battle-round.js`

### Bug 修复
- **玄冥二老联动无限循环**：由 `_isLinkAttack` 标记阻止递归调用；初始不触发问题由精英钩子注册机制修复
- **小昭姐妹**：姐姐附身后格子不立即消失、本体仍可被攻击 → 附身成功后立即 flush 事件；妹妹飞天触发条件缺失 → 改为监听 `beforeDamageApply` 信号
- **双分隔符**：播放器 `handleInfo` 跳过空文本日志（仅用于传递事件的虚拟条目）
- **B 队坚盾不重置**：`11battle-round.js` 的 B 队 forEach 循环补上 `_fortifyThisRound = 0`
- **isXiaoZhao 废弃标记**：所有 `u.isXiaoZhao` 替换为 `(u.isXiaoZhaoSister || u.isXiaoZhaoBrother)`
- **复制按钮失效**：`copyLog` 函数被同名 DOM 元素 id 全局属性覆盖 → 改名为 `copyLogToClipboard`
- **圣火令首回合无 logo**：行列生成从 `applyHolyFlameBonus` 延迟执行改为 `createBuffObject` 中立即生成

### 战斗机制重做
- **战士破防重做**：概率 = 目标 def × 2.5%（封顶 100%），效果为 -3 防御
- **防战坚盾重做**：受击 60% 概率 +1 防御（成昆 +2/次），每回合上限 3，无全局上限
- **飞行突进目标选择**：新增 `selectFlyTarget` 函数，后排 > 中排 > 前排优先级，十字相邻攻击判定，路径不能穿越敌方
- **日志三态弹窗**：`detailMode`（布尔）改为 `logLevel`（'detailed'/'brief'/'debug'），调试模式输出信号追踪日志

### UI / 控制层重构
- **按钮绑定迁移**：`31main-5v5-test.js` 中约 150 行按钮事件全部迁移到 `38ui-controls.js`
- **Buff 槽更新迁移**：`updateBuffSlots` 从 `35main-battle.js` 移至 `38ui-controls.js`
- **复制日志迁移**：`copyLogToClipboard` 从 `31main-5v5-test.js` 移至 `30main-utils.js`
- **光带系统删除**：`initGlowSystem` 全部代码删除
- **圣火令行列生成提前**：从 `04buff-system.js` 移至 `35main-battle.js` 的 `createBuffObject`
- 涉及文件：`ui/38ui-controls.js`、`ui/31main-5v5-test.js`、`ui/35main-battle.js`、`ui/30main-utils.js`

### 文件变动统计
- **新增 4 个**：`core/00-event-bus.js`、`modules/20elite-imperial.js`、`modules/21elite-sixsects.js`、`modules/22elite-mingjiao.js`
- **删除 11 个**：旧精英组件 9 个 + `manifest.json` + `sw.js`（PWA 放弃）
- **大幅修改 ~10 个**：`10battle-attack.js`（-31%）、`11battle-round.js`、`12battle-attack-steps.js`（-11%）、`03battle-utils.js`（+196%）、`38ui-controls.js`、`31main-5v5-test.js`、`04buff-system.js`、`14buff-effects.js`、`13battle-shared.js`、`02unit.js`
- **轻微修改 ~7 个**：`06battle-engine-core.js`、`07battle-engine-5v5-test.js`、`26player-core.js`、`30main-utils.js`、`33main-state.js`、`35main-battle.js`、`15elite-skills.js`

### 事件总线重构遗留问题修复
- 补全缺失导出函数：`butterflyReturn`、`registerEmptyColBonus`、`registerXuanmingLink`
- 修复所有精英组件 EventBus 监听器方法引用错误
- 修复 `window._battleEvents` 残留引用和 `processUnitAttack` async 调用链
- 修复小昭姐妹格子即时刷新、职业图标同步、眩晕清除
- 修复首回合 Buff 生效、战报统计、附身攻击叠加
- 优化白骨爪回血日志合并、乾坤反弹/衍生飘字
- 调整姐姐附身转移比例 `1/2 → 1/3`，飞回血量纳入死亡队友
- 涉及文件：`core/00-event-bus.js`、`core/10battle-attack.js`、`core/12battle-attack-steps.js`、`core/14buff-effects.js`、`modules/15elite-skills.js`、`modules/20elite-imperial.js`、`modules/21elite-sixsects.js`、`modules/22elite-mingjiao.js`

### core 层反向依赖与状态管理重构
- **反向依赖修复**：`core/12battle-attack-steps.js` 删除 `buildAttackGroup` 末尾动态 `import('../fx/45fx-butterfly-spider.js')`；`player/26player-core.js` 在 `handleAttackGroup` 开头补上蝶蛛特效触发
- **文件重编号**：`core/50buff-effects.js` → `core/14buff-effects.js`；同步更新 `core/04buff-system.js` 和 `core/12battle-attack-steps.js` 的 import 路径
- **Store 拆分**：新增 `modules/19battle-store.js`，提取 `createStore`、`battleReducer`、`GAME_STATE_FIELDS`；`player/26player-core.js` 改为 import
- **状态管理统一**：`ui/33main-state.js` 的 `getState`/`setState`/`getPlayerContext` 全部改为从 `GlobalStore` 读写
- **播放器 handler 提取**：新增 `player/28event-handlers.js`，提取 9 个 handler 函数；`player/26player-core.js` 删除本地定义，改为 import
- **退役函数清理**：`checkXiaoZhaoPermanentDoubleStrike` 已确认死代码并删除；误删的 `canXingFenTrigger`/`consumeXingFen`/`applyPhantomDisguise`/`applyXiaoZhaoMindControl` 已恢复
- 涉及文件：`core/12battle-attack-steps.js`、`player/26player-core.js`、`core/04buff-system.js`、`core/14buff-effects.js`、`modules/19battle-store.js`、`ui/33main-state.js`、`player/28event-handlers.js`、`modules/15elite-skills.js`

## V5.2.1 — 2026-07-24

### 工程维护
- **工具路径同步更新**：文件复制器 `tools/49-toolkit.js` 的 `ALL_PROJECT_FILES` 与函数提取器 `tools/50-toolkit-more.js` 的 `TARGET_FILES` 同步调整精英角色模块路径，移除旧的 `modules/91elite-xiaozhao-brother.js` ~ `modules/99elite-zhangwuji.js`，新增注册 `modules/20elite-imperial.js`、`modules/21elite-sixsects.js`、`modules/22elite-mingjiao.js`，并新增注册 `core/00-event-bus.js`
- **工具箱版本号升级**：`tools/49-toolkit.js`、`tools/50-toolkit-more.js` 版本号由 V5.2.0 升级到 V5.2.1

## V5.2.0 — 2026-07-20 ~ 2026-07-23

### 工程维护
- **文件头部字节数校准**：统一检查并修正 56 个 JS/HTML/CJS 文件第二行注释中的 `~XXXXX bytes` 预估值，使其与当前实际文件大小保持一致；为原本缺少字节数的文件补全该字段
- **工具路径同步确认**：文件复制器 `tools/49-toolkit.js` 的 `ALL_PROJECT_FILES` 与函数提取器 `tools/50-toolkit-more.js` 的 `TARGET_FILES` 已包含 `core/50buff-effects.js`、`fx/45fx-butterfly-spider.js`、`modules/23replay.js`、`tools/51-shop.html` 等新增文件
- **换行符策略回退**：删除 `.gitattributes`，仓库级 `core.autocrlf` 恢复为 `true`，避免 Windows 本地反复出现 CRLF/LF 导致的假变更

### 蝶蛛双生系统：小昭姐妹拆分
- **小昭拆分为姐妹双形态**：姐姐（附身流）和妹妹（飞天流），在 `modules/15elite-skills.js` 中实现
- **姐姐·蝶变附身**（`butterflyAttach` / `butterflyReturn`）：每回合开始化为蝴蝶附身到队友身上，转移攻/防/血上限给宿主；回合结束飞回原位，恢复属性并接受宿主返还的一半生命。附身期间自身不可选中、不可攻击。
- **妹妹·蛛变飞天**（`spiderTransform` / `spiderFlyCheck` / `spiderLand`）：每回合随机变职业（不重复），记录精通；受致命伤时化为蜘蛛飞天免疫本次伤害（每场限 2 次），回合结束落下并攻击最近敌人，附带穿透 + 精通加成伤害。
- **新增特效模块 `fx/45fx-butterfly-spider.js`**：四段完整特效——
  - `showButterflyFlyOut`：🦋 蝴蝶从姐姐格子飞出，波浪轨迹飞向宿主，命中后粉色闪烁
  - `showButterflyFlyBack`：🦋 蝴蝶从宿主飞回姐姐原位，带正弦波动
  - `showSpiderAscend`：🕷️ 妹妹格子变紫色 + 克隆体 + 蜘蛛丝 + 蜘蛛图标缩小上升消失
  - `showSpiderDescend`：🕷️ 蜘蛛从上方沿丝线降下，落地紫色闪烁
- **播放器接入**（`player/26player-core.js`）：新增 `getButterflyFx()` 动态加载兼容函数（优先 `window` 全局、回退 `await import`），识别「🦋 蝶变」「🦋 飞回」「🕷️ 飞天」「🕷️ 蛛落」日志自动触发对应特效
- 涉及文件：`modules/15elite-skills.js`、`fx/45fx-butterfly-spider.js`、`player/26player-core.js`、`core/01config-5v5-test.js`、`core/02unit.js`

### 海克斯效果函数库（core/50buff-effects.js）
- 新增独立模块 `core/50buff-effects.js`，按身份拆分海克斯效果函数：
  - `_Normal`：普通团队版
  - `_Sister`：姐姐强化版（小昭姐在场时团队海克斯强化）
  - `_Brother`：妹妹永久版（团队海克斯过期后妹妹单独续上）
- 涵盖 9 类海克斯：嗜血狂刀、热血奋战、乘风突袭、流星赶月、严阵以待、流云身法、圣火令、Carry 加成、惑人心智
- 从 `core/04buff-system.js` 中提取效果执行逻辑，解耦 Buff 数值计算与效果触发
- 涉及文件：`core/50buff-effects.js`、`core/04buff-system.js`

### 回放系统（modules/23replay.js）
- 新增 `ReplayManager` 全局回放管理器：
  - `startRecording(snapshot)`：记录战斗初始快照
  - `pushStep(step, round, ally, enemy)`：每步推进时记录日志 + 事件 + 双方状态
  - `finishRecording(winner)`：标记胜者，触发下载按钮
  - `download(filename)`：导出 JSON 回放文件
  - `importFile(file)`：导入回放文件并自动开始重放
  - `startReplay(data)`：逐帧播放日志，支持速度控制
- 涉及文件：`modules/23replay.js`

### 飞撞特效优化（fx/41fx-crash-5v5-test.js）
- 飞走单位改用 `opacity` + `scale` 过渡替代 `removeChild`，避免 DOM 移除导致的布局抖动和残留问题
- 所有飞撞/格挡/闪避特效不再直接移除 DOM 元素

### 构建脚本更新（tools/46build-5v5.cjs）
- ROOT 路径修正为 `path.resolve(__dirname, '..')`（原为 `__dirname`，指向 tools/）
- MODULES 列表新增：`core/50buff-effects.js`、`fx/45fx-butterfly-spider.js`、`modules/23replay.js`
- VER_GLOBAL_MAP 同步新增对应映射

### 文件整理
- `00index.html` 重命名为 `index.html`，适配 GitHub Pages 默认入口
- 新增 `.gitattributes` 强制所有文本文件 LF 行尾，解决手机 acode 上 BOM 导致的 git 误判变更
- `README.md`、`game-design.md`、`kaifazhunze.md`、`to do list.md` 等 MD 文件 BOM 清理

### 版本号统一
- 全部 61 个文件版本号统一升级到 V5.2.0

## V5.1.0 — 2026-07-13 ~ 2026-07-20

### 版本与文档
- **版本号统一升级到 V5.1.0**：00index.html、mode-5v5-test.html、README.md、kaifazhunze.md、game-design.md 以及 core/01-07/47/48/49/50、fx/15-20、modules/23/24/28/46、player/08-11、tests/25/30/37/38/45/46/60-68、tools/27/32/33、ui/12-14/39-44 等 59 个文件全部从 V5.0.x 升级到 V5.1.0，覆盖战斗引擎、播放器、UI、特效、体检、工具链全链路
- **新增核心模块文件**
  - `core/10battle-attack.js`：攻击流程编排模块，负责按步骤调用目标选择、命中判定、伤害计算、结果应用、日志构建，并处理连击/性奋/联动等递归攻击
  - `core/11battle-round.js`：回合循环与生成器模块，从 `core/06battle-engine-core.js` 拆分，负责整回合的 buff 结算、拒马召唤、单位行动轮询与胜负判定
  - `core/12battle-attack-steps.js`：攻击步骤拆分模块，从 `core/10battle-attack.js` 拆分，提供 `selectAttackTarget`、`resolveAttackHit`、`calcFinalDamage`、`applyAttackResult`、`buildAttackGroup` 等纯步骤函数
  - `core/13battle-shared.js`：新增战斗共享工具模块，从 `core/06battle-engine-core.js` 提取 `emitEvent`、`emitFullUnitState`、`finalizeDeaths`、`getNextAvailableUnit`、`checkZhangSwitch`，解决 `06` 与 `48` 之间的循环依赖
  - `modules/18global-store.js`：新增全局状态管理模块，统一收敛原先散落在 `window._*` 上的全局变量
- **`game-design.md` 大更新**：新增小昭专属体系（蝶变、永久海克斯、海克斯强化对比表）、全局状态管理章节、行动规则补充 `_stunned` 处理、严阵以待/圣火令/热血奋战/巨马阵/流星赶月/乘風突袭等 Buff 数值与规则同步 V5.1.0
- **README.md 同步**：补充 V5.1.0 版本说明与开发准则更新

### 全局状态管理收敛（modules/18global-store.js）
- 新增 `GlobalStore`：`getState` / `setState` / `subscribe` / `on(key, fn)` / `effect(key, fn)` / `get(key)` / `set(key, value)`，支持按 key 订阅与副作用
- 收敛的全局变量包括：`fastForwardActive`、`voteScore`、`voteChoice`、`battleHasZhang`、`bugMode`、`crashMode`、`currentBattleState`、`battleStore`、`forceXiaoZhao`、`skipBuffPopup`、`battleEvents`、`gs`
- 兼容层已移除，统一通过 `GlobalStore.get/set` 读写状态；`window.GlobalStore = GlobalStore` 保留供旧代码无缝迁移
- 涉及文件：`modules/18global-store.js`、`ui/31main-5v5-test.js`、`ui/33main-state.js`、`ui/34main-dialogs.js`、`ui/35main-battle.js`、`ui/37fx-trigger.js`、`ui/38ui-controls.js`、`player/24player-text.js`、`player/25player-buff-ui.js`、`player/26player-core.js`、`fx/39fx-common-5v5-test.js`、`fx/40fx-arrows-5v5-test.js`、`fx/41fx-crash-5v5-test.js`、`modules/15elite-skills.js`、`core/04buff-system.js`、`core/11battle-round.js`、`core/12battle-attack-steps.js`

### 战斗引擎拆分（core/47/48/49）
- `core/10battle-attack.js` 职责收窄为“攻击流程编排”：不再内含具体步骤实现，改为 `import` `core/12battle-attack-steps.js` 中的 5 个步骤函数，并负责连击/性奋/玄冥二老联动/小昭永久连击等递归调用
- `core/12battle-attack-steps.js` 新增并导出：
  - `selectAttackTarget`：目标选择（含成昆幻影伪装、小昭永久惑人心智）
  - `resolveAttackHit`：未命中 + 闪避判定（含韦一笑吸血、眩晕、反击致死）
  - `calcFinalDamage`：伤害计算（战士破防、防战坚盾、混元霹雳、叛逆突袭、鹿角杖法、伤害修正）
  - `applyAttackResult`：应用伤害结果（击杀、战士斩杀、成昆幻影变身、拒马反伤、严阵以待反弹）
  - `buildAttackGroup`：构建 `attack-group` 日志并触发攻击后效果
- `core/11battle-round.js` 从 `core/06battle-engine-core.js` 拆出完整回合生成器 `createRoundStepper`，包含回合开始 buff 结算、玄冥毒、拒马召唤、苦练、概率连击、行动轮询、回合结束清理
- `core/06battle-engine-core.js` 与 `core/07battle-engine-5v5-test.js` 移除 `runBattle` 导出，播放器改为直接使用 `createRoundStepper` 逐步推进战斗
- 涉及文件：`core/10battle-attack.js`、`core/11battle-round.js`、`core/12battle-attack-steps.js`、`core/06battle-engine-core.js`、`core/07battle-engine-5v5-test.js`、`tools/47auto-battle-utils.js`、`ui/31main-5v5-test.js`、`ui/35main-battle.js`

### 战斗共享模块提取（core/13battle-shared.js）
- 新增 `core/13battle-shared.js`，从 `core/06battle-engine-core.js` 提取以下 5 个公共函数：
  - `emitEvent`：统一事件入队（兼容 `window._battleEvents` 与 `GlobalStore`）
  - `emitFullUnitState`：完整单位状态快照事件
  - `finalizeDeaths`：将 `hp <= 0` 的单位标记为死亡并发送 `hp-change` / `unit-remove`
  - `getNextAvailableUnit`：获取下一个未行动存活单位
  - `checkZhangSwitch`：张无忌前排无友军时切换近战形态
- `core/06battle-engine-core.js` 删除上述函数的内部实现与 `window._emitEvent` 挂载，改为从 `core/13battle-shared.js` 导入并重新导出；文件体积从约 22000 字节精简为入口/转发层
- 涉及文件：`core/13battle-shared.js`、`core/06battle-engine-core.js`、`core/11battle-round.js`

### 海克斯系统大更新（2026-07-17）
- **新增「小昭海克斯强化」机制**：小昭在场时，部分团队海克斯会获得强化效果；团队海克斯过期后，小昭可继承对应的永久弱化版。强化参数统一收敛到 `CONFIG.ELITE_SKILLS.xiaoZhao.hexEnhance`
  - 概率连击：普通 80% → 小昭强化 100% 必连击，且被遮挡单位可无视遮挡进攻
  - 你就是 carry：普通仅 5 号位 → 小昭强化 4/5/6 号位同步享受；过期后小昭自身固定两层精通加成
  - 流云身法：普通仅未行动单位闪避 → 小昭强化无视行动状态均闪避；过期后小昭自身永久闪避且无视职业
  - 圣火令：普通随机两列攻 + 两行防 → 小昭强化额外给自己 +30% 攻防；过期后每回合仅给自己 +30% 攻防
  - 巨马阵：普通 0/5/25 巨马 → 小昭强化 0/30/30 巨马，且攻击巨马者受 5 点反伤；过期后小昭每回合自己召一匹普通巨马
  - 流星赶月：普通溅射 50% → 小昭强化溅射命中后攻击者每命中 1 人额外 +2 攻；过期后小昭远程且拥有普通流星赶月时生效
  - 嗜血狂刀：普通战士吸血 80% → 小昭强化吸血后再补一刀，斩杀线 15% → 20%；过期后小昭战士且拥有普通嗜血时生效
  - 严阵以待：普通反弹 50% 伤害差 → 小昭强化反弹同时恢复等量生命；过期后小昭防战且拥有普通严阵时生效
  - 乘風突袭：普通飞行 80% 波及 / 60% 击退 → 小昭强化 100% 波及 / 80% 击退；过期后小昭飞行且拥有普通乘風时生效
  - 热血奋战：普通恢复已损失 15%、每 3 次翻倍 → 小昭强化 20%、每 2 次翻倍；过期后小昭单独拥有强化版
  - 惑人心智：普通 80% 乱敌方 / 40% 乱己方、持续 2 回合 → 小昭强化 95% / 50%、持续 3 回合；过期后小昭自身 15% 永久惑心
- **圣火令重构**：从原来的单 `col`/`row` 改为 `cols`/`rows` 数组（随机两列攻 + 两行防），`logBuffSummary` 与 `computeBuffStats` 同步支持多列多行；选择海克斯时 `holyFlame` 自动生成不重复的 `cols`/`rows`
- **严阵以待**：首次选择海克斯时不再出现，必须等待已有 Buff 剩余回合 > 0 后才可刷出
- **巨马阵**：属性改为 0 攻 / 5 防 / 25 血；小昭强化时改为 0/30/30；召唤位置不再考虑敌方站位，使用 Fisher-Yates 洗牌保证真随机
- **热血奋战**：普通版恢复已损失生命 15%，每 3 次翻倍；小昭强化版 20%，每 2 次翻倍
- **乘風突袭/流星赶月/惑人心智/严阵以待/你就是 carry**：全部接入 `getXiaoZhaoHexEnhance` 读取强化参数
- 涉及文件：`core/01config-5v5-test.js`、`core/04buff-system.js`、`core/05battle-horse.js`、`modules/15elite-skills.js`、`ui/35main-battle.js`、`ui/32ui-render-5v5-test.js`

### 倍速逻辑调整
- 速度档位数值全面调整（延迟值）：原 `500/250/71/125/1800` → 新 `1000/600/100/300/1600`
  - 0.5x：`1800ms` → `1600ms`
  - 1x：`500ms` → `1000ms`（取消高亮任何按钮）
  - 2x：`250ms` → `600ms`（默认进入战斗后自动锁定 2x 高亮）
  - 4x：`125ms` → `300ms`
  - 7x 按钮改名为 8x，延迟 `71ms` → `100ms`
- 快进状态统一走 `GlobalStore.get/set('fastForwardActive')`，替换所有 `window._fastForwardActive` 直接读取
- 滚动降速逻辑同步新阈值：`1800` → `1600`
- 涉及文件：`ui/38ui-controls.js`、`player/26player-core.js`、`player/24player-text.js`、`fx/39fx-common-5v5-test.js`、`fx/40fx-arrows-5v5-test.js`、`ui/37fx-trigger.js`、`ui/31main-5v5-test.js`

### 攻击弹窗位置确认与优化
- 新增/优化 `showAtkBuffFloat`（`fx/39fx-common-5v5-test.js`）：飘字内容从「攻+N」改为「+N」，水平位置左移、zIndex 提升到 `10004`，解决加攻弹幕被遮挡问题
- 播放器 `handleInfo` 识别「乾坤衍生：攻击+」日志，延迟 180ms 触发 `showAtkBuffFloat`；同时识别白骨爪触发的衍生加攻也弹出加攻弹幕
- 涉及文件：`fx/39fx-common-5v5-test.js`、`player/26player-core.js`

### UI 渲染优化（ui/13、ui/14、ui/39-44）
- **`ui/32ui-render-5v5-test.js`**
  - 角色图标增加眩晕状态 `💫`（未死亡时优先显示）
  - 防御显示改为基于 `_baseDef` 计算加成，解决坚盾叠加后防御显示异常
  - 成昆幻影伪装新增 `_phantomFlash` 闪烁动画
  - 详情弹窗宋青书技能描述改为读取 `CONFIG.ELITE_SKILLS` 实时数值
  - `spawnVictoryEffects` 胜利弹幕只在 `ctx.gs === 'GAMEOVER'` 时写入日志，修复战斗结束后错误追加台词
- **`ui/34main-dialogs.js`**
  - 战报弹窗增加防残留：游戏不在 `GAMEOVER` 状态时不创建弹窗
  - 关闭战报后改为隐藏并生成右下角浮动 📊 按钮，可恢复查看；游戏重置为 `IDLE` 时自动销毁
  - 投票选择改用 `GlobalStore.set('voteChoice', choice)`
- **`ui/35main-battle.js`**
  - 移除 `runBattle` 导入，战斗由播放器逐步驱动
  - `forceXiaoZhao`、`bugMode`、`battleHasZhang` 统一走 `GlobalStore`
  - 强制小昭替换入队时继承被替换单位的 `pos`，不再置为 `null`
  - 新增 `showBugModeBuffSelection`，Bug 模式下可选全部 Buff
  - 圣火令选择时生成 `cols`/`rows`
- **`ui/33main-state.js`**：`setState.gs` 写入时同步到 `GlobalStore.set('gs', v)`
- **`ui/37fx-trigger.js`**：`fastForwardActive` 走 `GlobalStore`
- **`ui/31main-5v5-test.js`**
  - 导入 `modules/18global-store.js`，移除 `runBattle` 导入
  - 所有 `window._voteScore`、`_voteChoice`、`_battleHasZhang`、`_debugMode`、`_crashMode`、`_forceXiaoZhao`、`_fastForwardActive`、`_skipBuffPopup` 改为 `GlobalStore`
  - 新增 `initBugAndXiaoZhaoModes` 并在 DOM 就绪前/后双保险执行
  - `GAMEOVER` 与重置时清理战报弹窗、浮动按钮、投票浮动按钮、Buff 浮动按钮、所有弹幕
  - 复制日志时支持 innerText 兜底、Buff 获取日志保留、过滤规则优化
- 涉及文件：`ui/31main-5v5-test.js`、`ui/32ui-render-5v5-test.js`、`ui/33main-state.js`、`ui/34main-dialogs.js`、`ui/35main-battle.js`、`ui/36audio-control.js`、`ui/37fx-trigger.js`、`ui/38ui-controls.js`

### Buff 系统修复（core/04）
- `computeBuffStats` 圣火令计算改为基于 `cols`/`rows` 数组，并接入小昭强化
- `carry` 加成位置条件放宽：原仅 5 号位，现当小昭在场时 4/5/6 号位均可享受
- 严阵以待防御加成仅对友方生效，避免敌方也吃到
- 流云身法闪避仅对友方生效
- `applyBuffEffectsBeforeAttack` 惑人心智概率在小昭在场且无 `mindControl_xiaoZhao` 标记时提升为 95% / 50%
- `applyBuffEffectsAfterAttack` 热血奋战、乘風突袭、流星赶月全部接入小昭强化参数
- 流星赶月溅射目标侧修正：当目标因惑心等效果与我方同阵营时，溅射正确作用到对应阵营
- 小昭永久圣火令直接给自己加攻防
- `logBuffSummary` 支持多列多行圣火令展示，并识别团队与小昭圣火令重叠
- 涉及文件：`core/04buff-system.js`

### 精英技能调整（modules/23）
- **九阴白骨爪**：使用 `GlobalStore.get('currentBattleState')` 判断张无忌是否存活；触发后调用 `GlobalStore.flushBattleEvents()`；无张无忌时小昭衍生技仅在小昭未眩晕时触发；攻击增益同时更新 `_baseAtk`
- **宋青书回血**：每次白骨爪触发时，宋青书恢复等量的生命（敌方阵营时找宋青书）
- **叛逆突袭**：真实伤害比例从 `10%` 提升到 `12%`（`CONFIG.ELITE_SKILLS.rebelStrike.currentHpRatio`）
- **性奋惩罚**：仅在 `_xingFenActive` 激活时扣减最大生命，且从第 1 次攻击就开始计数扣减
- **苦练**：属性加成改为 `+1.5 攻 / +0.5 防 / +2 血上限`（配置化）
- **新婚快乐**：回血百分比序列从 `[0.16, 0.08, 0.04, 0.02, 0.01]` 调整为 `[0.16, 0.10, 0.06, 0.03]`
- **小昭蝶变**：每回合随机变职业，但不会与上回合重复；记录精通职业并修正变身加成；每次变身 +5 生命上限
- **小昭衍生技**：张无忌在场时失效；小昭眩晕时不再触发；攻击增益更新 `_baseAtk`
- **成昆幻影伪装**：修复目标查找逻辑——被混淆的目标应在攻击者自己的阵营（`allySide`）中查找；被模仿者自身免疫该次伪装；攻击前清除旧的 `_phantomTarget`
- **小昭永久惑人心智**：触发概率从 `20%` 下调到 `15%`
- 新增 `getXiaoZhaoHexEnhance(allyTeam, activeBuffs, hexKey)` 统一读取海克斯强化配置
- 涉及文件：`modules/15elite-skills.js`、`core/01config-5v5-test.js`、`core/02unit.js`、`core/04buff-system.js`

### 单位与战斗状态
- `core/02unit.js`：新增 `_stunned` 字段标识本回合被闪避反击眩晕；`clone()` 同步拷贝
- `core/11battle-round.js`：行动轮询跳过 `_stunned` 单位并输出「被眩晕，无法行动」；被遮挡近战单位休息回血从 `10` 提升到 `20`；回合结束清理休息状态与定时器
- `core/12battle-attack-steps.js`：闪避反击成功后给攻击者设置 `_stunned = true`、`_acted = true`
- 涉及文件：`core/02unit.js`、`core/11battle-round.js`、`core/12battle-attack-steps.js`

### 播放器修复（player/09、player/10）
- `player/26player-core.js`
  - 恢复 `GAME_STATE_FIELDS` 常量，Store 同步 UI 时改为字段级 `syncFields`，不再整对象替换，解决状态字段遗漏
  - `playBattle` 改为基于 `c.snapshot` 启动，不再依赖 `c.UI.currentResult`
  - 注册 `GlobalStore.effect('fastForwardActive', ...)`，快进时把播放器速度提到 1ms、调度器 50 倍速；恢复时回到原速
  - `handleAttackGroup` 重要文本（战斗行、伤害行）保底常速 600ms，次要文本（波动、计算）按倍速 ×0.8；波动/计算/info/buff 行间增加 120ms 间隔防止连冲
  - 休息回血判断从「恢复 10」改为「恢复 20」
  - 闪避时不再把攻击者 `_acted` 强制设为 true，避免闪避后单位被错误标记为已行动
  - 战斗结束写日志、投票结算均增加 `c.gs === 'GAMEOVER'` 保护，防止战斗重开后残留写入
  - 投票积分、voteChoice、fastForwardActive 全部走 `GlobalStore`
- `player/25player-buff-ui.js`
  - `bugMode` 读取改为 `GlobalStore.get('bugMode')`
  - Buff 弹窗关闭后生成的浮动按钮在 `gs === 'IDLE'` 时自动销毁
  - `handleBuffLeech` 可在敌我双方中查找 `healUnitUid`
- `player/24player-text.js`
  - `playLineText` 增加 `forcedSpeed` 参数
  - 分隔符、系统信息、small 类提示文本直接显示不走逐字动画
  - 速度阈值调整为新倍速档位
  - 快进判断走 `GlobalStore`
- 涉及文件：`player/24player-text.js`、`player/25player-buff-ui.js`、`player/26player-core.js`

### 特效修复（fx/17、fx/19、fx/20、fx/15、fx/16、fx/18）
- `fx/41fx-crash-5v5-test.js`：`flyMode` 从 `window._crashMode` 改为 `GlobalStore.get('crashMode')`
- `fx/42fx-position-swap.js`、`fx/43fx-push-back.js`、`fx/44fx-dodge-bullet.js`：把原本依赖 `fx/15` 的 `getCellElement`、`wait` 改为本地定义，解除循环依赖；`wait` 的快进判断保留 `window._fastForwardActive` 本地兼容
- `fx/39fx-common-5v5-test.js`
  - 移除 `getCellElement` 与 `wait` 公共导出
  - `applyImpactShrink`、`showBoneClaw` 等使用 `GlobalStore.get('fastForwardActive')`
  - 加攻飘字位置、层级、文案优化
- `fx/40fx-arrows-5v5-test.js`：`showBoneClaw` 通过 `GlobalStore` 读取战场状态判断小昭是否在场
- 涉及文件：`fx/39fx-common-5v5-test.js`、`fx/40fx-arrows-5v5-test.js`、`fx/41fx-crash-5v5-test.js`、`fx/42fx-position-swap.js`、`fx/43fx-push-back.js`、`fx/44fx-dodge-bullet.js`

### 文件复制器与函数替换器路径同步
- `tools/49-toolkit.js`：ALL_PROJECT_FILES 增加 `core/12battle-attack-steps.js`、`modules/18global-store.js`、`to do list.md`；用户可选文件列表排除带空格的文件名
- `tools/50-toolkit-more.js`：TARGET_FILES 同步增加 `core/12battle-attack-steps.js`、`modules/18global-store.js`
- 涉及文件：`tools/49-toolkit.js`、`tools/50-toolkit-more.js`

### 其他修复
- **spawnVictoryEffects 变量修复**：`ui/32ui-render-5v5-test.js` 中 `spawnVictoryEffects` 原本错误使用未定义的 `c.gs`，改为 `ctx.gs`，避免 GAMEOVER 判断失效
- **`core/05battle-horse.js`**：巨马属性改为 0 攻 / 5 防 / 25 血；小昭强化时 0/30/30；召唤位置使用 Fisher-Yates 洗牌；不再把敌方位置计入占用
- **`tests/25unit-tests.js` / `tests/57health-utils.js`**：新增/同步小昭相关测试与健康检查规则
- **`00index.html` / `mode-5v5-test.html`**：版本号、脚本引用同步 V5.1.0

## V5.0.3 — 2026-07-09 ~ 2026-07-12
- **事件链路重构**（`core/06battle-engine-core.js`）：所有数据修改必发 emitEvent，击杀路径补齐 uidD/isDead，败方清零/拒马销毁/新婚扣血/Buff 效果全部补 emitEvent，确保 Store 与引擎状态同步
- **连击系统修复**（`core/06` + `core/04`）：K 值索引错位修正、连击每回合复位、连击 logo 改 🔗 与闪避 ⚡ 区分、连击 uid 同步 Store
- **闪避子弹时间重写**（`fx/44fx-dodge-bullet.js`）：大幅重写闪避特效，修复子弹时间触发与恢复逻辑
- **近身碰撞优化**（`fx/41fx-crash-5v5-test.js`）：重写碰撞检测与虚影蓝色化，修复手机端位置推算
- **击退特效改走 Store**（`fx/43fx-push-back.js`）：animatePushBack 不再直接操作 DOM，统一通过 Store 读取 alive 状态
- **播放器精简**（`player/26player-core.js`）：playBattle 大幅瘦身，移除冗余逻辑，胜利弹幕改用 Store 读取
- **体检系统大改**（`tests/`）：58health-rules/ 拆分为 60-68 独立规则文件；新增 56health-monitor.js（监控器）、45health-auto.js（自动体检）、57health-utils.js（工具函数）；删除旧 29health-rules.js、36runtime-sampler.js、38health-ui.js 答题部分
- **删除未使用特效**：移除 `fx/21fx-blood-slash.js`、`fx/22fx-fortify-counter.js`
- **删除 Test Runnerlogo.md**：00index.html 按钮事件一并清除
- **文件复制器优化**：一键序列发送第一包直接复制（无需再点一次）、弹窗显示当前包序号（非下一个）、GROUP_PROMPTS 精简提示词
- **新增文件**：`成功经验.md`、`to do list.md`

## V5.0.2 — 2026-07-08
- **统一 Store 状态管理**（多文件）：修复闪避/自动手动/重玩等 7 个 Bug，动画/换位/击退统一走 Store dispatch
- **击杀路径补齐字段**（`core/06` + `modules/23`）：所有击杀路径补 uidD 和 isDead，防止 Store 残留死单位
- **删除体检答题功能**：移除 `35quiz-bank.js`、`38health-ui.js` 答题部分、`55test-runner.html` 答题页签
- **文件复制器序列发送**（`tools/49-toolkit.js`）：一键序列发送改用 GROUP_PROMPTS 提示词，按主题分批
- **版本号升级**：全部代码文件统一为 V5.0.2

## V5.0.1 — 2026-07-07
- **版本号统一**：全部代码文件版本号统一为 V5.0.1（详见版本清单）
- **文件复制器优化**：按主题分批发送（核心战斗 → 播放器 → UI → ...），每包前后添加分析提示词
- **文件列表同步**：移除6个不存在文件（00-*前缀），补充6个新增UI文件（39-44）
- **函数替换器同步**：TARGET_FILES 补充 42audio-control/43fx-trigger/44ui-controls

## V4.1.2 — 2026-07-06
- **九阴白骨爪重写**（`modules/15elite-skills.js`）：伤害改为「已损失生命 × 5%（张无忌在场时 8%）」+ 首次必触发、后续 88% 概率 + 连锁触发自己（chainProcChance 88%，最多 3 次）+ 血量 ≤10% 直接斩杀；日志加 `isClawHit` 标记供播放器触发特效
- **配置重写**（`core/01config-5v5-test.js`）：`nineYinClaw` 新增 `firstProcChance/procChance/chainProcChance/maxChain/lostHpRatio/jealousLostHpRatio/executeThreshold`，废弃 `bonusRatio/jealousBonus`
- **播放器接入白骨爪**（`player/26player-core.js`）：`handleInfo` 检测 `isClawHit` 后 fire-and-forget 调用 `showBoneClaw`（不 await、不设 isPaused，避免卡住播放器）
- **叛逆突袭改百分比**（`modules/15elite-skills.js`）：`getRebelTarget` 从绝对 hp 比较改为 `hp/maxHp` 百分比
- **死人不再行动**（`core/06battle-engine-core.js`）：行动循环开头加 `if (!unit.alive) continue;`，防止队列构建后被白骨爪斩杀/玄冥毒/反击打死的单位继续行动
- **拒马阵亡统一清理**（`core/06battle-engine-core.js`）：回合结束统一清理所有死拒马（含 destroyHorse 销毁的 + 被攻击致死的），emit unit-remove 并从 team 移除，避免堆积导致下回合 spawnHorse 同位、UI 残留
- **热血奋战检查 alive/满血**（`core/04buff-system.js`）：加 `unit.alive` 和 `hp < maxHp` 检查，死人不回血、满血不推日志（计数仍累加以保证翻倍节奏）
- **热血奋战 UI 血条同步**（`player/26player-core.js`）：hotBlood 分支补 `healUnit.hp` 更新与 `dispatch(SYNC_UNIT)`，修复飘数字但血条不动的 bug（DeepSeek 79b7bcc 丢失项补回）
- **精英技能 logo**（`ui/32ui-render-5v5-test.js`）：cell-name 加 🐾 周芷若 / 💥 宋青书 精英技能图标
- **白骨爪描述精简**（`ui/32ui-render-5v5-test.js`）：周芷若详情弹窗描述改为新机制一句话
- **分隔符逻辑**：按用户要求本次未改动，单独讨论

### V4.1.2 补记：DeepSeek 分支已重做覆盖的修复（历史履历同步）
> 以下修复原本在 DeepSeek 分支（c1736a1 孤儿覆盖事件中被切断的另一条历史线），经逐一对比确认 main 上已有等价或更好实现，补记于此保持履历完整。
- **carry 计算四处修复**（`core/06`、`core/05`）：删 `_baseMaxHp` 覆盖行修指数级叠加、spawnHorse 补 `_baseMaxHp` + carry 判断加 `!u.isHorse`、`allyTeamWithDead = A.slice()` 保留本回合毒死单位计死亡加成、删无忌豁免让其变身换后排按战士休息
- **惑人心智死锁修复**（`player/10`）：`handleBuffSwap` 改为 `await showBuffBanner` + `await animatePositionSwap` 后直接解 isPaused，不依赖 scheduler，消除死锁
- **战斗结束 mainCtx is not defined**（`player/10`）：循环外 `let mainCtx = window._getPlayerContext ? ...` 并带 null 保护
- **站位预览与实际不一致**（`tools/33`）：先按 ELITE_POOL.pos 画精英、再画模板（跳过已占位），与 generateSnapshot 对齐
- **闪避反击方向修正**（`core/06`）：resolveDodge 改为 `uidA:target.uid, uidD:unit.uid`，攻击者/防御者对调
- **叛逆突袭伤害位置调整**（`core/06`）：`getRebelDmgBonus` 放到 processUnitAttack 作用域，避免重复计算
- **emitEvent 事件系统**（`core/06`）：19 处调用 + helper + `group._events` 快照 + `_isAbsolute` 标记
- **miss 后仍检查连击/性奋**（`core/06`）：miss 分支不再提前 return
- **cloudBody 仅 ally**（`core/04`）：流云身法闪避只对己方生效
- **张无忌近战第 2 次第二句台词**（`core/06`）：`nearAtkCount === 2` 触发
- **删 07 冗余 window 挂载**（`core/07`）：33 个函数挂载移除，仅保留 `ALL_VERS` 版本串
- **站位对齐 config + 一致性检测**（`tools/27`、`tools/33`）：精英读 config pos、跑 generateSnapshot 比对
- **体检修复**（`tools/33`、`tests/`）：多回合状态对比、死亡标记、beforeAllies 时机、攻防公式

## V4.1.1 — 2026-07-06
- **白骨爪利爪对敌人**（`fx/40fx-arrows-5v5-test.js`）：新增 `showBoneClaw`，SVG 三道弯曲细长爪痕，整体 `rotate(angle)` 让爪尖朝向目标；凝结（放大浮现）→ 飞行（飞箭速度，不再旋转）→ 命中触发受击反馈
- **通用受击反馈函数**（`fx/39fx-common-5v5-test.js`）：`applyImpactShrink` 统一缩小+颤动+黄色短闪，飞箭/溅射/白骨爪/飞撞/近身通用
- **飞箭接入受击反馈**（`fx/40fx-arrows-5v5-test.js`）：`showRangedArrow` 受击改用 `applyImpactShrink(defCell, 300, ...)`；`showSplashArrows` 改用 `applyImpactShrink(defCell, 250, ...)`
- **飞撞/近身接入受击反馈**（`fx/41fx-crash-5v5-test.js`）：`showMeleeCrash` 和 `showCloseRangeFX` 受击改用 `applyImpactShrink`
- **虚影蓝色化**（`fx/41fx-crash-5v5-test.js`）：ghost 模式改为 opacity=0.5 + 蓝色背景 `rgba(30,100,255,0.28)` + 蓝色 border + 蓝色 boxShadow，保留明显视觉残留

## V4.0.2 — 2026-07-02
- **README 更新改代码展示规则**：去掉字节数示例括号、删除"改动点多优先贴完整函数"冗余段落，规则更精简
- **CHANGELOG 同步新增 V4.0.2 条目**
- **新增 game-design.md 游戏设计文档**

## V4.0.1 — 2026-06-29
- **修复BGM无声**：`BGM_LOCAL` 路径补上 `assets/` 前缀，`17audio-manager.js` 改用本地路径替代已删除的网络地址
- **README 补充改代码规则**：发代码必须「一组一旧一新」，超过3处询问后发完整文件不准省略
- **CHANGELOG/开发准则/Test Runner Logo 同步更新**

## V4.0.0 — 2026-06-29
- **大版本升级**：所有文件版本号统一为 V4.0.0，清理冗余注释
- **文件结构整理**：`47auto-battle-utils.js` 移入 `tools/`，新增 `36runtime-sampler.js`
- **文件复制器路径修正**：适配最新文件结构
- **README/开发准则/Test Runner Logo 更新**：同步最新版本信息

## V3.1.2 — 2026-06-25
- **第五关玄冥二老拆分**：鹿杖客（远程/玄冥神掌）+ 鹤笔翁（飞行/鹿角杖法）两个独立单位
- ENEMY_SQUADS[5] 人数 5→6，站位模板 random 2→3
- 新增体检规则：第五关敌方单位=6
- **修复新婚扣血 bug**：`applyXinHunDeduction` 传入 `enemySide`→`allySide`，宋青书攻击时能正确找到周芷若
- **精英怪技能描述补充**：鹿杖客/鹤笔翁/成昆详情弹窗改为双行描述
- 确认 `_kuaiLeStack` 在 `02unit.js` 构造函数和 `clone()` 中均已初始化
- 代码复制器更新：路径适配新文件夹结构，加入 35/37/38 新文件

## V3.1 — 2026-06-25
- **项目结构重组**：JS/HTML/资源文件按功能分入 `core/` `player/` `ui/` `fx/` `modules/` `tests/` `tools/` `assets/` 子文件夹
- **优化体检等待逻辑**：取消模拟 btnMain/投票/Buff 流程，改用 `doManualReset`/`window.selectStage` + `waitCtx`，解决超时
- **清空历史记录弹窗修复**：浏览器原生 `confirm` 替换为页面内自定义确认弹窗
- **下掉环境诊断页签**：游戏运行时已有内置报错弹窗，`30test-runner` 中的环境诊断页签移除（代码保留）
- **版本号统一升级**：`00index.html` → V3.1，`55test-runner.html` → V4.3，`37health-core.js` → V2.2，`38health-ui.js` → V2.2

## V3.0 — 2026-06-23
- 修复 `showDanmaku` 安全调用
- 修复 carry 阵亡加成计算
- 修复闪避血量显示

## V3.0 之前 (2026-06-20 ~ 2026-06-22)
- 体检功能上线：全面体检 + 历史记录 + 答题
- 流云身法闪避从 30% 调整为 25%，同步配置/测试/题库
- 环境诊断页签上线（后在 V3.1 下掉）
- 暴露 `window.selectStage`/`window.forceStopGame`/`window.doManualReset` 供 test runner 调用
- 单元测试扩展至 25 条
- 30test-runner 内联 JS 全部移入 38health-ui.js