# 光明顶 5v5 - 更改履历

> 完整历史（V6.2.0 及更早，74 条）见 `文件汇总20260730/记录-历史履历归档.md`（2026-09-24 切分归档）。

## V7.4.2 — 2026-09-25 闪避面板改读 computeBuffStats：修「流云身法」永不显示

### 一、问题（根因）

闪避构成面板（`render/32`）读的是 `unit.buffDodgeBonus` —— 该字段在 V6.1.1 词条化时已被剥离，
**全库无写入源**。后果：「**流云身法**」那行加成一格永远读不到值，面板上永不显示。

注意这不是「闪避数值算错」：闪避真值本身由 `core/10` / `core/12` 现算，一直是好的，坏的只是面板的读法。

### 二、改动（1 文件）

| 文件 | 版本 | 改动 |
|---|---|---|
| `render/32-grid-render.js` | V6.4.0 → **V6.4.1** | 加 `import { computeBuffStats }`（`core/04`）；闪避面板改读 `computeBuffStats(unit, activeBuffs, allyTeam).dodgeBonus`，与 `core/10` / `core/12` **同源现算**，不再读死字段 |

## V7.4.1 — 2026-09-24 谢逊站位收进「固定 3 位」+ 幼狮召唤 30% → 50%

### 一、站位：谢逊不再单独锁死（`modules/29battle-init.js` V6.5.0 → **V6.5.1**）

V7.4.0 把谢逊 7 号位改成 `xie.fixed = true` **单独锁死**——本版推翻，删掉这一行：
他与其他精英同待遇，只占「**固定 3 位**」里的一个名额。

改法：谢逊并入 `toLock` 名单并按序截断 —— `[zhang, wei, xz, xie].filter(Boolean).slice(0, 3)`。

**修掉的 bug**：谢逊在队时明教会冒出 **4 个固定位**；且补足到 3 的随机补位可能落到无关的普通弟子身上。

### 二、平衡：幼狮召唤概率 30% → **50%**（`content/200game-data.json` 5.6.10 → 5.6.11）

`summonLion.prob` 0.3 → 0.5，`desc` 同步为 50%。其余不变（成长 80%、1-6 号位成雄狮 / 7-9 号位成母狮）。

> 本条同时更正 V7.4.0 的两处：其「三、站位」的「锁死」写法已作废；
> 其「五、遗留」第 2 条（29 头注释未提 `fixed`）随之关闭 —— V6.5.1 头注释已写明本次改动。

## V7.4.0 — 2026-09-24 平衡三改 + 远程闪避演出修正

### 一、平衡（`content/200game-data.json`）

| 项 | 改动 | 实测效果 |
|---|---|---|
| 张三丰·生生不息 | 新增「实际回血 → 永久防御」转化，`defPerHeal` 1 → 0.5 → **0.3**（两次试跑收敛） | 第 2 关明教加权胜率 64.6% → **约 37%**，落回「偏难但不离谱」区间 |
| 灭绝师太·反击 | `prob` 1.0 → **0.8** | 第 7 关明教加权胜率约 19% → **约 20%**，收效甚微 |

第 7 关瓶颈是「明教 5 人轮流打她 = 5 次反击机会」的放大器，本关暂定稿。

### 二、机制（`modules/26elite-sixsects.js`）

生生不息新增「**实际回血量 × defPerHeal → 永久防御词条**」（`group: endlessBreath`，`ttl: permanent`）。
只在真回血时加（满血不加）；张三丰本人与溢出接盘队友**各按各自实际回复量**加防。
注意：八卦阵被攻击时也触发生生不息 → 挨打越多防越高，是正反馈，数值需跑评测盯。

### 三、站位（`modules/29battle-init.js`）

✅**已作废**（V7.4.1 推翻）：金毛狮王谢逊 7 号位曾由「固定」升级为「**锁死**」——加 `fixed = true`，玩家摆位不可移动。
该写法导致明教在队时冒出 4 个固定位，V7.4.1 已删除此分支，改为并入「**固定 3 位**」名单。

### 四、演出（`fx/88fx-trigger.js`、`render/39-actions-defs.js`）

简单模式「**被闪避**」优先于职业分流：远程与近战同款走 `showMeleeDodge`（冲过去被挡回）。
原先远程被闪避走 `showRangedArrow(isMiss=true)`，演成「箭射偏」，读作未命中而非闪避。
华丽模式的子弹时间仍由 `render/39` 的 DODGE stage action 演，不受影响。

### 五、提交与遗留

| 提交 | 内容 |
|---|---|
| `e6fd19a` | 演出修正（`fx/88` + `render/39`） |
| `5d38cd9` | 平衡 + 机制 + 站位（`content` / `modules/26` / `modules/29`）+ 履历切分 |

**头注释未同步（留痕待办，未擅改）**：

- `modules/26elite-sixsects.js` V6.14.2：头注释最新一条仍是三击 / 莽撞等旧改动，**未提本次 `defPerHeal`**。
- `modules/29battle-init.js` V6.5.0：头注释仍是 2026-09-22 精英池扩容，**未提谢逊 `fixed`**。
- `render/39-actions-defs.js` V1.2.1：头注释写「远程闪避补弧线」，描述的是**被本次推翻**的旧行为，未同步为「同款飞撞」。

## V7.3.0 — 2026-09-23 妆造字段视图表：渲染态与引擎账本解耦

### 一、目的

把 5 个**纯渲染态**字段（`_flash` / `_renderFlyMode` / `_phantomFlash` / `_hasXingFen` / `_hasKuaiLe`）
从引擎单位对象上摘出来，收口到独立的**视图表**（`uid → 妆造对象`）。

这 5 个字段只影响「看起来怎样」，不影响「谁打谁 / 掉多少血 / 死没死」。混在 unit 上会被
clone / `toJSON` / 联网序列化反复搬运，每回合重置还要逐个照顾。

**定案：units 只装账本，妆造独立存本表**——写走 dispatch hook（`applyActionToView`），读走 `getView`。

### 二、新增文件（1 个）

| 文件 | 版本 | 内容 |
|---|---|---|
| `infra/61-view-sheet.js` | **V1.1.0** | `VIEW_FIELDS` 白名单（5 字段）+ `setView` / `getView` / `getUnitView` / `clearView` / `clearAllView` + `applyActionToView`（dispatch hook：`SET_FLASH` / `CLEAR_UNIT_FLASH` / `CLEAR_ALL_FLASH` / `SET_VISUAL` 四类 action → 写表） |

### 三、改动清单（9 文件）

| 文件 | 版本 | 改动 |
|---|---|---|
| `modules/24battle-store.js` | **V6.1.0** | `SET_FLASH`/`CLEAR_*` 只换 units 引用触发重绘；`SET_VISUAL` 不再写妆造；`dispatch` 挂 `applyActionToView`；删 `STAT_BONUS_CHANGE` case、`UNIT_TOP_FIELDS` 4 字段、`UNIT_ADD` 妆造初始化；`createStore` 加 `clearAllView` |
| `render/32-grid-render.js` | **V6.3.4** | `_flash` / `_renderFlyMode` / `_phantomFlash` / `_hasKuaiLe` / `_hasXingFen` 读点全切 `getView`；`cheerClass` 与 `data-flash` 残留改用 `flashVal` |
| `core/02unit.js` | **V6.3.0** | 加 `toJSON()`（`_fsm` 只输出 `current`）；删构造函数妆造初始化 |
| `core/04buff-system.js` | **V6.2.0** | `_bloodthirstStriked` 从顶层收口进 `state`（**顺带修 core/11 重置不到的 bug**） |
| `core/11battle-round.js` | **V6.3.0** | 删 `buffXxxBonus` 写入 A/B 两段；删 `_restingTimer` 清理；删 `emitEvent STAT_BONUS_CHANGE` |
| `infra/60-net-pvp.js` | **V7.4.0** | 删 `plainUnit`；`plainStep` / `plainLineup` 去掉 `.map(plainUnit)` |
| `infra/56-battle-enums.js` | **V1.3.0** | 删 `STORE_ACTION_TYPES.STAT_BONUS_CHANGE` + `UNIT_EVENT_TYPES.STAT_BONUS_CHANGE` |
| `player/42player-core.js` | **V6.8.0** | `syncStoreFromStep` 删 `preservedTopFields` 死代码 |
| `ui/69reset-runtime.js` | **V6.0.0** | 删 `u._flash = null` 死写；加 `clearAllView()` |

### 四、遗留（搜索核对后发现的断链，本次未处理）

迁移后全库已无这 5 个字段的直接赋值（`字段名 = 值` 形式 **0 处**，精确正则核对通过），
但顺着 dispatch 链路追，**两处写入源缺失**：

| 字段 | 白名单 | SET_VISUAL hook 分支 | 读点 | 写入源 |
|---|---|---|---|---|
| `_phantomFlash` | ✓ | **✗ 缺分支** | `render/32:370` | **✗ 全库 0 处** |
| `_hasKuaiLe` | ✓ | ✓ | `render/32:401` | **✗ 全库 0 处** |

后果：成昆幻影伪装的闪烁动画、周芷若的 💖 图标因没有 dispatch 方而不会触发。
（对称的 `_hasXingFen` 在 `player/42` 两处有写入，链路完整，可作参照。）

另：`core/02unit.js:125` 的 `clone()` 里 `key !== '_flash'` 例外项已无意义（`_flash` 不再挂 unit 顶层）。

版本号疑点：本表 `core/11battle-round.js` 与 `core/02unit.js` 的版本号**未随本次迁移递增**——
当前值与 V7.2.0 条目所记完全相同（均为 V6.3.0），而两次改动内容不同，属漏升（未擅改，留痕待办）。

## V7.2.0 — 2026-09-23 胖远桥三技能改版 + 金毛狮王谢逊改版

### 一、胖远桥（六大派 · 武当 · 战士 · 精英 · M107）

| 技能 | 效果 | 参数键 |
|---|---|---|
| 莽撞 | 每次挨打自身攻击 **+3**，永久累计、无上限 | `rageOnHit.atkPerHit` |
| 正义国字脸 | 攻击前掷骰触发：**全体敌人**本回合后续只能攻击自己，该次伤害 ×0.5 | `taunt.*` |
| 年轻气盛 | 攻击前掷骰触发：随机目标 **1.5 倍**伤害 + 击退；1-6 位换位/移动，7-9 位退无可退转眩晕 | `youngBlood.*` |

掷骰公式（二者互斥，必触发其一）：`T = 0.10 + (atk-30)/200`，`p_打歪 = clamp((1-血量比)/(1-T), 0, 1)`，`p_嘲讽 = 1 - p_打歪`。
即**满血必嘲讽、血量 ≤ T 必打歪**。击退/眩晕抽公共函数 `core/13battle-shared.js` 的 `resolvePushOrStun`（乘风突袭同步复用）。

### 二、金毛狮王谢逊（明教 · 战士 · 精英 · M115 → **M107**）

**删除**旧三技能：替死 / 集火 / 母狮狮吼。改为一条「狮子成长链」：

| 技能 | 效果 | 参数键 |
|---|---|---|
| 召唤狮子 | 每回合开始 **80%** 概率在随机空位召唤 1 只**幼狮**（防战 · M20 · 攻0 · 防4 · 血40） | `summonLion.{prob,cub,grow}` |
| 幼狮成长 | 幼狮存活一回合后，按所在位置成形：**1-6 号位 → 雄狮**（战士 · M70 · 攻24 · 防18 · 血上限90）；**7-9 号位 → 母狮**（远程 · M70 · 攻30 · 防16 · 血上限80） | `summonLion.grow` |
| 振奋 | **雄狮**命中后，己方全体存活角色（含自己、谢逊、幼狮、拒马）永久 **+2 攻** | `lionInspire.atkPerHit` |
| 随动 | **母狮**命中后，其他雄狮/母狮与谢逊各随动攻击一次（**无视遮挡**），打母狮**同一个目标**；目标已阵亡则整条随动取消 | `lionFollow` |

**关键设计（本次定案）**：拒马与幼狮「不可攻击」**不是身份硬编码**，而是**攻 = 0 的自然结果**。
`core/11battle-round.js` 的行动轮询只在 `getStat(u,'atk') <= 0` 时把它们推进休息通道（回血 15）；
一旦被振奋之类的加攻词条抬到攻 > 0，就放行进正常攻击流程——职业是防战，`core/12` 自然走防战公式
（`penPart + def*k + maxHp*_hpDmgRatio`，`spawnUnit` 已为防战形态锁好 `_hpDmgRatio`）。

### 三、改动清单（13 文件，谢逊部分）

| 文件 | 版本 | 改动 |
|---|---|---|
| `content/200game-data.json` | — | `roster.mingM.谢逊` 115→107；`characters.谢逊.skills` 整段重写（删 `lionSacrifice`/`focusFire`/`lionRoar`，加 `lionInspire`/`lionFollow`，`summonLion` 改幼狮+成长参数） |
| `modules/29battle-init.js` | — | 谢逊 m 115→107（`eliteConfigs` + `new Unit` 两处） |
| `core/02unit.js` | V6.2.0 → **V6.3.0** | `applyHeroFlags` 增补三狮形态标记 `isLionMale` / `isLionCub`（与 `isLioness` 互斥） |
| `core/05battle-horse.js` | V6.1.0 → **V6.2.0** | `spawnUnit` 加第 6 参 `stats`：传了就写死 atk/def/maxHp 与 `_base/_init`，不走 M 随机与职业加成 |
| `core/10battle-attack.js` | V6.1.0 → **V6.2.0** | 额外攻击 `reason:'lionFollow'` 计入 `_isLinkAttack`（防随动乒乓链） |
| `core/11battle-round.js` | V6.2.0 → **V6.3.0** | 拒马/幼狮 pass 判据由身份改为 `getStat(atk) <= 0`；休息回血 reason 增 `'幼狮休息'` |
| `core/17-state-keys.js` | V2.3.0 → **V2.4.0** | 删 `_focusUsedRound`（集火已移除） |
| `infra/50-event-bus.js` | V6.3.0 → **V6.4.0** | `ROUND_START` 加 `LION_GROW:15`（必须早于 `XIE_SUMMON:16`）、`AFTER_DAMAGE_APPLIED` 加 `LION_INSPIRE:23`/`LION_FOLLOW:24`；移除 `XIE_ROAR`/`XIE_FOCUS`/`XIE_SACRIFICE` |
| `infra/56-battle-enums.js` | V1.2.0 → **V1.3.0** | `FACT_TYPES` 的 `LION_SACRIFICE` 换成 `LION_GROW` |
| `infra/58-fact-contract.js` | V1.3.0 → **V1.4.0** | 同上契约替换（`requiredFields: ['name','pos']`，`translateFn: null`） |
| `modules/27elite-mingjiao.js` | V6.1.0 → **V6.2.0** | `createXieXunComponent` 重写为「成长 / 召唤 / 振奋 / 随动」四个 handler；import 去 `findFreePos`、`getUnitCol`，加 `applyHeroFlags` |
| `render/35-facts-effect.js` | V1.0.3 → **V1.0.4** | 删 `renderLionSacrificeFact`，加 `renderLionGrowFact`；`renderPassFact` 支持 `'幼狮休息'`（文案「🐱 xx 尚幼，无法攻击」） |
| `render/32-grid-render.js` | V6.3.1 → **V6.3.2** | 幼狮格子图标加 `🐱`（成长后自动回到职业图标） |

### 四、验证（实跑，非仅语法）

- `node tests/smoke-headless.mjs 30`：**30/30 局跑完无异常**，FACT_SPECS 77 条渲染映射完整。
- 强制谢逊阵容**实跑 20 局**：**错误 0**。触发计数：**召唤 92 / 成长（雄狮 11 · 母狮 24）/
  幼狮休息 55 / 振奋横幅 46 / 随动横幅 82**；出手计数：雄狮 51 次、母狮 111 次。

## V7.1.0 — 2026-09-22 三个新角色转正 + 关卡循环正式扩到 7 关

### 一、目标

V7.0.7~V7.0.9 三个角色（胖远桥 / 灭绝师太 / 谢逊）都是「靠 demo 开关才能看到」的状态。
本版把它们**合进主代码正式流程**：正常玩就能遇到，不依赖任何 localStorage 开关。

### 二、三个角色各自的转正方式（用户拍板）

| 角色 | 转正方式 | 说明 |
|---|---|---|
| **胖远桥** | 第三关随机二选一（保持） | 50% 胖远桥阵容 / 50% 宋青书阵容，走战斗 RNG，PVP 双端同源 |
| **灭绝师太** | 第七关 boss | 为此把**关卡循环从 6 关正式扩到 7 关** |
| **金毛狮王谢逊** | 进明教随机精英轮盘 | 与张无忌 / 韦一笑 / 小昭同性质，随机出现 |

demo 开关（`_forcePang` / `_forceXieXun` / `_startStage`）与 dev-index 三个按钮**保留**，作为调试入口，不影响主流程。

### 三、改动清单（6 文件）

| 文件 | 版本 | 改动 |
|---|---|---|
| `ui/61main-5v5-test.js` | V6.11.0 → **V6.12.0** | 关卡循环三处 6 → 7：全自动推进上限（L563）、PVP 关卡推进（L578）、普通关卡推进（L615） |
| `modules/29battle-init.js` | V6.4.0 → **V6.5.0** | ① `eliteConfigs` 加第 4 人谢逊 ② `eliteCount===3` 分支改写 ③ 抽取前过滤已在队精英 |
| `content/200game-data.json` | — | `elitePower` 加谢逊 140；`eliteRate` 加谢逊 0.20 |
| `tools/102-toolkit.html` | V6.0.0 → **V6.0.1** | elite / sc / bc 三组关卡复选框各补一个「7」 |
| `tools/108-hex-dashboard.js` | V6.0.4 → **V6.0.5** | `allStages` 数组含 7；进度 / ETA 改为按数组长度算，不再写死 6；`total = 7 * per` |
| `tools/health_auto_check.py` | — | `STAGES` 默认值 6 → 7 |

### 四、精英池扩容踩到的三个坑（重点）

谢逊从「独立强制块」变成「轮盘第 4 人」后，暴露出三个必须一起修的问题：

1. **`eliteCount === 3` 会超编**
   原代码是 `picked.push(...pool)` —— 池子 3 人时正好全抽，池子变 4 人后就会抽出 **4 个精英**（超编）。
   改为按权重循环抽满 3 个。

2. **`forceXieXun` 会抽出两个谢逊**
   强制块在 L83-92 先加了谢逊，紧接着 L94 的随机轮盘还可能再抽到他。
   改为抽取前先过滤已在队的精英。

3. **去重不能只看 `name`**
   小昭抽中后会被改名成「小昭·姊 / 小昭·妹」，`name` 对不上 `eliteConfigs` 里的 `'小昭'`。
   所以过滤条件要同时比对身份标记（`isZhang` / `isWei` / `isXiaoZhaoBrother` / `isXieXun`）。

### 五、验证（实跑，全部 0 错误）

| 场景 | 局数 | 结果 |
|---|---|---|
| 谢逊随机出场率 | 200 | **16.5%**（权重预期 16.7%，吻合） |
| 第 1/3/5/6/7 关 | 各 30 | 0 错误、0 重复名；第 7 关敌方首位 = 灭绝师太 ✓ |
| `forceXieXun` | 30 | **双谢逊 0**、错误 0 ✓ |

`node tools/118-import-export-check.mjs`：79 文件 / 896 导入 / **0 问题**。

**⚠️ 第 7 关难度观察（供你判断，未擅自调）**：
无头模拟胜率 第1关 50% → 第3关 10% → 第5关 7% → 第6关 3% → **第7关 0%**（30 局全败）。
递减趋势与既有各关一致，第 7 关只是「最难的一关」，属于预期内；但 0% 意味着无头自动打必败，
实际玩家手动摆位 + buff 未必如此。要不要调 `mingTargetPower["7"]`（现 600）/ 敌方阵容，等你定。

## V7.0.9 — 2026-09-22 新角色 demo 改为「进游戏实战」：dev-index 三个按钮直达；删除 demo-stage3.html

### 一、为什么推翻上一版

V7.0.7 做的 `demo-stage3.html` 是**无头模拟页**——跑 `runBattle`、统计日志文案、输出表格。
用户的真实需求是**点按钮进游戏真打一局**（有画面、有演出、能看特效），不是看文字统计。
所以本版把独立演示页整个删掉，改成复用 `dev-index.html` 已有的「封面传参 → index.html」模式。

### 二、机制：和「张无忌 / 韦一笑」按钮完全同款

```
dev-index 按钮 → localStorage 开关 → window.open('index.html')
  → ui/60main-utils.js 的 initBugAndXiaoZhaoModes() 读取 → GlobalStore.set(...) → 立即 removeItem（一次性）
  → modules/29battle-init.js / ui 层消费
```

### 三、改动清单（5 文件，1 删除）

| 文件 | 版本 | 改动 |
|---|---|---|
| `demo-stage3.html` | — | **删除**（纯理论页，需求方向已改） |
| `dev-index.html` | V6.2.0 → **V6.3.0** | 主入口加三按钮：🥷 胖远桥 / 🗡️ 灭绝师太 / 🦁 谢逊；移除隐藏入口 `·` 与其 CSS |
| `ui/60main-utils.js` | V6.0.2 → **V6.1.0** | 新增 `_forceXieXun` / `_forcePang` / `_startStage` 的一次性消费 |
| `modules/29battle-init.js` | V6.3.0 → **V6.4.0** | 新增 `forcePang`：第三关必定抽含「胖远桥」的变体 |
| `ui/68ui-controls.js` | V7.6.0 → **V7.7.0** | 选关弹窗 1~6 扩到 **1~7**（第七关是灭绝师太 demo 关） |
| `tools/106-ai-pack-config.js` | V6.0.10 → **V6.0.11** | 移除已删文件的登记项（2 处） |

### 四、三个按钮分别设什么

| 按钮 | 开关 | 效果 |
|---|---|---|
| 🥷 胖远桥 | `_forcePang=1` + `_startStage=3` | 第三关开局，敌方必出胖远桥（否则二选一随机，看不稳） |
| 🗡️ 灭绝师太 | `_startStage=7` | 第七关开局，敌方主将就是灭绝师太（`enemySquads["7"]` 首位） |
| 🦁 谢逊 | `_forceXieXun=1` | 明教侧固定加入谢逊（站 7 号位），关卡不变 |

`_startStage` 能生效的时序依据：`initBugAndXiaoZhaoModes()` 在 `DOMContentLoaded` 时执行，
晚于 `ui/63main-state.js` **模块顶层**的 `GlobalStore.set('currentStage', 1)`，
又早于用户点封面触发的首次 `doInitBattle` —— 覆盖有效。

### 五、两个必须记住的坑

1. **`forcePang` 要"先抽再判"**。如果强制时跳过 `_rng.nextInt()`，RNG 流会和"不强制"时错开，
   PVP 双端同源就废了。所以代码先照抽一次 `roll`，再用 `find` 覆盖结果；
   找不到含胖远桥的变体直接 `throw`，不做兜底。
2. **`_forceXieXun` 原先没有一次性消费**。V7.0.7 只在 `29battle-init` 里直读 localStorage 兜底，
   读完不清除 → 会一直生效。本版接进 `initBugAndXiaoZhaoModes` 统一处理。

### 六、验证（实跑，全部 0 错误）

| 场景 | 局数 | 结果 |
|---|---|---|
| 第三关 不强制 | 20 | 胖远桥 10 / 宋青书 10（轮换正常） |
| 第三关 `forcePang` | 20 | **胖远桥 20 / 20** ✓ |
| 第七关 | 20 | 灭绝师太 20 / 20 ✓ |
| 第七关 + `forceXieXun` | 10 | 灭绝师太 10/10、0 错误 ✓ |

`node tools/118-import-export-check.mjs`：79 文件 / 896 导入 / **0 问题**。

⚠️ 遗留观察（**非本次改动引入，待单独定位**）：
第七关 + `forceXieXun` 的 10 局里出现 1 次 `[FSM] 非法转换: "normal" → "attached"，允许: attaching`。
来自 `modules/27elite-mingjiao.js` 小昭·姊的蝶变附身 FSM（`_executeAttach` L298 直接 `transition('attached')`），
本次改动未触碰小昭 / FSM / `executeAttach` 调用链（`infra/50` 新增的 key 都在 `ROUND_START` / `AFTER_DAMAGE_APPLIED` 等，
未改动 `BUTTERFLY_*` 所在层级）。战斗正常完成，不影响结果。

## V7.0.8 — 2026-09-22 第三关胖远桥阵容独立化：换武当七侠 + 自带站位模板 + 固定 2 号位

### 一、问题（实跑取证）

胖远桥阵容原先是「把宋青书换个名字」——队友 M 值、职业、站位全部照抄。
12 局实测：胖远桥阵容固定为 `1:防战 2:战士 3:防战 4:胖远桥 8:远程`。

胖远桥是"挨打叠攻（莽撞）+ 嘲讽"的**近战坦克**，却被排在中排 4 号位、前面三个队友挡着，
莽撞拿不到挨打机会；而宋青书是**飞行**，站 4 号位（不吃前排阻挡、走 `selectFlyTarget`）才合理。

### 二、改了什么

| 项 | 改前 | 改后 |
|---|---|---|
| 队友 | 匿名杂兵（104/104/100/97） | **武当七侠具名**：俞莲舟(104/防战)、张松溪(104/远程)、俞岱岩(100/远程)、殷梨亭(97/战士) |
| M 值总和 | 405 | **405（不变）** |
| 职业分布 | 防战2 + 战士1 + 远程1 | 防战1 + 战士1 + **远程2** |
| 站位 | 共用 `enemyPosTemplates["3"]` → 胖远桥落 4 号位 | **自带模板** `{ 防战:[1], 战士:[3], 远程:[7,8] }`，2 号位留空给胖远桥 |
| 实测结果 | `1:防战 2:战士 3:防战 4:胖远桥 8:远程` | `1:俞莲舟(防战) 2:胖远桥(战士) 3:殷梨亭(战士) 7:张松溪(远程) 8:俞岱岩(远程)`，**47 局站位组合唯一** |

### 三、改动清单（3 文件）

| 文件 | 版本 | 改动 |
|---|---|---|
| `content/200game-data.json` | 5.6.5 → **5.6.6** | `squadVariants["3"]` 每项由「数组」升级为 `{ squad, posTemplate? }`；胖远桥项换武当七侠 + 自带 posTemplate |
| `modules/29battle-init.js` | V6.2.0 → **V6.3.0** | ① 变体取值改 `variant.squad` / `variant.posTemplate`（缺省才回落关卡级模板）② `otherElites` 加 `isPangYuanQiao` → `priority = [2,1,3,...]` |
| `dev-index.html` | V6.1.0 → **V6.2.0** | demo 入口由几乎看不见的「·」（11px）改为可辨认的「🥷 demo」（18px） |

### 四、一个必须记住的坑：别把专属队友塞进 ENEMY_M 池

第一版把武当七侠也加进了 `roster.enemyM`（为了"名册完整"），结果**宋青书阵容的数字项会随机抽到他们**
——两套阵容的辨识度直接糊掉（实测宋青书阵容里冒出俞岱岩、张松溪）。
`ENEMY_M` 只服务「数字项取名池」，对象项（`{name, role, m}`）**不查池子**，所以专属队友必须只写在 squad 里。

### 五、验证（实跑）

- 80 局第三关：**错误 0**，轮换 47 / 33（≈50/50）。
- 胖远桥阵容 **47 局站位组合唯一**（设计意图达成）；宋青书阵容 33 种（原样未动）。
- 明教胜率：胖远桥阵容 **29.8%** vs 宋青书阵容 **9.1%**。
  ⚠️ 胖远桥阵容胜率较 V7.0.6 的 69.5%（当时 120 局）明显下降，
  主因是队友职业从「防战2 + 远程1」变成「防战1 + 远程2」——前排变薄、胖远桥一个人扛。**待调参**。

## V7.0.7 — 2026-09-22 新增角色「灭绝师太」（第七关）+「金毛狮王谢逊」（明教），demo 页扩到三角色

### 一、做了什么

新增两个角色，都做成组件（不走 JSON 声明原语），并各自接进对应的关卡/阵容：

| 角色 | 阵营 / 位置 | 技能 | 参数键 |
|---|---|---|---|
| **灭绝师太**（六大派·峨眉·战士·M112） | 第七关，站位优先 2 号位 | 反击 100%（不可闪避，伤害 ×0.6） | `counterAttack.{prob,dmgRatio}` |
| | | 每第三次出手伤害 ×2 且吸血 200% | `thirdStrike.{dmgMultiplier,leechRatio}` |
| | | 开局召唤周芷若（每局 1 次） | `summonZhou.{m,posPriority}` |
| | | 队友命中后 30% 跟随攻击（无每回合上限） | `mechanics[].type='followAttack'` |
| **金毛狮王谢逊**（明教·M115） | demo 固定 7 号位 | 每回合召唤 1 只狮子（1 雄狮 / 4 幼狮 / 8·9 母狮） | `summonLion.lions[]` |
| | | 狮子替死：谢逊待死时消耗 1 只存活狮子，回 50% 上限血 | `lionSacrifice.reviveHpPct` |
| | | 集火：谢逊出手后随机 2 名存活友方各追加一次攻击（每回合 1 次） | `focusFire.count` |
| | | 母狮狮吼：母狮命中后，同列敌人全部恐惧（失去下次攻击） | `lionRoar` |

### 二、改动清单（15 文件）

| 文件 | 版本 | 改动 |
|---|---|---|
| `content/200game-data.json` | 5.6.4 → **5.6.5** | 加 `characters.灭绝师太` / `characters.金毛狮王谢逊`；`encounters` 加第七关 `enemySquads`/`enemyPosTemplates`/`elitePool`/`mingTargetPower`；`roster.mingM`/`enemyM` 各加 1 条；根级 `taunts` 加 2 条 |
| `core/05battle-horse.js` | V6.0.0 → **V6.1.0** | 新增 `findFreePos`（按优先级找空格）+ `spawnUnit`（指定格召唤，与开局单位同口径含 `_hpDmgRatio` 分档） |
| `core/02unit.js` | V6.1.0 → **V6.2.0** | `HERO_FLAGS` 加 `灭绝师太`/`金毛狮王谢逊`/`雄狮`·`幼狮`·`母狮`；母狮另打 `isLioness` |
| `core/12battle-attack-steps.js` | V6.2.1 → **V6.3.0** | ① `resolveDeaths` 逐条复查 `_pendingDeath`（支持"替死"取消死亡）② 新增 `_ignoreDodge` 不可闪避 |
| `core/10battle-attack.js` | V6.0.3 → **V6.1.0** | 额外攻击支持跨阵营（灭绝反击换边）+ `req.ignoreDodge`；`followAttack` 计入 `_isLinkAttack` 防乒乓 |
| `core/15-skill-mechanisms.js` | V6.0.3 → **V6.1.0** | 新增 `followAttack` 机制（`AFTER_ATTACK` 阶段，按 `decl.chance` 跟随，无每回合上限） |
| `core/17-state-keys.js` | V2.1.0 → **V2.2.0** | `BATTLE_STATE_SCHEMA` 加 `_attackCount`/`_thirdStrike`/`_summonedZhou`/`_focusUsedRound`/`_ignoreDodge` |
| `infra/50-event-bus.js` | V6.1.0 → **V6.2.0** | 相位表增补 `MIEJUE_*`/`XIE_*` 具名优先级；新增 `ON_BEFORE_DEATH: { XIE_SACRIFICE: 10 }` |
| `infra/56-battle-enums.js` | V1.0.1 → **V1.1.0** | `FACT_TYPES` 加 `SUMMON_UNIT` / `LION_SACRIFICE` |
| `infra/58-fact-contract.js` | V1.1.1 → **V1.2.0** | `FACT_SPECS` 加上述 2 条契约 |
| `render/35-facts-effect.js` | V1.0.1 → **V1.0.2** | 加 `renderSummonUnitFact` / `renderLionSacrificeFact` + 尾部注册 |
| `modules/26elite-sixsects.js` | V6.9.0 → **V6.10.0** | 新增 `createMieJueShiTaiComponent()` 并 `registerElite('灭绝师太', ...)` |
| `modules/27elite-mingjiao.js` | V6.0.3 → **V6.1.0** | 新增 `createXieXunComponent()` 并 `registerElite('金毛狮王谢逊', ...)` |
| `modules/29battle-init.js` | V6.1.0 → **V6.2.0** | ① 新增 `forceXieXun`（demo 强制谢逊入队、固定 7 号位）② 灭绝师太站位优先 2 号位 |
| `demo-stage3.html` | V1.0.1 → **V1.1.0** | 扩到 4 个按钮（胖远桥 / 宋青书 / 灭绝师太·第7关 / 谢逊·明教）；**修 import 前缀 `../` → `./`** |

### 三、三个必须记住的坑

1. **`demo-stage3.html` 的 import 前缀是 `./` 不是 `../`**。本页与 `index.html`/`dev-index.html` 同级，
   都在服务根目录（`new-acode-repo/`）。第一版写成 `../infra/...` 会直接 404、整页白屏。
2. **跨阵营额外攻击要按 `req.unit.camp` 重算两侧**。`processUnitAttack` 的 `allySide`/`enemySide` 是
   **行动者视角**；灭绝师太在另一侧反击，若沿用原攻击者的两侧，队友会被当成敌人打。
3. **"替死"靠 `resolveDeaths` 的逐条复查**。`ON_BEFORE_DEATH` 的 `data.units` 是 `pending` 的同一引用，
   监听器 `pending.push(lion)` 能被 `for...of` 继续迭代；但只清 `_pendingDeath` 是取消不掉循环的
   （`pending` 是发射前快照），必须把循环改成 `if (!u.state._pendingDeath) continue;`。
   另外**必须先清 `_pendingDeath` 再 `applyStatChange` 回血**，否则 hp≤0 会把标记重新打上。

### 四、验证（实跑，非仅语法）

- `node tools/118-import-export-check.mjs`：79 文件 / 896 具名导入 / **问题 0 处**。
- 无头跑**第七关 20 局**：**错误 0**，灭绝师太 20/20 在场（阵容 19 种）。
  触发计数：**反击 103 / 跟随 105 / 灭绝三击 14 / 召唤（周芷若）17**。
- 无头跑**第三关 + 强制谢逊 20 局**：**错误 0**，谢逊 20/20 在场。
  触发计数：**召唤 273 / 狮吼 164 / 集火 13 / 替死 2 / 莽撞 167 / 出手没分寸 15 / 脾气大 68**。

## V7.0.6 — 2026-09-22 新增角色「胖远桥」+ 第三关阵容轮换（demo 页可看）

### 一、做了什么

第三关原来固定出「宋青书」精英阵容。现在每局在**宋青书阵容**与**胖远桥阵容**之间随机二选一，
并新增六大派精英**胖远桥**（武当 · 宋远桥年轻时 · 战士 · M107），三技能：

| 技能 | 效果 | 参数键 |
|---|---|---|
| 莽撞 | 每次挨打自身攻击 +2，永久累计、无上限 | `rageOnHit.atkPerHit` |
| 出手没分寸 | 攻击时 25% 打歪，改打另一个可选敌人 | `clumsySwing.procChance` |
| 脾气大 | 每回合 1 次嘲讽：被骂者下次只能打他，且该次伤害 ×0.5 | `hotTemper.dmgMultiplier` |

### 二、改动清单（9 文件）

| 文件 | 版本 | 改动 |
|---|---|---|
| `content/200game-data.json` | 5.6.3 → **5.6.4** | 加 `characters.胖远桥`、`roster.enemyM`、`taunts`；新增 `encounters.squadVariants["3"]`；`elitePool["3"]` 追加胖远桥 |
| `core/01config-5v5-test.js` | V6.1.0 → **V6.2.0** | 新增 getter `ENCOUNTER_VARIANTS` |
| `core/02unit.js` | V6.0.0 → **V6.1.0** | `HERO_FLAGS` 加 `'胖远桥': 'isPangYuanQiao'` |
| `core/17-state-keys.js` | V2.0.0 → **V2.1.0** | `BATTLE_STATE_SCHEMA` 加 4 个 BOOLEAN 键（`_tauntedByPang` 需跨回合活到"他下次攻击"，必须登记才被 clone 带走） |
| `infra/50-event-bus.js` | V6.0.0 → **V6.1.0** | 相位表增补 `PANG_*` 具名优先级 |
| `modules/29battle-init.js` | V6.0.0 → **V6.1.0** | 生成六大派阵容时，`ENCOUNTER_VARIANTS[stage]` 存在则用 `_rng` 随机抽一组 |
| `modules/26elite-sixsects.js` | V6.8.0 → **V6.9.0** | 新增 `createPangYuanQiaoComponent()` 并 `registerElite('胖远桥', ...)` |
| `core/06battle-runner.js` | V1.0.0 → **V1.1.0** | 新增可选 `onStep` 回调（见下节"为什么必须加"） |
| `demo-stage3.html` | 新建 **V1.0.1** | 独立演示页：轮换抽样 + 单局胖远桥技能日志；`dev-index.html` 页脚加了隐藏入口（几乎看不见的 `·`） |

### 三、两个必须记住的坑（本页第一版就踩了）

1. **demo 页必须自己 `await loadGameData()`**。主游戏里这步由 `ui/61` 启动时完成；
   独立页不加载它，`CONFIG` 所有 getter 读到 `null`，一取阵容就 `Cannot read properties of null (reading 'roster')`。
2. **页面里 `eventBus.on(...)` 收不到任何战斗事件**。`core/11battle-round.js` 每回合开头
   `eventBus.clearAll()`，把非 `fx:` 前缀的监听器全部清掉——页面级监听器活不过第一回合。
   所以「读日志文案」只能走 `runBattle` 的 `onStep` 回调（本次为此给 core/06 加了可选 `onStep`，
   纯增量、零行为变化），不能自己在页面里挂监听器。

### 四、验证（实跑，非仅语法）

- 120 局第三关抽样：**宋青书阵容 61 局 / 胖远桥阵容 59 局**（≈50/50，轮换生效）。
- 10 局胖远桥阵容合计触发：**莽撞 48 次 / 出手没分寸 4 次 / 脾气大 19 次**（三技能全部生效，文案可读）。
- 抽样口径下的明教胜率：宋青书阵容 **14.8%** vs 胖远桥阵容 **69.5%** —— 换掉宋青书后第三关明显变简单，
  **两者强度不对等，待调参**（宋青书带连锁白骨爪 + 周芷若联动，是硬精英）。

## V7.0.5 — 2026-09-22 fx/87 补转发 showMeditateEffect，89 订阅层恢复执行

### 一、现象

```
Uncaught SyntaxError: The requested module './87fx-manager.js' does not provide an export named 'showMeditateEffect'
```

ES module **静态链接期**错误，`fx/89fx-subscriber.js` 整个模块不执行。89 是特效信号订阅层，
它一挂，所有走信号的特效全部失效（伤害飘字、治疗飘字、蛛袭、buff banner、闪避气泡等）。

### 二、根因：89 从 87 取，87 却从未转发这个名字

- 消费端：`fx/89fx-subscriber.js` L8-13 从 `./87fx-manager.js` import `showMeditateEffect`
- 实际定义：`fx/80fx-common-5v5-test.js` L253 `export function showMeditateEffect(unit)`
- 当初给"生生不息"加这个特效时，只往 89 的 import 列表挂了名，**忘了在 87 补转发**。

### 三、修法（1 文件）

| 文件 | 改动 |
|---|---|
| `fx/87fx-manager.js` V6.0.0 → **V6.0.1** | import 清单（取自 80）与 export 转发清单**各补一行** `showMeditateEffect` |

修在 87 补转发，而非把 89 改回从 80 直连——符合 89"特效统一从 87 取"的设计与既有架构决策
（`fx 调用集中到 87fx-manager`）。

### 四、验证（实跑，非仅语法）

- 静态检查器全项目具名导入/导出对账：**79 文件 / 887 具名导入 → 0 处问题**（改前报 1 处，即此名）。
- `node --check fx/87fx-manager.js` 通过。

## V7.0.4 — 2026-09-22 概率连击在原目标同击致死后不再白跳

### 一、现象

```
明教 张无忌 造成 38 伤害，六大派 武当弟子 14 → 0 💀阵亡
⚡ 概率连击触发！
- - - - -
明教 张无忌 无法选择目标
锁定目标已阵亡，跳过行动        ← 白跳一次，没改打别人
```

### 二、根因：额外攻击的 targetUid 回退只判 `alive`，漏了 `_pendingDeath`

原目标被这一击打死后，**`alive` 仍是 `true`**（要等 `resolveDeaths` 才清），只有 `state._pendingDeath` 是 `true`。
构造端据此把"待死"的 uid 锁给第二次攻击，消费端却用严判据 `alive && !_pendingDeath` 找人 → 找不到 → 跳过。

- 构造端（**真正的病灶**）：`core/03battle-utils.js` L383（概率连击）、`modules/27elite-mingjiao.js` L565（小昭·妹连击）
- 消费端：`core/10battle-attack.js` L43 `u.alive && !u.state._pendingDeath`

### 三、修法（3 文件）

| 文件 | 改动 |
|---|---|
| `core/03battle-utils.js` V6.1.0 → **V6.1.1** | L383 判据补 `&& !target.state._pendingDeath` |
| `modules/27elite-mingjiao.js` V6.0.2 → **V6.0.3** | L565 同口径（小昭·妹连击） |
| `core/10battle-attack.js` V6.0.2 → **V6.0.3** | L231/L296 的 fallback 一并补严（配套兜底） |

判 null 后 → 消费端不锁定 → 走正常选目标流程 → **概率连击自动改打别人**。
**指定了 `req.targetUid` 的机制不受影响**（玄冥联动、白骨爪连锁、星粉重试），它们本就该打固定目标。

### 四、⚠️ 教训：第一版改错了地方

首版只改了**消费端** `core/10` L231/L296 的 fallback（`req.targetUid || (...)`）。
实跑复测**白跳 20 次，一次没少**——因为 req **已经带了**（错误的）`targetUid`，`||` 左边的值非空，
fallback 根本没被走到。真正的病灶在**构造端**（`extraRequests.push` 那两处）。

**经验**：改"回退/默认值"类判据前，先确认上游到底传没传值——`a || b` 里的 `b` 可能永远轮不到。

### 五、验证（实跑，非仅语法）

- `node --check` ×3 通过。
- **实跑 1-5 关各 6 局（给双方注入「概率连击」Buff）**：271 回合、连击成功 170 次。
  - "连击后紧跟『锁定目标已阵亡，跳过行动』"：**修复前 20 次 → 修复后 0 次**
  - 连击确实发起了第二次攻击 **159 次**（样本：`明教弟子1 → 宗维侠`、`韦一笑 → 少林弟子`…）
  - 剩余 4 次"锁定目标已阵亡"属**其他锁定机制**（本就该打固定目标），非本次范围。

## V7.0.3 — 2026-09-22 小昭·妹飞天保命被误判死 + 蛛袭恢复日志

### 一、飞天保命"血退回来了，人还是死了"（core/10battle-attack.js V6.0.1 → V6.0.2）

**现象**：小昭·妹血量低时挨一击致死的攻击，日志正常打出"🕷️ 飞天：即将阵亡，免疫本次攻击的 N 点伤害，化为蜘蛛遁走"，但战斗随即判定明教全灭。

**根因**：免疫是「先扣血、后退血」两段式，退血时**只退了 hp，没退致死标记**。
1. `core/10` L128 `applyAttackResult` 先结算 → `core/12` L348 血≤0 时设 `target.state._pendingDeath = true`
2. `core/10` L129-131 查到免疫声明 → `applyStatChange(target,'hp',+dmg)` 把血退回来，**标记留着**
3. `core/12` L417 `resolveDeaths` 过滤 `_pendingDeath && alive` → L424 设 `alive = false`

**为什么只有第 10 回合炸**：血线触发（70%/40%）时那一击不致死，`_pendingDeath` 根本不会被设 → 正常。
只有"这一击足以致死"才会走 `_pendingDeath` 分支，所以第 8 回合（77 血挨 45.4）看不出问题。

**修法**：免疫回退块内补 `target.state._pendingDeath = false;`（血都退回来了，致死标记本就该撤）。

### 二、蛛袭恢复日志文本（render/35 V1.0.0 → V1.0.1、infra/58 V1.1.0 → V1.1.1）

`renderSpiderStrikeFact` 原为 `return null`（2026-08-23 `89c8cc1 重构播放器逻辑` 改的，改为纯 stageAction 驱动特效），
导致蛛袭这一下"打谁、掉多少血"全无文字。现恢复为：

```
🕷️ 蛛袭：小昭·妹 对 静虚 造成 24 伤害（穿透14 + 精通10）
```

- 特效**仍由 stageAction 驱动**（`translateSpiderStrike` 未动），只补文字行，不重复。
- `infra/58` 的 `SPIDER_STRIKE` 契约 `requiredFields` 从 `[]` 补为 `['unitName','targetName','totalDmg']`
  （渲染要读这三个字段，不补则契约校验形同虚设）。

### 三、验证（实跑，非仅语法）

- `node --check` ×3 通过。
- **实跑 1-5 关各 8 局（强制小昭·妹在场）**：432 回合。
  - 飞天共 **74 次**，其中"**即将阵亡**"触发（本次修复针对的场景）**13 次** → 这 13 次小昭·妹**全部 `alive=true`、
    `flying=true`、血量为正**（17~42），矛盾状态（飞天中却被判死）**0**。
  - 蛛袭 fact 渲染 **48 次，返回 null 0 次**，文本形如 `🕷️ 蛛袭：小昭·妹 对 空性 造成 33 伤害（穿透23 + 精通10）`。
- 排查记录：第一版脚本用 `factType === 'spiderFly'` 统计飞天得 0 次，属**指标错误**——飞天日志实际走
  `IMMUNE` fact 的 `data.flyData`（`renderImmuneFact` 内部查表调 `renderSpiderFlyFact`），并非独立 fact。

## V7.0.2 — 2026-09-22 渲染层拆分（第三批 · 收口）：效果域挪出，render/30 变纯入口

### 一、拆了什么

`render/30-fact-renderer.js` 上批拆走攻击域后仍剩 546 行（67 个渲染函数 + 67 条注册）。
本批把**剩余全部**一次性挪进 `render/35-facts-effect.js`，30 正式变成纯装配入口：

- `render/35-facts-effect.js`（新，535 行 / 32.8 KB）：67 个渲染函数 + 67 条 `registerFactRenderer`。
- `render/30-fact-renderer.js`（V7.0.0 → V7.1.0，546 行 → 20 行 / 1042 B）：只剩 2 个副作用导入 +
  3 条 re-export + `validateRegistry('renderer')`。

函数与注册是 **1:1**（67 = 67），逐字搬迁，零改写。

### 二、为什么 67 条一批走完，不再细分 36/37

原计划按机制再分 35（效果 36 条）/ 36（buff 21 条）/ 37（精英 10 条）。实际执行时判断：
一次性挪完的收益（30 立刻变入口、加新 fact 只有一个落点）已经拿到，再切两刀只是把同一个大表
拆成三个中表，边际收益低而每次切都有出错成本。故一批收口。

### 三、跨域引用约定（写进 35 头部）

域之间**禁止互相 import**，跨域取渲染器一律 `getFactRenderer(FACT_TYPES.X)(data)`。
本批完成后 `34 → 33`、`35 → 33` 各自单向，`30 → {34,35} → 33`，无环。
34 里那行 `getFactRenderer(FACT_TYPES.SPIDER_FLY)(fact.flyData)` 现在真命中 35 的
`renderSpiderFlyFact`（上批时它还注册在 30）。

### 四、口径与行为

- `renderBuffSummaryFact` 是 3 参数签名（`buff, allyTeamUids, doubleStrikeUid`），注册行保留特化包装
  `(data) => renderBuffSummaryFact(data.buff, data.allyTeamUids, data.doubleStrikeUid)` —— 随函数一起搬到 35。
- 35 的 import 按**实际用量**取（`CONFIG` 8 处、`makeFXSnapshot` 3、`fmtHp` 2、`BUFF_TYPES` 10、
  `BUFF_SUBTYPES` 20、`CAMP_TYPES` 4、`ROLE_TYPES` 4、`findUnitSnapshotByUid` 1），不是照抄 30 的全集。
- 外部出口不变：`renderLog` / `projectFactEntry` / `findUnitSnapshotByUid` 仍从 30 可 import
  （`player/42player-core.js` 是唯一消费者，不用改）。

### 五、工具与体检清单

- `tools/106-ai-pack-config.js`、`tests/123static-scan.js`：清单加 `render/35-facts-effect.js`。
- 另：`tools/106` 补登记夜间体检新增的 `tests/health-rules/147-149`（`tests/121health-monitor.js`
  早已 import rule94-96，仅打包清单漏登）。补后清单 140 条全部指向存在的文件，`render/`（8 个）与
  `health-rules/`（27 个）均无未登记文件。

### 六、验证（实跑，非仅语法）

- `node --check` ×2 通过。
- 装配：已注册渲染器 **74 个** = 58 里 `renderFn` 非空的条目数（74），`validateRegistry` 告警 **0**。
- **实跑 1-5 关各 2 局**：129 回合、1534 条 fact（30 种）→ `renderLog` 抛错 **0**。
  返回 null 52 次，来源仅两类且均属预期：`buffSummary×46`（对部分 buff key 本就返回 null）、
  `spiderStrike×6`（函数内注释明写"蛛袭不再产生日志文本，由导演 stageAction 驱动特效"）。
- 出口检查：`renderLog` / `projectFactEntry` / `findUnitSnapshotByUid` 三个都仍能从 `render/30` 取到。

### 七、渲染层拆分 · 最终形态

| 文件 | 行数 | 职责 |
|---|---|---|
| `render/30-fact-renderer.js` | 20 | 装配 + 出口 |
| `render/31-stage-actions.js` | 10 | 装配 + 转出口 |
| `render/32-grid-render.js` | 460 | 格子渲染（未拆） |
| `render/33-fact-registry.js` | 128 | 注册表 + `renderLog`/`projectFactEntry`/`findUnitSnapshotByUid` |
| `render/34-facts-attack.js` | 268 | 攻击域（7 条 fact） |
| `render/35-facts-effect.js` | 535 | 效果域（67 条 fact） |
| `render/38-actions-translate.js` | 618 | 翻译域（54 条翻译器） |
| `render/39-actions-defs.js` | 410 | 演出域（26 条 def） |

加新特效的落点：fact 渲染 → 34/35；翻译器 → 38；演出定义 → 39；契约 → `infra/58`；枚举 → `infra/56`。

### 八、遗留（未做，用户决定停在此）

- `render/32-grid-render.js`（460 行）未拆。
- `render/33` 的 `registerFactTranslator` / `registerStageAction` 接口已建但**无人调用**（38/39 沿用原普通对象，
  零行为变更口径）。若日后要启用，需把 38 的 `FACT_TRANSLATORS`、39 的 `DEFS` 改成逐条注册。
- `infra/58` 的 `buildRendererMap` / `buildTranslatorMap` 现为死代码（保留未删）。

## V7.0.1 — 2026-09-22 渲染层拆分（第二批）：舞台动作拆成翻译域 + 演出域

### 一、拆了什么

`render/31-stage-actions.js` 原本 1018 行 / 47.5 KB，一个文件里塞了三件事：
翻译器（fact → stageAction）、演出定义（stageAction → 画面/音效）、单位查找工具。
本次按职责切成三份，入口只留装配：

- `render/38-actions-translate.js`（新，618 行 / 26.1 KB）：`FACT_TRANSLATORS` 全表 + `translateFact` +
  `makeAttackAction` / `makeHealAction` + `translateFactsToStageActions` + 末尾的缺翻译器校验循环。
- `render/39-actions-defs.js`（新，410 行 / 22.2 KB）：`STAGE_ACTION_DEFS` 全表（26 条 kind）+
  `findUnitByUidLocal`（只被演出定义用，28 处）。
- `render/31-stage-actions.js`（V6.1.0 → V7.0.0，1018 行 → 10 行 / 544 B）：只做副作用导入 + 转出口。

### 二、为什么这么切（依赖方向）

`31 → {38, 39}`，38 与 39 之间**无相互依赖**，是两片叶子。

- `findUnitByUidLocal` 判给了 39 而不是 38：全文件 28 处调用**全在 `STAGE_ACTION_DEFS` 的 fx/store 回调里**，
  翻译域一处都没用（`makeAttackAction` 也不调它）。所以 38 不需要 import 它，也就不存在 38 ↔ 39 的来回。
- 翻译器**沿用原来的普通对象**（`FACT_TRANSLATORS`），没有改用 33 的 `registerFactTranslator` 注册表 ——
  保持零行为变更。33 里的注册表接口仍未被调用，属后续可选项。

### 三、口径与行为

- **逐字搬迁，零改写**：38/39 的正文与原文经脚本逐行比对完全一致（38 两段 423 + 184 行、39 两段 11 + 385 行，
  全等），原文每一行都有去处，无遗漏、无重写。
- 外部出口不变：`player/42player-core.js` 是唯一消费者（import `STAGE_ACTION_DEFS` +
  `translateFactsToStageActions`），由 31 转出口顶上，**该文件不用改**。
- 副作用导入顺序：先 `import './38'` 再 `import './39'`。38 顶层的校验循环只读 `FACT_SPECS`，不读 39，
  两边无时序耦合。

### 四、工具与体检清单

- `tools/106-ai-pack-config.js`、`tests/123static-scan.js`：清单加 `render/38`、`render/39`
  （不加则复制器/静态扫描器扫不到）。

### 五、验证（实跑，非仅语法）

- `node --check` ×3 通过。
- **实跑 1-5 关各 2 局**：129 回合、1534 条 fact（30 种 factType）、翻译出 **1916 个 stageAction（18 种 kind）**
  → `renderLog` 抛错 **0**、`translateFactsToStageActions` 抛错 **0**。
- 从 `render/31` 入口取到的 `STAGE_ACTION_DEFS` 有 **26** 条、`translateFactsToStageActions` 是 function
  → 证明转出口链路通。
- kind 覆盖：attack / banner / buffEffect / dodge / dot / emptyTarget / flyMode / heal / hpPctDanmaku /
  immune / miss / rebound / rest / roundEnd / roundStart / spiderStrike / statChange / transform。

## V7.0.0 — 2026-09-22 渲染层拆分（第一批）：注册表 + 攻击域

### 一、为什么要拆

`infra/58` 的 `FACT_SPECS` 早就是 fact 唯一事实源，但**实现**全挤在 `render/30`（825 行）与 `render/31` 里，
加一条 fact 仍要进这两个大文件翻。本次把「注册」与「实现」分开：实现按域散在 34-37，注册表只负责收集。

### 二、新增两个文件

- `render/33-fact-registry.js`（新）：注册表本体 —— `registerFactRenderer` / `registerFactTranslator` /
  `registerStageAction` + 三个 getter + `validateRegistry(layer)`。另收容三个跨域公共件：
  `findUnitSnapshotByUid`（原住 render/30）、`projectFactEntry`、`renderLog`。
  - **`renderLog` / `projectFactEntry` 为什么必须搬**：34-37 每个域都要调它们，若留在 render/30 就形成
    `30 ↔ 34` 循环依赖。33 只做调度（查表 + 契约校验），不认识任何具体 fact。
- `render/34-facts-attack.js`（新）：攻击域 8 个渲染函数 / 7 个 fact —— miss、dodge、attack、emptyTarget、
  immune、drop、breakDef（+ 内部用的 `renderKillLineFact`）。

### 三、render/30 变成纯装配入口（V6.2.0 → V7.0.0）

- 删掉已迁走的 8 个函数、`buildRendererMap` 装配、本地 `findUnitSnapshotByUid` / `projectFactEntry` / `renderLog`。
- 尾部改为 **67 行 `registerFactRenderer(FACT_TYPES.X, fn)`**（其余域待拆，暂住本文件；拆一批挪一批）。
  其中 `BUFF_SUMMARY` 保留原特化包装 `(data) => renderBuffSummaryFact(data.buff, data.allyTeamUids, data.doubleStrikeUid)`。
- 对外出口保持与拆分前一致：`export { renderLog, projectFactEntry, findUnitSnapshotByUid } from './33'` +
  `export * from './34'` → `player/42player-core.js` 是唯一外部消费者，**不用改**。
- ⚠️ **`validateRegistry('renderer')` 必须放在全部 `registerFactRenderer` 之后**（本文件末尾）。
  首版放在文件中部，导致 30 自己那 67 行还没执行就校验，误报 67 条「未注册」。
- 跨域调用改走注册表查表：`renderImmuneFact` 取小昭·妹飞天渲染器用
  `getFactRenderer(FACT_TYPES.SPIDER_FLY)(fact.flyData)`，不 import 37 —— 永久免环，37 建好后自动生效。

### 四、口径与行为

- **fact 的渲染输出零变化**：34 里的函数体是逐字搬迁（含公式明细读 `dmgCalc.formula`、`fmtHp` 显示）。
- `renderLog` 保留原语义：58 显式声明 `renderFn: null` 的 fact 返回 `null`；漏注册才 `throw`。
- 58 的 `buildRendererMap` 现无人调用（保留未删，属后续批的收尾项）。

### 五、工具与体检清单

- `tools/106-ai-pack-config.js`、`tests/123static-scan.js`：文件清单加 `render/33`、`render/34`（不加则复制器/扫描器扫不到）。

### 六、验证（实跑，非仅语法）

- `node --check` ×3 通过。
- 装配：已注册渲染器 **74 个**（7 + 67，与 58 里 `renderFn` 非空的条目数一致），`validateRegistry` 告警 **0**。
- **实跑 1-5 关各 2 局**：85 回合、1937 条 fact、覆盖 34 种 factType → `renderLog` 抛错 **0**、控制台红字 **0**；
  返回 null 20 次全是 `buffSummary`（该函数对部分 buff key 本就返回 null，属预期）。
  关键链路均命中：`attack×588`（含 `breakDef×105` 走 `projectFactEntry` 前置）、`miss×15`、`dodge×11`、
  `emptyTarget×2`、`xuanmingLinkAttack×16`（第 5 关玄冥联动）、`clawHit×46`（第 4 关宋青书）、`endlessBreath×75`。
- 定向冒烟补 `IMMUNE + flyData`（实跑未触发的低频分支）：走 `getFactRenderer` 查表成功，未抛错。

### 七、后续批（未做）

- 拆 35（效果域）/ 36（buff 域）/ 37（精英域），把 render/30 剩下的 67 项注册逐步挪出；
- 翻译器（`render/31`）与舞台动作的注册表化，届时启用 33 的 `registerFactTranslator` / `registerStageAction`；
- 58 的 `buildRendererMap` 清理（现为死代码）。


---

## 体检序列（V6.1.x —— 仅体检代码/文档，不涉主代码）

## V6.1.36 — 2026-09-25 体检第 20 轮：四方向优化落地（真值源统一 / 不变量套件 / 规则脱钩产品实现 / 基线二分）

（本轮按用户指令**改在 main 分支直接进行**；主代码一行未改，用户未提交的 render/32 V6.4.1 原样保留未代提交）

- `tests/121health-monitor.js` V6.1.0 → **V6.2.0**：新增 `teamsFromStore()`，引擎/UI/规则/结算四组检查一律
  改取 battleStore 真值（原四组全吃 `ctx.UI.allyTeam` 开战副本＝系统性假绿）；采样循环中途调用＝中期真值，
  故「中期溢出后被修回」这类漂移也能抓到。
- `tests/rules-replay.mjs` V6.1.11 → **V6.2.0**：新增不变量套件，逐步断言 hp∈[0,maxHp] / maxHp>0 /
  同阵营 pos 唯一 / factType 是否注册渲染器，与机制规则分开报告并计入退出码。首版两条判据假阳性已作废
  （hp 整数误报 11901 条、按渲染产出判映射缺口误报 929 条）。
- `tests/140-baseline.js` V1.2.0 → **V1.3.0**：`--check` 的 DIFF 二分 —— winner 翻转归回归（硬失败、报红不重录），
  winner 未变且基线后有业务提交归预期变更（单列打印提交清单、不硬失败），不再一律退码 1。
- 移植 test 分支上 main 缺失的三条规则修复：`139`（宿主阵亡窗口关闭）、`141`（严阵以待回合读配置）、
  `146`（幼狮原地改名别名比对）→ 回放 fail 4→2、恒 skip 2→1。
- 带入 test 分支上的体检记档文档（迭代日志/更改总览/收获与方法/复盘报告）—— main 此前完全没有这些文档。
- 方向③（规则不许复制产品旧实现）由用户在主仓完成：`render/32` V6.4.1 改走 `computeBuffStats().dodgeBonus`、
  规则 140 改为直接调 `getDodgeBreakdown` 断言面板输出 → 规则 140 由恒红转 pass=4 fail=0。

待确认（报业务侧，本轮未动）：pos 同格冲突 26 例；基线 seed=42 stage=3 winner 六大派→明教 翻转（红线，未重录）；
拒马阵双重清算 fail=4；carry 五号位超配置推导上界 fail=1。
