以下是我们在多轮讨论中积累的所有设计想法与方案汇总，按模块分类整理，便于查阅和推进。

---

## 光明顶 5v5 游戏 —— 创意与设计方案汇总

### 一、核心战斗机制深化

基于现有 M 值拆分、防战 K 值表、遮挡系统，提出以下深化方向：

1.  **韦一笑 “血魔流” 体系**
    -   新 Buff 「寒冰绵掌」：韦一笑攻击时，若目标血量百分比高于自身，附加 `差值 × 0.5` 的伤害。
    -   玩法：刻意控血换取高爆发，再靠吸血回复，形成刀尖跳舞的风格。搭配 “你就是 carry” Buff 能在残局成为强力翻盘点。

2.  **宋青书/周芷若博弈深化**
    -   新 Buff 「攻心为上」：我方女性角色在场时，宋青书的 “性奋” 额外攻击有 40% 概率目标改为周芷若。
    -   联动：周芷若被宋青书攻击 3 次后，会对他释放一次无法闪避的九阴白骨爪。

3.  **拒马战略化**
    -   新 Buff 「拒马枪阵」：拒马阵亡时，对摧毁者造成 `10 + 攻击者 atk × 0.3` 的真实伤害，让破阵付出代价。

---

### 二、新角色与叙事机制

1.  **殷天正（白眉鹰王）** —— 战士
    -   「鹰爪擒拿」：25% 概率将目标抓至身前空位，此次攻击必中且无视 50% 防御。
    -   「父子连心」：张无忌血量低于 30% 时，殷天正获得一回合狂暴（攻击 +20%，防御 -10%），并移动到张无忌同列前排挡刀，触发特殊叙事台词。

2.  **黛绮丝（紫衫龙王）** —— 飞行（近战位）
    -   「金花打穴」：攻击不造成直接伤害，改为叠加 “金花” 印记。
    -   「龙王真身」：任意单位阵亡时，对场上所有带印记敌人造成 `印记层数 × atk × 0.5` 真实伤害，随后变身为防战形态并获得护盾。一个前期隐忍、中期爆发的战术支点。

3.  **战场事件系统**
    -   **光明顶圣火**：第 5 回合开始，若明教存活少于 3 人，全体获得一次 “免死金牌”（致死伤害保留 1 血），持续 2 回合。
    -   **周芷若的抉择**（张无忌在场，周芷若血量低于 40%）：随机触发 “旧情难忘”（清除自身快乐层数，为张无忌回血 20%）或 “因爱成恨”（永久攻击 +20%，九阴白骨爪连锁上限升至 5 次），大幅提升戏剧性。

4.  **新精英角色：小昭**
    -   定位：辅助型飞行单位，M 值 112。
    -   「星移斗转」被动：明教全体每回合首次受伤减免 15%。
    -   「圣火令指引」主动：治疗血量最低队友 `maxHp × 10% + 小昭 atk × 0.2`，并使其下次攻击 +20% 伤害。
    -   隐藏联动：张无忌近战形态时，小昭治疗翻倍，且附加免疫一次遮挡的 “乾坤护体”。

---

### 三、Buff 技能系统（11 技能修订版）

已由总策划完成修订，核心变动与实现要点：

| 技能 | 效果摘要 | 关键注意点 |
|------|----------|------------|
| 概率连击 | 随机队友 80% 额外攻击一次 | |
| 你就是 carry | 5 号位获队友基础加成（8%攻/4%防/5%血），**阵亡队友那份加成翻倍** | 需单独追踪每个队友状态 |
| 流云身法 | 全体闪避 +30% | |
| 巨马阵 | 每回合开始在随机空格生成 20 血/0 攻/5 防拒马，回合结束 50% 销毁 | 时序：生成→战斗→销毁 |
| 流星赶月 | 远程伤害 +50%，溅射周围 8 格 50% | |
| 嗜血狂刀 | 近战吸血 50% | |
| 严阵以待 | 防战防御 +50%，反弹 50% 伤害差 | |
| 乘风突袭 | 飞行单位 80% 波及同行，60% 击退一格 | 详见击退换位特效 |
| 圣火令 | 随机一列攻击 +30%，随机一行防御 +30% | |
| 热血奋战 | 攻击回血 15%，**每个单位独立计数**，每第 3 次翻倍 | 独立计数器 |
| 惑人心智 | 最前排单位攻击时：40% 扰乱敌方换位，20% 扰乱己方换位 | 换位在攻击前结算 |

**优化建议**：为避免后期可选池缩小，随机抽取 3 个技能时，若与已生效技能重复，应重新抽取，确保每次都是全新选项。

---

### 四、Roguelike 装备对战（1v1 新模式）

独立于 5v5 的全新模式，核心循环：“投资→掉落→合成→战斗”。

-   **投资机制**：初始 100 分，免费 2 次。后续消耗按 `1,2,2,3,3,3,4...` 递增，每次从 9 个词条中随机掉落白装到九宫格。
-   **合成体系**：3 件同名白装自动合成蓝装，并返还 `上次付费价格 × 已合成数量` 的分数。合成后该类型锁定，场上同类其他词条变为死格。
-   **死格与锤子**：合成获得锤子次数，可双击清除死格。未用完的锤子战斗后返还分数。
-   **战斗**：1v1 回合制，步进模式。胜利返还 `投资分数 × (1.5 + 己方蓝装数 × 0.2)`。
-   **选秀**：若干回合后开启，旋转轮盘争夺对方道具。
-   **架构**：敌我状态完全分离，合成检测与执行分离。待定：锤子规则、循环上限、选秀触发回合。

**补充建议**：
-   设定合成返还分数的最低保底（如 5 分），避免前期反馈过弱。
-   明确暴击掉 2 件与天降 +5 分的优先级（先加分，再算掉落）。
-   步进模式中输出详细状态日志。
-   选秀首次触发设于第 5 轮投资后，之后每 3 轮一次。

---

### 五、PVE 敌人站位模板（六关递进配置）

为六关 PVE 设计敌人站位模板，教学曲线清晰：

| 关卡 | 定位 | 必选站位 | 精英 | 随机 | 设计意图 |
|------|------|----------|------|------|----------|
| 1 | 入门试刀 | 1防(1),1战(2),1远(7) | - | 2 | 基础阵型 |
| 2 | 站位启蒙 | 1防(1),1战(4),1飞(7),1远(8) | - | 1 | 完整阵型 |
| 3 | 精英初现 | 1防(1),1远(7) | 1 | 2 | 引入精英 |
| 4 | 双重考验 | 1防(1),1战(2),1远(8) | 2 | 0 | 精英+站位施压 |
| 5 | 数值碾压 | 1战(1),1远(7) | 2 | 1 | **故意不配防战**，让玩家体会站位价值 |
| 6 | 终极奇袭 | 1防(1),1战(2),1远(8) | 2 | 0 | 完美阵型+技能联动 |

实现时需注意精英站位覆盖普通单位，以及随机池的可用职业。

---

### 六、击退换位特效（乘风突袭视觉规范）

已提供完整演示代码，三阶段动画：猛退挤开（550ms）→ 交换闪烁（280ms）→ 落位弹入（350ms）。

**接入真实系统需要对齐的要点**：
1.  **击退方向泛化**：根据攻击者站位计算方向，而不是根据阵营写死；超出棋盘边界则击退失败。
2.  **批量处理**：同回合多个击退同时播放，避免串行。
3.  **状态刷新**：换位后立即重算遮挡和前排判定。
4.  **边界情况**：拒马不可被挤开；锁定角色（张无忌、韦一笑）不可被挤开；击退到空位时简化动画。

---

### 七、合成特效融合方案（蓝装合成动画）

五阶段流程，总时长约 4.7 秒，视觉层次分明：

| 阶段 | 时长 | 核心动作 |
|------|------|----------|
| 触发升天 | 500ms | 白装发光，武将格子变绿，虚影留原地 |
| 升空消散 | 900ms | 武将格上升至汇聚点，白装光雾环绕 |
| 旋转汇聚 | 2000ms | 蓄力旋转→加速吸入→收束成绿核 |
| 蓝光爆点 | 600ms | 绿核转蓝，全屏闪光，冲击波扩散 |
| 结果弹出 | 700ms | 蓝装图标弹跳浮现，合成成功飘字 |

**优化建议**：提供跳过按钮；绿转蓝用 HSL 插值；抖动用 `transform: translate` 避免重排；死格变灰延迟 200ms 触发避免视觉过载；蓄力阶段配合音效渐强。

---

### 八、圣火令与局外养成系统

> 本系统是**唯一完整方案源**（待办-事项待办.md 已收敛为行动清单）。

-   **圣火令双用途**：战斗消耗或商店货币。战斗消耗每关限 1 次、二选一：恢复我方单角色 30% 最大生命 / 对敌单体造成 15% 最大生命真实伤害。
-   **获取方式**：积分兑换（50 积分/1 个，积分来自"猜胜负"）、战斗掉落（击杀精英 25%，首次通关必掉，有保底）。
-   **商店商品**：指定角色（固定养成对象）、技能书（5 种）、装备（5 件）、属性点道具、经验包，具体列表已设计，详见之前对话。
-   **养成机制**：等级（经验包提升 M 值）、属性点道具（同时转化为等量经验）、养成角色永久保留。
-   **上场规则**：布阵时可用养成角色替换一名常规队友。

---

### 九、云端存储方案（Leancloud）

将游戏从单机升级为可成长的网页游戏：

-   **方案**：使用 Leancloud BaaS，替换 `localStorage`。
-   **数据表**：`PlayerData`（积分、圣火令、仓库、养成角色）、`CultivatedHero`（可选）、`BattleLog`（战斗日志）。
-   **核心 API**：用户注册/登录，数据云端读写，ACL 权限保证安全。
-   **实施步骤**：注册应用获取 Key，引入 SDK，改写积分、圣火令、养成相关的存储逻辑。
-   **后续扩展**：排行榜、商城兑换、云端存档。

---

### 十、其他系统优化建议

-   **AI 模仿学习**：记录玩家布阵/Buff 选择，训练不同风格 AI 供挑战。
-   **Buff 筛选与 Ban/Pick**：高难度下开战前禁用 1 个 Buff，增加策略深度。
-   **阵法共鸣**：特定职业站相邻位置触发小幅属性加成（如 1 号防战 + 5 号远程 = “土生金”）。
-   **全自动模式与全日志系统**：自动选 Buff、自动进入下一关、常驻战报；日志分 6 类展示行动、伤害、状态、Buff、元数据，支持导出。
-   **工具箱海克斯匹配**：同阵容不同 Buff 组合 PK 测试，对方无 Buff 时给纯数值加成以对比强度。

---

以上共涵盖 **10 大模块**，从核心战斗深化、新角色、新模式、视觉特效、经济养成到网络化方案，构成了一套完整的进化路线图。可根据优先级逐步落地，最先推进的是 Buff 系统修订和 PVE 站位模板，它们改动小、见效快。需要任何模块的详细代码实现或伪代码，随时可以展开。

---

### 附录：重构时机对账（"事不过三"）

> **判断标准**：不是"看着像就抽"，而是"**抽完调用方更清楚、且未来加同类不用复制**"。三个但抽了更糊涂的不抽；四个而方向对路的才抽。
> 下列事实均已对代码逐条核实（行号为 2026-09-15 版本）。

**先澄清一处误判**：飘字**没有**"远程一份、近身一份"——全仓只有 `fx/80fx-common-5v5-test.js` 一个定义点，远程/近身/攻击组/fx 触发层统一调它。真正的重复不在调用方，在**函数族内部**。

| 候选 | 事实（已核实） | 判定 |
|------|----------------|------|
| 四个飘字函数 | `fx/80`：`showDamageFloat`(76) / `showDodgeBubble`(88) / `showHealFloat`(100) / `showAtkBuffFloat`(115)。骨架完全一致（取格位 → 池取元素 → 设文案/位置 → 定时回收），差异仅颜色、偏移、时长；每个还各带 `createXxxEl` + `initPool` 配套样板，合计约 8 行 | **值得抽**（见 `待办-事项待办.md` 第 10 项） |
| 三个 `_buildFsm` | `modules/27`：26（张无忌）/ 155（小昭·姊）/ 403（小昭·妹）。外壳同为 `states` + `new StateMachine` + 挂 `unit._fsm`，但 `states` 内容差异极大（形态转换 vs 附身/回归 vs 飞天/降落） | **到线不抽**：共用仅 3 行，抽出来省的比调用层样板还少；且与"明教不搬表"裁决同向 |
| 三个 CG 三件套 | `ui/72`、`73`、`74` 各有 `DONE_KEY` + `isXxxDone/markXxxDone/resetXxxDone`；当前仅 72 接入 61 | **到线不抽**：73/74 仍是候选版本未定稿，先拍版本再谈合并 |
| `_Normal/_Sister/_Brother` 三胞胎族 | `core/14`：`applyCloudBodyDodge_Normal`(24) 与 `_Brother`(26) 函数体**一字不差**；`_Sister`(25) 才走 `hexEnhance` 数据源。另有 `applyFortifyDef` / `applyHolyFlame` 等成组出现 | **不单开一刀**：属纯冗余而非灰色地带，但正规收敛路径是词条系统继续走数据源统一；真要动也只是一行别名 |

**结论**：本轮只落地飘字收敛；其余三条记录在案，待条件变化（CG 版本拍板、词条系统继续推进）再议。

### 附录：玄冥二老联动体系梳理（2026-09-19）

**结论**：主体已完善，具备"新机制样板"价值；**0 处必改、3 处可选**——原判"必改"的 `_acted` 双源一项已于 2026-09-19 复核撤回（见第 1 条）。

**一、已完善的部分（样板价值）**

| 环节 | 做法 | 证据 |
| --- | --- | --- |
| 声明 | content 写 `mechanics: { type: 'linkAttack', partnerNames }`，参数走 `getSkillParams`，无代码兜底 | `content/200game-data.json`（鹿杖客/鹤笔翁条目）、`modules/25elite-imperial.js` |
| 注册 | 经 `registerElite` 统一入口，核心层不 import 精英模块 | `core/08-elite-registry.js` |
| 防乒乓链 | `_isLinkAttack` 置/清配对（联动攻击不再触发联动） | `core/15-skill-mechanisms.js:311`、`core/10battle-attack.js:288,291` |
| 防重复跟随 | `_linkTriggered` 走状态 schema 声明（每回合复位），搭档每回合只跟随一次 | `core/17-state-keys.js:32`、`core/15-skill-mechanisms.js:316-318` |
| 借调可回退 | `actedMode: 'restore'` + `actedSnapshot`，攻击完恢复搭档原行动状态（修"吞回合"） | `core/15-skill-mechanisms.js:321`、`core/10battle-attack.js:292-294` |
| 日志标准化 | `XUAN_MING_LINK_ATTACK` 枚举 + 专用 renderer | `infra/56-battle-enums.js`、`render/30-fact-renderer.js:702-704` |
| 回归守护 | rule73 守着"吞回合"复发 | `tests/health-rules/126-xuanming-link.js` |

**二、可优化点**

1. **可选｜`_acted` 双源写入（复核后由"必改"降级）**
   语义字段 `_acted` 有两个写侧：模拟层 `core/`（`10battle-attack.js:31/36/54`、`12battle-attack-steps.js:132/194`、`11battle-round.js:381`），表现层 `fx/`、`player/`（`player/46attack-group.js:42/127`、`fx/82fx-crash-5v5-test.js:14/104/190/272`、`fx/85fx-dodge-bullet.js:213/214`）各自向 store `dispatch({ type: SET_VISUAL, _acted: true })`。
   渲染只读 store（`render/32-grid-render.js` 据此加 `acted` class），而 `core/10battle-attack.js:292-294` 的恢复**只改模拟层对象、不回灌 store**。
   **2026-09-19 复核撤回原判**（当时写"格子永久显示已行动"是夸大）：`player/42player-core.js:233` 每步末调 `syncStoreFromStep`，用模拟层真值整体覆盖 store（`modules/24battle-store.js:44-47`），表现层那份 `_acted: true` 在同一步结束即被冲掉——灰色只在该步播放期间短暂出现，**自愈**，不是永久残留；用户亦已实机确认问题不再出现。故降为可选：仅建议表现层不写语义字段（改视觉镜像字段），避免日后别的机制依赖它时踩坑。

2. **可选｜额外攻击静默丢弃**
   `core/10battle-attack.js:279-284` 用 `executedUids` 保证"每单位每回合只执行一次额外攻击"。同一单位同时收到 `doubleStrike` 与 `xuanmingLink` 请求时，排序后只执行一个，**另一个静默消失且无日志**。建议补日志或明确优先级说明。

3. **可选｜两个防重标记命名易混**
   `_isLinkAttack`（防乒乓，攻击期间临时）与 `_linkTriggered`（防重复跟随，每回合复位）职责不同但名字相近，读代码时容易误判。建议补注释或改名为 `_linkFollowing`。

### 附录：三条共性架构问题（2026-09-21）

> 三条其实是同一件事：**引擎规则没有单一来源**。处置顺序：**先修第 1 条**——它一修，工具和游戏同源，后面两条才好办。

**1.「打完一局」这件事，代码里写了五遍**
游戏一遍，四个工具各一遍。后果：在工具里调平衡时，工具和游戏实际不是同一套规则——这两轮调九阳、生生不息，就是照工具调的。
> **现状（2026-09-21 核实）**：已开工。`core/06battle-runner.js` 已新建（导出 `runBattle`），`tools/110`、`tools/116`、`tools/101` 已改调 runner；**`tests/140-baseline.js` 尚未收口**，仍保留自己的 `while (battleState.round <= MAX_ROUND)` 整局循环（`389aa43` 只给它改了 4 行 buff 递减）。

**2. 加个新特效，得改老文件**
别的地方都是"加个新文件就行"，只有画面这块要动老文件。加得越多，那个文件越挤。

**3. 伤害怎么算的，引擎说一遍，画面自己又算一遍**
两份算法，早晚对不上。

**明确不做两条**（已定案，记在 `准则-架构决策.md` 的"既有决策"）：不拆 Unit（动的是战斗逻辑本体，早定过案）；不上打包工具（会拆掉现在"纯文件、能离线、AI 好读"的好处）。

### 附录：三个新角色设计草稿（2026-09-22 · 参数待定）

> 用户 2026-09-22 澄清：本次共 **3 个新角色**（不是 1 个）。
> 分支：`feat/miejue-stage3-rotation`（demo 用，不合并主代码）。行号按 2026-09-22 版本核实。

| # | 角色 | 归属 | 名字 |
|---|------|------|------|
| A | 胖远桥（六大派 · 武当 · 宋远桥年轻时） | 第三关，与宋青书**每局随机二选一** | 已定 |
| B | 灭绝师太（六大派·峨眉掌门·战士·M112） | 新第七关精英 | 已定 |
| C | 金毛狮王谢逊（明教 · 站 7 号位） | 明教玩家侧 | 已定 |

---

#### A · 胖远桥（六大派 · 武当 · 宋远桥年轻时）

| 技能 | 类型 | 机制 | 参数（初值） | 落地方式 |
|------|------|------|--------------|----------|
| 挨打加攻 | 被动 | 每次被攻击后自身攻击 +N | +2 攻/次，永久词条 `group:'rageOnHit'`，无上限 | 组件钩 `AFTER_DAMAGE_APPLIED`，走 `addMod(unit,'atk',…)`（`core/13`），再由 `getStat` 现算 |
| 打歪 | 被动 | 攻击时概率打错目标 | 25%；目标改为随机一个其他可选敌人 | 钩 `BEFORE_SELECT_TARGET`，写 `declaration.targetResult`（同 `submitSpiderMindControlDeclaration` 写法，`core/27` L501） |
| 嘲讽 | 主动 | 强制对面攻击他一次，该次伤害减半 | 每回合 1 次；被嘲讽者本回合目标强制为嘲讽者；伤害 ×0.5 | 钩 `BEFORE_SELECT_TARGET` 强制 `targetResult`；伤害减半走 `EFFECT_TYPES.DMG_REDUCTION` |

**已定名（2026-09-22）**：**胖远桥**（武当 · 宋远桥年轻时）——设定"胖、脾气大、出手没分寸"，正好对上三个技能：抗揍（挨打加攻）、没分寸（打歪）、脾气大（嘲讽）。

#### B · 灭绝师太（六大派 · 峨眉掌门 · 战士 · M 112）→ 新第七关

| 技能 | 类型 | 机制 | 参数（初值） | 落地方式 |
|------|------|------|--------------|----------|
| 反击 | 被动 | 被攻击后反击攻击者一次 | 概率 100%；反击伤害 ×0.6；**无每回合上限**；不可被闪避 | 组件钩 `AFTER_DAMAGE_APPLIED` 推 `extraRequests`（消费路径已在 `core/10battle-attack.js` L184 发、L101 收）；靠 `extraRequests` 的 `executedUids` 去重防死循环 |
| 跟随攻击 | 被动 | 队友攻击后概率跟随一次 | 概率 30%；**无每回合上限** | 复用 `linkAttack`（`core/15-skill-mechanisms.js` L310），扩为"任意队友 + 概率"变体；必须用 `_isLinkAttack` 挡住"跟随攻击再触发跟随"的自链 |
| 每第三次攻击 | 被动 | 第 3 次攻击伤害翻倍 + 吸血 200% | 计数每单位独立、永久累计；翻倍 ×2；吸血 = 本次伤害 ×2.0 | 组件维护 `_attackCount`；翻倍走 `EFFECT_TYPES.DMG_MULTIPLIER`，吸血走 `EFFECT_TYPES.LEECH` / `onHitEffects: leech` |
| 召唤周芷若 | 主动 | 召唤周芷若入场 | 每局 1 次；M 107；落 2 号位；血量按 M 正常生成 | 仿 `spawnHorse`（`core/05battle-horse.js`）新建 `spawnUnit`；周芷若机制已在 `content.characters` 数据驱动，`installFromGameData` 每回合全量注册，召唤即生效 |

#### C · 金毛狮王谢逊（明教 · 站 7 号位）

| 技能 | 类型 | 机制 | 参数（初值） | 落地方式 |
|------|------|------|--------------|----------|
| 召唤狮子 | 被动 | 每回合召唤一只狮子 | 每回合 1 只；优先 4 号位，被占则 1 号位；两格都满则落 8/9 号位 | 仿 `spawnHorse`（`core/05`）新建 `spawnUnit(team, pos, …)` 指定格版本 |
| 狮子形态 | 被动 | 按落点决定种类 | **1 号位＝雄狮**（防战/坦克，高防高血）；**4 号位＝幼狮**（战士，高攻）；**8/9 号位＝母狮**（后排技能型） | `spawnUnit` 按 pos 决定 role + 属性 |
| 替死 | 被动 | 狮子替谢逊死 | 每次致死伤害消耗 1 只狮子 | **需改主代码**：`core/12battle-attack-steps.js` `resolveDeaths` L422 循环补 `if (!u.state._pendingDeath) continue;`（`pending` 是发射前快照，只清标记无法取消死亡） |
| 集火 | 主动 | 攻击时发动一次集火 | 每回合 1 次；随机 2 名存活友方各追加一次攻击 | 组件推 `extraRequests`（`core/10` L184→L101） |
| 狮吼（母狮专属） | 主动 | 一竖列敌人恐惧 | 作用范围＝母狮所在列全部敌人；恐惧＝失去**下一次**攻击机会（1 次） | 恐惧沿用既有"跳过行动"字段（同眩晕）；列内取敌用既有列索引工具 |

> 原"稻草人后排吟唱 2 回合跳出 + 持续伤害"已被本次"母狮 · 狮吼"取代，DoT 不做。

---

#### 第三关轮换

现状：`modules/29battle-init.js` L222 直接读 `C.ENEMY_SQUADS[3]`（固定数组），精英由 L282 的 `ELITE_POOL[3]` 识别。

方案（推荐）：`ENEMY_SQUADS` 形状不动，新增 `encounters.squadVariants` = `{ "3": [ [宋青书阵容], [A 阵容] ] }`，`29battle-init` 取该关变体时随机抽一组（走 `_rng`，PVP 双端同源）。不动既有形状 → `tools/104`、`tools/101`、`tests/140`、站位预览不受影响。
**已定（2026-09-22）**：每局随机二选一。

#### 需新增的代码点

1. `content/200game-data.json`：A / B / C 三个 `characters.*`（技能参数 + taunts）、第三关 `squadVariants`、第七关 `enemySquads / elitePool / enemyPosTemplates / mingTargetPower`、`roster.enemyM`、`taunts`
2. `modules/26elite-sixsects.js`：A、B 的组件 + `registerElite`
3. `modules/27elite-mingjiao.js`：C 的组件 + `registerElite`
4. `core/02unit.js` `HERO_FLAGS`：加 3 条（`isMieJueShiTai` 等）
5. `core/05battle-horse.js`（或新文件）：`spawnUnit` 指定格召唤
6. `core/12battle-attack-steps.js`：`resolveDeaths` 支持取消死亡（替死，唯一动核心战斗逻辑处）
7. fact 新增：改 4 处 —— `infra/56`（枚举）、`infra/58`（契约）、`render/35-facts-effect.js`（渲染 + 注册）、`render/31-stage-actions.js`（翻译）
8. `modules/29battle-init.js`：读第三关 `squadVariants` 随机抽一组

#### 可行性结论

| 项 | 结论 | 依据 |
|----|------|------|
| A 挨打加攻 | 可行（低） | `addMod` + `getStat` 既有属性通道 |
| A 打歪 / 嘲讽 | 可行（中） | `BEFORE_SELECT_TARGET` 已支持 `declaration.targetResult` 改目标 |
| B 反击 / 跟随 / 翻倍吸血 / 召唤 | 可行（中） | `extraRequests`、`linkAttack`、`DMG_MULTIPLIER`/`LEECH`、`spawnHorse` 均有既有实现 |
| C 召唤 / 形态 / 集火 / 吟唱 | 可行（中） | `spawnHorse` 同款 + `extraRequests` + 既有眩晕/DoT 字段 |
| C 替死 | 可行（低，但**动核心**） | `resolveDeaths` 补一行取消死亡判据 |
| 第三关轮换 | 可行（低） | 新增 `squadVariants` 键，不动既有形状 |

**风险**：召唤单位当回合拿不到 `_linkedPartnerUid`（与宋青书联动只在下一回合成立）；新机制必须自加防重标记（`core/10battle-attack.js` L102 已注明"新机制忘标记会死循环"）。