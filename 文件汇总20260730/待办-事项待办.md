# 光明顶 5v5 · 事项待办

> 圣火令与养成系统的**方案细节**见 `优化-想法和优化.md` 第八节（唯一方案源），此处仅保留行动项。

## 待办清单

> **核心提炼**（2026-09-26 复核清理，重编号）：① 存量验证类——严阵以待长局站位、数值 A/B 复核（嗜血狂刀吸血 / 流云身法闪避）；② 重构类——飘字函数族收敛、资产守卫核对脚本。
>
> **已完成移除**（2026-09-26 核对代码库后清理）：原 1 圣火令商店（已落地 `tools/105-shop.html` V1）；原 7 工具箱海克斯匹配（已落地 `tools/108-hex-dashboard.js`，迭代至 V6.0.5）；原 12 三个新角色（履历 V7.1.0 转正，胖远桥/谢逊后续仍在迭代）。

| 编号 | 项目 | 说明 |
|------|------|------|
| 1 | 严阵以待长局站位验证 | 防战前排居中 vs 偏侧在 R9+ 存活率对比；防战+远程 vs 防战+飞行（源自严阵以待实验待排查项） |
| 2 | 数值 A/B 复核 | 嗜血狂刀 80% 吸血是否过高、流云身法实际闪避覆盖率（控制台脚本跑分布验证）。注：流云身法「面板永不显示」的读值 bug 已在履历 V7.4.2 修复，本项只剩数值强度复核 |
| 3 | 飘字函数族收敛 | `fx/80fx-common-5v5-test.js` 四个飘字函数（`showDamageFloat` / `showDodgeBubble` / `showHealFloat` / `showAtkBuffFloat`）骨架一致，抽成一个 `showFloat(unit, text, { color, offsetY, className, duration })`，连各自的 `createXxxEl` + `initPool` 配套一并收；判定依据见 `优化-想法和优化.md` 附录「重构时机对账」 |
| 4 | 资产守卫核对脚本 | 读 `资产-核心资产清单.md` 的「判据清单」表，逐条核对锚点，只报漂移项并附 git 溯源（`git log -1 -S"锚点"`）；规格见该文件「核对脚本规格」小节。建议与 `tests/123static-scan.js` 同族，不另开工具链入口 |

## Bug 待修

- 暂无（原独立文件 `待办-bug待修.md` 2026-09-26 并入本节，此后发现新 Bug 直接记在这里）

## 待讨论/待明确

- 圣火令商店 V1（`tools/105-shop.html`）已有击杀/通关掉落与宝箱雏形，与 `优化-想法和优化.md` 第八节完整方案之间的概率规则、技能/装备列表待对齐
- 全自动模式中"全日志系统"的具体展示字段
